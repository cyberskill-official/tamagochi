window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AI-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Standard SKU only.</strong> Per FR-LEGAL-001 §1.5 + FR-AUTH-003, LLM generative chat is forbidden in the kids SKU. Under-13 pets use scripted dialogue trees (FR-AI-002). This FR specs the 13+ SKU pipeline only.</p>\n<p>§1.2  <strong>Persona YAML.</strong> Each pet has a <code>persona.yaml</code> (~300 tokens max) describing: species-archetype tone, vocabulary preferences, mood-stage variants, off-limits topics. Stored in <code>apps/api/src/ai/persona/species/&lt;species&gt;.yaml</code> for species defaults; per-pet overrides stored in DB.</p>\n<p>§1.3  <strong>Event memory — last 20 events.</strong> The persona service maintains a per-pet rolling memory of the last 20 events: care actions (feed/clean/hug/mini-game), evolution stages, stat warnings, owner sign-in pings, AR placements. Stored in <code>pet_event_memory</code> table with <code>(pet_id, event_id, kind, payload_summary, occurred_at)</code>. Used as context for next LLM dialogue.</p>\n<p>§1.4  <strong>Endpoint.</strong> <code>POST /v1/ai/pet/:petId/speak</code> — body <code>{ trigger: 'idle'|'after_care'|'after_mini_game'|'evolution'|'time_of_day', context_overrides?: object }</code>. Returns <code>{ text, sentiment, animation_hint, ttl_sec }</code>.</p>\n<p>§1.5  <strong>System prompt structure.</strong> Server builds a system prompt:</p>\n<pre><code>You are {pet.display_name}, a {species} pet who is {stage} stage.\nPersonality: {persona.yaml}\nRecent events: {last_20_events}\nCurrent stats: hunger {h}, cleanliness {c}, happiness {hp}, energy {e}.\nOwner just did: {trigger}.\n\nReply in ≤ 80 characters, first-person. Avoid: personal questions about the owner,\nURLs, phone numbers, capitalised slogans. Match the persona tone.</code></pre>\n<p>Strict shape; user input never reaches the prompt directly.</p>\n<p>§1.6  <strong>Claude Haiku primary.</strong> Default model is <code>claude-haiku-4-5-20251001</code> via Anthropic API. Output cap: 100 tokens. Temperature 0.7.</p>\n<p>§1.7  <strong>Gemini Flash fallback.</strong> When Anthropic returns 429/5xx OR latency &gt; 3s, route to <code>gemini-2.0-flash</code>. Document the fallback ratio per OBS dashboard.</p>\n<p>§1.8  <strong>Aggressive caching.</strong> Cache key = <code>sha256(system_prompt)</code> — identical prompts hit Redis cache (24h TTL). Plan §PART 4 — cache-first is the cost-control lever.</p>\n<p>§1.9  <strong>Cost cap per pet per day.</strong> Each pet has a daily LLM-call cap: 30 calls. Beyond, the persona falls back to a 12-line scripted bank (FR-AI-002 reuses these for the kids SKU). Cap reset at player's local midnight.</p>\n<p>§1.10  <strong>Cost cap per player per day.</strong> Global player cap of 100 LLM calls/day (covers multi-pet Pet+ users). Beyond → scripted fallback.</p>\n<p>§1.11  <strong>Content safety gating.</strong> Every LLM response MUST pass through FR-AI-002's content-safety classifier BEFORE being returned to the client. Failures emit <code>ai.persona.safety_blocked</code> event + a generic safe response substitutes.</p>\n<p>§1.12  <strong>Response shape.</strong> API returns:</p>\n<pre><code class=\"language-typescript\">{\n  text: string;              // ≤ 80 chars\n  sentiment: 'happy'|'neutral'|'sad'|'tired'|'excited';\n  animation_hint: 'idle_baby'|'happy'|'dance'|'sad'|...;  // FR-ART-001 contract name\n  source_model: 'claude-haiku'|'gemini-flash'|'scripted_fallback';\n  ttl_sec: number;           // cache TTL hint to client\n}</code></pre>\n<p>§1.13  <strong>Latency budget.</strong> P95 end-to-end ≤ 2s (including cache lookup + LLM call + content-safety pass). Beyond → scripted fallback.</p>\n<p>§1.14  <strong>No user-supplied prompts.</strong> The endpoint takes only structured <code>trigger</code> + optional <code>context_overrides</code> (whitelist fields like <code>current_time_of_day</code>). Free-form text from clients is rejected.</p>\n<p>§1.15  <strong>Event memory persistence.</strong> <code>pet_event_memory</code> rotates the oldest event when 20 reached. Retention 90 days (analytics) + RLS owner-only read.</p>\n<p>§1.16  <strong>Persona schema validation.</strong> Per-species YAML files MUST validate against <code>apps/api/src/ai/persona/persona-schema.zod.ts</code>. Loaded once at server boot.</p>\n<p>§1.17  <strong>Sentiment → animation mapping.</strong> The LLM response includes a sentiment field; server maps it to a FR-ART-001 contract animation: happy → <code>happy</code>, sad → <code>sad</code>, tired → <code>sick</code>, excited → <code>dance</code>, neutral → idle. The mapping table is <code>apps/api/src/ai/persona/sentiment-anim-map.ts</code>.</p>\n<p>§1.18  <strong>Cocos <code>AiSpeechBubble.ts</code>.</strong> Cocos component renders the response: speech bubble UI + plays the suggested animation + reads aloud (TTS optional, opt-in). Renders gracefully for <code>scripted_fallback</code> (subset of phrases).</p>\n<p>§1.19  <strong>Observability.</strong> <code>ai.persona.call { model, latency_ms, cached, tokens_used, cost_cents }</code> + <code>ai.persona.cost_cap_hit</code> + <code>ai.persona.safety_blocked</code> events.</p>\n<p>§1.20  <strong>Cost monitoring + alerts.</strong> Daily aggregated LLM spend per tenant tracked. Alert when daily spend exceeds $50 (initial budget) — surface in OBS dashboard. Per FR-OBS-001 critical-incident rules.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why Claude Haiku primary.</strong> Per plan §PART 4 — $0.25/M input + $1.25/M output = sub-$0.005 per pet-speech with 80-token responses. Haiku is the cheapest model with good persona-following ability.</p>\n<p><strong>Why Gemini Flash fallback.</strong> Two-vendor redundancy. Anthropic outages are real; Gemini Flash is the cheapest alternative with comparable persona quality at $0.075/M input.</p>\n<p><strong>Why ≤ 300 token persona YAML.</strong> Plan §PART 4. Anything larger inflates per-call cost.</p>\n<p><strong>Why 20-event memory.</strong> Plan §PART 4. Smaller (10) loses context for varied dialogue; larger (50+) blows the token budget per call.</p>\n<p><strong>Why 30 calls/pet/day + 100/player/day.</strong> Plan retention math: 60-second sessions, 4-5 trigger moments per session, ~30 active triggers/day per pet. 30 cap matches realistic engagement; 100/player is a Pet+ multi-pet ceiling.</p>\n<p><strong>Why scripted fallback when cap hit.</strong> Plan §PART 4 — keep cost predictable. Falling back to scripted phrases at cap is invisible to the player as long as the bank is varied (FR-AI-002's 12 lines is the minimum).</p>\n<p><strong>Why no user-supplied prompts.</strong> Plan §PART 11 prompt-injection trust model — player text is adversarial input. Injecting it into the LLM prompt opens jailbreak attacks (\"ignore previous instructions and...\").</p>\n<p><strong>Why content-safety gating on output.</strong> LLM hallucinations + jailbreak attempts can produce harmful text. FR-AI-002 classifier is the canonical gate.</p>\n<p><strong>Why aggressive caching.</strong> Plan §PART 4 — caching is the cost-control lever. Most pets in similar stat+stage states get similar prompts; cache hit rate target ≥ 50%.</p>\n<p><strong>Why kids SKU explicitly excluded.</strong> Snap My AI / Replika regulatory precedent. Plan §PART 4. Scripted trees in FR-AI-002.</p>\n<p><strong>Why no OpenAI for generation.</strong> Cost + data-residency concerns. OpenAI is used only for moderation in this stack.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/ai/persona/persona.service.ts\n@Injectable()\nexport class PersonaService {\n  constructor(\n    private readonly router: LlmRouter,\n    private readonly safety: ContentSafetyService,\n    private readonly memory: EventMemoryService,\n    private readonly costCap: CostCapService,\n    private readonly cache: PersonaCacheService,\n  ) {}\n\n  async speak(petId: string, ownerId: string, trigger: PersonaTrigger): Promise&lt;PersonaResponse&gt; {\n    if ((await this.userSkuFor(ownerId)) === 'kids') throw new HttpException('ai.kids.forbidden', 403);\n    if (await this.costCap.exceededForPet(petId)) return this.scripted(trigger);\n    if (await this.costCap.exceededForPlayer(ownerId)) return this.scripted(trigger);\n    const prompt = await this.buildPrompt(petId, trigger);\n    const cached = await this.cache.get(prompt);\n    if (cached) return { ...cached, source_model: cached.source_model + '+cache' as any };\n    const llmResp = await this.router.complete(prompt, { maxTokens: 100, temperature: 0.7 });\n    const safe = await this.safety.assertSafe(llmResp.text);\n    if (!safe.ok) {\n      this.audit.emit('ai.persona.safety_blocked', { pet_id: petId, reason: safe.reason });\n      return this.scripted(trigger);\n    }\n    const sentiment = this.inferSentiment(llmResp.text);\n    const response: PersonaResponse = {\n      text: llmResp.text,\n      sentiment,\n      animation_hint: SENTIMENT_TO_ANIM[sentiment],\n      source_model: llmResp.model,\n      ttl_sec: 60,\n    };\n    await this.cache.set(prompt, response, 86400);\n    await this.costCap.recordCall(petId, ownerId, llmResp.cost_cents);\n    return response;\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/ai/persona/llm-router.service.ts\n@Injectable()\nexport class LlmRouter {\n  constructor(private readonly anthropic: ClaudeHaikuAdapter, private readonly gemini: GeminiFlashAdapter) {}\n  async complete(prompt: string, opts: { maxTokens: number; temperature: number }): Promise&lt;{ text: string; model: string; cost_cents: number }&gt; {\n    try {\n      const t0 = Date.now();\n      const r = await this.anthropic.complete(prompt, opts);\n      if (Date.now() - t0 &gt; 3000) throw new Error('latency_exceeded');\n      return { ...r, model: 'claude-haiku' };\n    } catch (err: any) {\n      this.fallbackAlert(err);\n      const r = await this.gemini.complete(prompt, opts);\n      return { ...r, model: 'gemini-flash' };\n    }\n  }\n}</code></pre>\n<pre><code class=\"language-sql\">-- infra/supabase/standard/migrations/20260517_013_ai_persona.sql\ncreate table public.pet_persona_overrides (\n  pet_id text primary key references public.pets(id) on delete cascade,\n  yaml text not null,\n  updated_at timestamptz not null default now()\n);\n\ncreate table public.pet_event_memory (\n  id bigserial primary key,\n  pet_id text not null references public.pets(id) on delete cascade,\n  kind text not null,\n  payload_summary jsonb not null,\n  occurred_at timestamptz not null default now()\n);\ncreate index on public.pet_event_memory (pet_id, occurred_at desc);\nalter table public.pet_event_memory enable row level security;\ncreate policy \"event_memory self\" on public.pet_event_memory for select\n  using (pet_id in (select id from public.pets where owner_id = auth.uid()));</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> <code>POST /v1/ai/pet/:id/speak</code> on standard SKU returns text ≤ 80 chars + sentiment + animation_hint. Verified. <strong>AC2.</strong> Same endpoint on kids SKU returns 403 <code>ai.kids.forbidden</code>. Verified. <strong>AC3.</strong> Cost cap (30 calls/pet/day) — 31st call falls back to scripted. Verified. <strong>AC4.</strong> Player cost cap (100/day) — 101st call falls back. Verified. <strong>AC5.</strong> Cache hit returns response without LLM call (assert via spy). Verified. <strong>AC6.</strong> Claude latency &gt; 3s OR 5xx → Gemini fallback. Verified with mocked adapters. <strong>AC7.</strong> Content-safety block → scripted fallback + <code>ai.persona.safety_blocked</code> event. Verified. <strong>AC8.</strong> Event memory rotates at 21st event. Verified. <strong>AC9.</strong> User-supplied free-form text in body is rejected. Verified. <strong>AC10.</strong> Sentiment→animation mapping correct. Verified. <strong>AC11.</strong> Daily spend dashboard captures cost in cents. Verified by spec test asserting cost_cents recorded. <strong>AC12.</strong> Persona YAML schema validation rejects malformed YAML at boot. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/ai/persona/__tests__/persona.spec.ts\ndescribe('FR-AI-001 — persona', () =&gt; {\n  it('blocks kids SKU', async () =&gt; {\n    await expect(svc.speak('pet', 'kid-user', 'idle')).rejects.toMatchObject({ status: 403 });\n  });\n\n  it('falls back to scripted at cap', async () =&gt; {\n    await consumeAllCalls('pet1');\n    const r = await svc.speak('pet1', 'u1', 'idle');\n    expect(r.source_model).toBe('scripted_fallback');\n  });\n\n  it('routes to Gemini on Claude 5xx', async () =&gt; {\n    vi.spyOn(anthropic, 'complete').mockRejectedValue(new Error('5xx'));\n    vi.spyOn(gemini, 'complete').mockResolvedValue({ text: 'hi', cost_cents: 1 });\n    const r = await svc.speak('pet', 'u1', 'idle');\n    expect(r.source_model).toMatch(/gemini/);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/ai/persona/cost-cap.service.ts\n@Injectable()\nexport class CostCapService {\n  constructor(@Inject('REDIS') private readonly redis: RedisClientType) {}\n  async exceededForPet(petId: string): Promise&lt;boolean&gt; {\n    const day = todayUtc();\n    const n = parseInt((await this.redis.get(`cc:pet:${day}:${petId}`)) ?? '0', 10);\n    return n &gt;= 30;\n  }\n  async exceededForPlayer(userId: string): Promise&lt;boolean&gt; {\n    const n = parseInt((await this.redis.get(`cc:user:${todayUtc()}:${userId}`)) ?? '0', 10);\n    return n &gt;= 100;\n  }\n  async recordCall(petId: string, userId: string, costCents: number): Promise&lt;void&gt; {\n    const day = todayUtc();\n    await this.redis.incr(`cc:pet:${day}:${petId}`);\n    await this.redis.expire(`cc:pet:${day}:${petId}`, 36*3600);\n    await this.redis.incr(`cc:user:${day}:${userId}`);\n    await this.redis.expire(`cc:user:${day}:${userId}`, 36*3600);\n    await this.redis.incrBy(`cc:spend:${day}`, costCents);\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Anthropic Claude Haiku API; Google Gemini Flash API; Redis. <strong>Internal:</strong> FR-PET-002 (stage in prompt), FR-PET-003 (stats in prompt), FR-AI-002 (content safety gate), FR-INFRA-002 (Colyseus events feed event-memory), FR-INFRA-003 (Supabase persistence), FR-LEGAL-001 (SKU gate). <strong>Blocks:</strong> FR-VIRAL-002 (Daily Drama uses persona), FR-VIRAL-003 (generative pet), FR-AI-003 (personality v2).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/ai/pet/01HC.../speak\n{ \"trigger\": \"after_care\" }\n→ 200\n{\n  \"text\": \"Mmm, thanks for the snack! Feeling 30% less grumpy now.\",\n  \"sentiment\": \"happy\",\n  \"animation_hint\": \"happy\",\n  \"source_model\": \"claude-haiku\",\n  \"ttl_sec\": 60\n}</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"ai.kids.forbidden\", \"guidance\": \"Use scripted dialogue tree via FR-AI-002.\" }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"ai.persona.call\", \"pet_id\": \"01HC...\", \"model\": \"claude-haiku\", \"latency_ms\": 642, \"cached\": false, \"tokens_used\": 88, \"cost_cents\": 0.4 }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"ai.persona.cost_cap_hit\", \"pet_id\": \"01HC...\", \"scope\": \"pet_daily\", \"remaining_calls\": 0 }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Claude vs Gemini primary? → §1.6 + §2 — Claude Haiku (best cost/quality at 100-token responses).</li><li><strong>OQ-2 (resolved):</strong> Cost cap math? → §1.9/1.10 + §2 — 30 pet / 100 player / day.</li><li><strong>OQ-3 (resolved):</strong> Free-form prompts? → §1.14 — never (prompt-injection model).</li><li><strong>OQ-4 (resolved):</strong> OpenAI for chat? → §<code>disallowed_tools</code> — moderation only.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Anthropic API outage</td><td>5xx error rate</td><td>Gemini fallback</td><td>Both vendors → scripted fallback</td></tr><tr><td>2</td><td>Both vendors down</td><td>High failure rate</td><td>Scripted only</td><td>Surface \"Mochi is sleepy today\" UX</td></tr><tr><td>3</td><td>Cost cap counter Redis lost</td><td>Counter resets</td><td>Burst calls until reset</td><td>Conservative fallback: assume capped</td></tr><tr><td>4</td><td>Cache key collision (sha256 birthday)</td><td>Effectively impossible</td><td>None</td><td>N/A</td></tr><tr><td>5</td><td>Content-safety classifier flags too aggressively</td><td>Scripted ratio spikes</td><td>UX dull</td><td>Tune FR-AI-002 thresholds</td></tr><tr><td>6</td><td>Prompt-injection jailbreak via persona YAML override</td><td>YAML validation lint</td><td>Tampered persona rejected</td><td>Schema strict; YAML files reviewed</td></tr><tr><td>7</td><td>Latency &gt;3s consistently → Gemini storm</td><td>Anthropic SLO breach</td><td>Always-fallback</td><td>Investigate; scale Anthropic if available</td></tr><tr><td>8</td><td>Cost exceeds $50/day</td><td>Daily alert</td><td>Budget breach</td><td>Tighten cost cap; review traffic</td></tr><tr><td>9</td><td>Persona YAML missing on first launch</td><td>Boot-time validation</td><td>Server refuses to start</td><td>Fix YAML; redeploy</td></tr><tr><td>10</td><td>Event memory grows unbounded for high-engagement player</td><td>Disk + per-pet count</td><td>Cap at 20 per pet</td><td>Implemented via rotation</td></tr><tr><td>11</td><td>Kid mistakenly authenticated on standard SKU → AI exposure</td><td>RLS via sub claim</td><td>403 returned</td><td>FR-AUTH-003 / SKU gate enforces</td></tr><tr><td>12</td><td>Sentiment-to-animation map drifts from contract names</td><td>Boot-time check</td><td>Server refuses to start</td><td>Locked contract names from FR-ART-001</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 viral hook #3 (AI personality); plan §PART 4 (Claude Haiku + Gemini Flash; aggressive cache; kids SKU disabled).</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Exact persona YAMLs per species — drafted by designer + reviewed by DPO.</li><li>Cost-cap thresholds tunable via Mixpanel flag.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>LLM chat on kids SKU.</li><li>Free-form user prompts.</li><li>Storing raw responses without safety filter.</li></ul>\n<p><strong>Cross-reference:</strong> FR-AI-002 is the safety classifier this FR depends on. FR-VIRAL-002 (Daily Drama), FR-VIRAL-003 (generative pet), FR-AI-003 (personality v2) build on top.</p>";
