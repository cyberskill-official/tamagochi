window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SOCIAL-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Trade definition.</strong> A trade is an atomic exchange of exactly 2 pets between 2 players. Player A offers pet X; player B offers pet Y; on mutual confirmation, ownership swaps in a single Postgres transaction.</p>\n<p>§1.2  <strong>Eligibility — pets.</strong> Pets must be: <code>status='active'</code> (not grandma, not tombstoned); <code>stage='adult'</code> (no baby/teen trading); owned by the offering player; not the player's first-ever-hatched pet (prevents fresh-account fraud); not currently incubating offspring (FR-PET-007).</p>\n<p>§1.3  <strong>Eligibility — players.</strong> Both players must be friends (FR-SOCIAL-001) AND both on standard SKU (kids cannot trade — defer to parental dashboard P3+). Both must have at least 1 free pet slot (receiver) or be trading equal-count (1-for-1).</p>\n<p>§1.4  <strong>Trade window — 4 phases.</strong></p>\n<ul><li><strong>Offer</strong>: A proposes pet X for B's pet Y. State <code>offered</code>. B has 5 min to lock.</li><li><strong>Lock</strong>: B locks their side (commits to the trade). State <code>locked</code>. A has 60s to confirm.</li><li><strong>Confirm</strong>: A confirms after seeing B's lock. State <code>confirmed</code>.</li><li><strong>Swap</strong>: Server executes atomic swap. State <code>completed</code>.</li></ul>\n<p>§1.5  <strong>Both-sides-show UI.</strong> Per plan §PART 3 + plan §PART 10 #7 — at lock + confirm phases, BOTH parties see EXACTLY the same trade summary: their offered pet, the other's offered pet, with full details (species, tier, palette, stat snapshot). No hidden modifiers.</p>\n<p>§1.6  <strong>Cancellation.</strong> Any party can cancel before <code>confirmed</code> state. After confirm, trade is committed and irreversible.</p>\n<p>§1.7  <strong>Timeout.</strong> Any state held &gt; 5 min auto-cancels. Sent as <code>trade.timeout</code> event.</p>\n<p>§1.8  <strong>Atomic swap.</strong> The actual swap MUST be a single Postgres transaction:</p>\n<pre><code class=\"language-sql\">BEGIN;\nUPDATE pets SET owner_id = B WHERE id = X AND owner_id = A;\nUPDATE pets SET owner_id = A WHERE id = Y AND owner_id = B;\n-- assert both updated:\nCOMMIT;</code></pre>\n<p>If either UPDATE returns 0 rows (e.g. pet ownership changed during trade), ROLLBACK + emit <code>trade.swap.race_detected</code>.</p>\n<p>§1.9  <strong>Endpoint — propose.</strong> <code>POST /v1/trades</code> body <code>{ partner_user_id: string, my_pet_id: string, their_pet_id: string }</code>.</p>\n<p>§1.10  <strong>Endpoint — lock.</strong> <code>POST /v1/trades/:tradeId/lock</code>. Only the recipient (B) can lock.</p>\n<p>§1.11  <strong>Endpoint — confirm.</strong> <code>POST /v1/trades/:tradeId/confirm</code>. Only the proposer (A) can confirm.</p>\n<p>§1.12  <strong>Endpoint — cancel.</strong> <code>DELETE /v1/trades/:tradeId</code>.</p>\n<p>§1.13  <strong>Endpoint — list.</strong> <code>GET /v1/trades</code> returns active + recent (7d) trades.</p>\n<p>§1.14  <strong>Rate limit.</strong> Per player: 10 trades initiated per day, 30 per week. Beyond → 429.</p>\n<p>§1.15  <strong>Per-pair rate-limit.</strong> Per friend-pair: 3 trades per 7 days (anti-laundering).</p>\n<p>§1.16  <strong>No real-money fees.</strong> Per FR-LEGAL-002 §1.1 — trade is free.</p>\n<p>§1.17  <strong>Audit.</strong> Every trade state transition + the final swap writes to <code>trades</code> table + emits analytics. 7-year retention for kids-adjacent + 2-year standard.</p>\n<p>§1.18  <strong>Anti-cheat — impossible-state.</strong> If a player attempts to trade a grandma pet OR a pet they don't own OR a pet currently in a different trade — FR-INFRA-002 §1.9 impossible-transition path engaged.</p>\n<p>§1.19  <strong>TradeRoom Colyseus.</strong> Each active trade has a dedicated Colyseus <code>TradeRoom</code> with <code>maxClients=2</code>. Real-time state updates (offer / lock / confirm) broadcast to both clients. Disconnect during trade pauses the timeout clock + on rejoin resumes.</p>\n<p>§1.20  <strong>Analytics.</strong> <code>social.trade.proposed</code>, <code>social.trade.locked</code>, <code>social.trade.confirmed</code>, <code>social.trade.completed</code>, <code>social.trade.cancelled { trigger }</code>, <code>social.trade.timeout</code>, <code>social.trade.race_detected</code> per FR-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 4-phase state machine.</strong> Plan §PART 10 #7 Adopt Me precedent — single-confirm trades led to bait-and-switch. The lock + confirm sequence forces both parties to see the final state before committing.</p>\n<p><strong>Why both-sides-show.</strong> Plan §PART 3 — every Adopt Me scam exploit involved last-second swap. Mandatory full-detail display closes this.</p>\n<p><strong>Why adult-only.</strong> Plan §PART 3 — adult-stage gating preserves tier identity (baby/teen pets haven't accrued value).</p>\n<p><strong>Why no first-pet trade.</strong> Anti-fraud — fresh accounts traded as throwaway assets.</p>\n<p><strong>Why timeout 5 min.</strong> Long enough to read details; short enough that an abandoned trade releases the involved pets back to the players.</p>\n<p><strong>Why per-pair rate limit.</strong> Anti-laundering — bot rings that trade among themselves to mint rare tiers.</p>\n<p><strong>Why atomic transaction.</strong> Plan §PART 4 anti-cheat — partial-state-failure must be impossible.</p>\n<p><strong>Why no real-money fee.</strong> Plan §PART 8 — fee-gated trade trades is loot-box-adjacent.</p>\n<p><strong>Why standard-SKU only.</strong> FR-AUTH-003 + COPPA — kid trades require parental dashboard.</p>\n<p><strong>Why Colyseus TradeRoom (not direct API).</strong> Real-time both-sides-show requires sub-second broadcast. Polling would feel laggy.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/social/trade/trade.service.ts (excerpt)\nasync propose(u: AuthedUser, partnerId: string, myPetId: string, theirPetId: string) {\n  await this.rateLimit.assert({ key: `trade:user:${u.id}`, perDay: 10, perWeek: 30 });\n  await this.pairRateLimit.assert(u.id, partnerId);\n  await this.assertEligibility(u, partnerId, myPetId, theirPetId);\n  const tradeId = generateUlid();\n  const { data: trade } = await this.supa.from('trades').insert({\n    id: tradeId, proposer_id: u.id, recipient_id: partnerId,\n    proposer_pet_id: myPetId, recipient_pet_id: theirPetId,\n    state: 'offered',\n    expires_at: new Date(Date.now() + 5 * 60 * 1000).toISOString(),\n  }).select().single();\n  await this.notify.sendTradeProposal(partnerId, trade);\n  return trade;\n}\n\nasync confirm(u: AuthedUser, tradeId: string) {\n  const trade = await this.supa.from('trades').select('*').eq('id', tradeId).single();\n  if (trade.data.proposer_id !== u.id) throw new HttpException('forbidden', 403);\n  if (trade.data.state !== 'locked') throw new HttpException('trade.state.invalid', 422);\n  return this.atomicSwap(trade.data);\n}\n\nprivate async atomicSwap(trade: TradeRow) {\n  const { data, error } = await this.supa.rpc('execute_trade_swap', {\n    _trade_id: trade.id, _proposer_id: trade.proposer_id, _recipient_id: trade.recipient_id,\n    _proposer_pet_id: trade.proposer_pet_id, _recipient_pet_id: trade.recipient_pet_id,\n  });\n  if (error) { this.audit.emit('social.trade.race_detected', { trade_id: trade.id }); throw error; }\n  await this.audit.emit('social.trade.completed', { trade_id: trade.id });\n  return { trade_id: trade.id, state: 'completed' };\n}</code></pre>\n<pre><code class=\"language-sql\">-- migration\ncreate table public.trades (\n  id text primary key check (id ~ '^[0-9A-HJKMNPQRSTVWXYZ]{26}$'),\n  proposer_id uuid not null references auth.users(id) on delete cascade,\n  recipient_id uuid not null references auth.users(id) on delete cascade,\n  proposer_pet_id text not null references public.pets(id) on delete cascade,\n  recipient_pet_id text not null references public.pets(id) on delete cascade,\n  state text not null check (state in ('offered','locked','confirmed','completed','cancelled','timeout')),\n  proposed_at timestamptz not null default now(),\n  locked_at timestamptz,\n  confirmed_at timestamptz,\n  expires_at timestamptz not null,\n  cancelled_by uuid,\n  tenant_id text not null default 'mochi'\n);\ncreate index on public.trades (proposer_id, proposed_at desc);\ncreate index on public.trades (recipient_id, proposed_at desc);\ncreate index on public.trades (state) where state in ('offered','locked','confirmed');\n\ncreate or replace function execute_trade_swap(_trade_id text, _proposer_id uuid, _recipient_id uuid, _proposer_pet_id text, _recipient_pet_id text)\n  returns void security definer language plpgsql as $$\nbegin\n  perform 1 from public.trades where id = _trade_id and state = 'locked' for update;\n  update public.pets set owner_id = _recipient_id where id = _proposer_pet_id and owner_id = _proposer_id and status = 'active';\n  if not found then raise exception 'trade.race'; end if;\n  update public.pets set owner_id = _proposer_id where id = _recipient_pet_id and owner_id = _recipient_id and status = 'active';\n  if not found then\n    -- Roll back the first swap.\n    update public.pets set owner_id = _proposer_id where id = _proposer_pet_id;\n    raise exception 'trade.race';\n  end if;\n  update public.trades set state = 'completed', confirmed_at = now() where id = _trade_id;\nend;\n$$;</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Full 4-phase flow (offer → lock → confirm → swap) works. Verified. <strong>AC2.</strong> Both-sides-show identical content on lock + confirm. Verified by Colyseus state broadcast inspection. <strong>AC3.</strong> Atomic swap rolls back on race condition. Verified by chaos test forcing ownership change mid-swap. <strong>AC4.</strong> 5-min timeout on each phase. Verified. <strong>AC5.</strong> Cross-SKU/tenant rejected. Verified. <strong>AC6.</strong> Adult-only enforced. Verified. <strong>AC7.</strong> First-pet trade rejected. Verified. <strong>AC8.</strong> Per-user 10/day + per-pair 3/7d enforced. Verified. <strong>AC9.</strong> Cancel before confirm works; after confirm impossible. Verified. <strong>AC10.</strong> No real-money fees in path. Verified. <strong>AC11.</strong> Disconnect pauses timeout. Verified. <strong>AC12.</strong> Audit row + analytics events all emit. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/social/trade/__tests__/atomic-swap.spec.ts\ndescribe('FR-SOCIAL-003 — atomic swap', () =&gt; {\n  it('rolls back if pet ownership changed during swap (chaos test)', async () =&gt; {\n    await proposeAndLock('u1', 'u2', 'petA', 'petB');\n    await db.pets.update({ owner_id: 'u3' }).where({ id: 'petA' });  // chaos: third party stole pet\n    await expect(svc.confirm(user('u1'), 'trade1')).rejects.toThrow(/trade.race/);\n    expect((await db.pets.byId('petB')).owner_id).toBe('u2');  // petB stayed with u2\n  });\n\n  it('completes successfully under normal conditions', async () =&gt; {\n    await proposeAndLock('u1', 'u2', 'petA', 'petB');\n    const r = await svc.confirm(user('u1'), 'trade1');\n    expect(r.state).toBe('completed');\n    expect((await db.pets.byId('petA')).owner_id).toBe('u2');\n    expect((await db.pets.byId('petB')).owner_id).toBe('u1');\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/realtime/src/rooms/TradeRoom.ts\nexport class TradeRoom extends _BaseTenantRoom {\n  maxClients = 2;\n  override async onCreate(opts: { trade_id: string }) {\n    this.setState(new TradeWindowState());\n    this.onMessage('lock', (client) =&gt; this.handleLock(client));\n    this.onMessage('confirm', (client) =&gt; this.handleConfirm(client));\n    this.clock.setTimeout(() =&gt; this.timeout(), 5 * 60 * 1000);\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Postgres atomic transaction; Colyseus. <strong>Internal:</strong> FR-PET-005 (slot check), FR-PET-008 (status check), FR-SOCIAL-001 (friend check), FR-INFRA-002 (TradeRoom), FR-INFRA-003 (Supabase). <strong>Blocks:</strong> none.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/trades\n{ \"partner_user_id\": \"01HU...\", \"my_pet_id\": \"petA\", \"their_pet_id\": \"petB\" }\n→ 201 { \"trade_id\": \"01HTRADE...\", \"state\": \"offered\", \"expires_at\": \"...\" }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"social.trade.completed\", \"trade_id\": \"01HTRADE...\", \"proposer_id\": \"u1\", \"recipient_id\": \"u2\", \"pets_swapped\": { \"to_proposer\": \"petB\", \"to_recipient\": \"petA\" } }</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"trade.race\", \"trade_id\": \"01HTRADE...\" }</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"trade.eligibility\", \"reason\": \"first_pet_protected\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> 4-phase vs 2-phase? → §1.4 + §2 — 4-phase.</li><li><strong>OQ-2 (resolved):</strong> Cancel after confirm? → §1.6 — no.</li><li><strong>OQ-3 (resolved):</strong> Multi-pet trade? → §1.1 — 1-for-1 only.</li><li><strong>OQ-4 (resolved):</strong> Currency in trade? → §1.16 — no.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Race during atomic swap</td><td><code>trade.race</code> exception</td><td>Rollback</td><td>Audit + UX retry</td></tr><tr><td>2</td><td>Disconnect during confirm</td><td>Colyseus tolerates</td><td>Timeout extended</td><td>Resume on rejoin</td></tr><tr><td>3</td><td>Per-pair rate-limit bypassed via account swap</td><td>Manual review</td><td>Limited recourse</td><td>DPO escalation</td></tr><tr><td>4</td><td>First-pet protection bypassed via cascade</td><td>Spec test</td><td>Fraud</td><td>Validation tightened</td></tr><tr><td>5</td><td>Cross-tenant trade attempted</td><td>RLS</td><td>Blocked</td><td>Audit</td></tr><tr><td>6</td><td>Timeout clock skew</td><td>Server time sync</td><td>Trades expire early/late</td><td>NTP-locked</td></tr><tr><td>7</td><td>Idempotent re-confirm</td><td>Idempotency</td><td>Same response</td><td>Cached</td></tr><tr><td>8</td><td>Both lock simultaneously (different rooms)</td><td>Per-trade Colyseus room</td><td>Each independent</td><td>OK</td></tr><tr><td>9</td><td>Trade with grandma pet</td><td>Eligibility check</td><td>422</td><td>Verified</td></tr><tr><td>10</td><td>Pet stolen mid-trade by trade.race</td><td>Detected</td><td>Rollback</td><td>Compensating</td></tr><tr><td>11</td><td>Audit retention</td><td>DPO</td><td>7-yr kids/2-yr standard</td><td>Configured</td></tr><tr><td>12</td><td>Both-sides-show mismatched (UI bug)</td><td>Visual regression</td><td>Trust collapse</td><td>Snapshot test</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 (trust-trade), plan §PART 10 #7 (Adopt Me scam precedent).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Rate limits Mixpanel-tunable.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Real-money fees.</li><li>Off-platform trades.</li><li>Hidden modifiers in trade view.</li><li>First-pet trade.</li></ul>\n<p><strong>Cross-reference:</strong> FR-PET-005 (slot check), FR-PET-008 (status check), FR-SOCIAL-001 (friend check).</p>";
