window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CARE-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Endpoint.</strong> <code>POST /v1/pets/:petId/clean</code> — body <code>{ soap_id: 'basic'|'premium', idempotency_key: string }</code>. JWT required.</p>\n<p>§1.2  <strong>Stage gate.</strong> Permitted only in <code>baby|teen|adult</code> per FR-PET-002 §1.6.</p>\n<p>§1.3  <strong>Ownership / co-parent check.</strong> Owner OR <code>active</code> co-parent (FR-SOCIAL-002 P2). Else 403.</p>\n<p>§1.4  <strong>Soap catalogue.</strong></p>\n<ul><li><code>basic</code>: 5 Coins, cleanliness +60, cooldown 10 min.</li><li><code>premium</code> (fragrant): 20 Coins, cleanliness +85 + happiness +3, cooldown 20 min.</li></ul>\n<p>§1.5  <strong>Currency spend.</strong> Stub at P1 via FR-ECON-001 ledger.</p>\n<p>§1.6  <strong>Cooldown.</strong> Per (pet, soap) Redis sliding-window; 429 with <code>retry_after_sec</code> on miss.</p>\n<p>§1.7  <strong>Stat apply.</strong> <code>StatDecayService.apply(stats, 'clean')</code> per FR-PET-003 §3.1 (cleanliness +60 baseline) for <code>basic</code>; premium adds +25 cleanliness + 3 happiness via extended action key <code>clean_premium</code>.</p>\n<p>§1.8  <strong>Energy cost.</strong> Clean costs energy -5 per FR-PET-003 §1.4. Energy can drop to 0; pet \"sleepy\" UX when energy &lt; 20.</p>\n<p>§1.9  <strong>Last-seen update.</strong> Per FR-PET-001 §1.11.</p>\n<p>§1.10  <strong>Audit + broadcast.</strong> <code>care_actions</code> row with action='clean' + <code>pet.cleaned</code> broadcast.</p>\n<p>§1.11  <strong>Animation handshake.</strong> 5-sec HMAC token returned; Cocos plays <code>care_clean</code> Spine animation.</p>\n<p>§1.12  <strong>Bubble particle.</strong> <code>BubbleParticle.ts</code> spawns 30 Particle2D bubble emitters at the bath origin point. Particle count cap = 30; under reduce-motion, count drops to 6 + fade. Particle pool reused.</p>\n<p>§1.13  <strong>Haptic.</strong> <code>HapticsAdapter.tap('light')</code> on success.</p>\n<p>§1.14  <strong>Rate limit.</strong> Overall 30 cleans/hr per player.</p>\n<p>§1.15  <strong>Analytics.</strong> <code>care.clean.success / failure { reason }</code>.</p>\n<p>§1.16  <strong>Impossible-state.</strong> Cleaning at cleanliness=100 → 422 <code>pet.stat.already_full</code> + <code>security.impossible-transition</code>.</p>\n<p>§1.17  <strong>Idempotency.</strong> Same shape as FR-CARE-001 §1.10.</p>\n<p>§1.18  <strong>Source attribution.</strong> <code>ui</code> / <code>co_parent_remote</code>.</p>\n<p>§1.19  <strong>No real-money soap</strong> per FR-LEGAL-002.</p>\n<p>§1.20  <strong>A11Y reduce-motion.</strong> Particle count + animation duration respect <code>prefers-reduced-motion</code>.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p>Mirrors FR-CARE-001 pattern (server-authoritative + cooldown + idempotent + audit). Differences:</p>\n<ul><li><strong>Energy cost on clean.</strong> Plan §PART 3 — real-world \"bathing takes energy.\" Slight penalty differentiates clean from other actions; player learns \"don't bath at 0 energy.\"</li><li><strong>Lower cooldown ceiling than feed.</strong> Cleanliness decays slower (FR-PET-003 §1.2) so the player legitimately cleans less; 10-20 min cooldowns match the natural cadence.</li><li><strong>Bubble particles.</strong> Plan §PART 5 visual delight + plan §PART 3 \"haptic moments.\" The particle pool is reused (no GC churn) and capped (no perf cliff on low-end devices).</li><li><strong>Reduce-motion cap.</strong> FR-A11Y-001 — particle bursts can trigger vestibular symptoms; reduce to 6 with fade.</li><li><strong>30/hr rate-limit (vs feed 60).</strong> Cleanliness has lower replay potential; tighter cap is fine.</li></ul>\n<p>Cross-references to CARE-001 are explicit so the implementer can follow the proven pattern.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/care/clean.controller.ts\n@Controller('v1/pets')\n@UseGuards(SupabaseJwtGuard)\nexport class CleanController {\n  constructor(private readonly svc: CleanService) {}\n  @Post(':petId/clean')\n  async clean(@CurrentUser() u: AuthedUser, @Param('petId') petId: string,\n              @Body() body: { soap_id: 'basic'|'premium'; idempotency_key: string }) {\n    return this.svc.clean(u, petId, body.soap_id, body.idempotency_key);\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/care/soap-catalogue.ts\nexport const SOAP_CATALOGUE = {\n  basic:   { cost: 5,  cooldown_sec: 10 * 60, cleanliness_delta: 60, energy_delta: -5 },\n  premium: { cost: 20, cooldown_sec: 20 * 60, cleanliness_delta: 85, happiness_delta: 3, energy_delta: -5 },\n} as const;</code></pre>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/care/BubbleParticle.ts\nimport { _decorator, Component, ParticleSystem2D } from 'cc';\nimport { prefersReducedMotion } from '../a11y/preferences';\nconst { ccclass, property } = _decorator;\n\n@ccclass('BubbleParticle')\nexport class BubbleParticle extends Component {\n  @property(ParticleSystem2D) emitter!: ParticleSystem2D;\n  burst() {\n    const count = prefersReducedMotion() ? 6 : 30;\n    this.emitter.totalParticles = count;\n    this.emitter.duration = prefersReducedMotion() ? 0.4 : 1.0;\n    this.emitter.resetSystem();\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/care/clean.service.ts (skeleton highlight)\nasync clean(u: AuthedUser, petId: string, soapId: 'basic'|'premium', idempKey: string) {\n  // ... idempotency / rate-limit / ownership / stage / cleanliness=100 / cooldown / currency / stat apply ...\n  const action = soapId === 'basic' ? 'clean' : 'clean_premium';\n  const after = this.stats.apply(before, action as any);\n  // ... persist + audit + broadcast ...\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Clean basic on cleanliness=20 returns 200 with cleanliness_after=80. <strong>AC2.</strong> Stage-locked for egg / grandma_house → 403. <strong>AC3.</strong> Clean at cleanliness=100 → 422. <strong>AC4.</strong> Cooldown enforced (10 min basic / 20 min premium). <strong>AC5.</strong> Energy decrement on clean: -5. <strong>AC6.</strong> Rate-limit 30/hr. <strong>AC7.</strong> Cross-owner attempt → 403. <strong>AC8.</strong> Idempotent on repeat key. <strong>AC9.</strong> Bubble particles cap at 30 (6 under reduce-motion). Verified by Cocos test. <strong>AC10.</strong> <code>pet.cleaned</code> broadcast over PetRoom. <strong>AC11.</strong> Animation handshake — Cocos rejects unsigned token. <strong>AC12.</strong> Audit row written with action='clean' + correct deltas.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/care/__tests__/clean.spec.ts\nimport { describe, it, expect } from 'vitest';\nimport { CleanService } from '../clean.service';\n\ndescribe('FR-CARE-002 — clean', () =&gt; {\n  it('basic +60 cleanliness, -5 energy', async () =&gt; {\n    const r = await svc.clean(user('u1'), 'pet-mochi', 'basic', 'k1');\n    expect(r.cleanliness_after).toBe(80);\n    expect(r.energy_after).toBe(95);\n  });\n\n  it('clamps at 100', async () =&gt; {\n    const r = await svc.clean(user('u1'), 'pet-mostly-clean', 'premium', 'k2');\n    expect(r.cleanliness_after).toBe(100);\n  });\n\n  it('rejects at cleanliness=100', async () =&gt; {\n    await expect(svc.clean(user('u1'), 'pet-clean', 'basic', 'k3')).rejects.toMatchObject({ status: 422 });\n  });\n});</code></pre>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/care/__tests__/CleanAction.spec.ts\ndescribe('FR-CARE-002 §1.12 — bubble particle reduce-motion', () =&gt; {\n  it('caps at 6 particles under reduce-motion', () =&gt; {\n    vi.spyOn(a11y, 'prefersReducedMotion').mockReturnValue(true);\n    const b = new BubbleParticle();\n    b.emitter = { totalParticles: 0, duration: 0, resetSystem: vi.fn() } as any;\n    b.burst();\n    expect(b.emitter.totalParticles).toBe(6);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/realtime/src/rooms/handlers/clean.handler.ts\nexport function installCleanHandler(room: PetRoom, svc: CleanService) {\n  room.onMessage('clean.intent', async (client, payload: { pet_id: string; soap_id: 'basic'|'premium'; idempotency_key: string }) =&gt; {\n    try {\n      const result = await svc.clean(client.user, payload.pet_id, payload.soap_id, payload.idempotency_key);\n      client.send('clean.success', result);\n      room.broadcast('pet.cleaned', result, { except: client });\n    } catch (err: any) {\n      client.send('clean.failure', { error: err.message, status: err.status });\n    }\n  });\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Redis (cooldown); Cocos Particle2D runtime. <strong>Internal:</strong> FR-CARE-001 (template), FR-PET-003 (stat apply + clean_premium action key needs adding to FR-PET-003 §3.2 stat-config), FR-ART-001 (care_clean animation), FR-INFRA-002 (Colyseus broadcast). <strong>Blocks:</strong> FR-CARE-005 (streak detection reads care_actions), FR-VIRAL-002 (Daily Drama can reference clean events).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/pets/01HC.../clean\n{ \"soap_id\": \"basic\", \"idempotency_key\": \"01HCCLEAN...\" }\n\n→ 200 OK\n{ \"pet_id\": \"01HC...\", \"soap_id\": \"basic\", \"cleanliness_after\": 80, \"energy_after\": 95, \"animation_token\": \"eyJ...\" }</code></pre>\n<pre><code class=\"language-json\">{\n  \"event\": \"care.clean.success\",\n  \"pet_id\": \"01HC...\",\n  \"soap_id\": \"basic\",\n  \"cleanliness_before\": 20,\n  \"cleanliness_after\": 80,\n  \"energy_delta\": -5,\n  \"source\": \"ui\"\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"id\": 4243,\n  \"action\": \"clean\",\n  \"cleanliness_before\": 20, \"cleanliness_after\": 80,\n  \"energy_before\": 100, \"energy_after\": 95,\n  \"source\": \"ui\"\n}</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"cooldown\", \"retry_after_sec\": 540 }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Premium soap stat shape? → §1.4 — +85 cleanliness + 3 happiness.</li><li><strong>OQ-2 (resolved):</strong> Particle count cap? → §1.12 — 30 default, 6 reduced-motion.</li><li><strong>OQ-3 (resolved):</strong> Energy cost reasonable? → §1.8 + §2 — slight penalty creates intuition.</li><li><strong>OQ-4 (resolved):</strong> Co-parent visibility same as feed? → §1.10 — same broadcast pattern.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Redis cooldown fail-closed</td><td>Cooldown service errors</td><td>All cleans rejected</td><td>Service-degraded UX</td></tr><tr><td>2</td><td>Particle pool exhausted on rapid clean spam</td><td>Cocos warning</td><td>Bubbles missing</td><td>Pool extension or cap visual</td></tr><tr><td>3</td><td>Reduce-motion check fails to read OS pref</td><td>a11y service errors</td><td>Full 30 particles even under preference</td><td>Default to reduced count on uncertainty</td></tr><tr><td>4</td><td>Energy clamps to 0 mid-clean → frustrated user</td><td>Stat audit</td><td>UX cue surfaces \"Mochi too tired\"</td><td>Pre-action energy check + warning</td></tr><tr><td>5</td><td>Concurrent clean + feed mutate stats out-of-order</td><td>Per-pet mutex catches</td><td>Race avoided</td><td>(covered by FR-PET-003 §10 row 12)</td></tr><tr><td>6</td><td>Premium soap accidentally surfaced as IAP</td><td>FR-LEGAL-002 lint</td><td>Build blocked</td><td>Catalogue field guarded by lint</td></tr><tr><td>7</td><td>Bubble particle texture missing</td><td>Cocos error</td><td>Fallback shape</td><td>Lottie fallback or text \"✨\"</td></tr><tr><td>8</td><td>Rate-limit collides with multi-pet user</td><td>Support ticket</td><td>Player blocked</td><td>Raise to 60/hr for Pet+</td></tr><tr><td>9</td><td>Co-parent broadcast lost</td><td>UX desync</td><td>Receipt notification missed</td><td>Persistent state on rejoin</td></tr><tr><td>10</td><td>Animation token expires before client plays</td><td>UX hang</td><td>Animation skipped</td><td>Static end-pose snap; Sentry warn</td></tr><tr><td>11</td><td>Audit growth from frequent cleans</td><td>Disk</td><td>90-day retention policy</td></tr><tr><td>12</td><td>Stage transition mid-clean (rare)</td><td>OK — cooldown survives</td><td>None</td><td>Cooldown is stage-agnostic</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 (clean as one of 4 care primitives), plan §PART 5 (haptic moments + particles), plan §PART 8 (no real-money soap).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Soap costs (5/20) Mixpanel-tunable; cleanliness deltas (60/85) tunable.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong> Real-money soap; cleaning grandma_house; particle bursts ignoring reduce-motion.</p>\n<p><strong>Cross-reference:</strong> FR-CARE-003/004 follow same pattern. FR-PET-003 §3.2 needs an extension <code>clean_premium</code> action key (-5 energy, +85 cleanliness, +3 happiness) — flagged in §7.</p>";
