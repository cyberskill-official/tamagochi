window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Zalo OA app registration.</strong> A Zalo Official Account (OA) app MUST be registered under CyberSkill's corporate Zalo Business account (zaloOA <code>cyberskill.world</code> or equivalent). The OA's app id, OA secret, and Login API credentials MUST be recorded in Supabase secrets (<code>ZALO_APP_ID</code>, <code>ZALO_APP_SECRET</code>, <code>ZALO_OA_ID</code>). Privacy policy URL + ToS URL + redirect URI MUST be registered in the Zalo developer portal.</p>\n<p>§1.2  <strong>Zalo Login API v4.</strong> The OAuth 2.0 flow MUST use Zalo Login API v4 with PKCE (Proof Key for Code Exchange) per RFC 7636. The Cocos client generates <code>code_verifier</code> + <code>code_challenge</code>; the native bridge passes the challenge to Zalo's authorize endpoint. The exchanged <code>access_token</code> + <code>refresh_token</code> are received server-side via the redirect URI callback.</p>\n<p>§1.3  <strong>Native bridges.</strong> iOS uses the Zalo OA SDK (<code>ZaloSDK.framework</code>); Android uses the Zalo OA SDK AAR (<code>com.zing.zalo.zalosdk:core:4+</code>); web uses Zalo's OAuth 2.0 redirect flow (no SDK — direct URL redirect). Each bridge MUST: (a) trigger Zalo authorization; (b) receive Zalo's auth code; (c) emit to Cocos via the documented native bridge interface (same <code>bridge_version</code> field as FR-AUTH-001).</p>\n<p>§1.4  <strong>Server-side code exchange.</strong> The Cocos client MUST NOT exchange the auth code for an access token directly — Zalo's access-token endpoint requires the OA secret which MUST NEVER be client-side. The Cocos client sends the auth code + PKCE verifier to <code>POST /v1/auth/zalo/callback</code>; the Nest API exchanges with Zalo's token endpoint, fetches the user profile, and creates/updates the Supabase user.</p>\n<p>§1.5  <strong>Custom Supabase external provider.</strong> Supabase Auth does not ship a built-in Zalo provider. The implementation MUST register Zalo as a <strong>custom external provider</strong> via Supabase Auth's <code>sign_in_with_id_token</code> semantics: the Nest API mints a deterministic Zalo-derived JWT (signed with <code>SUPABASE_AUTH_SECRET</code>) representing the Zalo identity, then exchanges it via Supabase's admin API to create / sign-in the user.</p>\n<p>§1.6  <strong>Profile fetch — minimal scope.</strong> The OAuth scope MUST be the minimum required: <code>id</code> + <code>name</code> + (<code>picture</code> only when the player explicitly opts in). The Zalo Graph API <code>GET /v3.0/me</code> MUST be called with <code>Authorization: Bearer &lt;token&gt;</code> on the server. PHONE and EMAIL scopes are explicitly NOT requested (PDPL data minimisation).</p>\n<p>§1.7  <strong>Account linking.</strong> A user who has previously signed in via Apple or Google can OPTIONALLY link their Zalo identity from <code>Settings → Linked accounts</code>. The link is one-direction: Zalo-linked identities can sign in via Zalo OR the originally-linked provider; they cannot be unlinked without account deletion. Cross-provider account merge is forbidden (Apple/Google ID + Zalo ID = separate Supabase users unless explicitly linked).</p>\n<p>§1.8  <strong>Zalo Mini App fallback.</strong> For VN users on low-end devices where the Cocos WebGL build is too heavy, the Zalo Mini App format MAY be a future deliverable (deferred to a P3 FR). This FR locks the foundational Zalo Login + profile-fetch APIs so the Mini App can reuse them.</p>\n<p>§1.9  <strong>VN-specific UX.</strong> The Vietnamese app build MUST surface Zalo Sign-In as the <strong>primary</strong> call-to-action on the sign-in screen (above Apple + Google), per VN market reality. Non-VN region builds surface Zalo as a third option after Apple + Google. The locale-driven order is configured in <code>apps/cocos/assets/i18n/&lt;locale&gt;/auth.json</code> (per FR-I18N-001).</p>\n<p>§1.10  <strong>Refresh token handling.</strong> Zalo access tokens TTL is ~60 minutes; refresh tokens TTL is ~90 days. The Nest API MUST proactively refresh Zalo tokens 5 min before expiry. Refresh failures (refresh token revoked by user) MUST gracefully return the user to the sign-in screen via the same path as FR-AUTH-001 §1.11.</p>\n<p>§1.11  <strong>Disable Zalo on kids SKU.</strong> Per FR-LEGAL-001 §1.5 + FR-AUTH-003, the kids SKU MUST NOT offer Zalo Sign-In. Build-target injection (FR-LEGAL-003 §1.3) MUST omit the Zalo native bridge entirely from the kids binary. Verified by the binary inspection script (FR-LEGAL-003 §1.10) which lints for <code>ZaloSDK</code> strings in the kids binary.</p>\n<p>§1.12  <strong>PDPL data-processing addendum.</strong> A Vietnam PDPL-compliant data-processing addendum MUST be authored at <code>docs/legal/zalo-oa-data-processing-addendum.md</code> covering: (a) data categories shared with Zalo (the user's Zalo ID is shared bidirectionally); (b) the Zalo OA's terms-of-service alignment; (c) the cross-border-transfer status (Zalo OA infrastructure is in VN — domestic processing, simpler than US/EU vendors). The addendum MUST be cited in the public Vietnamese privacy policy.</p>\n<p>§1.13  <strong>Zalo OAuth callback security.</strong> The <code>POST /v1/auth/zalo/callback</code> endpoint MUST: (a) verify the PKCE <code>code_verifier</code> against the recorded <code>code_challenge</code> per session (5-min TTL); (b) verify the auth code with Zalo's token endpoint; (c) verify the returned Zalo <code>user_id</code> is internally consistent (no impersonation by re-submitting another user's code); (d) emit <code>auth.zalo.signin.success</code> + <code>auth.zalo.signin.failure</code> Sentry events.</p>\n<p>§1.14  <strong>Rate limit + abuse.</strong> The Zalo OAuth callback MUST be rate-limited at 10 attempts per IP per hour, 5 attempts per phone-number-hash per hour (Zalo IDs are correlated to phone numbers; a brute-force attempt would manifest as multiple attempts per phone). Exceeding triggers <code>auth.zalo.callback.rate_limit_exceeded</code> + temporary block.</p>\n<p>§1.15  <strong>Observability + funnel metrics.</strong> Every step of the Zalo flow MUST emit metrics: <code>auth.zalo.authorize.initiated</code>, <code>auth.zalo.authorize.granted</code>, <code>auth.zalo.callback.attempted</code>, <code>auth.zalo.callback.success</code>, <code>auth.zalo.callback.failure { error_class }</code>, <code>auth.zalo.profile.fetched</code>. P95 end-to-end Zalo sign-in latency MUST be ≤ 4 seconds on a mid-tier VN device + 4G.</p>\n<p>§1.16  <strong>Zalo OA banner on sign-in.</strong> Per Zalo OA policy, the sign-in surface MUST display the \"Sign in with Zalo\" Zalo-provided button (icon + Vietnamese label \"Đăng nhập với Zalo\" — not a custom-styled button). The button MUST be visible above the fold.</p>\n<p>§1.17  <strong>Disconnect / revoke flow.</strong> Users MUST be able to disconnect their Zalo identity from their Tamagochi account via <code>Settings → Linked accounts → Disconnect Zalo</code>. The disconnect MUST: (a) revoke the Zalo refresh token via the Zalo Graph API; (b) zero out the <code>zalo_user_id</code> column; (c) emit <code>auth.zalo.disconnect</code> audit row. Disconnecting the last sign-in provider triggers the account-deletion flow per FR-AUTH-001 §1.19.</p>\n<p>§1.18  <strong>Test seam.</strong> A <code>ZaloIdTokenVerifier.verifyForTest</code> seam (gated on <code>NODE_ENV !== 'production'</code>) MUST allow unit tests to bypass live Zalo verification.</p>\n<p>§1.19  <strong>Zalo SDK version pin.</strong> iOS Zalo SDK + Android Zalo SDK versions MUST be exact-pinned in <code>Podfile</code> and <code>build.gradle</code>. Vendoring per FR-INFRA-001 §1.8 does not apply (Zalo SDK is a first-party vendor binary, not a marketplace package).</p>\n<p>§1.20  <strong>Apple Sign-In remains required on iOS.</strong> Per Apple Guideline 4.8, an iOS build that offers any third-party social sign-in MUST also offer Apple Sign-In. The combined ordering on iOS+VN builds: Apple → Zalo → Google (Apple-first per Apple Review; Zalo prominence preserved via the \"primary\" label inside Apple's sign-in row).</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why Zalo Sign-In is mandatory in VN.</strong> Plan §PART 4 + plan §PART 7 — Vietnam is the primary soft-launch market; Zalo has 70M+ MAU there with ~95% reach for Gen Z. Pop-ups asking for \"Sign in with Apple\" or \"Sign in with Google\" convert poorly with Vietnamese users who do not associate Apple/Google identity with their primary social graph. The Vietnamese \"default app for everything\" is Zalo. Without it, conversion drops 30-50% (Zalo Pay + Zalo Mini Apps + Zalo OA play in the same ecosystem).</p>\n<p><strong>Why custom Supabase external provider rather than waiting for built-in.</strong> Supabase's roadmap doesn't include Zalo natively (the VN market is too small for them to prioritise). Building it as a custom external provider via <code>sign_in_with_id_token</code> is the canonical pattern Supabase supports for non-Big-Tech providers.</p>\n<p><strong>Why minimum scope.</strong> PDPL data minimisation: only <code>id</code> + <code>name</code> are required for a pet-care game. Requesting PHONE / EMAIL would be over-broad and trigger user-trust issues at sign-in (Vietnamese users are increasingly privacy-aware post-2025 PDPL launch).</p>\n<p><strong>Why server-side code exchange.</strong> Zalo's OA secret is not safe in client code; Zalo's access-token endpoint requires it. A client-side exchange would leak the secret on day one.</p>\n<p><strong>Why PKCE.</strong> PKCE protects against authorization-code interception attacks (especially relevant for native mobile flows where the redirect URI is custom-scheme rather than HTTPS). RFC 7636 — universally recommended for OAuth 2.0 native flows.</p>\n<p><strong>Why account-linking is one-direction (no unlink).</strong> Allowing arbitrary unlink-then-relink creates an account-merging attack surface (impersonate someone's Zalo if you steal their session). The disconnect flow exists but it's structured (last-provider triggers account deletion), not an arbitrary \"swap providers.\"</p>\n<p><strong>Why no Zalo Sign-In on the kids SKU.</strong> COPPA-2025 requires no behavioural-tracking-enabled social sign-in for under-13 accounts. Zalo's terms grant Zalo the right to use sign-in events for analytics, which would be a behavioural-tracking surface. The under-13 SKU uses the parental-consent invite-code flow (FR-AUTH-003) exclusively.</p>\n<p><strong>Why VN-specific UX order.</strong> Plan §PART 7 soft-launch — VN first. UX research from plan §PART 4 (Vietnamese auth providers) implies Zalo-primary is what Vietnamese users expect.</p>\n<p><strong>Why proactive refresh.</strong> Same reasoning as FR-AUTH-001 §1.11 — token expiry mid-Colyseus-session drops the WebSocket.</p>\n<p><strong>Why disable on kids SKU at binary level.</strong> Defence in depth: a runtime feature flag could fail open; omitting the bridge entirely makes the kids binary fail-closed.</p>\n<p><strong>Why PDPL data-processing addendum.</strong> Vietnam PDPL Decree 356/2025/ND-CP requires processor-to-processor data-sharing disclosures. Tamagochi → Zalo (sharing user IDs for sign-in) is a processor relationship; the addendum is the formal documentation regulators expect.</p>\n<p><strong>Why rate-limit per phone-number-hash.</strong> Zalo IDs are 1:1 with phone numbers. A brute-force attack on the callback would manifest as multiple attempts against the same phone hash — catching it at the phone-hash level is the right granularity.</p>\n<p><strong>Why Apple-first ordering on iOS.</strong> Apple Guideline 4.8 mandates this; non-compliance is a hard rejection.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Cocos Zalo Sign-In</h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/auth/ZaloSignIn.ts\nimport { ZaloBridge } from '../../native/ZaloBridge';\nimport { generatePkcePair } from './pkce';\n\nexport class ZaloSignIn {\n  async signIn(): Promise&lt;{ authCode: string; codeVerifier: string }&gt; {\n    const { verifier, challenge } = await generatePkcePair();\n    const authCode = await ZaloBridge.startAuthorize({\n      codeChallenge: challenge,\n      scope: 'id name',         // minimum per §1.6\n    });\n    return { authCode, codeVerifier: verifier };\n  }\n}</code></pre>\n<h3>3.2 — Nest callback controller</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/auth/zalo/zalo-oauth-callback.controller.ts\n@Controller('v1/auth/zalo')\nexport class ZaloOAuthCallbackController {\n  constructor(\n    private readonly svc: ZaloOAuthCallbackService,\n    private readonly limit: AuthZaloRateLimit,\n  ) {}\n\n  @Post('callback')\n  async callback(@Body() body: { authCode: string; codeVerifier: string }, @Ip() ip: string) {\n    await this.limit.assertWithinLimit({ ip, codePrefix: body.authCode.slice(0, 8) });\n    const { zaloUserId, accessToken, refreshToken } = await this.svc.exchange(body);\n    const profile = await this.svc.fetchProfile(accessToken);\n    const supaSession = await this.svc.upsertAndMintSupabaseSession({ zaloUserId, profile });\n    return supaSession;\n  }\n}</code></pre>\n<h3>3.3 — Zalo OAuth callback service</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/auth/zalo/zalo-oauth-callback.service.ts\n@Injectable()\nexport class ZaloOAuthCallbackService {\n  async exchange({ authCode, codeVerifier }: { authCode: string; codeVerifier: string }) {\n    const resp = await fetch('https://oauth.zaloapp.com/v4/access_token', {\n      method: 'POST',\n      headers: { secret_key: process.env.ZALO_APP_SECRET!, 'content-type': 'application/x-www-form-urlencoded' },\n      body: new URLSearchParams({\n        app_id: process.env.ZALO_APP_ID!,\n        grant_type: 'authorization_code',\n        code: authCode,\n        code_verifier: codeVerifier,\n      }),\n    });\n    if (!resp.ok) throw new Error(`zalo.token.exchange.failed ${resp.status}`);\n    const json = await resp.json() as { access_token: string; refresh_token: string; expires_in: number };\n    const profile = await this.fetchProfile(json.access_token);\n    return {\n      zaloUserId: profile.id,\n      accessToken: json.access_token,\n      refreshToken: json.refresh_token,\n      expiresAt: Date.now() + json.expires_in * 1000,\n    };\n  }\n\n  async fetchProfile(accessToken: string) {\n    const r = await fetch('https://graph.zalo.me/v3.0/me?fields=id,name', {\n      headers: { access_token: accessToken },\n    });\n    if (!r.ok) throw new Error(`zalo.profile.fetch.failed ${r.status}`);\n    return r.json() as Promise&lt;{ id: string; name: string }&gt;;\n  }\n\n  async upsertAndMintSupabaseSession({ zaloUserId, profile }: { zaloUserId: string; profile: { id: string; name: string } }) {\n    // Mint a Supabase-compatible JWT representing the Zalo identity.\n    const jwt = await mintSupabaseJwt({\n      sub: `zalo:${zaloUserId}`,\n      email: `${zaloUserId}@zalo.local`,    // synthetic — Zalo email scope not requested\n      provider: 'zalo',\n      app_metadata: { provider: 'zalo', zalo_user_id: zaloUserId },\n      user_metadata: { display_name: profile.name },\n    });\n    return { session: jwt };\n  }\n}</code></pre>\n<h3>3.4 — Zalo provider migration</h3>\n<pre><code class=\"language-sql\">-- infra/supabase/standard/migrations/20260517_005_zalo_provider.sql\nalter table public.app_users\n  add column if not exists zalo_user_id text unique;\ncreate index if not exists idx_app_users_zalo_user_id on public.app_users (zalo_user_id);</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Zalo authorize flow on iOS returns an auth code on the simulator with a sandbox Zalo OA. Verified by Detox UI test.</p>\n<p><strong>AC2.</strong> Zalo authorize flow on Android returns an auth code on emulator with sandbox OA. Verified by Detox test.</p>\n<p><strong>AC3.</strong> Web Zalo redirect flow returns auth code via Playwright headed test.</p>\n<p><strong>AC4.</strong> <code>POST /v1/auth/zalo/callback</code> exchanges the auth code + PKCE verifier → Supabase session. Verified by <code>__tests__/zalo-oauth-callback.spec.ts</code> with a Zalo API mock.</p>\n<p><strong>AC5.</strong> PKCE verifier mismatch returns HTTP 400 with <code>error: \"zalo.pkce.mismatch\"</code>. Verified by spec test with a tampered verifier.</p>\n<p><strong>AC6.</strong> Rate limit blocks at 10 attempts per IP per hour. Verified by spec test pumping requests + asserting 429 response.</p>\n<p><strong>AC7.</strong> Zalo Sign-In is omitted from the kids binary. Verified by FR-LEGAL-003 §1.10 binary inspection — <code>strings &lt;kids.ipa&gt; | grep -i ZaloSDK</code> returns empty.</p>\n<p><strong>AC8.</strong> Account-linking flow: an Apple-signed-in user successfully links Zalo, then signs in via Zalo, arriving at the same Supabase user. Verified by <code>__tests__/auth.spec.ts</code>.</p>\n<p><strong>AC9.</strong> Disconnect Zalo revokes the refresh token via Zalo Graph API. Verified by spec test asserting Zalo's revoke endpoint is called.</p>\n<p><strong>AC10.</strong> Disconnecting the only sign-in provider triggers account-deletion grace window per FR-AUTH-001 §1.19. Verified by spec test.</p>\n<p><strong>AC11.</strong> VN locale build surfaces Zalo as primary CTA. Verified by Playwright test reading the rendered sign-in screen.</p>\n<p><strong>AC12.</strong> P95 Zalo sign-in latency ≤ 4 s on test cohort. Verified by Sentry transaction metric (manually reviewed in soft-launch).</p>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 — Zalo OAuth callback unit test</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/auth/zalo/__tests__/zalo-oauth-callback.spec.ts\nimport { describe, it, expect, vi } from 'vitest';\nimport { ZaloOAuthCallbackService } from '../zalo-oauth-callback.service';\n\ndescribe('FR-AUTH-002 — Zalo OAuth callback', () =&gt; {\n  it('exchanges code and fetches profile', async () =&gt; {\n    global.fetch = vi.fn()\n      .mockResolvedValueOnce({ ok: true, json: async () =&gt; ({ access_token: 'tok', refresh_token: 'rtok', expires_in: 3600 }) } as any)\n      .mockResolvedValueOnce({ ok: true, json: async () =&gt; ({ id: 'zalo123', name: 'Linh Nguyen' }) } as any);\n    const svc = new ZaloOAuthCallbackService();\n    const r = await svc.exchange({ authCode: 'ac', codeVerifier: 'cv' });\n    expect(r.zaloUserId).toBe('zalo123');\n    expect(r.accessToken).toBe('tok');\n  });\n\n  it('throws on token endpoint failure', async () =&gt; {\n    global.fetch = vi.fn().mockResolvedValueOnce({ ok: false, status: 400 } as any);\n    const svc = new ZaloOAuthCallbackService();\n    await expect(svc.exchange({ authCode: 'bad', codeVerifier: 'cv' })).rejects.toThrow(/zalo.token.exchange.failed/);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/auth/zalo/zalo-id-token-verifier.ts\nimport { createHash } from 'node:crypto';\nimport { decodeJwt } from 'jose';\n\nexport class ZaloIdTokenVerifier {\n  static verifyForTest(token: string, expectedNonceHash: string): boolean {\n    const payload = decodeJwt(token);\n    return createHash('sha256').update(String(payload['nonce'])).digest('hex') === expectedNonceHash;\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Zalo OA + Login API + Graph API (developer account at developers.zalo.me). Vietnam corporate identity for Zalo Business registration. Zalo OA SDK iOS framework + Android AAR.</p>\n<p><strong>Internal:</strong></p>\n<ul><li>FR-AUTH-001 (Supabase Auth scaffold + auth controller foundation).</li><li>FR-INFRA-003 (Supabase + RLS).</li></ul>\n<p><strong>Blocks:</strong> FR-AUTH-003 (kid invite-code parental email may be a Zalo OA push — falls back to Resend if not).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — <code>POST /v1/auth/zalo/callback</code></h3>\n<pre><code class=\"language-http\">POST /v1/auth/zalo/callback\nContent-Type: application/json\n\n{\n  \"authCode\": \"AC...\",\n  \"codeVerifier\": \"dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk\"\n}\n\n→ 200 OK\n{\n  \"session\": {\n    \"access_token\": \"eyJ... (Supabase JWT)\",\n    \"refresh_token\": \"eyJ...\",\n    \"expires_at\": \"2026-08-12T15:36:01Z\"\n  }\n}</code></pre>\n<h3>8.2 — Zalo profile fetched</h3>\n<pre><code class=\"language-json\">{ \"id\": \"1234567890123456789\", \"name\": \"Linh Nguyễn\" }</code></pre>\n<h3>8.3 — Disconnect Zalo audit row</h3>\n<pre><code class=\"language-json\">{\n  \"who\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"what\": \"auth.zalo.disconnect\",\n  \"occurred_at\": \"2026-08-12T14:36:01Z\",\n  \"tenant_id\": \"mochi\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Built-in Supabase Zalo provider? → §1.5 + §2 — custom external via <code>sign_in_with_id_token</code>.</li><li><strong>OQ-2 (resolved):</strong> Phone scope for parental-consent in VN? → §1.6 — phone NOT requested; data minimisation per PDPL.</li><li><strong>OQ-3 (resolved):</strong> Zalo Mini App in P0? → §1.8 — deferred to P3.</li><li><strong>OQ-4 (resolved):</strong> VN ordering above Apple-first on iOS? → §1.20 — Apple Guideline 4.8 takes precedence; Zalo is prominent within the row order.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Zalo OA suspended (e.g. ToS violation)</td><td>Zalo developer-portal email</td><td>All Zalo sign-ins blocked</td><td>Resolve with Zalo support; surface \"Zalo temporarily unavailable\" UX with Apple/Google fallback</td></tr><tr><td>2</td><td>Zalo Login API outage</td><td><code>auth.zalo.callback.failure</code> spike</td><td>VN signups blocked</td><td>Status splash; route users to Apple/Google fallback</td></tr><tr><td>3</td><td>PKCE verifier session TTL expires (5 min)</td><td><code>zalo.pkce.expired</code> error</td><td>User sees \"session timed out, please retry\"</td><td>Restart authorize flow</td></tr><tr><td>4</td><td>Zalo OA secret rotation breaks server-side exchange</td><td><code>zalo.token.exchange.failed</code></td><td>All sign-ins blocked</td><td>Update secret in Supabase env vars; redeploy</td></tr><tr><td>5</td><td>Zalo Graph API rate limit hit</td><td><code>zalo.profile.fetch.failed { 429 }</code></td><td>Sign-ins delayed</td><td>Backoff + retry; surface \"high traffic\" UX</td></tr><tr><td>6</td><td>Zalo SDK iOS / Android version skew</td><td>Native bridge fails</td><td>iOS or Android users blocked</td><td>Update SDK pin; ship hotfix</td></tr><tr><td>7</td><td>Zalo Sign-In accidentally enabled on kids binary</td><td>Binary inspection (FR-LEGAL-003 §1.10)</td><td>Submission blocked</td><td>Fix build-target injection; re-build kids binary</td></tr><tr><td>8</td><td>Zalo phone-hash brute-force attack</td><td>Rate-limit metric spike</td><td>Temporary block triggered</td><td>Investigate IP source; legal escalation if persistent</td></tr><tr><td>9</td><td>Cross-provider Zalo + Apple merge attempt</td><td><code>auth.zalo.merge.refused</code></td><td>User sees error</td><td>UX clarifies separate accounts; manual DPO can merge if owner proves identity</td></tr><tr><td>10</td><td>Zalo refresh token revoked by user (Zalo app uninstall)</td><td>Refresh fails on next session</td><td>User returned to sign-in</td><td>Preserve game state via Colyseus reconciliation</td></tr><tr><td>11</td><td>PDPL DPA expires / Zalo updates ToS</td><td>Annual compliance review</td><td>DPA out of date</td><td>Re-execute DPA; update privacy policy version</td></tr><tr><td>12</td><td>Zalo Login API v4 deprecation</td><td>Zalo developer announcement</td><td>Migration required</td><td>Pin to v4 patch; raise upgrade FR; coordinate with Zalo DevRel</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 4 (Auth: Zalo mandatory in VN), §PART 6 (VN payment rails — MoMo/ZaloPay/VNPay/ViettelPay; Zalo's role in the broader ecosystem), §PART 7 (community: Zalo Official Account for outreach).</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Specific Zalo OA approval timeline + corporate-identity submission documents — locked in the Zalo OA registration packet.</li><li>Zalo OA broadcast schedule (for marketing) — owned by UA marketer (post-P0 hire per plan §J Phase 2).</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Client-side OA-secret embed.</li><li>Requesting PHONE/EMAIL scope.</li><li>Allowing Zalo on the kids SKU.</li><li>Webview-based Zalo Login in a way that breaks PKCE.</li><li>Cross-provider account merge.</li></ul>\n<p><strong>Cross-reference:</strong> Completes the consumer-SKU provider trio (Apple + Google + Zalo). FR-AUTH-003 (kid invite-code) is the under-13 path that intentionally has none of these providers.</p>";
