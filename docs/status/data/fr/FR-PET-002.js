window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PET-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Stage definitions.</strong> A pet's lifecycle MUST progress through five stages: <code>egg → baby → teen → adult → grandma_house</code>. <code>grandma_house</code> is the Permadeath-Lite state (FR-PET-008) — neglect, not natural progression. The natural arc is <code>egg → baby → teen → adult</code>, irreversible.</p>\n<p>§1.2  <strong>Age durations (real-time hours).</strong> Each natural stage MUST have a configurable duration in real-time hours:</p>\n<ul><li><code>egg → baby</code>: 0.5 h (the hatch ceremony — short, completed via FR-PET-001).</li><li><code>baby → teen</code>: 48 h (≈ 2 days).</li><li><code>teen → adult</code>: 168 h (≈ 7 days).</li></ul>\n<p>Durations are read from <code>apps/api/src/pets/evolution/evolution-config.ts</code>. A/B tunable per FR-OBS-001 feature flags (e.g. <code>evolution.baby_to_teen.duration_h</code> overrides default for a cohort).</p>\n<p>§1.3  <strong>Wall-clock-resistant age timer.</strong> Pet age MUST be computed server-side from the <strong>monotonic UTC timestamp</strong> of <code>pets.born_at</code> (FR-PET-001 §1.1). The client never sends \"minutes since X\" — it asks <code>GET /v1/pets/:id</code> which returns <code>age_minutes</code> computed at request time. Client-side rendering displays the server's <code>age_minutes</code> only.</p>\n<p>§1.4  <strong>Stage advancement check.</strong> Every Colyseus <code>PetRoom</code> tick (default 30 s; configurable via <code>clock.setInterval</code>) MUST: (a) check every active pet against <code>age-clock.shouldAdvance(petBornAt, currentStage)</code>; (b) on <code>true</code>, emit <code>pet.stage.advance</code> server-side event; (c) update <code>pets.stage</code> AND <code>pets.hatched_at</code> (if relevant); (d) broadcast new <code>PetState.stage</code> via Colyseus state diff.</p>\n<p>§1.5  <strong>Care prerequisite for evolution.</strong> A pet MUST NOT advance from <code>baby → teen</code> if any stat-bar (hunger / cleanliness / happiness / energy) has been &lt; 30 for &gt; 50% of the prior 24 h. Similarly <code>teen → adult</code> requires &lt; 30 hours-below-30 in prior 72 h. This makes care meaningful — neglected pets stall at their current stage instead of auto-evolving. Stage-advancement check MUST evaluate this prerequisite.</p>\n<p>§1.6  <strong>Stage-gated unlocks.</strong> Each stage gates feature access:</p>\n<ul><li><code>egg</code>: only <code>hatch_animation</code> + naming (FR-PET-001).</li><li><code>baby</code>: all care actions (FR-CARE-001/002/003), 1 mini-game (FR-CARE-004), no AR (FR-AR-001 disabled), no co-parent invite (FR-SOCIAL-002 disabled).</li><li><code>teen</code>: AR placement enabled, co-parent invite enabled, mini-games full set.</li><li><code>adult</code>: breeding eligibility (FR-PET-007), trade eligibility (FR-SOCIAL-003), wedding-ceremony eligibility (FR-SOCIAL-004), generative-pet reproduction (FR-VIRAL-003).</li><li><code>grandma_house</code>: NO actions except the daily-ritual rescue (FR-PET-008).</li></ul>\n<p>Gate enforcement MUST live in <code>stage-gates.ts</code> as a single source of truth queried by every action handler.</p>\n<p>§1.7  <strong>Spine skin swap on advancement.</strong> Per FR-ART-001 §1.3, the skeleton has three skin presets (<code>baby</code>, <code>teen</code>, <code>adult</code>). On stage advance, the Cocos <code>EvolutionFlow.ts</code> MUST: (a) play the <code>evolve_baby_to_teen</code> (or <code>evolve_teen_to_adult</code>) contract animation; (b) at the animation's midpoint, swap the Spine skin; (c) play 1 s of confetti particles + <code>success</code> haptic; (d) emit <code>pet.evolution.viewed</code> analytics.</p>\n<p>§1.8  <strong>TikTok-native share moment.</strong> A stage advance is a viral moment — <code>EvolutionFlow.ts</code> MUST surface a \"Share this milestone?\" prompt to the player after the celebration, routing to FR-VIRAL-001's TikTok export pipeline. The prompt is opt-in; on the kids SKU, it's disabled outright (no out-of-app sharing for under-13 per FR-LEGAL-003 §1.4).</p>\n<p>§1.9  <strong>Idempotent advancement.</strong> Stage-advancement MUST be idempotent: if the server tick processes the same pet twice in rapid succession, the second pass MUST be a no-op (the row already has the new stage). Implementation MUST use Postgres <code>update ... where stage = $expected_old_stage</code> returning row count, then act on <code>row_count == 1</code>.</p>\n<p>§1.10  <strong>Skip-time anti-cheat.</strong> Per FR-OBS-002 (P4) but with FR-PET-002's groundwork, any attempt to manipulate <code>born_at</code> (e.g. via direct DB write through a leaked service-role key) MUST be detected by a daily reconciliation job that compares <code>born_at</code> against the <code>pet_age_events</code> audit table: a <code>born_at</code> row earlier than the first <code>pet.hatched</code> audit event is suspicious + alerts the DPO.</p>\n<p>§1.11  <strong>Pet age events audit.</strong> Every stage transition MUST write a row to <code>pet_age_events</code> table: <code>(pet_id, prev_stage, new_stage, observed_at, gating_reason)</code>. <code>gating_reason</code> enumerates why the transition happened or didn't — <code>'time_reached'</code>, <code>'time_reached_care_gated'</code>, <code>'time_not_reached'</code>, <code>'rescue_from_grandma'</code>, etc. Retention 7 years for kids, 2 years for standard.</p>\n<p>§1.12  <strong>Bulk advancement job.</strong> A Supabase scheduled function MUST run every 5 min and check ALL active pets across both projects for pending stage advancements that haven't been picked up by their PetRoom (e.g. owner offline for the full 48h baby→teen window). This is the catch-up path so offline pets still age correctly.</p>\n<p>§1.13  <strong>Sleep-hour respect for advancement notifications.</strong> On stage advance, push notification (FR-VIRAL-005) is suppressed during the player's local 22:00–07:00 window (or 20:00–08:00 for under-13). The advancement still happens; the notification is delivered the following morning.</p>\n<p>§1.14  <strong>Cocos <code>EvolutionFlow.ts</code> UI.</strong> The Cocos component MUST: (a) subscribe to <code>PetState.stage</code> changes; (b) on change to a new stage, play the FR-ART-001 contract animation; (c) handle the swap-skin moment cleanly without flicker; (d) preserve the camera framing across the swap; (e) emit <code>pet.evolution.viewed</code> even if the player misses the animation (came back later).</p>\n<p>§1.15  <strong>Stale-state reconciliation on rejoin.</strong> When a player rejoins a <code>PetRoom</code> after offline time, the server MUST: (a) compute the expected stage from <code>born_at</code>; (b) compare to <code>pets.stage</code>; (c) if different (catch-up not done), update + emit advancement events for each missed stage in chronological order; (d) broadcast to client which queues animations to play in sequence.</p>\n<p>§1.16  <strong>Care-gated stall surfaced to player.</strong> When a pet would advance by age but is stalled by §1.5, the client MUST display a \"Mochi seems unsure about growing up — make sure they're happy + clean + full!\" message + a per-stat progress bar so the player knows what to fix. The UX must NEVER be punitive; it's a soft nudge.</p>\n<p>§1.17  <strong>No retrograde stages.</strong> Once a pet reaches <code>adult</code>, it MUST NOT return to <code>teen</code> or <code>baby</code>. The <code>grandma_house</code> state is the only \"downward\" transition + is explicitly Permadeath-Lite (FR-PET-008) not natural ageing.</p>\n<p>§1.18  <strong>Server-side test seam.</strong> <code>age-clock.ts</code> MUST expose a <code>setNowForTest(timestamp)</code> seam (gated on <code>NODE_ENV !== 'production'</code>) for unit tests. Production builds export a frozen <code>Date.now()</code> proxy that throws on tampering.</p>\n<p>§1.19  <strong>Feature-flag tunability.</strong> Stage durations + care-gate thresholds MUST be readable via Mixpanel feature flags (per FR-OBS-001 §1.10) so live-ops can tune live without redeploy. Defaults baked into code so the system works even if Mixpanel is down (FR-OBS-001 §10 row 3).</p>\n<p>§1.20  <strong>Analytics events.</strong> <code>pet.stage.advance</code>, <code>pet.stage.stall { reason }</code>, <code>pet.evolution.viewed</code>, <code>pet.evolution.shared</code> MUST be emitted per FR-OBS-001 §1.4 taxonomy. Schema-validated per FR-OBS-001 §1.5.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why server-authoritative age computation.</strong> The single biggest Tamagotchi-style cheat (since 1996) is jumping the device clock to skip pet-care time. Client-supplied \"minutes elapsed\" is wholly untrusted; only the server's monotonic UTC clock + <code>pets.born_at</code> is authoritative. Plan §PART 4 anti-cheat reasoning applies directly.</p>\n<p><strong>Why 0.5 h + 48 h + 168 h durations.</strong> Plan §PART 3 60-second-session loop assumes daily engagement. 48 h baby + 168 h teen gives the player ~9 days from hatch to adult — long enough that retention metrics depend on consistent engagement (D7 ≥ 18% target), short enough that growth feels achievable. Tunable per feature flag for soft-launch tuning.</p>\n<p><strong>Why care-gated evolution.</strong> Plan §PART 3 — \"Permadeath-Lite\" + \"care-gated growth\" make care meaningful. If pets auto-evolve regardless of care, players have no reason to feed/clean/hug. The 50% / 30 stat threshold is gentle enough that a forgetful day doesn't stall the pet, but consistent neglect does.</p>\n<p><strong>Why stage gates as a single source of truth.</strong> Every gate (\"is this pet adult enough to breed?\") will be queried by many action handlers (<code>POST /v1/breed</code>, <code>POST /v1/trade</code>, etc.). Centralising in <code>stage-gates.ts</code> prevents drift where one handler accidentally allows breeding for teens.</p>\n<p><strong>Why Spine skin swap + half-animation timing.</strong> The Spine skin mechanism is bandwidth-cheap (no skeleton reload). Swapping at the midpoint of the evolve animation hides the swap behind a particle burst — feels seamless. A pre-animation swap reveals the new stage too early; post-animation swap looks janky.</p>\n<p><strong>Why TikTok-share prompt on evolution.</strong> Plan §PART 3 viral hooks #5 — stage advance is a documented share-worthy moment. Auto-export with consent prompts is the canonical pattern.</p>\n<p><strong>Why kids SKU disables share.</strong> FR-LEGAL-003 §1.4 — out-of-app links require parental gate. Stage-share to TikTok is an out-of-app action. Disabling on kids SKU is simpler than gating; the kids version saves the milestone locally for the parent to see in the parental dashboard (FR-SUB-002).</p>\n<p><strong>Why idempotent advancement.</strong> Colyseus <code>setInterval</code> ticks can overlap during reconnection storms; without idempotency, a pet could advance twice from the same <code>born_at</code> arithmetic. Postgres <code>update ... where stage = $expected</code> is the canonical idempotency primitive.</p>\n<p><strong>Why bulk advancement scheduled function.</strong> A player offline for 60+ hours misses the in-room tick. The 5-min scheduled function catches them up server-side so they see the right pet stage when they return.</p>\n<p><strong>Why sleep-hour push suppression for advancement.</strong> Plan §PART 3 retention mechanics — \"local-time push that respects sleep hours.\" Waking a kid at 02:00 to tell them their pet evolved is a churn signal + a real-world annoyance.</p>\n<p><strong>Why stale-state reconciliation animates sequentially.</strong> A player offline 240 h might miss baby→teen + teen→adult. Animating both in sequence + a 2-second pause between them lets the player experience the progression instead of a single jarring \"your pet is now an adult\" jump.</p>\n<p><strong>Why care-gated stall surfaced + non-punitive.</strong> Kid-skewing apps must avoid FOMO trauma + shame. Webkinz Next's \"your pet is sad\" UX has been criticised; tamagochi's care-gated stall surfaces actionable next steps without guilt.</p>\n<p><strong>Why no retrograde stages.</strong> Otherwise a player could \"un-age\" a pet for breeding eligibility (adult-then-not-adult). Strict forward-only matches biological intuition.</p>\n<p><strong>Why feature-flag tunability.</strong> Soft-launch tuning is the canonical f2p economy lever. Plan §PART 9 phase 2 — \"Live-ops cadence weekly.\"</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Age clock</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/evolution/age-clock.ts\nimport { DurationsByStage } from './evolution-config';\n\nconst DURATIONS_MS: DurationsByStage = {\n  egg:    0.5 * 3600 * 1000,\n  baby:   48  * 3600 * 1000,\n  teen:   168 * 3600 * 1000,\n  adult:  Infinity,\n};\n\nlet nowOverride: number | null = null;\n\nexport const ageClock = {\n  now(): number { return nowOverride ?? Date.now(); },\n  setNowForTest(ts: number) {\n    if (process.env.NODE_ENV === 'production') throw new Error('setNowForTest forbidden in production');\n    nowOverride = ts;\n  },\n  shouldAdvance(bornAtIso: string, currentStage: 'egg'|'baby'|'teen'|'adult'): { advance: boolean; nextStage?: 'baby'|'teen'|'adult' } {\n    if (currentStage === 'adult') return { advance: false };\n    const ageMs = this.now() - new Date(bornAtIso).getTime();\n    const stages: ('egg'|'baby'|'teen'|'adult')[] = ['egg','baby','teen','adult'];\n    const idx = stages.indexOf(currentStage);\n    const elapsedThreshold = stages.slice(0, idx + 1).reduce((acc, s) =&gt; acc + (DURATIONS_MS[s] === Infinity ? 0 : DURATIONS_MS[s]), 0);\n    if (ageMs &gt;= elapsedThreshold) {\n      const next = stages[idx + 1];\n      return next ? { advance: true, nextStage: next as any } : { advance: false };\n    }\n    return { advance: false };\n  },\n};</code></pre>\n<h3>3.2 — Evolution service</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/evolution/evolution.service.ts\n@Injectable()\nexport class EvolutionService {\n  constructor(\n    private readonly supa: SupabaseClient,\n    private readonly clock: typeof ageClock,\n    private readonly stallCheck: CareStallCheckService,\n    private readonly audit: AuditLogService,\n  ) {}\n\n  async maybeAdvance(petId: string): Promise&lt;{ advanced: boolean; reason: string }&gt; {\n    const { data: pet } = await this.supa.from('pets').select('*').eq('id', petId).single();\n    if (!pet || pet.stage === 'adult' || pet.stage === 'grandma_house') return { advanced: false, reason: 'no_op' };\n\n    const decision = this.clock.shouldAdvance(pet.born_at, pet.stage);\n    if (!decision.advance) return { advanced: false, reason: 'time_not_reached' };\n\n    if (decision.nextStage !== 'baby') {       // baby gate is the hatch ceremony itself\n      const stalled = await this.stallCheck.isCareStalled(petId, pet.stage);\n      if (stalled) {\n        await this.audit.write({ who: pet.owner_id, what: 'pet.stage.stall', what_keys: { pet_id: petId, reason: 'care_threshold' } });\n        return { advanced: false, reason: 'time_reached_care_gated' };\n      }\n    }\n\n    const { data: updated, error } = await this.supa.from('pets').update({\n      stage: decision.nextStage,\n      hatched_at: decision.nextStage === 'baby' ? new Date().toISOString() : pet.hatched_at,\n    }).eq('id', petId).eq('stage', pet.stage).select().single();\n    if (error || !updated) return { advanced: false, reason: 'race_lost' };\n\n    await this.supa.from('pet_age_events').insert({\n      pet_id: petId, prev_stage: pet.stage, new_stage: decision.nextStage, gating_reason: 'time_reached',\n    });\n    return { advanced: true, reason: 'time_reached' };\n  }\n}</code></pre>\n<h3>3.3 — Stage gates</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/evolution/stage-gates.ts\nexport type StageGate = 'care_actions' | 'mini_game' | 'ar_placement' | 'co_parent_invite'\n                     | 'breeding' | 'trading' | 'wedding' | 'rescue_ritual' | 'generative_repro';\n\nconst GATES: Record&lt;StageGate, ('egg'|'baby'|'teen'|'adult')[]&gt; = {\n  care_actions:       ['baby','teen','adult'],\n  mini_game:          ['baby','teen','adult'],\n  ar_placement:       ['teen','adult'],\n  co_parent_invite:   ['teen','adult'],\n  breeding:           ['adult'],\n  trading:            ['adult'],\n  wedding:            ['adult'],\n  rescue_ritual:      [],                    // grandma_house only, handled separately\n  generative_repro:   ['adult'],\n};\n\nexport function canDo(gate: StageGate, stage: string): boolean {\n  return (GATES[gate] as readonly string[]).includes(stage);\n}</code></pre>\n<h3>3.4 — Pet age events migration</h3>\n<pre><code class=\"language-sql\">-- infra/supabase/standard/migrations/20260517_007_pet_age_events.sql\ncreate table public.pet_age_events (\n  id bigserial primary key,\n  pet_id text not null references public.pets(id) on delete cascade,\n  prev_stage text not null,\n  new_stage  text not null,\n  observed_at timestamptz not null default now(),\n  gating_reason text not null\n);\ncreate index on public.pet_age_events (pet_id, observed_at);\nalter table public.pet_age_events enable row level security;\n-- Service role only — analytics + DPO read.</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> <code>ageClock.shouldAdvance(bornAt, 'egg')</code> returns <code>{advance:true, nextStage:'baby'}</code> when <code>bornAt</code> is 31 min ago. Verified by <code>__tests__/age-clock.spec.ts</code>.</p>\n<p><strong>AC2.</strong> <code>ageClock.shouldAdvance(bornAt, 'baby')</code> returns <code>{advance:false}</code> at 47.99 h ago, <code>{advance:true, nextStage:'teen'}</code> at 48.01 h ago. Verified by spec test.</p>\n<p><strong>AC3.</strong> <code>EvolutionService.maybeAdvance</code> is idempotent: calling twice in race conditions advances once. Verified by <code>__tests__/evolution.spec.ts</code> with parallel calls + asserting <code>row_count == 1</code>.</p>\n<p><strong>AC4.</strong> Care-stalled pet does NOT advance baby → teen even though time threshold reached. Verified by spec test with seeded <code>last_seen_at</code> + stat history.</p>\n<p><strong>AC5.</strong> Stage-gates: <code>canDo('breeding', 'teen')</code> returns false; <code>canDo('breeding', 'adult')</code> returns true. Verified by <code>__tests__/stage-gates.spec.ts</code>.</p>\n<p><strong>AC6.</strong> Cocos <code>EvolutionFlow.ts</code> plays <code>evolve_baby_to_teen</code> animation + swaps skin at midpoint. Verified by <code>__tests__/EvolutionFlow.spec.ts</code> with mocked Spine player.</p>\n<p><strong>AC7.</strong> Stale-state reconciliation: a pet whose <code>born_at</code> is 240 h ago + <code>stage='baby'</code> reconciles to <code>'adult'</code> on player rejoin with 2 sequential animation broadcasts. Verified by integration test.</p>\n<p><strong>AC8.</strong> Bulk advancement scheduled function picks up offline pets — pets whose owner is not in any PetRoom advance correctly. Verified by <code>__tests__/bulk-advancement.spec.ts</code> with a 5-min tick mock.</p>\n<p><strong>AC9.</strong> Push notification on advancement is suppressed during 22:00-07:00 local. Verified by spec test with mocked region + time-of-day.</p>\n<p><strong>AC10.</strong> Kids SKU disables the share prompt on evolution. Verified by <code>__tests__/EvolutionFlow.spec.ts</code> with <code>BUILD_TARGET=kids</code>.</p>\n<p><strong>AC11.</strong> <code>pet.stage.advance</code> analytics event is emitted with the documented schema. Verified by spec test + obs schema validation.</p>\n<p><strong>AC12.</strong> Care-stalled UX surfaces actionable next steps (per-stat progress bar). Verified by Playwright UI test on the stalled state.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 — Age-clock test</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/evolution/__tests__/age-clock.spec.ts\nimport { describe, it, expect, beforeEach } from 'vitest';\nimport { ageClock } from '../age-clock';\n\ndescribe('FR-PET-002 — age clock', () =&gt; {\n  beforeEach(() =&gt; { ageClock.setNowForTest(Date.parse('2026-08-12T14:00:00Z')); });\n\n  it('egg → baby at 31 min', () =&gt; {\n    const bornAt = new Date(ageClock.now() - 31 * 60 * 1000).toISOString();\n    expect(ageClock.shouldAdvance(bornAt, 'egg')).toEqual({ advance: true, nextStage: 'baby' });\n  });\n\n  it('baby → teen at 48 h', () =&gt; {\n    const bornAt = new Date(ageClock.now() - 48.01 * 3600 * 1000).toISOString();\n    expect(ageClock.shouldAdvance(bornAt, 'baby')).toEqual({ advance: true, nextStage: 'teen' });\n  });\n\n  it('does NOT advance baby at 47.99 h', () =&gt; {\n    const bornAt = new Date(ageClock.now() - 47.99 * 3600 * 1000).toISOString();\n    expect(ageClock.shouldAdvance(bornAt, 'baby').advance).toBe(false);\n  });\n\n  it('does not advance adult', () =&gt; {\n    expect(ageClock.shouldAdvance(new Date(0).toISOString(), 'adult')).toEqual({ advance: false });\n  });\n\n  it('setNowForTest forbidden in production', () =&gt; {\n    const orig = process.env.NODE_ENV; process.env.NODE_ENV = 'production';\n    expect(() =&gt; ageClock.setNowForTest(0)).toThrow(/forbidden/);\n    process.env.NODE_ENV = orig;\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/realtime/src/rooms/handlers/evolution.handler.ts\nimport { Room } from 'colyseus';\nimport { EvolutionService } from '@/api-shared/evolution';\n\nexport function installEvolutionTick(room: Room&lt;any&gt;, svc: EvolutionService) {\n  room.clock.setInterval(async () =&gt; {\n    for (const [, pet] of room.state.pets) {\n      const r = await svc.maybeAdvance(pet.id);\n      if (r.advanced) {\n        pet.stage = r.nextStage!;            // schema update broadcasts diff to client\n        room.broadcast('pet.evolved', { pet_id: pet.id, new_stage: pet.stage });\n      }\n    }\n  }, 30_000);\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Postgres scheduled functions (Supabase Edge Functions cron — <code>pg_cron</code> extension).</p>\n<p><strong>Internal:</strong> FR-PET-001 (pets table + born_at column); FR-ART-001 (Spine contract animations <code>evolve_baby_to_teen</code>, <code>evolve_teen_to_adult</code> + skin presets); FR-INFRA-002 (Colyseus tick loop + room broadcast).</p>\n<p><strong>Blocks:</strong> FR-PET-003 (stat-bar decay uses stage to set decay rates); FR-PET-007 (breeding requires <code>stage='adult'</code>); FR-PET-008 (Permadeath-Lite reads stage); FR-CARE-005 (streak system); FR-VIRAL-001 (share-on-evolution).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — <code>pet.stage.advance</code> event</h3>\n<pre><code class=\"language-json\">{\n  \"event\": \"pet.stage.advance\",\n  \"pet_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"owner_id\": \"01HC7QGZK4XN8YA1J3WB6XX99\",\n  \"prev_stage\": \"baby\",\n  \"new_stage\": \"teen\",\n  \"born_at\": \"2026-08-10T14:00:00Z\",\n  \"advanced_at\": \"2026-08-12T14:00:01Z\",\n  \"gating_reason\": \"time_reached\",\n  \"tenant_id\": \"mochi\"\n}</code></pre>\n<h3>8.2 — <code>pet.stage.stall</code> event (care-gated)</h3>\n<pre><code class=\"language-json\">{\n  \"event\": \"pet.stage.stall\",\n  \"pet_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"current_stage\": \"baby\",\n  \"would_be_stage\": \"teen\",\n  \"reason\": \"care_threshold\",\n  \"care_violations\": { \"hunger_hours_below_30\": 16, \"happiness_hours_below_30\": 12 },\n  \"occurred_at\": \"2026-08-12T14:00:01Z\"\n}</code></pre>\n<h3>8.3 — <code>pet_age_events</code> row</h3>\n<pre><code class=\"language-json\">{\n  \"id\": 42,\n  \"pet_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"prev_stage\": \"baby\",\n  \"new_stage\": \"teen\",\n  \"observed_at\": \"2026-08-12T14:00:01Z\",\n  \"gating_reason\": \"time_reached\"\n}</code></pre>\n<h3>8.4 — Stage-gate check at care handler</h3>\n<pre><code class=\"language-typescript\">if (!canDo('co_parent_invite', pet.stage)) {\n  throw new HttpException('pet.stage.locked', 403);\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Real-time hours or game-time accelerated? → §1.2 — real-time hours; tunable via feature flag for events (e.g. \"double speed event\").</li><li><strong>OQ-2 (resolved):</strong> Care-stall threshold — 30%? → §1.5 — &lt; 30 for &gt; 50% of window; tunable.</li><li><strong>OQ-3 (resolved):</strong> Re-evaluate stage on stat recovery? → §1.4 — next tick after recovery picks it up.</li><li><strong>OQ-4 (resolved):</strong> Bulk advancement frequency — 5 min? → §1.12 — 5 min is the balance between freshness and Supabase scheduled-function cost.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Race condition: in-room tick and bulk job both try to advance simultaneously</td><td>DB <code>update ... where stage = $expected</code> returns row_count 0 for the second</td><td>Second call no-ops</td><td>Idempotency by design</td></tr><tr><td>2</td><td><code>born_at</code> tampered via direct DB write</td><td>Daily reconciliation comparing <code>born_at</code> vs first <code>pet_age_events.observed_at</code></td><td>DPO alert</td><td>Restore from backup; rotate service-role key</td></tr><tr><td>3</td><td>Care-stall threshold too aggressive (everyone stalls)</td><td>Feature-flag-driven; A/B cohort retention drops</td><td>Soft-launch fix</td><td>Lower threshold via Mixpanel feature flag</td></tr><tr><td>4</td><td>Spine skin swap mid-animation fails (skeleton corrupted)</td><td>Cocos error event</td><td>Pet renders missing slot</td><td>Force a full reload via <code>loadPetSkeleton</code>; emit Sentry</td></tr><tr><td>5</td><td>Bulk-advancement scheduled function fails (Supabase outage)</td><td>Supabase scheduled-task error log</td><td>Offline pets don't advance</td><td>Re-run on recovery; surface \"your pet is catching up\" UX on rejoin</td></tr><tr><td>6</td><td>Stale-state reconciliation animates wrong sequence</td><td>UI test fixture</td><td>UX feels off</td><td>Re-order broadcast events; pause between</td></tr><tr><td>7</td><td>Push notification delivered during sleep hours</td><td>OBS alert + user complaints</td><td>Churn risk</td><td>Tighten time-zone resolution; respect DST</td></tr><tr><td>8</td><td>Kids SKU share prompt accidentally enabled</td><td>Playwright test fails</td><td>COPPA / store-policy hit</td><td>Build-target injection bug; patch + hotfix</td></tr><tr><td>9</td><td>Feature-flag override accidentally sets baby duration to 0</td><td>Mixpanel audit log</td><td>Pets instantly evolve</td><td>Revoke flag; manual reconciliation of affected pets</td></tr><tr><td>10</td><td>Pets that should be adult are still teen after migration to a new evolution config</td><td>Migration script</td><td>Stage backlog</td><td>Bulk re-run advancement on affected rows</td></tr><tr><td>11</td><td>Postgres deadlock on concurrent <code>update pets set stage = X where stage = Y</code></td><td>DB error log</td><td>Retry storm</td><td>Use <code>for update skip locked</code> pattern; widen tick interval</td></tr><tr><td>12</td><td>Cocos <code>EvolutionFlow.ts</code> fails to play if entered mid-animation</td><td>UI test</td><td>Pet stuck visually</td><td>On scene re-enter, snap to terminal frame; emit Sentry</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 (evolution arc + retention), plan §PART 4 (anti-cheat — server-authoritative timers).</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Final stage-duration tuning happens in soft launch — Mixpanel flags <code>evolution.baby_to_teen.duration_h</code> + <code>evolution.teen_to_adult.duration_h</code>.</li><li>Care-stall threshold tuning via flags <code>evolution.care_stall.hours_below_30</code> + <code>evolution.care_stall.window_pct</code>.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Client-sent <code>age_minutes</code> value.</li><li>Device wall-clock for ageing.</li><li>Retrograde stages (adult → teen).</li><li><code>Math.random()</code> for evolution outcomes (although none currently random; rule applied prospectively for FR-PET-007 breeding traits).</li></ul>\n<p><strong>Cross-reference:</strong> This FR is the pet-lifecycle substrate. FR-PET-003 (stat decay rates by stage), FR-PET-007 (breeding adult gate), FR-PET-008 (Permadeath-Lite from any active stage) all depend on stage being trustworthy.</p>";
