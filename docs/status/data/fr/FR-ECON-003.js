window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-ECON-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>UGC scope.</strong> Users design outfits for the <code>accessory_head</code> + <code>accessory_body</code> slots only (per FR-ART-001 §1.4 — body/face are off-limits). Each design has palette + simple Spine attachment shape.</p>\n<p>§1.2  <strong>Cocos designer UI.</strong> <code>CoutureDesignerUi.ts</code> exposes:</p>\n<ul><li>Slot picker (head/body).</li><li>Palette picker (RGBA grid).</li><li>Pattern picker (8 built-in patterns: stripes/dots/etc.).</li><li>Preview on player's current pet.</li></ul>\n<p>No free-form image upload. Pattern + palette only at P3.</p>\n<p>§1.3  <strong>Submission flow.</strong> Submitter pays 50 Coins (Coin sink) → designer-output JSON serialised → submission row created with <code>status='pending_review'</code>.</p>\n<p>§1.4  <strong>Manual T&amp;S review.</strong> A T&amp;S team member reviews each submission within 7 business days. Reviewer checks: appropriate visual content; no copyrighted patterns; no offensive imagery. Approved → status <code>approved</code>; rejected → status <code>rejected</code> with reason.</p>\n<p>§1.5  <strong>Name + description content safety.</strong> Submitter provides name (max 30 chars) + description (max 100 chars). Both pass through FR-AI-002 content safety + PII regex.</p>\n<p>§1.6  <strong>Marketplace listing.</strong> Approved UGC listed at <code>/v1/ugc/marketplace?slot=head&amp;order=popular</code>. Pricing set by creator within range 5–500 Coins.</p>\n<p>§1.7  <strong>Purchase flow.</strong> Buyer spends Coins (via FR-ECON-001 ledger). Coins split:</p>\n<ul><li>70% to system sink (kept by platform / fund operations).</li><li>30% to creator's account (also Coins — held in creator's <code>revshare_balance</code> separate ledger account).</li></ul>\n<p>§1.8  <strong>Creator payout.</strong> Creator can convert Coins from <code>revshare_balance</code> to Hearts at a 100:1 ratio after accumulating 10,000 Coins. Hearts then usable on IAP-equivalent items. No real-money payout to creators at P3 (defer to P4+).</p>\n<p>§1.9  <strong>Endpoint — submit.</strong> <code>POST /v1/ugc/submit</code> body <code>{ slot, palette, pattern_id, name, description, price_coins }</code>.</p>\n<p>§1.10  <strong>Endpoint — buy.</strong> <code>POST /v1/ugc/marketplace/:itemId/buy</code>.</p>\n<p>§1.11  <strong>Endpoint — list creator earnings.</strong> <code>GET /v1/ugc/me/earnings</code> returns earned Coins + sale count.</p>\n<p>§1.12  <strong>Per-creator earning cap.</strong> 50,000 Coins / month per creator (anti-farming via socks).</p>\n<p>§1.13  <strong>Kids SKU disabled.</strong> Per FR-AUTH-003 — no submission, no purchase on kids SKU.</p>\n<p>§1.14  <strong>No real-money payout at P3.</strong> Defer to a future regulatory + compliance review (creator monetization may trigger Anti-Money-Laundering compliance).</p>\n<p>§1.15  <strong>Approved UGC tenant scoping.</strong> UGC submitted on consumer tenant (Mochi) is consumer-only. B2B tenants (FR-B2B-001) have their own UGC pool or none.</p>\n<p>§1.16  <strong>Audit + DSR.</strong> Every submission + sale row retained 7 years. Creator can DSR-export.</p>\n<p>§1.17  <strong>Rejection feedback.</strong> Creator receives rejection reason notification. Up to 3 resubmissions per design (then permanent block).</p>\n<p>§1.18  <strong>Revenue receipts.</strong> Each sale writes ledger rows: -&lt;price&gt; buyer wallet → +&lt;70%&gt; system_sink + +&lt;30%&gt; creator_revshare.</p>\n<p>§1.19  <strong>Featured rotation.</strong> Editorial team can feature UGC items in marketplace. Featured items get boosted visibility for 7 days.</p>\n<p>§1.20  <strong>Analytics.</strong> <code>ugc.submitted</code>, <code>ugc.reviewed { verdict }</code>, <code>ugc.purchased</code>, <code>ugc.creator.payout</code>, <code>ugc.featured</code> per FR-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why pattern + palette (no image upload).</strong> Plan §PART 8 + IP risk — free image upload would let users submit copyrighted/inappropriate art. Pattern + palette is creative-but-bounded.</p>\n<p><strong>Why manual T&amp;S review.</strong> Plan §PART 8 + Roblox UGC issues — auto-approval leads to abusive content. Manual review prevents.</p>\n<p><strong>Why 70/30 split.</strong> Plan §PART 6 + Roblox precedent — 30% creator share is industry standard.</p>\n<p><strong>Why no real-money payout at P3.</strong> Anti-Money-Laundering compliance is non-trivial. Cap to in-game value first.</p>\n<p><strong>Why 100:1 Coin→Heart ratio.</strong> Plan §PART 6 — Coins are easier to earn (mini-games etc.); 100:1 keeps Hearts genuinely premium.</p>\n<p><strong>Why 50,000 Coin monthly cap.</strong> Anti-sock-farm — prevents creator alts buying their own UGC.</p>\n<p><strong>Why 3 resubmissions then block.</strong> Anti-spam.</p>\n<p><strong>Why kids SKU disabled.</strong> COPPA — UGC market involves payments + creator interactions.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/econ/ugc/ugc.service.ts (excerpt)\nasync submit(u: AuthedUser, design: UgcDesign) {\n  if (await this.sku(u) === 'kids') throw new HttpException('ugc.kids_forbidden', 403);\n  await this.safety.assertSafe(design.name, { audience: '13+' });\n  await this.safety.assertSafe(design.description ?? '', { audience: '13+' });\n  await this.ledger.spend(u.id, 'coins', 50, `ugc.submit:${design.client_id}`, u.tenant_id);\n  const { data: submission } = await this.supa.from('ugc_submissions').insert({\n    id: generateUlid(),\n    creator_id: u.id, slot: design.slot,\n    palette: design.palette, pattern_id: design.pattern_id,\n    name: design.name, description: design.description ?? '',\n    price_coins: design.price_coins,\n    status: 'pending_review',\n    tenant_id: u.tenant_id,\n  }).select().single();\n  await this.tsQueue.enqueue(submission.id);\n  return { submission_id: submission.id, status: 'pending_review' };\n}\n\nasync buy(u: AuthedUser, itemId: string) {\n  const item = await this.fetchApprovedItem(itemId);\n  await this.ledger.spend(u.id, 'coins', item.price_coins, `ugc.buy:${itemId}`, u.tenant_id);\n  const creatorShare = Math.floor(item.price_coins * 0.30);\n  await this.ledger.grant(item.creator_id, 'coins', creatorShare, `ugc.revshare:${itemId}`, item.tenant_id, 'creator_revshare');\n  await this.supa.from('ugc_sales').insert({ ugc_item_id: itemId, buyer_id: u.id, creator_id: item.creator_id, price_coins: item.price_coins });\n  return { item_id: itemId, granted: true };\n}</code></pre>\n<pre><code class=\"language-sql\">-- migration\ncreate table public.ugc_submissions (\n  id text primary key,\n  creator_id uuid not null references auth.users(id) on delete restrict,\n  slot text not null check (slot in ('accessory_head','accessory_body')),\n  palette jsonb not null,\n  pattern_id text not null,\n  name text not null check (length(name) &lt;= 30),\n  description text not null check (length(description) &lt;= 100),\n  price_coins int not null check (price_coins between 5 and 500),\n  status text not null default 'pending_review' check (status in ('pending_review','approved','rejected')),\n  rejection_reason text,\n  resubmission_count int not null default 0,\n  tenant_id text not null default 'mochi',\n  submitted_at timestamptz not null default now(),\n  reviewed_at timestamptz\n);\n\ncreate table public.ugc_sales (\n  id bigserial primary key,\n  ugc_item_id text not null references public.ugc_submissions(id) on delete restrict,\n  buyer_id uuid not null,\n  creator_id uuid not null,\n  price_coins int not null,\n  sold_at timestamptz not null default now(),\n  tenant_id text not null default 'mochi'\n);</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Submission flow: 50 Coins debit, row inserted with pending_review. Verified. <strong>AC2.</strong> Content safety on name + description. Verified. <strong>AC3.</strong> T&amp;S review approves/rejects. Verified by mock review. <strong>AC4.</strong> Marketplace lists approved items only. Verified. <strong>AC5.</strong> Buy: 70% to platform sink, 30% to creator revshare. Verified. <strong>AC6.</strong> Creator payout requires 10,000 Coin minimum + 100:1 conversion. Verified. <strong>AC7.</strong> 50,000/month creator cap enforced. Verified. <strong>AC8.</strong> Kids SKU forbidden. Verified. <strong>AC9.</strong> Body/face slots forbidden. Verified. <strong>AC10.</strong> Rejection: 3 resubmission cap. Verified. <strong>AC11.</strong> DSR export works. Verified. <strong>AC12.</strong> Audit retention 7-year. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('FR-ECON-003 — UGC', () =&gt; {\n  it('splits revenue 70/30', async () =&gt; {\n    await fund('buyer', 1000);\n    await mockApprovedUgc({ creator: 'creator1', price: 100 });\n    await svc.buy(user('buyer'), 'ugc1');\n    expect(await ledger.balance('creator1', 'coins', 'creator_revshare')).toBe(30);\n  });\n\n  it('blocks kids', async () =&gt; {\n    const u = user('kid', { audience: 'under-13' });\n    await expect(svc.submit(u, mockDesign)).rejects.toMatchObject({ status: 403 });\n  });\n\n  it('enforces 3-resubmission cap', async () =&gt; {\n    for (let i = 0; i &lt; 3; i++) await rejectAndResubmit('design1');\n    await expect(rejectAndResubmit('design1')).rejects.toThrow(/blocked/);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/econ/ugc/revshare.service.ts\n@Injectable()\nexport class RevshareService {\n  async creatorPayout(userId: string): Promise&lt;{ hearts_credited: number; remaining_coins: number }&gt; {\n    const balance = await this.ledger.balanceInAccount(userId, 'coins', 'creator_revshare');\n    if (balance &lt; 10000) throw new HttpException('revshare.below_threshold', 422);\n    const hearts = Math.floor(balance / 100);\n    const coinsConsumed = hearts * 100;\n    await this.ledger.spendFromAccount(userId, 'coins', coinsConsumed, 'creator_revshare', 'revshare.payout');\n    await this.ledger.grant(userId, 'hearts', hearts, `revshare.payout:${userId}`, 'mochi', 'iap_source');\n    return { hearts_credited: hearts, remaining_coins: balance - coinsConsumed };\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Manual T&amp;S review process (post-launch hire). <strong>Internal:</strong> FR-ECON-001 (ledger with creator_revshare account), FR-ECON-002 (Hearts grants), FR-AI-002 (content safety), FR-ART-001 (Spine slot system), FR-AUTH-003 (kids SKU). <strong>Blocks:</strong> none.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/ugc/submit\n{ \"slot\": \"accessory_head\", \"palette\": {...}, \"pattern_id\": \"stripes_a\", \"name\": \"Cozy Beanie\", \"description\": \"Warm and stylish\", \"price_coins\": 50 }\n→ 201 { \"submission_id\": \"01HUGC...\", \"status\": \"pending_review\" }</code></pre>\n<pre><code class=\"language-http\">POST /v1/ugc/marketplace/01HUGC.../buy\n→ 200 { \"item_id\": \"01HUGC...\", \"granted\": true, \"creator_revshare_to\": \"01HU...\" }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"ugc.purchased\", \"item_id\": \"01HUGC...\", \"buyer_id\": \"01HU...\", \"creator_id\": \"01HU...\", \"price_coins\": 50, \"creator_share_coins\": 15 }</code></pre>\n<pre><code class=\"language-json\">{\n  \"id\": \"01HUGC...\",\n  \"creator_id\": \"01HU...\",\n  \"slot\": \"accessory_head\",\n  \"status\": \"approved\",\n  \"price_coins\": 50,\n  \"total_sales\": 42,\n  \"creator_earnings_coins\": 630\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Pattern+palette vs image? → §1.2 — pattern+palette only.</li><li><strong>OQ-2 (resolved):</strong> Real-money payout? → §1.14 — defer to P4+.</li><li><strong>OQ-3 (resolved):</strong> 70/30 split? → §1.7.</li><li><strong>OQ-4 (resolved):</strong> Kids UGC? → §1.13 — disabled.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>T&amp;S review queue backlog &gt;7 days</td><td>SLA breach</td><td>Creator frustration</td><td>Hire more reviewers + auto-approve simple palette swaps after 3 days if no flags</td></tr><tr><td>2</td><td>Inappropriate UGC slips through</td><td>User report</td><td>Brand risk</td><td>Immediate removal + retroactive credit to buyers</td></tr><tr><td>3</td><td>Sock-farm creator earning cap</td><td>Per-month cap</td><td>Verified</td><td>OK</td></tr><tr><td>4</td><td>Body/face slot bypass via API</td><td>Constraint catches</td><td>Rejected</td><td>Validated</td></tr><tr><td>5</td><td>Kids buy UGC via SKU bypass</td><td>RLS + SKU gate</td><td>Verified</td><td>Audit</td></tr><tr><td>6</td><td>Spine slot integrity broken by malformed palette</td><td>Validation</td><td>Reject</td><td>Schema check</td></tr><tr><td>7</td><td>Revshare ledger drift</td><td>Daily reconciliation</td><td>Alert</td><td>Investigate + reconcile</td></tr><tr><td>8</td><td>Creator payout exceeds threshold but blocked</td><td>Bug</td><td>Frustration</td><td>Manual review</td></tr><tr><td>9</td><td>Rejection feedback unclear</td><td>Player support</td><td>UX issue</td><td>Standardise reason codes</td></tr><tr><td>10</td><td>Featured rotation favouritism</td><td>Editorial guidelines</td><td>Brand risk</td><td>Public editorial policy</td></tr><tr><td>11</td><td>DSR export PII leak</td><td>Audit</td><td>Privacy</td><td>Schema lint</td></tr><tr><td>12</td><td>Audit growth</td><td>Disk</td><td>7-year retention</td><td>Configured</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 Pet Couture UGC; plan §PART 6 Roblox UGC playbook.</p>\n<p><strong>Sub-decisions punted to ops:</strong> T&amp;S team size + review SLA + featured-rotation editorial policy.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Auto-approval.</li><li>Body/face slot UGC.</li><li>Real-money payout at P3.</li><li>Kids UGC.</li></ul>\n<p><strong>Cross-reference:</strong> FR-AI-002 content safety, FR-ART-001 slot system, FR-ECON-001 ledger.</p>";
