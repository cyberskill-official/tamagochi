window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-B2B-003"]="<h2>§1 — Specification (BCP-14 normative)</h2>\n<p>§1.1  <strong>Operator SSO.</strong> Operators authenticate via SSO (Apple/Google + corporate SAML if needed). Operator account binds to one tenant slug (or many for CyberSkill staff).</p>\n<p>§1.2  <strong>Entitlement tier.</strong> Console enforces tier:</p>\n<ul><li><strong>Setup-only</strong> ($25K-150K one-off): operator can configure theme + quests; CyberSkill operates.</li><li><strong>SaaS</strong> ($2K-25K/mo): full self-service.</li><li><strong>Rev-share</strong> (15-30%): self-service + monthly settlement.</li><li><strong>Per-MAU</strong> ($0.05-0.20): self-service + monthly MAU billing.</li></ul>\n<p>§1.3  <strong>Theme upload.</strong> Operator uploads ZIP containing palette + logo + Spine skin JSON + override strings. Console validates signature, schema, and contract preservation.</p>\n<p>§1.4  <strong>Quest CMS.</strong> Operator authors quests via web UI: title, description, trigger (sign-in, save 100K VND, etc.), reward (Coins). Saved to per-tenant CMS API consumed by FR-B2B-001 manifest.</p>\n<p>§1.5  <strong>KPI dashboard.</strong> Read-only dashboard: DAU/MAU, retention curves, IAP revenue, top quests by completion. Powered by FR-OBS-001 per-tenant analytics workspace.</p>\n<p>§1.6  <strong>Audit log view.</strong> Read-only audit timeline per FR-LEGAL-001 + FR-OBS-002 — DSR requests, ban events, theme uploads, etc.</p>\n<p>§1.7  <strong>Billing tab.</strong> Per FR-B2B-002 SLA + tier — monthly invoice, MAU usage, payment status.</p>\n<p>§1.8  <strong>DPO data export.</strong> Operator can export tenant data (DSR-compliant) for offboarding. Atomic + signed.</p>\n<p>§1.9  <strong>Multi-operator support.</strong> Tenant can have multiple operators with role: admin / editor / viewer. Audit logs operator action.</p>\n<p>§1.10  <strong>Endpoint — theme upload.</strong> <code>POST /v1/petos/tenant/:slug/theme</code> — multipart upload + signature + validation.</p>\n<p>§1.11  <strong>Endpoint — quest list.</strong> <code>GET /v1/petos/tenant/:slug/quests</code> + <code>POST</code> create + <code>PATCH</code> update + <code>DELETE</code>.</p>\n<p>§1.12  <strong>Endpoint — KPIs.</strong> <code>GET /v1/petos/tenant/:slug/kpis?range=7d</code>.</p>\n<p>§1.13  <strong>No tenant cross-access.</strong> Operators see only their tenant. Cross-tenant requires DPO grant per FR-B2B-002 §1.8.</p>\n<p>§1.14  <strong>Audit retention.</strong> 7 years.</p>\n<p>§1.15  <strong>Console hosted at admin.petos.tamagochi.app.</strong> Separate hostname from consumer game.</p>\n<p>§1.16  <strong>DPO review of quest copy.</strong> Per FR-AI-002 + FR-LEGAL-001 — kid-tenant quest copy requires DPO sign-off before live.</p>\n<p>§1.17  <strong>Staging vs production.</strong> Operator can preview changes in staging environment before push-to-prod.</p>\n<p>§1.18  <strong>Bulk operations rate-limited.</strong> Theme upload max 10/day per tenant; quest changes 100/day.</p>\n<p>§1.19  <strong>2FA mandatory.</strong> Operator accounts require 2FA for any write action.</p>\n<p>§1.20  <strong>Analytics.</strong> <code>petos.operator.login</code>, <code>petos.theme.uploaded</code>, <code>petos.quest.changed</code>, <code>petos.dsr.export_initiated</code> per FR-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why Next.js console.</strong> Plan §PART 6 — separate web app from game client. Different audience + auth model.</p>\n<p><strong>Why operator entitlement tiers.</strong> Plan §PART 6 — different B2B contracts.</p>\n<p><strong>Why DPO review of quest copy for kids.</strong> FR-LEGAL-001 — kid-tenant content must pass review.</p>\n<p><strong>Why staging environment.</strong> Plan §PART 4 — operators need confidence before push-to-prod.</p>\n<p><strong>Why bulk rate-limit.</strong> Anti-abuse + accidental misconfig protection.</p>\n<p><strong>Why 2FA mandatory.</strong> Plan §PART 8 — operator account compromise = brand-blast risk.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/petos-console/src/app/theme/page.tsx\nexport default function ThemePage() {\n  const [file, setFile] = useState&lt;File | null&gt;(null);\n  async function upload() {\n    const form = new FormData();\n    form.append('theme', file!);\n    const r = await fetch(`/api/petos/tenant/${tenantSlug}/theme`, { method: 'POST', body: form });\n    return r.json();\n  }\n  return &lt;FileInput onChange={setFile} onUpload={upload} /&gt;;\n}</code></pre>\n<pre><code class=\"language-sql\">create table public.petos_operators (\n  id uuid primary key default gen_random_uuid(),\n  tenant_slug text not null references public.tenants(slug),\n  email text not null,\n  role text not null check (role in ('admin','editor','viewer')),\n  invited_by uuid,\n  joined_at timestamptz,\n  last_seen_at timestamptz,\n  unique (tenant_slug, email)\n);\n\ncreate table public.petos_audit_log (\n  id bigserial primary key,\n  tenant_slug text not null,\n  operator_id uuid not null,\n  action text not null,\n  payload jsonb,\n  occurred_at timestamptz not null default now()\n);\n\ncreate table public.tenant_quests (\n  id text primary key,\n  tenant_slug text not null references public.tenants(slug),\n  title text not null,\n  description text not null,\n  trigger_type text not null,\n  trigger_config jsonb,\n  reward_coins int default 0,\n  status text not null default 'draft' check (status in ('draft','staging','production','archived')),\n  created_by uuid,\n  created_at timestamptz default now(),\n  updated_at timestamptz default now()\n);</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Operator can log in via SSO. Verified. <strong>AC2.</strong> Theme upload signed validation. Verified. <strong>AC3.</strong> Quest CMS create/edit/delete. Verified. <strong>AC4.</strong> KPIs render per range. Verified. <strong>AC5.</strong> Operator can't access other tenant. Verified. <strong>AC6.</strong> Audit log captures actions. Verified. <strong>AC7.</strong> DSR export atomic + signed. Verified. <strong>AC8.</strong> Multi-operator roles enforced. Verified. <strong>AC9.</strong> 2FA mandatory for writes. Verified. <strong>AC10.</strong> Staging preview works. Verified. <strong>AC11.</strong> DPO review enforced for kids tenants. Verified. <strong>AC12.</strong> Bulk rate-limit. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('FR-B2B-003 — PetOS console', () =&gt; {\n  it('operator restricted to own tenant', async () =&gt; {\n    await mockOperator('alice', 'techcombank', 'admin');\n    await expect(api.get('/petos/tenant/viettel/quests', operatorToken('alice'))).rejects.toMatchObject({ status: 403 });\n  });\n\n  it('2FA required for theme upload', async () =&gt; {\n    await expect(api.post('/petos/tenant/techcombank/theme', themeFile, opToken('alice', { '2fa': false }))).rejects.toMatchObject({ status: 401 });\n  });\n\n  it('kids tenant quest requires DPO review', async () =&gt; {\n    await mockTenant('techcombank', { audience_default: 'under-13' });\n    const r = await api.post('/petos/tenant/techcombank/quests', { title: 'Test', status: 'production' });\n    expect(r.status).toBe(202); // pending DPO review\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/petos-console/src/lib/sso-handshake.ts\nexport async function ssoHandshake(provider: 'google' | 'apple' | 'saml'): Promise&lt;{ token: string; tenant_slug: string; role: string }&gt; {\n  const r = await fetch(`/api/petos/auth/sso/${provider}`);\n  if (!r.ok) throw new Error('sso.failed');\n  const j = await r.json();\n  return j;\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Apple/Google SSO; Next.js hosting (Vercel or Cloudflare). <strong>Internal:</strong> FR-B2B-001 (manifest base), FR-B2B-002 (tenant partition), FR-AUTH-001 (auth), FR-OBS-001 (per-tenant analytics), FR-LEGAL-001 (DPO). <strong>Blocks:</strong> FR-B2B-004/005.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/petos/tenant/techcombank/theme\nAuthorization: Bearer &lt;op-token&gt;\nX-2FA: 123456\nmultipart/form-data:\n  theme: &lt;ZIP&gt;\n→ 201 { \"theme_version\": \"2026-08-01\", \"validated\": true, \"production_at\": null }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"petos.theme.uploaded\", \"tenant\": \"techcombank\", \"operator\": \"alice@techcombank.com\", \"version\": \"2026-08-01\" }</code></pre>\n<pre><code class=\"language-json\">{\n  \"kpis\": {\n    \"dau_7d\": [4200, 4350, 4180, 4500, 4620, 4480, 4720],\n    \"mau\": 35000,\n    \"iap_revenue_usd_7d\": 8430,\n    \"top_quests\": [\n      { \"id\": \"save-100k\", \"completions_7d\": 2401 }\n    ]\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"petos.cross_tenant_forbidden\", \"tenant_requested\": \"viettel\", \"operator_tenant\": \"techcombank\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> SSO provider? → §1.1.</li><li><strong>OQ-2 (resolved):</strong> Tier entitlement? → §1.2.</li><li><strong>OQ-3 (resolved):</strong> DPO review? → §1.16.</li><li><strong>OQ-4 (resolved):</strong> Staging? → §1.17.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Theme validation fails</td><td>Signature check</td><td>Reject</td><td>Operator re-uploads</td></tr><tr><td>2</td><td>Cross-tenant access attempt</td><td>RLS</td><td>Rejected</td><td>Audit</td></tr><tr><td>3</td><td>2FA bypass</td><td>Validation</td><td>Rejected</td><td>Investigate</td></tr><tr><td>4</td><td>DPO review backlog</td><td>Daily</td><td>Quests stuck draft</td><td>Hire reviewers</td></tr><tr><td>5</td><td>Operator compromise</td><td>Audit</td><td>Bulk damage</td><td>Disable account; rotate keys</td></tr><tr><td>6</td><td>KPI dashboard slow</td><td>Caching</td><td>UX issue</td><td>Cache layer</td></tr><tr><td>7</td><td>DSR export incomplete</td><td>Audit</td><td>Compliance gap</td><td>Manual completion</td></tr><tr><td>8</td><td>Operator role escalation</td><td>Audit</td><td>Privacy issue</td><td>DPO escalation</td></tr><tr><td>9</td><td>Bulk theme upload abuse</td><td>Rate-limit</td><td>Blocked</td><td>Adjust limit</td></tr><tr><td>10</td><td>Staging diff to prod</td><td>Visual regression</td><td>Customer surprise</td><td>Force review</td></tr><tr><td>11</td><td>Audit retention</td><td>DPO</td><td>7-year</td><td>Configured</td></tr><tr><td>12</td><td>Console hosting outage</td><td>Vercel/CF</td><td>Unavailable</td><td>Multi-region</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 6 (PetOS console).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Tier pricing negotiation; SAML integration per-tenant.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Cross-tenant operator access.</li><li>Theme bypass signature.</li><li>Kid-tenant quest without DPO review.</li></ul>\n<p><strong>Cross-reference:</strong> FR-B2B-001 manifest; FR-B2B-002 partition; FR-B2B-004 Techcombank; FR-B2B-005 Viettel.</p>";
