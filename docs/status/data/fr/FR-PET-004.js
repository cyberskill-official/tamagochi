window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PET-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>90-second target.</strong> The onboarding flow from \"user successfully signed in for the first time\" to \"user dismisses tutorial + sees their pet in idle state\" MUST complete in <strong>≤ 90 seconds median, ≤ 150 seconds P95</strong> for a player who follows the happy path. Player-driven pauses (e.g. choosing a name) MUST NOT count against this budget.</p>\n<p>§1.2  <strong>Five-step flow.</strong> The onboarding flow MUST present EXACTLY five steps, in order:</p>\n<ol><li><strong>Hatch ceremony</strong> — Egg animation (FR-ART-001 <code>hatch</code>); ~20 seconds; auto-progresses;</li><li><strong>Name</strong> — Text input + content-safety (FR-PET-001 §1.5); player-paced;</li><li><strong>First pat</strong> — Single big \"tap your pet\" cue, haptic + <code>care_pet</code> animation + +5 happiness (no stat displayed yet);</li><li><strong>Co-parent stub</strong> — \"Invite a grown-up / friend to share?\" — at P1 this is a stub that records intent only and shows \"Coming soon — we'll let you know!\"; FR-SOCIAL-002 in P2 wires the real action;</li><li><strong>Tutorial dismiss</strong> — A single-screen tutorial card showing the 4 stat-bars + \"Tap your pet to interact.\" Single \"Got it\" CTA dismisses + reveals the main game scene.</li></ol>\n<p>§1.3  <strong>State machine.</strong> Onboarding state MUST be persisted to <code>public.onboarding_state</code> (one row per user) so a player who closes mid-onboarding resumes at the same step on next launch. Columns: <code>(user_id pk, current_step text, hatch_completed_at, name_set_at, first_pat_at, co_parent_intent boolean, tutorial_dismissed_at, started_at)</code>.</p>\n<p>§1.4  <strong>Skip conditions.</strong> A returning user with <code>tutorial_dismissed_at IS NOT NULL</code> MUST skip onboarding entirely. A user who completed steps 1-3 but not step 4-5 resumes at step 4 on next launch. There is no \"skip all\" path — every player MUST complete steps 1-3 (hatch ceremony is the bonding moment).</p>\n<p>§1.5  <strong>First pat haptic.</strong> The \"first pat\" step MUST: (a) display a single oversized tap target on the pet's body; (b) on tap, fire <code>HapticsAdapter.tap('light')</code> + play <code>care_pet</code> animation; (c) immediately apply <code>apply(stats, 'hug')</code> (+25 happiness) via the server; (d) emit <code>onboarding.first_pat.completed</code> event; (e) advance to step 4.</p>\n<p>§1.6  <strong>Optional co-parent stub at P1.</strong> At P1, the co-parent step records intent only. The UI MUST: (a) show \"Want to share Mochi with a friend / grown-up?\"; (b) offer \"Yes — tell me later\" + \"No thanks\" CTAs; (c) regardless of choice, advance; (d) on \"Yes\", record <code>co_parent_intent=true</code> and queue a re-prompt notification when FR-SOCIAL-002 (P2) ships. NO email or invite code is collected at P1.</p>\n<p>§1.7  <strong>No IAP / sub gating during onboarding.</strong> Apple App Store Review Guideline 3.1.3(b) (and Google equivalents) forbid upsells during onboarding for free-with-IAP apps. Onboarding MUST NOT show IAP catalogue or subscription pitches; first IAP pitch MUST be deferred to ≥ 5 minutes after tutorial dismiss.</p>\n<p>§1.8  <strong>Step-budget instrumentation.</strong> Each step MUST emit:</p>\n<ul><li><code>onboarding.&lt;step&gt;.entered</code></li><li><code>onboarding.&lt;step&gt;.completed</code> with <code>duration_ms</code></li></ul>\n<p><code>duration_ms</code> is the time from <code>entered</code> to <code>completed</code> per step. Server-side aggregates per-cohort funnel via FR-OBS-001 pipeline.</p>\n<p>§1.9  <strong>Drop-off analytics.</strong> Players who abandon mid-flow (close app, time out, sign out) MUST be recorded: <code>onboarding.&lt;step&gt;.abandoned { reason }</code>. <code>reason</code> enumerates <code>app_killed</code>, <code>timeout_60s</code>, <code>signed_out</code>, <code>error_other</code>.</p>\n<p>§1.10  <strong>Localised copy.</strong> Onboarding strings MUST live in <code>apps/cocos/assets/i18n/&lt;locale&gt;/onboarding.json</code> (per FR-I18N-001). EN + VI MUST be authored before P1 release. Other locales follow in P4.</p>\n<p>§1.11  <strong>Under-13 SKU adaptations.</strong> On the kids SKU, onboarding MUST:</p>\n<ul><li>skip the co-parent step entirely (kids cannot independently invite anyone — parental dashboard controls friend additions in FR-SUB-002);</li><li>show the tutorial-card in larger fonts (1.3× base);</li><li>ensure all CTAs are at least 88×88 pt (Apple Kids Category accessibility minimum);</li><li>play <code>hatch</code> audio at 50% default volume (audio default is muted on kids SKU per FR-ART-001 §1.14, so this only applies if the player manually enabled audio).</li></ul>\n<p>§1.12  <strong>Hatch ceremony — no skip until midpoint.</strong> The Spine <code>hatch</code> animation (~20s) MUST play at least to midpoint (~10s) before any \"Skip\" CTA appears. This prevents impatient first-time players from blowing past the bonding moment. After midpoint, a \"Skip ahead\" CTA appears.</p>\n<p>§1.13  <strong>A11Y reduce-motion adaptation.</strong> When <code>prefers-reduced-motion</code> is enabled (FR-A11Y-001), the hatch ceremony plays the simplified path: fade-in instead of egg-shake; no confetti particles; static <code>idle_baby</code> skip directly to step 2.</p>\n<p>§1.14  <strong>Name step UX.</strong> The name step MUST: (a) suggest \"Mochi\" pre-filled (player can clear); (b) accept submission with Enter or \"Save\" button; (c) show server-side rejection inline (\"That name is taken in your roster\" / \"Please choose a friendlier name\" / \"Names should be 2-24 characters\"); (d) on rejection, focus the input + preserve text.</p>\n<p>§1.15  <strong>AC analytics funnel.</strong> A scheduled job MUST query the <code>onboarding_state</code> table daily to compute the P0-pipeline funnel: signups → step 1 entered → step 1 completed → step 2 ... → tutorial dismissed. Funnel rendered in the Grafana dashboard <code>pet-care-funnel.json</code> (FR-OBS-001 §1.18).</p>\n<p>§1.16  <strong>Onboarding-state RLS.</strong> The <code>onboarding_state</code> table MUST have RLS — only the player's own row is readable / writable. DPO read for support.</p>\n<p>§1.17  <strong>Resume token.</strong> When a player resumes mid-flow, the client MUST: (a) call <code>GET /v1/onboarding/state</code>; (b) read <code>current_step</code>; (c) navigate to that scene; (d) preserve previously-entered data (e.g. the player's pet ID from hatch).</p>\n<p>§1.18  <strong>Onboarding completion grants.</strong> On tutorial dismiss, the player MUST receive: 100 soft Coins + the \"First Pat\" badge. Badges live in a future FR (FR-VIRAL-004 battle pass scaffold) but the badge slot is reserved here.</p>\n<p>§1.19  <strong>Sentry breadcrumbs.</strong> Each onboarding step MUST emit a Sentry breadcrumb with the step name + timestamp. This makes the first-launch crash debugging path traceable.</p>\n<p>§1.20  <strong>No third-party data collection during onboarding.</strong> Per FR-OBS-001 §1.17 (PII consent before identify), <code>Analytics.identify</code> is deferred to AFTER tutorial dismiss. During onboarding, all analytics are session-scoped (anonymous identifier) until the player has consented to the policy version.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why ≤ 90 seconds.</strong> Plan §PART 5 — Onboarding (≤ 90 seconds). This is a documented industry benchmark for casual mobile games. Beyond 90s, drop-off accelerates aggressively (data from Outfit7 + Roblox studios show &gt;50% drop on onboardings over 2 minutes).</p>\n<p><strong>Why exactly 5 steps.</strong> The hatch ceremony + naming are the bonding moments — non-negotiable. First pat is the haptic conversion moment (the player feels the device respond — that's the \"I'm in\" instant). Co-parent stub is the optional viral hook (set the seed for P2). Tutorial dismiss is the explicit-exit transition. Any more steps inflates duration; any fewer skips a documented retention lever.</p>\n<p><strong>Why a stub for co-parent at P1.</strong> FR-SOCIAL-002 (PetPair co-parent) lands in P2. But the co-parent VIRAL hook is plan §PART 3 #1 — designing the placeholder at P1 ensures the slot exists, the storyboard locks in, AND the data on \"what % of players said yes\" tells us in advance whether to prioritise P2 PetPair work.</p>\n<p><strong>Why no IAP gating during onboarding.</strong> Apple Guideline 3.1.3(b) — forced subscription / IAP screens during onboarding for free-with-IAP apps is a hard rejection. The 5-minute defer matches Apple Review's empirical pattern.</p>\n<p><strong>Why first-pat haptic is non-skippable until midpoint.</strong> Plan §PART 3 — haptic + animation = the \"moment of conversion.\" Letting impatient players skip it loses the conversion event. Midpoint compromise: bonding ceremony plays, but a \"skip ahead\" appears if the player genuinely wants to move on.</p>\n<p><strong>Why server-side stat application during onboarding.</strong> Per FR-PET-003 §1.5 server-authoritative. The +25 happiness from first-pat is a real stat mutation, persisted from the start. No special onboarding-only state model — consistency with the production stat system.</p>\n<p><strong>Why drop-off analytics with reasons.</strong> Funnel drop-off without reasons is useless. Knowing \"60% drop at step 2 with reason=timeout_60s\" vs \"60% drop at step 2 with reason=app_killed\" suggests different fixes (name UX vs onboarding length).</p>\n<p><strong>Why under-13 SKU larger fonts + bigger CTAs.</strong> Apple Kids Category Guideline + UX research on children 5-12 fine-motor skills + reading speed. 1.3× font + 88×88 pt minimum tap target are Apple's published baselines.</p>\n<p><strong>Why kids SKU skips co-parent step.</strong> Kids cannot independently invite anyone (COPPA + FR-SOCIAL-001 §1 + FR-AUTH-003 design). Surfacing the co-parent invite on kids SKU would be confusing UX + a regulator question.</p>\n<p><strong>Why pre-fill \"Mochi\" as suggested name.</strong> Plan §PART 3 example pet name. Most players accept defaults; \"Mochi\" branded as the launch hero + becomes the recognizable franchise mascot. Players CAN change it.</p>\n<p><strong>Why 100 Coins + badge at completion.</strong> Concrete reward = retention reinforcement. 100 Coins is enough to do 1-2 things in the first session but not enough to skip future engagement loops. Badge slot reserved for FR-VIRAL-004 battle pass.</p>\n<p><strong>Why analytics <code>identify</code> deferred until after tutorial dismiss.</strong> FR-OBS-001 §1.17 — policy-version stamping happens at sign-in (FR-AUTH-001 §1.7), but <code>identify</code> to analytics vendors carries PII context. Deferring until after tutorial dismiss aligns with \"first meaningful intent\" — the player has demonstrably engaged with the product.</p>\n<p><strong>Why scene-based architecture in Cocos.</strong> Cocos's scene system is the canonical pattern for sequential gameplay flow. Each onboarding step is a scene; navigation between scenes is testable + restorable from <code>current_step</code>.</p>\n<p><strong>Why localized copy from day one (EN + VI).</strong> P1 soft-launch is VN-first (plan §PART 7). Without VI copy at P1, the launch market UX is broken. EN is the dev / fallback locale.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Onboarding state migration</h3>\n<pre><code class=\"language-sql\">-- infra/supabase/standard/migrations/20260517_009_onboarding_state.sql\ncreate table public.onboarding_state (\n  user_id uuid primary key references auth.users(id) on delete cascade,\n  current_step text not null default 'hatch'\n                check (current_step in ('hatch','name','first_pat','co_parent_stub','tutorial','done')),\n  hatch_completed_at      timestamptz,\n  name_set_at             timestamptz,\n  first_pat_at            timestamptz,\n  co_parent_intent        boolean default false,\n  tutorial_dismissed_at   timestamptz,\n  started_at              timestamptz not null default now(),\n  tenant_id               text not null default 'mochi'\n);\nalter table public.onboarding_state enable row level security;\ncreate policy \"onboarding self\" on public.onboarding_state for select using (user_id = auth.uid());\ncreate policy \"onboarding self update\" on public.onboarding_state for update using (user_id = auth.uid());</code></pre>\n<h3>3.2 — Onboarding state service</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/onboarding/onboarding-state.service.ts\n@Injectable()\nexport class OnboardingStateService {\n  constructor(private readonly supa: SupabaseClient) {}\n\n  async getOrInit(userId: string): Promise&lt;OnboardingState&gt; {\n    const { data, error } = await this.supa.from('onboarding_state')\n      .select('*').eq('user_id', userId).maybeSingle();\n    if (data) return data as OnboardingState;\n    const { data: inserted } = await this.supa.from('onboarding_state')\n      .insert({ user_id: userId }).select().single();\n    return inserted as OnboardingState;\n  }\n\n  async markStepCompleted(userId: string, step: OnboardingStep, durationMs: number): Promise&lt;void&gt; {\n    const fieldByStep: Record&lt;OnboardingStep, string&gt; = {\n      hatch: 'hatch_completed_at',\n      name: 'name_set_at',\n      first_pat: 'first_pat_at',\n      co_parent_stub: 'co_parent_intent',  // boolean\n      tutorial: 'tutorial_dismissed_at',\n    };\n    const field = fieldByStep[step];\n    const nextStep = this.computeNextStep(step);\n    await this.supa.from('onboarding_state').update({\n      [field]: step === 'co_parent_stub' ? true : new Date().toISOString(),\n      current_step: nextStep,\n    }).eq('user_id', userId);\n    await this.audit.write({ who: userId, what: `onboarding.${step}.completed`, what_keys: { duration_ms: durationMs } });\n  }\n\n  private computeNextStep(step: OnboardingStep): OnboardingStep {\n    const order: OnboardingStep[] = ['hatch','name','first_pat','co_parent_stub','tutorial','done'];\n    return order[order.indexOf(step) + 1] as OnboardingStep;\n  }\n}</code></pre>\n<h3>3.3 — Cocos <code>OnboardingFlow.ts</code></h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/onboarding/OnboardingFlow.ts\nimport { director, _decorator, Component } from 'cc';\nimport { OnboardingState } from './types';\n\nconst { ccclass } = _decorator;\n\n@ccclass('OnboardingFlow')\nexport class OnboardingFlow extends Component {\n  async start() {\n    const state = await this.fetchOnboardingState();\n    if (state.current_step === 'done') {\n      director.loadScene('Game');\n      return;\n    }\n    director.loadScene(this.sceneForStep(state.current_step), () =&gt; this.passContext({ state }));\n  }\n\n  private sceneForStep(step: string): string {\n    return {\n      hatch: 'OnboardingHatch',\n      name: 'OnboardingName',\n      first_pat: 'OnboardingFirstPat',\n      co_parent_stub: 'OnboardingCoParentStub',\n      tutorial: 'OnboardingTutorial',\n    }[step]!;\n  }\n}</code></pre>\n<h3>3.4 — Cocos <code>FirstPatScene.ts</code> excerpt</h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/onboarding/FirstPatScene.ts\nimport { _decorator, Component, Node, EventTouch } from 'cc';\nimport { HapticsAdapter } from '../art/HapticsAdapter';\nimport { SpineLoader } from '../art/SpineLoader';\nimport { OnboardingApi } from './api';\n\n@ccclass('FirstPatScene')\nexport class FirstPatScene extends Component {\n  @property(Node) petNode!: Node;\n  private entered = Date.now();\n\n  start() {\n    OnboardingApi.markEntered('first_pat');\n    this.petNode.on(Node.EventType.TOUCH_END, (e: EventTouch) =&gt; this.onPat(e));\n  }\n\n  private async onPat(_e: EventTouch) {\n    HapticsAdapter.tap('light');\n    new SpineLoader().playContractAnimation(this.petNode.getComponent('Spine')!, 'care_pet');\n    await OnboardingApi.applyFirstPat();\n    await OnboardingApi.markCompleted('first_pat', Date.now() - this.entered);\n    director.loadScene('OnboardingCoParentStub');\n  }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Happy-path onboarding completes in ≤ 90 s median across 100 test runs on a 2024 mid-tier device + 4G. Verified by Detox UI test with timing assertions.</p>\n<p><strong>AC2.</strong> Onboarding state persists across app kill: closing app at step 3 + reopen resumes at step 3. Verified by Detox test simulating background → kill → relaunch.</p>\n<p><strong>AC3.</strong> Returning user with <code>tutorial_dismissed_at</code> set skips onboarding entirely. Verified by <code>__tests__/skip-conditions.spec.ts</code>.</p>\n<p><strong>AC4.</strong> Each step emits <code>entered</code> + <code>completed</code> + <code>duration_ms</code> analytics events with valid schema. Verified by <code>__tests__/OnboardingFlow.spec.ts</code> + obs schema validation.</p>\n<p><strong>AC5.</strong> Drop-off events emitted on app kill (<code>app_killed</code>) and on 60-second-no-input (<code>timeout_60s</code>). Verified by spec test.</p>\n<p><strong>AC6.</strong> First-pat haptic fires + happiness stat increments by 25 server-side. Verified by integration test driving the touch event.</p>\n<p><strong>AC7.</strong> Co-parent stub at P1 records <code>co_parent_intent</code> boolean but does NOT collect an email. Verified by spec test.</p>\n<p><strong>AC8.</strong> Under-13 SKU skips the co-parent step. Verified by spec test with <code>BUILD_TARGET=kids</code>.</p>\n<p><strong>AC9.</strong> Under-13 SKU renders tutorial in 1.3× font + 88×88 pt minimum tap targets. Verified by Playwright visual regression test.</p>\n<p><strong>AC10.</strong> Hatch ceremony Skip CTA appears at or after midpoint (≥ 10 s), not before. Verified by Detox timing test.</p>\n<p><strong>AC11.</strong> Reduce-motion mode plays simplified hatch (fade-in, no confetti). Verified by Playwright with <code>prefers-reduced-motion: reduce</code>.</p>\n<p><strong>AC12.</strong> Localized copy: VI locale renders Vietnamese strings + EN locale renders English. Verified by Detox snapshot tests in both locales.</p>\n<p><strong>AC13.</strong> Onboarding-state RLS — player A cannot read player B's onboarding_state. Verified by <code>__tests__/onboarding-state.spec.ts</code> with two seeded users.</p>\n<p><strong>AC14.</strong> <code>Analytics.identify</code> is NOT called during onboarding scenes; first identify call happens after tutorial dismiss. Verified by spy on <code>Analytics.identify</code>.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 — Onboarding state spec</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/onboarding/__tests__/onboarding-state.spec.ts\nimport { describe, it, expect } from 'vitest';\nimport { OnboardingStateService } from '../onboarding-state.service';\n\ndescribe('FR-PET-004 — onboarding state', () =&gt; {\n  const svc = new OnboardingStateService(mockSupa);\n\n  it('initializes at step hatch on first call', async () =&gt; {\n    const state = await svc.getOrInit('u1');\n    expect(state.current_step).toBe('hatch');\n  });\n\n  it('marks step completed + advances current_step', async () =&gt; {\n    await svc.markStepCompleted('u1', 'hatch', 18234);\n    const state = await svc.getOrInit('u1');\n    expect(state.current_step).toBe('name');\n    expect(state.hatch_completed_at).toBeTruthy();\n  });\n\n  it('co_parent_stub records boolean intent', async () =&gt; {\n    await svc.markStepCompleted('u1', 'co_parent_stub', 2100);\n    const state = await svc.getOrInit('u1');\n    expect(state.co_parent_intent).toBe(true);\n  });\n\n  it('returning user with tutorial_dismissed skips entire flow', async () =&gt; {\n    await svc.markStepCompleted('u1', 'tutorial', 1500);\n    const state = await svc.getOrInit('u1');\n    expect(state.current_step).toBe('done');\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/onboarding/api.ts\nimport { Analytics } from '../obs/Analytics';\n\nexport class OnboardingApi {\n  static async markEntered(step: string): Promise&lt;void&gt; {\n    Analytics.track(`onboarding.${step}.entered`, { ts: Date.now() });\n  }\n  static async markCompleted(step: string, durationMs: number): Promise&lt;void&gt; {\n    Analytics.track(`onboarding.${step}.completed`, { duration_ms: durationMs });\n    await fetch(`${process.env.API_URL}/v1/onboarding/step-completed`, {\n      method: 'POST',\n      headers: { 'content-type': 'application/json', Authorization: `Bearer ${getJwt()}` },\n      body: JSON.stringify({ step, duration_ms: durationMs }),\n    });\n  }\n  static async applyFirstPat(): Promise&lt;void&gt; {\n    await fetch(`${process.env.API_URL}/v1/pets/first-pat`, {\n      method: 'POST', headers: { Authorization: `Bearer ${getJwt()}` },\n    });\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> None (uses internal Supabase + analytics scaffolds).</p>\n<p><strong>Internal:</strong> FR-PET-001 (pet created), FR-PET-002 (stage transitions show in flow), FR-PET-003 (first pat applies happiness), FR-ART-001 (hatch + care_pet animations + haptics), FR-AUTH-001/003 (signed-in player), FR-OBS-001 (analytics + Sentry breadcrumbs), FR-I18N-001 (locale bundles), FR-LEGAL-001 (<code>identify</code> deferral).</p>\n<p><strong>Blocks:</strong> FR-CARE-001 (first feed assumes pet is past onboarding), FR-CARE-005 (streak system starts after tutorial dismiss).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — <code>onboarding.hatch.completed</code> event</h3>\n<pre><code class=\"language-json\">{\n  \"event\": \"onboarding.hatch.completed\",\n  \"duration_ms\": 18234,\n  \"user_id\": \"01HC7QGZK4XN8YA1J3WB6XX99\",\n  \"pet_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"sku\": \"standard\",\n  \"build_target\": \"standard\",\n  \"ts\": \"2026-08-12T14:36:01Z\"\n}</code></pre>\n<h3>8.2 — Onboarding state row</h3>\n<pre><code class=\"language-json\">{\n  \"user_id\": \"01HC7QGZK4XN8YA1J3WB6XX99\",\n  \"current_step\": \"tutorial\",\n  \"hatch_completed_at\": \"2026-08-12T14:36:01Z\",\n  \"name_set_at\":       \"2026-08-12T14:36:35Z\",\n  \"first_pat_at\":      \"2026-08-12T14:36:50Z\",\n  \"co_parent_intent\":  true,\n  \"tutorial_dismissed_at\": null,\n  \"started_at\":        \"2026-08-12T14:35:00Z\",\n  \"tenant_id\": \"mochi\"\n}</code></pre>\n<h3>8.3 — Drop-off event</h3>\n<pre><code class=\"language-json\">{\n  \"event\": \"onboarding.first_pat.abandoned\",\n  \"reason\": \"timeout_60s\",\n  \"duration_ms\": 60001,\n  \"ts\": \"2026-08-12T14:37:50Z\"\n}</code></pre>\n<h3>8.4 — Localised string bundle</h3>\n<pre><code class=\"language-jsonc\">// apps/cocos/assets/i18n/vi/onboarding.json\n{\n  \"hatch.welcome\": \"Chào mừng! Hãy nở quả trứng của bạn!\",\n  \"name.placeholder\": \"Mochi\",\n  \"first_pat.cta\": \"Chạm vào Mochi để chào nhé!\",\n  \"co_parent.title\": \"Muốn chia sẻ Mochi với bạn bè/người thân?\",\n  \"tutorial.dismiss_cta\": \"Hiểu rồi!\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> 90-second target vs 60-second? → §1.1 + §2 — 90 s aligns plan + industry data.</li><li><strong>OQ-2 (resolved):</strong> Co-parent at P1 vs deferred entirely? → §1.6 + §2 — stub at P1 captures intent + data for P2 prioritisation.</li><li><strong>OQ-3 (resolved):</strong> Default name pre-fill? → §1.14 + §2 — \"Mochi\" default; player can clear.</li><li><strong>OQ-4 (resolved):</strong> Tutorial single-card or carousel? → §1.2 — single card; carousel inflates time + decision fatigue.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Player closes app mid-flow</td><td><code>app_killed</code> reason</td><td>Onboarding paused</td><td>Resume from <code>current_step</code> on next launch</td></tr><tr><td>2</td><td>Onboarding state row missing on first launch</td><td><code>getOrInit</code> returns null</td><td>Init created</td><td>Auto-insert; emit <code>onboarding.state.init.fallback</code> info</td></tr><tr><td>3</td><td>First-pat API 500 (stat write fails)</td><td>Sentry alert</td><td>UX hangs</td><td>Retry once; on 2nd fail, advance optimistically + reconcile on next stat tick</td></tr><tr><td>4</td><td>Hatch ceremony animation fails to load</td><td>Cocos error</td><td>UX broken</td><td>Fallback to static egg image + auto-advance after 20 s; Sentry alert</td></tr><tr><td>5</td><td>Localised string missing</td><td>Resource lookup returns key</td><td>English fallback</td><td>Auto-fallback to EN; report missing-key event</td></tr><tr><td>6</td><td>Drop-off analytics not emitted (app killed before send)</td><td>Funnel drop unexplained</td><td>Bad metric</td><td>Use Sentry breadcrumb as secondary signal</td></tr><tr><td>7</td><td>Co-parent stub renders on kids SKU</td><td>Playwright UI test</td><td>Confusing UX</td><td>Build-target check in scene init; hotfix</td></tr><tr><td>8</td><td>Reduce-motion mode not detected</td><td>Hatch animation plays full</td><td>A11Y regression</td><td>Add an \"I have motion sensitivity\" toggle in Settings as fallback</td></tr><tr><td>9</td><td>Tutorial dismiss CTA tap not registered</td><td>UX hang</td><td>Player stuck</td><td>Add 60 s auto-dismiss as safety net + emit <code>tutorial.auto_dismissed</code></td></tr><tr><td>10</td><td>Onboarding-state RLS misconfigured</td><td>Spec test fails</td><td>Privacy exposure</td><td>Re-tighten RLS; audit recent reads</td></tr><tr><td>11</td><td>Sentry breadcrumbs not emitted (init failure)</td><td>Crash trace empty</td><td>Debugging blind</td><td>Init Sentry early; surface init failures via console</td></tr><tr><td>12</td><td>First pat clamps to existing 100 happiness</td><td>No visible stat change</td><td>UX disappointment</td><td>UX shows \"Mochi loves it!\" regardless of clamp; stat math correct</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 (60-sec session loop), plan §PART 5 (≤ 90 s onboarding), plan §PART 7 (TikTok-shareable hatch moment).</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Final tutorial card copy + visual design — owned by designer post-soft-launch.</li><li>Coin grant amount (100 default) — Mixpanel feature flag tunable.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Modal blocking overlays.</li><li>IAP / sub upsell during onboarding.</li><li>Skip CTA before midpoint of hatch ceremony.</li><li>Collecting email or invite code at the P1 co-parent stub.</li></ul>\n<p><strong>Cross-reference:</strong> This FR completes the PET slice (FR-PET-001..004). With it, every P1 gameplay FR can assume the player has a hatched + named + first-patted pet. FR-CARE-001 picks up from \"main game scene\" after tutorial dismiss.</p>";
