window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-VIRAL-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Categories.</strong> Push notifications fall into categories with per-category opt-in:</p>\n<ul><li><strong>care_reminder</strong> (default ON): \"Mochi misses you!\".</li><li><strong>streak</strong> (default ON): forgiveness + Cozy Hour reminders.</li><li><strong>petpair_receipt</strong> (default ON): co-parent receipt push.</li><li><strong>breeding_hatched</strong> (default ON): \"Your new pet is here!\".</li><li><strong>grandma_warning</strong> (default ON): per FR-PET-008.</li><li><strong>drama_share</strong> (default OFF): Daily Drama prompts.</li><li><strong>marketing</strong> (default OFF): sales, new features.</li></ul>\n<p>§1.2  <strong>Sleep-hour respect.</strong></p>\n<ul><li>Standard SKU: 22:00–07:00 local quiet hours (player's region).</li><li>Kids SKU: 20:00–08:00 local (wider window per COPPA-2025 spirit).</li><li>Critical notifications (grandma rescue available) still suppressed during sleep.</li></ul>\n<p>§1.3  <strong>Frequency caps.</strong></p>\n<ul><li>Standard SKU: max 3 pushes per day per user, max 1 per category per day.</li><li>Kids SKU: max 1 push per day per user (engagement-pushing restriction per FR-LEGAL-001 §1.5(e)).</li></ul>\n<p>§1.4  <strong>Parental override.</strong> Per FR-SUB-002 — Family tier parent can disable per-child push categories from dashboard.</p>\n<p>§1.5  <strong>APNs + FCM tokens.</strong> Cocos client (per platform bridge) registers for push; tokens posted to <code>/v1/notify/register-token</code>. Server stores per (user_id, platform, token). Token rotation handled via webhook.</p>\n<p>§1.6  <strong>Send flow.</strong> Internal services call <code>pushService.queue({ user_id, category, title_key, body_key, payload, scheduled_for?})</code>. Service:</p>\n<ul><li>Checks user opt-in for category.</li><li>Checks sleep-hour for user's region.</li><li>Checks frequency cap.</li><li>Translates title_key + body_key per user's locale.</li><li>Queues delivery via Supabase Edge Function.</li><li>Edge Function calls FCM or APNs.</li></ul>\n<p>§1.7  <strong>Localised copy.</strong> Title + body MUST come from a locale string bundle keyed by <code>title_key</code> per FR-I18N-001. No raw strings server-side.</p>\n<p>§1.8  <strong>Idempotency.</strong> Each push has <code>idempotency_key</code>. Same key within 5 min = single delivery.</p>\n<p>§1.9  <strong>Delivery audit.</strong> Every push attempt logged: <code>(user_id, category, title_key, scheduled_for, delivered_at, suppressed_reason?, idempotency_key)</code>. Retention 90 days.</p>\n<p>§1.10  <strong>No engagement-pushing for under-13.</strong> Per FR-LEGAL-001 §1.5(e). For kids SKU, only categories: <code>breeding_hatched</code>, <code>grandma_warning</code>, <code>petpair_receipt</code> (parent-initiated only). NO <code>care_reminder</code> engagement nudges.</p>\n<p>§1.11  <strong>Push token lifecycle.</strong> Tokens auto-revoked on:</p>\n<ul><li>App uninstall (FCM/APNS feedback).</li><li>User sign-out.</li><li>Account deletion (FR-LEGAL-001 §1.9).</li><li>Family-tier parent disable.</li></ul>\n<p>§1.12  <strong>Marketing opt-in.</strong> Marketing pushes require explicit opt-in via Settings. Default OFF. Per GDPR + ePrivacy.</p>\n<p>§1.13  <strong>Deep linking.</strong> Each push includes a deep-link URL: <code>tamagochi://pet/&lt;id&gt;</code> (opens game to that pet). Per FR-LEGAL-003 §1.4 — kid pushes deep-link to in-app only, never external.</p>\n<p>§1.14  <strong>Time-zone handling.</strong> Player's <code>region_of_record</code> (FR-AUTH-001) + IANA tz lookup. DST-aware.</p>\n<p>§1.15  <strong>Cross-tenant scoping.</strong> Push per-tenant; never cross-leak.</p>\n<p>§1.16  <strong>Tenant override.</strong> B2B tenants (FR-B2B-001) may have custom push category sets + Pet+/Family-tier overrides — defined in tenant config.</p>\n<p>§1.17  <strong>Latency budget.</strong> Push queued → delivered &lt; 30 sec P95 (excluding scheduled-future pushes).</p>\n<p>§1.18  <strong>APNS + FCM webhooks.</strong> Process delivery receipts + token-invalidation events.</p>\n<p>§1.19  <strong>DSR.</strong> Notification history exportable; tokens deleted on account-delete.</p>\n<p>§1.20  <strong>Analytics.</strong> <code>push.scheduled</code>, <code>push.suppressed { reason }</code>, <code>push.delivered</code>, <code>push.opened</code>, <code>push.failed { reason }</code>, <code>push.token.registered</code>, <code>push.token.invalidated</code> per FR-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why per-category opt-in.</strong> Plan §PART 3 + ICO AADC — granular opt-in prevents push fatigue + complies with kid-app dark-pattern guidance.</p>\n<p><strong>Why narrower sleep window for kids.</strong> COPPA-2025 §1.5(e) — engagement-pushing restrictions for under-13.</p>\n<p><strong>Why suppress critical pushes during sleep too.</strong> Plan §PART 3 ethical retention — even \"important\" pushes wake users. Defer to morning.</p>\n<p><strong>Why 1/day cap for kids.</strong> COPPA-2025 §1.5(e) — engagement-pushing restrictions.</p>\n<p><strong>Why no engagement-push for under-13.</strong> COPPA-2025 §1.5(e) — explicit restriction.</p>\n<p><strong>Why FCM + APNs (not third-party push service).</strong> Reliability + cost. Plan §PART 4.</p>\n<p><strong>Why locale keys server-side.</strong> Localised copy maintained centrally; client-side fallback covers locale gaps.</p>\n<p><strong>Why deep-link in-app for kids.</strong> FR-LEGAL-003 §1.4 — out-of-app for under-13 requires parental gate. Push deep-link goes back to game.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/notify/push.service.ts (excerpt)\nasync queue(input: { user_id: string; category: PushCategory; title_key: string; body_key: string; payload?: object; scheduled_for?: Date; idempotency_key: string; tenant_id: string }) {\n  const dedupe = await this.dedupe.lookup(input.user_id, input.idempotency_key);\n  if (dedupe) return dedupe;\n\n  const audience = await this.audience(input.user_id);\n  if (audience === 'under-13' &amp;&amp; this.isEngagementCategory(input.category)) {\n    this.audit.emit('push.suppressed', { reason: 'kid_engagement_blocked', category: input.category });\n    return { status: 'suppressed', reason: 'kid_engagement_blocked' };\n  }\n\n  if (!await this.optIn.isOn(input.user_id, input.category)) {\n    return { status: 'suppressed', reason: 'opt_out' };\n  }\n\n  if (await this.sleep.isQuietHourFor(input.user_id, audience)) {\n    const wakeAt = await this.sleep.nextWakeFor(input.user_id, audience);\n    input.scheduled_for = wakeAt;\n  }\n\n  if (await this.frequency.exceeded(input.user_id, input.category, audience)) {\n    return { status: 'suppressed', reason: 'frequency_cap' };\n  }\n\n  const tokens = await this.tokensFor(input.user_id);\n  if (!tokens.length) return { status: 'suppressed', reason: 'no_tokens' };\n\n  const locale = await this.localeFor(input.user_id);\n  const title = this.i18n.t(input.title_key, locale);\n  const body = this.i18n.t(input.body_key, locale);\n\n  for (const tok of tokens) {\n    if (tok.platform === 'ios') await this.apns.send(tok.token, { title, body, deep_link: input.payload?.['deep_link'] });\n    else await this.fcm.send(tok.token, { title, body, deep_link: input.payload?.['deep_link'] });\n  }\n  await this.frequency.increment(input.user_id, input.category);\n  await this.dedupe.store(input.user_id, input.idempotency_key, { status: 'delivered' });\n  return { status: 'delivered' };\n}</code></pre>\n<pre><code class=\"language-sql\">create table public.push_tokens (\n  id bigserial primary key,\n  user_id uuid not null references auth.users(id) on delete cascade,\n  platform text not null check (platform in ('ios','android','web')),\n  token text not null,\n  registered_at timestamptz not null default now(),\n  invalidated_at timestamptz,\n  tenant_id text not null default 'mochi',\n  unique (user_id, token)\n);\n\ncreate table public.push_preferences (\n  user_id uuid not null references auth.users(id) on delete cascade,\n  category text not null check (category in ('care_reminder','streak','petpair_receipt','breeding_hatched','grandma_warning','drama_share','marketing')),\n  enabled boolean not null,\n  primary key (user_id, category)\n);\n\ncreate table public.push_audit (\n  id bigserial primary key,\n  user_id uuid not null,\n  category text not null,\n  title_key text not null,\n  delivered_at timestamptz,\n  suppressed_reason text,\n  occurred_at timestamptz not null default now(),\n  tenant_id text not null default 'mochi'\n);</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Push delivery via FCM (Android) + APNs (iOS) works. Verified by mocked adapters. <strong>AC2.</strong> Sleep-hour 22:00-07:00 standard SKU pushes deferred to wake. Verified by fake-clock. <strong>AC3.</strong> Kids SKU 20:00-08:00. Verified. <strong>AC4.</strong> Engagement category blocked for under-13. Verified. <strong>AC5.</strong> Frequency cap 3/day standard, 1/day kids. Verified. <strong>AC6.</strong> Per-category opt-in respected. Verified. <strong>AC7.</strong> Marketing default OFF. Verified. <strong>AC8.</strong> Localised copy fetched. Verified. <strong>AC9.</strong> Token invalidation on app uninstall. Verified by FCM/APNs webhook test. <strong>AC10.</strong> Deep link kids in-app only. Verified. <strong>AC11.</strong> Idempotent dedupe. Verified. <strong>AC12.</strong> Cross-tenant push blocked. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('FR-VIRAL-005 — push', () =&gt; {\n  it('suppresses engagement push for under-13', async () =&gt; {\n    await mockKid('u1');\n    const r = await svc.queue({ user_id: 'u1', category: 'care_reminder', title_key: 't', body_key: 'b', idempotency_key: 'k1', tenant_id: 'mochi' });\n    expect(r.reason).toBe('kid_engagement_blocked');\n  });\n\n  it('schedules to wake when in sleep hours', async () =&gt; {\n    await setRegionAndTime('u1', 'VN', '23:30');\n    const r = await svc.queue({ user_id: 'u1', category: 'streak', title_key: 't', body_key: 'b', idempotency_key: 'k1', tenant_id: 'mochi' });\n    expect(r.scheduled_for).toBeAfter(localTime('07:00'));\n  });\n\n  it('frequency cap enforces 3/day standard', async () =&gt; {\n    for (let i = 0; i &lt; 3; i++) await svc.queue({ user_id: 'u1', category: 'care_reminder', title_key: 't', body_key: 'b', idempotency_key: `k${i}`, tenant_id: 'mochi' });\n    const r4 = await svc.queue({ user_id: 'u1', category: 'streak', title_key: 't2', body_key: 'b2', idempotency_key: 'k4', tenant_id: 'mochi' });\n    expect(r4.reason).toBe('frequency_cap');\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/notify/sleep-hour.service.ts\n@Injectable()\nexport class SleepHourService {\n  async isQuietHourFor(userId: string, audience: '13+' | 'under-13'): Promise&lt;boolean&gt; {\n    const tz = await this.tzFor(userId);\n    const localHour = new Date().toLocaleString('en-US', { timeZone: tz, hour: 'numeric', hour12: false });\n    const hour = parseInt(localHour, 10);\n    if (audience === 'under-13') return hour &gt;= 20 || hour &lt; 8;\n    return hour &gt;= 22 || hour &lt; 7;\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> FCM, APNs, IANA tz database. <strong>Internal:</strong> FR-CARE-005 streak notifications, FR-LEGAL-001 COPPA restrictions, FR-AUTH-001 region, FR-SUB-002 parental disable. <strong>Blocks:</strong> none.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/notify/register-token\n{ \"platform\": \"ios\", \"token\": \"abc123...\" }\n→ 201 { \"registered\": true }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"push.delivered\", \"user_id\": \"01HU...\", \"category\": \"streak\", \"title_key\": \"streak.day_milestone.7\", \"delivered_at\": \"...\" }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"push.suppressed\", \"user_id\": \"01HU...\", \"category\": \"care_reminder\", \"reason\": \"kid_engagement_blocked\" }</code></pre>\n<pre><code class=\"language-json\">{\n  \"title\": \"Mochi misses you!\",\n  \"body\": \"It's been 6 hours since your last visit\",\n  \"deep_link\": \"tamagochi://pet/01HC...\",\n  \"category\": \"care_reminder\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Sleep hours? → §1.2.</li><li><strong>OQ-2 (resolved):</strong> Kids cap? → §1.3 — 1/day.</li><li><strong>OQ-3 (resolved):</strong> FCM/APNs vs third-party? → §<code>allowed_tools</code>.</li><li><strong>OQ-4 (resolved):</strong> Marketing default? → §1.12 — OFF.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>FCM/APNs outage</td><td>Adapter error</td><td>Push delayed</td><td>Retry queue</td></tr><tr><td>2</td><td>Token revoked silently</td><td>Delivery error</td><td>Re-register</td><td>Webhook handling</td></tr><tr><td>3</td><td>DST transition affects sleep window</td><td>TZ-aware</td><td>OK</td><td>IANA db</td></tr><tr><td>4</td><td>Locale missing key</td><td>EN fallback</td><td>Mild UX</td><td>Audit</td></tr><tr><td>5</td><td>Engagement push slips for under-13</td><td>Audit</td><td>COPPA risk</td><td>Test catches</td></tr><tr><td>6</td><td>Idempotency dedupe Redis lost</td><td>Possible duplicate</td><td>Bounded</td><td>5-min window</td></tr><tr><td>7</td><td>Cross-tenant push leak</td><td>Audit</td><td>Privacy</td><td>RLS + tenant scoping</td></tr><tr><td>8</td><td>Token cascade on account delete</td><td>Cascade</td><td>OK</td><td>Verified</td></tr><tr><td>9</td><td>Deep link to external URL for kids</td><td>FR-LEGAL-003 gate</td><td>Blocked</td><td>Validation</td></tr><tr><td>10</td><td>Push during sleep hour due to bug</td><td>Audit</td><td>UX issue</td><td>Pre-deliver TZ check</td></tr><tr><td>11</td><td>Frequency cap reset timezone</td><td>TZ-locked</td><td>OK</td><td>IANA</td></tr><tr><td>12</td><td>Marketing opt-in defaulted ON</td><td>Check default</td><td>Opt-in violation</td><td>Test enforced</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 retention + plan §PART 8 COPPA push restrictions.</p>\n<p><strong>Sub-decisions punted to ops:</strong> Per-category push copy DPO-reviewed.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Engagement push for kids.</li><li>Push during sleep.</li><li>Marketing default-ON.</li><li>External deep links for kids.</li></ul>\n<p><strong>Cross-reference:</strong> FR-CARE-005 streak, FR-PET-008 grandma, FR-SOCIAL-002 PetPair receipt, FR-LEGAL-001 COPPA, FR-SUB-002 parental disable.</p>";
