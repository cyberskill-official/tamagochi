window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OBS-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Anti-cheat layers.</strong> Anti-cheat is defence-in-depth across:</p>\n<ul><li><strong>L1 Auth gate</strong> (FR-AUTH-001 JWT) — caught at sign-in.</li><li><strong>L2 Message signing</strong> (FR-INFRA-002 §1.7 HMAC) — caught per-message.</li><li><strong>L3 Rate limiting</strong> (FR-INFRA-002 §1.8) — caught per-volume.</li><li><strong>L4 Impossible-state guard</strong> (FR-INFRA-002 §1.9) — caught per-action.</li><li><strong>L5 Ledger reconciliation</strong> (FR-ECON-001 §1.12) — caught daily.</li><li><strong>L6 This FR — ban-decision orchestrator</strong> + audit dashboard.</li></ul>\n<p>§1.2  <strong>Impossible-state event sources.</strong> Aggregates events from:</p>\n<ul><li>Negative stat after care (FR-PET-003).</li><li>Feed at hunger=100 (FR-CARE-001).</li><li>Trade with grandma pet (FR-SOCIAL-003).</li><li>Breeding non-adult pets (FR-PET-007).</li><li>Score mismatch in mini-game (FR-CARE-004).</li><li>Currency negative or above grant (FR-ECON-001).</li><li>Cross-tenant operation (FR-INFRA-003).</li><li>Cross-SKU operation (FR-AUTH-003).</li><li>HMAC signature invalid (FR-INFRA-002).</li><li>Rate-limit exceeded patterns (&gt;5×/hour).</li></ul>\n<p>§1.3  <strong>Per-account flag counter.</strong> Each impossible-state event increments per-user <code>cheat_flag_score</code>:</p>\n<ul><li>Score +1 per non-critical event (rate-limit, hunger-at-100).</li><li>Score +5 per critical (score mismatch, cross-tenant).</li><li>Score +20 per HMAC forgery (signature failure).</li></ul>\n<p>§1.4  <strong>3-strike ban thresholds.</strong></p>\n<ul><li>Flag score ≥ 10 in 24h: <strong>Yellow</strong> — DPO review queue, no action.</li><li>Flag score ≥ 20 in 24h: <strong>Orange</strong> — temporary suspension (24h cooldown).</li><li>Flag score ≥ 50 lifetime OR ≥ 30 in 24h: <strong>Red</strong> — perma-ban pending appeal.</li></ul>\n<p>§1.5  <strong>Ban execution.</strong> Banned account:</p>\n<ul><li>Cannot sign in (FR-AUTH-001 returns 403 <code>account.banned</code>).</li><li>Pets archived to <code>grandma_house</code> (re-entry path on appeal).</li><li>Currency frozen (no spend, no grant).</li><li>Friends notified (\"X is unavailable\").</li></ul>\n<p>§1.6  <strong>Appeal mechanism.</strong> Banned user can <code>POST /v1/security/appeal-ban</code> with explanation. DPO reviews within 7 business days. Reversal preserves prior state.</p>\n<p>§1.7  <strong>Daily ledger reconciliation.</strong> Per FR-ECON-001 §1.12, scheduled job recomputes balances + detects drift &gt; 0.1%. Drift alerts via Sentry → DPO review.</p>\n<p>§1.8  <strong>Per-tenant ban scope.</strong> Ban applies within tenant only. A B2B Techcombank tenant ban does NOT affect the consumer Mochi tenant for the same Apple/Google identity.</p>\n<p>§1.9  <strong>Audit dashboard.</strong> A Grafana dashboard surfaces:</p>\n<ul><li>Top suspicious accounts (by flag score 24h).</li><li>Ledger drift over time.</li><li>HMAC failure rate.</li><li>Score-mismatch rate per game.</li><li>Trade race detection rate.</li></ul>\n<p>§1.10  <strong>Run-book.</strong> A <code>docs/security/anti-cheat-runbook.md</code> documents:</p>\n<ul><li>Each impossible-state event type + remediation.</li><li>Ban escalation procedure.</li><li>Appeal review checklist.</li><li>False-positive recovery.</li></ul>\n<p>§1.11  <strong>Endpoint — security state.</strong> <code>GET /v1/security/my-flags</code> returns the user's own flag history + ban status (transparency).</p>\n<p>§1.12  <strong>Endpoint — security report.</strong> <code>POST /v1/security/report</code> allows player to report another for suspicious behavior. Adds to DPO review queue.</p>\n<p>§1.13  <strong>Endpoint — appeal.</strong> <code>POST /v1/security/appeal-ban</code>.</p>\n<p>§1.14  <strong>DPO review queue.</strong> Postgres <code>dpo_review_queue</code> table with FIFO. T&amp;S reviewer dashboard.</p>\n<p>§1.15  <strong>False-positive recovery.</strong> When DPO determines false positive:</p>\n<ul><li>Flag score reset.</li><li>Ban reversed (if applied).</li><li>Audit row noting reversal.</li><li>User notified.</li></ul>\n<p>§1.16  <strong>Anti-cheat metric retention.</strong> 7-year for kids; 2-year standard.</p>\n<p>§1.17  <strong>No client-side ban decisions.</strong> Plan §PART 4 — all anti-cheat decisions server-side.</p>\n<p>§1.18  <strong>Cross-tenant ban learn.</strong> When a player is banned in consumer tenant for ledger fraud, a flag is shared (anonymized) with B2B tenants via <code>security.cross_tenant.suspicion</code> — tenant can decide their own action.</p>\n<p>§1.19  <strong>Sentry alert SLA.</strong> Critical anti-cheat events (HMAC failure burst, ledger drift &gt; 1%) page on-call within 5 minutes.</p>\n<p>§1.20  <strong>Analytics + audit.</strong> <code>security.flag.incremented</code>, <code>security.ban.applied { tier }</code>, <code>security.appeal.submitted</code>, <code>security.appeal.resolved { verdict }</code>, <code>security.false_positive.recovered</code>, <code>ledger.reconciliation.drift_detected</code> per FR-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why defence-in-depth.</strong> Plan §PART 4 — single-layer anti-cheat is bypassable. 6 layers force attackers through all.</p>\n<p><strong>Why per-account flag scoring.</strong> Plan §PART 10 — graduated response avoids both under-reaction (let everything slide) and over-reaction (auto-ban on single signal).</p>\n<p><strong>Why 3-strike with DPO review.</strong> Plan §PART 8 — auto-ban without human review risks regulator complaint + false positives. DPO loop preserves due process.</p>\n<p><strong>Why appeal mechanism.</strong> Plan §PART 10 — false bans destroy trust + create support burden. Explicit appeal path with documented review.</p>\n<p><strong>Why per-tenant ban scope.</strong> Plan §PART 6 — B2B tenants want their own moderation policies. Cross-tenant info-sharing OK (anonymized suspicion); cross-tenant enforcement not OK.</p>\n<p><strong>Why cross-tenant suspicion sharing.</strong> Plan §PART 4 — known-bad accounts in consumer tenant are likely-bad in B2B tenants too. Anonymized signal helps tenants decide.</p>\n<p><strong>Why audit retention 7-yr kids / 2-yr standard.</strong> COPPA + GDPR alignment + accounting tax retention.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/anticheat/impossible-state-monitor.ts\n@Injectable()\nexport class ImpossibleStateMonitor {\n  async record(userId: string, kind: ImpossibleStateKind, tenantId: string, context?: any): Promise&lt;void&gt; {\n    const severity = IMPOSSIBLE_STATE_SEVERITY[kind];\n    const flag_delta = severity === 'critical' ? 5 : severity === 'forgery' ? 20 : 1;\n    const { data: current } = await this.supa.from('cheat_flags').select('score_24h, score_lifetime').eq('user_id', userId).maybeSingle();\n    const newScore24h = (current?.score_24h ?? 0) + flag_delta;\n    const newScoreLifetime = (current?.score_lifetime ?? 0) + flag_delta;\n    await this.supa.from('cheat_flags').upsert({\n      user_id: userId, score_24h: newScore24h, score_lifetime: newScoreLifetime,\n      last_event_at: new Date().toISOString(), tenant_id: tenantId,\n    });\n    await this.supa.from('cheat_events').insert({\n      user_id: userId, kind, severity, flag_delta, context, tenant_id: tenantId,\n    });\n    this.audit.emit('security.flag.incremented', { user_id: userId, kind, severity, score_24h: newScore24h });\n    if (newScore24h &gt;= 30 || newScoreLifetime &gt;= 50) await this.ban.execute(userId, 'red');\n    else if (newScore24h &gt;= 20) await this.ban.execute(userId, 'orange');\n    else if (newScore24h &gt;= 10) await this.dpoQueue.enqueue(userId, 'review_yellow');\n  }\n}</code></pre>\n<pre><code class=\"language-sql\">create table public.cheat_flags (\n  user_id uuid primary key references auth.users(id) on delete cascade,\n  score_24h int not null default 0,\n  score_lifetime int not null default 0,\n  last_event_at timestamptz,\n  tenant_id text not null default 'mochi',\n  ban_tier text check (ban_tier in ('yellow','orange','red')),\n  banned_at timestamptz,\n  banned_until timestamptz,\n  ban_reason text\n);\n\ncreate table public.cheat_events (\n  id bigserial primary key,\n  user_id uuid not null,\n  kind text not null,\n  severity text not null check (severity in ('low','medium','critical','forgery')),\n  flag_delta int not null,\n  context jsonb,\n  tenant_id text not null default 'mochi',\n  occurred_at timestamptz not null default now()\n);\ncreate index on public.cheat_events (user_id, occurred_at desc);\n\ncreate table public.dpo_review_queue (\n  id bigserial primary key,\n  user_id uuid not null,\n  reason text not null,\n  state text not null default 'pending' check (state in ('pending','reviewing','resolved_ok','resolved_ban','resolved_false_positive')),\n  tenant_id text not null default 'mochi',\n  enqueued_at timestamptz not null default now(),\n  resolved_at timestamptz,\n  reviewer_id uuid\n);</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Negative stat triggers flag +1. Verified. <strong>AC2.</strong> HMAC forgery triggers flag +20. Verified. <strong>AC3.</strong> Flag score 30 in 24h → red ban. Verified. <strong>AC4.</strong> Banned account cannot sign in. Verified. <strong>AC5.</strong> Appeal endpoint creates DPO review row. Verified. <strong>AC6.</strong> DPO false-positive reverses ban + resets flags. Verified. <strong>AC7.</strong> Ledger drift &gt; 0.1% pages on-call. Verified. <strong>AC8.</strong> Cross-tenant suspicion sharing anonymized. Verified. <strong>AC9.</strong> Per-tenant ban scope respected. Verified. <strong>AC10.</strong> Grafana dashboard renders. Verified. <strong>AC11.</strong> 7-year retention for kids tenant. Verified. <strong>AC12.</strong> Sentry alert SLA &lt; 5min for critical events. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('FR-OBS-002 — anti-cheat', () =&gt; {\n  it('escalates to red ban on 30 flags in 24h', async () =&gt; {\n    for (let i = 0; i &lt; 6; i++) await monitor.record('u1', 'score_mismatch', 'mochi');  // 6 * 5 = 30\n    const flags = await getFlags('u1');\n    expect(flags.ban_tier).toBe('red');\n  });\n\n  it('appeal creates review row', async () =&gt; {\n    await banUser('u1');\n    const r = await svc.appealBan(user('u1'), 'I was hacked');\n    expect(r.review_id).toBeTruthy();\n  });\n\n  it('false-positive recovery resets flags', async () =&gt; {\n    await flag('u1', 50);\n    await dpo.resolveFalsePositive('u1');\n    const flags = await getFlags('u1');\n    expect(flags.score_24h).toBe(0);\n    expect(flags.ban_tier).toBeNull();\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/anticheat/ban-decision.service.ts\n@Injectable()\nexport class BanDecisionService {\n  async execute(userId: string, tier: 'yellow' | 'orange' | 'red') {\n    const until = tier === 'orange' ? new Date(Date.now() + 24*3600*1000).toISOString() : null;\n    await this.supa.from('cheat_flags').update({ ban_tier: tier, banned_at: new Date().toISOString(), banned_until: until }).eq('user_id', userId);\n    if (tier === 'orange' || tier === 'red') {\n      await this.archivePets(userId);\n      await this.freezeCurrency(userId);\n      await this.notifyFriends(userId);\n      await this.audit.emit('security.ban.applied', { user_id: userId, tier });\n    }\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Sentry, Grafana. <strong>Internal:</strong> FR-ECON-001 (ledger drift), FR-INFRA-002 (HMAC, rate-limit), FR-AUTH-001 (ban gate at sign-in), FR-PET-008 (grandma archive), FR-LEGAL-001 (DPO + audit). <strong>Blocks:</strong> FR-B2B-002 (B2B tenants depend on anti-cheat SLA).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">GET /v1/security/my-flags\n→ 200 { \"score_24h\": 5, \"score_lifetime\": 12, \"ban_tier\": null, \"events\": [...] }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"security.flag.incremented\", \"user_id\": \"01HU...\", \"kind\": \"score_mismatch\", \"severity\": \"critical\", \"score_24h\": 25 }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"security.ban.applied\", \"user_id\": \"01HU...\", \"tier\": \"red\", \"reason\": \"score_lifetime&gt;=50\" }</code></pre>\n<pre><code class=\"language-http\">POST /v1/security/appeal-ban\n{ \"explanation\": \"I was just trying to test the game.\" }\n→ 202 { \"review_id\": \"01HRVW...\", \"sla\": \"7 business days\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Auto-ban thresholds? → §1.4.</li><li><strong>OQ-2 (resolved):</strong> Appeal SLA? → §1.6 — 7 business days.</li><li><strong>OQ-3 (resolved):</strong> Cross-tenant ban? → §1.8 — no.</li><li><strong>OQ-4 (resolved):</strong> Manual DPO review? → §1.4 — yellow tier yes.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>False positive ban</td><td>Player support + DPO</td><td>Reversal path</td><td>§1.15</td></tr><tr><td>2</td><td>DPO queue backlog</td><td>Daily metric</td><td>Slow response</td><td>More reviewers</td></tr><tr><td>3</td><td>Flag score race</td><td>Atomic SQL</td><td>OK</td><td>Mutex</td></tr><tr><td>4</td><td>Cross-tenant suspicion leak (deanonymized)</td><td>Privacy audit</td><td>COPPA/GDPR risk</td><td>Anonymization audit</td></tr><tr><td>5</td><td>Banned user creates new account</td><td>Device fingerprint</td><td>Limited</td><td>Apple/Google account ban scope</td></tr><tr><td>6</td><td>Ban prevents legitimate appeal</td><td>UX surfaces appeal</td><td>OK</td><td>Designed</td></tr><tr><td>7</td><td>Reconciliation false drift</td><td>Investigation</td><td>DPO review</td><td>Recompute</td></tr><tr><td>8</td><td>Sentry alert misroute</td><td>On-call drill</td><td>Slow page</td><td>Test quarterly</td></tr><tr><td>9</td><td>Per-tenant scope bypass</td><td>Audit</td><td>Privacy issue</td><td>RLS enforced</td></tr><tr><td>10</td><td>Retention non-compliance</td><td>DPO</td><td>Audit gap</td><td>Configured</td></tr><tr><td>11</td><td>Grafana dashboard stale</td><td>Daily check</td><td>Operational blindness</td><td>Auto-refresh</td></tr><tr><td>12</td><td>Run-book outdated</td><td>Quarterly review</td><td>Slow response</td><td>Update procedure</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 4 anti-cheat; plan §PART 10 risk #7 trading scam.</p>\n<p><strong>Sub-decisions punted to ops:</strong> Flag-score thresholds Mixpanel-tunable; DPO team size.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Auto-ban without review on borderline events.</li><li>Cross-tenant ban scope.</li><li>Client-side decisions.</li></ul>\n<p><strong>Cross-reference:</strong> FR-ECON-001 ledger drift; FR-INFRA-002 HMAC + rate-limit; FR-B2B-002 multi-tenant requirement.</p>";
