window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PET-007"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Eligibility.</strong> Breeding requires:</p>\n<ul><li>Both parent pets at <code>stage='adult'</code> per FR-PET-002 §1.6.</li><li>Both pets owned by SAME player (cross-player breeding deferred to a future FR).</li><li>Both pets <code>status='active'</code> (not grandma_house, not tombstoned).</li><li>Cooldown 7 days between breedings for the same parent.</li><li>Owner has at least 1 free pet slot (FR-PET-005).</li></ul>\n<p>§1.2  <strong>Cost.</strong> Breeding costs 500 Coins (earned, no IAP path — FR-LEGAL-002). Stub at P2 via FR-ECON-001.</p>\n<p>§1.3  <strong>Endpoint.</strong> <code>POST /v1/pets/breed</code> — body <code>{ parent_a_id: string, parent_b_id: string, idempotency_key: string }</code>. JWT required.</p>\n<p>§1.4  <strong>Deterministic trait inheritance.</strong> Per FR-LEGAL-002 §1.6, breeding MUST be a pure function of parent inputs:</p>\n<pre><code>inheritTraits(parentA, parentB, seed = SHA256(parentA.id + parentB.id + breeding_event_id)) → offspring</code></pre>\n<ul><li><strong>Species</strong>: 50/50 from parent A or B species (selected by <code>seed.bit[0]</code>).</li><li><strong>Tier</strong>: tier of LOWER-tier parent + occasionally upgrade. Specifically: 80% same as lower parent, 15% lower+1, 5% lower+2 (capped at legendary). Per FR-LEGAL-002 §1.6 the 80/15/5 is deterministic-by-seed (not random).</li><li><strong>Palette seed</strong>: XOR of parents' palette seeds.</li><li><strong>Stat bias</strong> (small initial stat boosts): bitwise blend.</li></ul>\n<p>§1.5  <strong>24h incubation.</strong> After breeding, the offspring egg is in <code>pending_breeding_offspring</code> table for 24 real-time hours. Player sees the egg in a special \"Incubator\" UI section. Can be tapped to inspect a vague silhouette (no full reveal until hatch).</p>\n<p>§1.6  <strong>Reveal at hatch.</strong> After 24h, the egg auto-hatches into a new pet row in <code>pets</code> table. The player gets a notification + an opportunity to name it (per FR-PET-001 §1.5 content-safety filter applies).</p>\n<p>§1.7  <strong>24h grace if slot full at hatch.</strong> Per FR-PET-005 §1.17, if player has 0 slots when egg hatches, egg stays in incubator another 24h. After total 48h with full slots, the player MUST choose: (a) archive an existing pet to grandma_house; (b) abandon the new offspring (one-time confirmation).</p>\n<p>§1.8  <strong>Cooldown enforcement.</strong> Per-parent 7-day cooldown via Redis TTL. Attempting breeding within cooldown → 429 <code>cooldown</code> with <code>retry_after_sec</code>.</p>\n<p>§1.9  <strong>Cross-tenant scoping.</strong> Both parents MUST be same <code>tenant_id</code>. Cross-tenant breeding forbidden.</p>\n<p>§1.10  <strong>Audit + receipt.</strong> Per FR-LEGAL-002 §1.4, breeding writes a <code>randomisation_receipts</code> row even though outcome is deterministic (audit trail). <code>mechanic_id = 'breeding'</code>, <code>drop_rate_version</code> reflects the 80/15/5 tier-up formula.</p>\n<p>§1.11  <strong>Offspring acquisition source.</strong> <code>pet_acquisitions.source = 'breeding'</code> per FR-PET-006 §1.12. <code>source_ref_id</code> = breeding_event_id.</p>\n<p>§1.12  <strong>Idempotency.</strong> <code>idempotency_key</code> MUST be a ULID-prefix; identical retry returns cached response.</p>\n<p>§1.13  <strong>No self-breeding.</strong> Validation: <code>parent_a_id != parent_b_id</code>.</p>\n<p>§1.14  <strong>Pet+ benefit.</strong> Pet+ subscribers (FR-SUB-001 stub) get -50% cooldown (3.5 days vs 7).</p>\n<p>§1.15  <strong>Surprise reveal animation.</strong> At hatch reveal, Cocos plays a Spine \"hatch\" sequence (FR-ART-001 contract animation) with palette set to the inherited seed. Player sees offspring for first time.</p>\n<p>§1.16  <strong>Co-parent breeding (P3+).</strong> Cross-player breeding (FR-SOCIAL-002 co-parented pets) is <strong>deferred</strong> to a future FR. At P2, breeding remains same-player only.</p>\n<p>§1.17  <strong>Stat reset on hatch.</strong> New offspring starts at stage='egg' with default stats (100/100/100/100). Tier-based decay modifier applies per FR-PET-006 §1.9.</p>\n<p>§1.18  <strong>Trait test seam.</strong> <code>trait-inheritance.ts</code> MUST be a pure function testable with property-based tests (fast-check). Same parents + same seed = same offspring.</p>\n<p>§1.19  <strong>Persona seed inheritance.</strong> Per FR-AI-001, persona YAML for offspring uses species default + parent palette XOR — no chat-personality inheritance complexity at P2.</p>\n<p>§1.20  <strong>Analytics.</strong> <code>pet.breeding.initiated</code>, <code>pet.breeding.completed { offspring_species, offspring_tier, parent_cooldown_remaining }</code>, <code>pet.breeding.aborted { reason }</code> per FR-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why adult-only.</strong> Plan §PART 3 — biological intuition + makes evolution → adult feel rewarding.</p>\n<p><strong>Why same-player at P2.</strong> Cross-player breeding adds trade-window-like complexity + abuse vectors (free pet farming). Defer to P3+ once economy matures.</p>\n<p><strong>Why 7-day cooldown.</strong> Prevents farm-spam. Pet+ -50% is a meaningful subscription perk.</p>\n<p><strong>Why 24h incubation.</strong> Plan §PART 3 — anticipation builds engagement (Tamagotchi Paradise pattern). Player checks back. Short enough to not feel punishing.</p>\n<p><strong>Why deterministic by SHA256 seed.</strong> FR-LEGAL-002 §1.6 mandates deterministic breeding for audit-replay + EU compliance. The seed = <code>SHA256(parentA + parentB + event_id)</code> ensures every breeding is reproducible from inputs.</p>\n<p><strong>Why 80/15/5 tier-up split.</strong> Plan §PART 3 — players want a hint of rarity surprise without RNG-grind. 80% same-tier feels predictable; 15% +1 is exciting; 5% +2 is rare-but-possible. All deterministic-by-seed.</p>\n<p><strong>Why species 50/50 random.</strong> Simpler than weighted; encourages variety.</p>\n<p><strong>Why no self-breeding.</strong> Biological intuition + abuse prevention.</p>\n<p><strong>Why receipt despite deterministic.</strong> FR-LEGAL-002 §1.4 audit-trail requirement; consistent with other randomised-feeling mechanics.</p>\n<p><strong>Why 48h grace at hatch slot-full.</strong> Plan §PART 3 ethical retention — losing offspring instantly is punishing.</p>\n<p><strong>Why no chat-personality inheritance.</strong> Plan §PART 4 LLM cost cap — variable persona inheritance bloats prompt context.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/breeding/trait-inheritance.ts\nimport { createHash } from 'node:crypto';\n\nexport function inheritTraits(input: {\n  parentA: { id: string; species: Species; tier: Tier; palette_seed: string };\n  parentB: { id: string; species: Species; tier: Tier; palette_seed: string };\n  breedingEventId: string;\n}): { species: Species; tier: Tier; palette_seed: string; stat_bias: { hunger: number; cleanliness: number; happiness: number; energy: number } } {\n  const seed = createHash('sha256').update(input.parentA.id + input.parentB.id + input.breedingEventId).digest();\n\n  // Species: bit 0 picks parentA or parentB\n  const species = (seed[0] &amp; 1) === 0 ? input.parentA.species : input.parentB.species;\n\n  // Tier: lower of two; then 80/15/5 upgrade based on seed[1]\n  const tierOrder = ['common','rare','epic','mythic','legendary'] as const;\n  const lower = Math.min(tierOrder.indexOf(input.parentA.tier), tierOrder.indexOf(input.parentB.tier));\n  const upgradeRoll = seed[1] / 255;\n  let upgrade = 0;\n  if (upgradeRoll &gt;= 0.95) upgrade = 2;\n  else if (upgradeRoll &gt;= 0.80) upgrade = 1;\n  const tier = tierOrder[Math.min(lower + upgrade, 4)];\n\n  // Palette seed: XOR\n  const paletteA = Buffer.from(input.parentA.palette_seed, 'hex');\n  const paletteB = Buffer.from(input.parentB.palette_seed, 'hex');\n  const palette = Buffer.alloc(paletteA.length);\n  for (let i = 0; i &lt; paletteA.length; i++) palette[i] = paletteA[i] ^ paletteB[i];\n\n  // Stat bias: -3 to +3 per stat from seed bytes\n  const statBias = {\n    hunger: (seed[2] % 7) - 3,\n    cleanliness: (seed[3] % 7) - 3,\n    happiness: (seed[4] % 7) - 3,\n    energy: (seed[5] % 7) - 3,\n  };\n\n  return { species, tier, palette_seed: palette.toString('hex'), stat_bias: statBias };\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/breeding/breeding.service.ts (excerpt)\nasync breed(u: AuthedUser, parentAId: string, parentBId: string, idempKey: string) {\n  if (parentAId === parentBId) throw new HttpException('pet.breeding.self_forbidden', 422);\n  const prior = await this.idemp.lookup(u.id, `breed:${idempKey}`);\n  if (prior) return prior;\n  const [parentA, parentB] = await Promise.all([this.pets.byId(parentAId), this.pets.byId(parentBId)]);\n  this.assertEligible(u, parentA);\n  this.assertEligible(u, parentB);\n  if (parentA.tenant_id !== parentB.tenant_id) throw new HttpException('pet.breeding.cross_tenant', 403);\n  await this.assertCooldown(parentA.id, parentB.id, u);\n  const slot = await this.inventory.canHatchNew(u.id);\n  if (!slot.allowed) throw new HttpException({ error: 'pet_slot_full' }, 402);\n  await this.coins.spend(u.id, this.pet_plus(u) ? 250 : 500);\n  const breedingEventId = generateUlid();\n  const offspring = inheritTraits({ parentA, parentB, breedingEventId });\n  const eggHatchAt = new Date(Date.now() + 24 * 3600 * 1000).toISOString();\n  await this.supa.from('pending_breeding_offspring').insert({\n    id: breedingEventId, parent_a_id: parentAId, parent_b_id: parentBId,\n    owner_id: u.id, offspring_species: offspring.species, offspring_tier: offspring.tier,\n    offspring_palette_seed: offspring.palette_seed, hatch_at: eggHatchAt,\n  });\n  await this.startCooldown(parentA.id, parentB.id, u);\n  await this.audit.write({ who: u.id, what: 'pet.breeding.initiated', what_keys: { event_id: breedingEventId } });\n  return { breeding_event_id: breedingEventId, hatch_at: eggHatchAt, offspring_silhouette: offspring.species };\n}</code></pre>\n<pre><code class=\"language-sql\">-- migration\ncreate table public.pending_breeding_offspring (\n  id text primary key check (id ~ '^[0-9A-HJKMNPQRSTVWXYZ]{26}$'),\n  parent_a_id text not null references public.pets(id) on delete cascade,\n  parent_b_id text not null references public.pets(id) on delete cascade,\n  owner_id uuid not null references auth.users(id) on delete cascade,\n  offspring_species text not null,\n  offspring_tier text not null,\n  offspring_palette_seed text not null,\n  hatch_at timestamptz not null,\n  hatched_pet_id text,\n  status text not null default 'incubating' check (status in ('incubating','hatched','expired','abandoned')),\n  tenant_id text not null default 'mochi',\n  created_at timestamptz not null default now()\n);\ncreate index on public.pending_breeding_offspring (owner_id, status);\ncreate index on public.pending_breeding_offspring (hatch_at) where status = 'incubating';\nalter table public.pending_breeding_offspring enable row level security;\ncreate policy \"breeding self\" on public.pending_breeding_offspring for select using (owner_id = auth.uid());</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Breeding two adult Mochi pets produces deterministic offspring (same inputs = same output). Verified by property-test. <strong>AC2.</strong> Self-breeding rejected. Verified. <strong>AC3.</strong> Non-adult breeding rejected. Verified. <strong>AC4.</strong> 7-day cooldown enforced. Verified by fake-clock test. <strong>AC5.</strong> Pet+ -50% cooldown. Verified. <strong>AC6.</strong> Cross-tenant blocked. Verified. <strong>AC7.</strong> 24h incubation honoured; auto-hatch fires via scheduled task. Verified. <strong>AC8.</strong> Slot-full grace 48h applied. Verified. <strong>AC9.</strong> Idempotent breeding. Verified. <strong>AC10.</strong> Receipt written per FR-LEGAL-002. Verified. <strong>AC11.</strong> Tier inheritance 80/15/5 distribution over 10k iterations. Verified by statistical test. <strong>AC12.</strong> Palette XOR computed correctly. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/breeding/__tests__/trait-inheritance.spec.ts\nimport fc from 'fast-check';\n\ndescribe('FR-PET-007 — trait inheritance', () =&gt; {\n  it('is deterministic: same inputs → same output', () =&gt; {\n    fc.assert(fc.property(fc.tuple(fc.constantFrom('mochi','pengu','bao','fluffit','tako'), fc.constantFrom('mochi','pengu','bao','fluffit','tako')), ([sA, sB]) =&gt; {\n      const inputA = { id: 'A', species: sA as any, tier: 'rare' as const, palette_seed: '0123456789abcdef' };\n      const inputB = { id: 'B', species: sB as any, tier: 'epic' as const, palette_seed: 'fedcba9876543210' };\n      const event = 'EVT123';\n      const r1 = inheritTraits({ parentA: inputA, parentB: inputB, breedingEventId: event });\n      const r2 = inheritTraits({ parentA: inputA, parentB: inputB, breedingEventId: event });\n      expect(r1).toEqual(r2);\n    }));\n  });\n\n  it('tier inheritance follows 80/15/5 distribution', () =&gt; {\n    const counts = { same: 0, up1: 0, up2: 0 };\n    for (let i = 0; i &lt; 10000; i++) {\n      const r = inheritTraits({\n        parentA: { id: `A${i}`, species: 'mochi', tier: 'common', palette_seed: '0'.repeat(16) },\n        parentB: { id: `B${i}`, species: 'mochi', tier: 'common', palette_seed: '0'.repeat(16) },\n        breedingEventId: `EVT${i}`,\n      });\n      if (r.tier === 'common') counts.same++;\n      if (r.tier === 'rare') counts.up1++;\n      if (r.tier === 'epic') counts.up2++;\n    }\n    expect(counts.same / 10000).toBeCloseTo(0.80, 1);\n    expect(counts.up1 / 10000).toBeCloseTo(0.15, 1);\n    expect(counts.up2 / 10000).toBeCloseTo(0.05, 1);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/breeding/incubator.service.ts\n@Injectable()\nexport class IncubatorService {\n  async hatchPendingEggs() {\n    const now = new Date().toISOString();\n    const { data: pending } = await this.supa.from('pending_breeding_offspring').select('*').lte('hatch_at', now).eq('status', 'incubating');\n    for (const egg of pending ?? []) {\n      const slot = await this.inventory.canHatchNew(egg.owner_id);\n      if (!slot.allowed) { await this.extendGrace(egg.id); continue; }\n      const petId = generateUlid();\n      await this.supa.from('pets').insert({\n        id: petId, owner_id: egg.owner_id, tenant_id: egg.tenant_id,\n        species: egg.offspring_species, tier: egg.offspring_tier,\n        display_name: 'Egg', stage: 'egg', palette_seed: egg.offspring_palette_seed,\n      });\n      await this.supa.from('pet_acquisitions').insert({\n        user_id: egg.owner_id, pet_id: petId, species: egg.offspring_species, tier: egg.offspring_tier,\n        source: 'breeding', source_ref_id: egg.id,\n      });\n      await this.supa.from('pending_breeding_offspring').update({ status: 'hatched', hatched_pet_id: petId }).eq('id', egg.id);\n      await this.notify.sendBreedingHatchedNotice(egg.owner_id, petId);\n    }\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Scheduled function (pg_cron) for incubator tick. <strong>Internal:</strong> FR-PET-005 (slot check), FR-PET-006 (species + tier model), FR-LEGAL-002 (receipts), FR-ECON-001 (Coin spend), FR-AUTH-001 (JWT). <strong>Blocks:</strong> FR-VIRAL-003 (generative pet at adoption — parallel path).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/pets/breed\n{ \"parent_a_id\": \"01HC...\", \"parent_b_id\": \"01HD...\", \"idempotency_key\": \"01HCBREED...\" }\n→ 200\n{ \"breeding_event_id\": \"01HCEVT...\", \"hatch_at\": \"2026-08-13T14:36:01Z\", \"offspring_silhouette\": \"pengu\" }</code></pre>\n<pre><code class=\"language-json\">{\n  \"id\": \"01HCEVT...\",\n  \"parent_a_id\": \"01HC...\", \"parent_b_id\": \"01HD...\",\n  \"owner_id\": \"01HU...\",\n  \"offspring_species\": \"pengu\", \"offspring_tier\": \"rare\",\n  \"offspring_palette_seed\": \"f0d3b5a78c4e1928\",\n  \"hatch_at\": \"2026-08-13T14:36:01Z\",\n  \"status\": \"incubating\"\n}</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"pet.breeding.completed\", \"owner_id\": \"01HU...\", \"offspring_pet_id\": \"01HC...\", \"offspring_species\": \"pengu\", \"offspring_tier\": \"rare\" }</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"cooldown\", \"retry_after_sec\": 432000 }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Cross-player breeding at P2? → §1.16 — deferred.</li><li><strong>OQ-2 (resolved):</strong> Tier upgrade distribution? → §1.4 + §2 — 80/15/5.</li><li><strong>OQ-3 (resolved):</strong> Self-breeding? → §1.13 — forbidden.</li><li><strong>OQ-4 (resolved):</strong> Cost? → §1.2 — 500 Coins (250 Pet+).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Trait inheritance non-deterministic (e.g. Node version bug in SHA256)</td><td>Property test</td><td>Audit broken</td><td>Pin Node version; CI test</td></tr><tr><td>2</td><td>Incubator scheduled function fails</td><td>Cron alert</td><td>Eggs stuck</td><td>Manual re-run; alert DPO</td></tr><tr><td>3</td><td>Slot-full grace expires while player tries to choose</td><td>UX hang</td><td>Frustration</td><td>Surface clear choice UI; 1h soft warning before expiry</td></tr><tr><td>4</td><td>Cooldown counter Redis lost</td><td>Counter resets</td><td>Back-to-back breeding</td><td>Conservative fallback: assume cooldown active</td></tr><tr><td>5</td><td>Both parents same pet via API replay</td><td>Validation catches</td><td>None</td><td>§1.13 enforced</td></tr><tr><td>6</td><td>Palette XOR collision (rare visual sameness)</td><td>Player report</td><td>Visual dup</td><td>Acceptable — deterministic by design</td></tr><tr><td>7</td><td>Statistical test fails</td><td>Code change</td><td>Distribution drift</td><td>Investigate; pin SHA256</td></tr><tr><td>8</td><td>Receipt write fails after coin spend</td><td>Audit</td><td>Compensating refund</td><td>Saga pattern</td></tr><tr><td>9</td><td>Tenant-mismatch undetected</td><td>RLS test</td><td>Audit issue</td><td>Tenant scoping enforced</td></tr><tr><td>10</td><td>Cross-tenant breeding via B2B race</td><td>B2B audit</td><td>Privacy issue</td><td>Pre-check tenant_id</td></tr><tr><td>11</td><td>24h incubation race with stage advancement</td><td>Edge case</td><td>Bizarre state</td><td>Lock pet stage during incubation</td></tr><tr><td>12</td><td>Breeding event id collision</td><td>Cosmic ray</td><td>Audit corrupt</td><td>ULID retry</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 (breeding mechanic — Tamagotchi Paradise + Adopt Me precedent), plan §PART 8 (deterministic per FR-LEGAL-002 §1.6).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Cooldown duration (7d default) Mixpanel-tunable.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Real-money breeding fee.</li><li>Randomised (non-deterministic) outcomes.</li><li>Self-breeding.</li><li>Cross-tenant breeding.</li></ul>\n<p><strong>Cross-reference:</strong> FR-VIRAL-003 generative pet is parallel — both produce new pets via deterministic functions of inputs.</p>";
