window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CARE-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Endpoint.</strong> <code>POST /v1/pets/:petId/feed</code> MUST execute server-authoritative feed. Body: <code>{ food_id: 'basic' | 'premium', idempotency_key: string }</code>. Header <code>Authorization: Bearer &lt;JWT&gt;</code> required.</p>\n<p>§1.2  <strong>Stage gate.</strong> Per FR-PET-002 §1.6, feeding MUST be permitted only when <code>stage ∈ {baby, teen, adult}</code>. Eggs and grandma_house pets reject with HTTP 403 <code>{ error: 'pet.stage.locked', current_stage }</code>.</p>\n<p>§1.3  <strong>Ownership check.</strong> Only the pet's <code>owner_id</code> OR an <code>active</code> co-parent (FR-SOCIAL-002 P2 wiring) MAY feed. Cross-tenant + non-owner attempts return HTTP 403.</p>\n<p>§1.4  <strong>Food catalogue.</strong> Two food items at P1:</p>\n<ul><li><code>basic</code>: cost 5 Coins, hunger +30, no other stat effect, cooldown 5 min.</li><li><code>premium</code>: cost 25 Coins, hunger +50 + happiness +5, cooldown 15 min.</li></ul>\n<p>Catalogue in <code>apps/api/src/care/food-catalogue.ts</code> is a single source of truth; FR-ECON-002 (P3) extends with cosmetic-tied food but the schema is locked here.</p>\n<p>§1.5  <strong>Currency spend.</strong> Server MUST deduct Coins via FR-ECON-001 (P3) ledger. At P1, the ledger is stubbed to \"always succeed\" with infinite Coins — FR-ECON-001 wires the real ledger. The contract MUST allow rejection with <code>{ error: 'insufficient_coins', required, have }</code> for forward-compatibility.</p>\n<p>§1.6  <strong>Cooldown.</strong> Per food, per pet, per player. Cooldown windows enforced via Redis sliding window keyed by <code>(pet_id, food_id)</code>. Within-cooldown reject: HTTP 429 <code>{ error: 'cooldown', retry_after_sec }</code>.</p>\n<p>§1.7  <strong>Stat apply.</strong> Apply via <code>StatDecayService.apply(stats, 'feed_basic' | 'feed_premium')</code> per FR-PET-003 §3.1. Result clamped 0-100.</p>\n<p>§1.8  <strong><code>last_seen_at</code> update.</strong> Each feed MUST update <code>pets.last_seen_at</code> so decay (FR-PET-003) restarts from now.</p>\n<p>§1.9  <strong>Audit row.</strong> Each feed writes to <code>care_actions</code> table: <code>(id, pet_id, owner_id, action='feed', food_id, hunger_before, hunger_after, occurred_at, source='ui'|'co_parent', tenant_id)</code>. Retention 90 days for analytics + 7 years for kids (COPPA audit).</p>\n<p>§1.10  <strong>Idempotency.</strong> <code>idempotency_key</code> MUST be a client-generated 16-char ULID-prefix. Server MUST cache the first response per (player, key) for 60 seconds and return identical response on retry. Prevents network-flake double-feeds.</p>\n<p>§1.11  <strong>Colyseus broadcast.</strong> Successful feed broadcasts <code>pet.fed { pet_id, food_id, hunger_after }</code> to all clients in the PetRoom (so co-parents see it live).</p>\n<p>§1.12  <strong>Animation handshake.</strong> Server response includes <code>animation_token</code> (5-sec HMAC) that the Cocos client surrenders to play the <code>eat</code> Spine animation. Without the token, client SHOULD NOT play the animation (defense against client-only stat displays).</p>\n<p>§1.13  <strong>Haptic.</strong> Client fires <code>HapticsAdapter.tap('light')</code> on successful response.</p>\n<p>§1.14  <strong>Rate limit (anti-spam).</strong> Beyond cooldown, a per-player overall feed rate-limit of 60 feeds/hour MUST apply (catches multi-pet spam). Exceeds → HTTP 429 with <code>retry_after_sec</code>.</p>\n<p>§1.15  <strong>Analytics.</strong> Emit <code>care.feed.success { food_id, hunger_before, hunger_after, source }</code> + <code>care.feed.failure { reason }</code> per FR-OBS-001 §1.4.</p>\n<p>§1.16  <strong>Impossible-state detection.</strong> Feeding when hunger=100 triggers FR-INFRA-002 §1.9 impossible-transition path; this FR's handler MUST call <code>recordImpossibleTransition</code> and reject with HTTP 422 <code>{ error: 'pet.stat.already_full' }</code>.</p>\n<p>§1.17  <strong>Source attribution.</strong> The <code>source</code> field distinguishes <code>ui</code> (the owner-tapped feed) from <code>co_parent_remote</code> (the co-parent fed in FR-SOCIAL-002). This is the substrate for the PetPair receipt notification (\"Linh fed Mochi while you were asleep\").</p>\n<p>§1.18  <strong>No real-money food.</strong> Per FR-LEGAL-002 §1.2, food is earned-currency only. The catalogue MUST NOT include any <code>iap_sku</code> field; FR-ECON-002's IAP catalogue cannot reference food items.</p>\n<p>§1.19  <strong>Server clock for cooldown.</strong> Cooldowns MUST be enforced server-side using monotonic clock (<code>Date.now()</code>); client-supplied \"I last fed at T\" is ignored.</p>\n<p>§1.20  <strong>Failure observability.</strong> Each failure category (cooldown, stage_locked, insufficient_coins, stat_already_full, rate_limit) MUST emit a separately-labelled Prometheus metric to enable per-category alerting.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why server-authoritative.</strong> Plan §PART 4 anti-cheat. Feed is the single most common care action; if client-spoofable, the entire stat economy collapses + breeding eligibility (FR-PET-007) becomes free.</p>\n<p><strong>Why two foods at P1.</strong> Plan §PART 3 economy basics. Two tiers give a meaningful currency-to-effectiveness gradient without overwhelming the player; FR-ECON-002 expands the catalogue in P3.</p>\n<p><strong>Why short cooldown.</strong> Plan §PART 3 60-second session loop. 5 min basic / 15 min premium balances \"don't feed-spam to 100\" against \"let the player actually engage.\"</p>\n<p><strong>Why idempotency-key not full request-deduplication.</strong> A flaky network producing two POSTs is the dominant case; key-based dedup handles it cheaply. Full request-hash dedup is overkill for a side-effect-light action.</p>\n<p><strong>Why broadcast over PetRoom.</strong> Co-parent presence (FR-SOCIAL-002) needs to see live state. Broadcasting the action explicitly (rather than relying solely on schema diff) gives a notification surface (\"Linh fed Mochi!\") + an analytics seam.</p>\n<p><strong>Why animation token.</strong> Without it, a tampered client could play the eat animation without an actual server-side feed, producing a UI lie. Token ties animation playback to a real server-validated event.</p>\n<p><strong>Why 60/hour overall rate-limit.</strong> With 3 pets free-tier and 5-min cooldown per pet, the theoretical max is 36/hr. 60 is generous headroom for legitimate Pet+ players with 10 pets without permitting clear abuse.</p>\n<p><strong>Why no real-money food.</strong> EU Digital Fairness Act + Belgium 2018 ban + plan §PART 8 — real-money randomised outcomes forbidden. Food has no randomness but real-money currency that converts to stat changes treads near \"pay to skip care.\" Earned-currency only sidesteps this entirely.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Controller</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/care/feed.controller.ts\n@Controller('v1/pets')\n@UseGuards(SupabaseJwtGuard)\nexport class FeedController {\n  constructor(private readonly svc: FeedService) {}\n\n  @Post(':petId/feed')\n  async feed(@CurrentUser() u: AuthedUser, @Param('petId') petId: string,\n             @Body() body: { food_id: 'basic'|'premium'; idempotency_key: string }) {\n    return this.svc.feed(u, petId, body.food_id, body.idempotency_key);\n  }\n}</code></pre>\n<h3>3.2 — Service</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/care/feed.service.ts\n@Injectable()\nexport class FeedService {\n  constructor(\n    private readonly supa: SupabaseClient,\n    private readonly cooldown: CooldownService,\n    private readonly stats: StatDecayService,\n    private readonly econ: CurrencyLedger,\n    private readonly limit: RateLimit,\n    private readonly idemp: IdempotencyStore,\n    private readonly audit: AuditLogService,\n    private readonly broadcast: RoomBroadcast,\n  ) {}\n\n  async feed(u: AuthedUser, petId: string, foodId: 'basic'|'premium', idempKey: string) {\n    const prior = await this.idemp.lookup(u.id, `feed:${idempKey}`);\n    if (prior) return prior;\n    await this.limit.assert({ key: `feed-overall:${u.id}`, perHour: 60 });\n    const { data: pet } = await this.supa.from('pets').select('*').eq('id', petId).single();\n    if (!pet) throw new HttpException('not_found', 404);\n    if (pet.owner_id !== u.id) throw new HttpException('forbidden', 403);\n    if (!['baby','teen','adult'].includes(pet.stage)) throw new HttpException({ error: 'pet.stage.locked', current_stage: pet.stage }, 403);\n    if (pet.hunger &gt;= 100) { recordImpossibleTransition({ player: u.id, kind: 'feed-at-max' }); throw new HttpException('pet.stat.already_full', 422); }\n    const ttlSec = await this.cooldown.check(petId, foodId);\n    if (ttlSec &gt; 0) throw new HttpException({ error: 'cooldown', retry_after_sec: ttlSec }, 429);\n    const food = FOOD_CATALOGUE[foodId];\n    await this.econ.spend(u.id, food.cost);\n    const before = { hunger: pet.hunger, cleanliness: pet.cleanliness, happiness: pet.happiness, energy: pet.energy };\n    const after = this.stats.apply(before, foodId === 'basic' ? 'feed_basic' : 'feed_premium');\n    await this.supa.from('pets').update({ ...after, last_seen_at: new Date().toISOString() }).eq('id', petId);\n    await this.supa.from('care_actions').insert({\n      pet_id: petId, owner_id: u.id, action: 'feed', food_id: foodId,\n      hunger_before: before.hunger, hunger_after: after.hunger, source: 'ui', tenant_id: u.tenant_id,\n    });\n    await this.cooldown.start(petId, foodId, food.cooldown_sec);\n    await this.broadcast.toPetRoom(petId, 'pet.fed', { pet_id: petId, food_id: foodId, hunger_after: after.hunger });\n    const result = {\n      pet_id: petId, food_id: foodId, hunger_after: after.hunger,\n      animation_token: signAnimationToken({ pet_id: petId, action: 'eat', ttl_sec: 5 }),\n    };\n    await this.idemp.store(u.id, `feed:${idempKey}`, result);\n    await this.audit.write({ who: u.id, what: 'care.feed.success', what_keys: { pet_id: petId, food_id: foodId } });\n    return result;\n  }\n}</code></pre>\n<h3>3.3 — Food catalogue</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/care/food-catalogue.ts\nexport const FOOD_CATALOGUE = {\n  basic:   { cost: 5,  cooldown_sec: 5 * 60,  hunger_delta: 30 },\n  premium: { cost: 25, cooldown_sec: 15 * 60, hunger_delta: 50, happiness_delta: 5 },\n} as const;</code></pre>\n<h3>3.4 — Migration</h3>\n<pre><code class=\"language-sql\">-- infra/supabase/standard/migrations/20260517_010_care_actions.sql\ncreate table public.care_actions (\n  id bigserial primary key,\n  pet_id text not null references public.pets(id) on delete cascade,\n  owner_id uuid not null,\n  action text not null check (action in ('feed','clean','hug','mini_game','sleep')),\n  food_id text,\n  hunger_before int, hunger_after int,\n  cleanliness_before int, cleanliness_after int,\n  happiness_before int, happiness_after int,\n  energy_before int, energy_after int,\n  source text not null default 'ui' check (source in ('ui','co_parent_remote','offline_reconciled')),\n  occurred_at timestamptz not null default now(),\n  tenant_id text not null default 'mochi'\n);\ncreate index on public.care_actions (pet_id, occurred_at desc);\ncreate index on public.care_actions (owner_id, occurred_at desc);\nalter table public.care_actions enable row level security;\ncreate policy \"care_actions self\" on public.care_actions for select using (owner_id = auth.uid());</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> <code>POST /v1/pets/:id/feed</code> with <code>food_id=basic</code> on a baby pet at hunger=50 returns 200 with <code>hunger_after=80</code>. Verified by <code>__tests__/feed.spec.ts</code>.</p>\n<p><strong>AC2.</strong> Feeding an egg returns 403 <code>pet.stage.locked</code>. Verified by spec test.</p>\n<p><strong>AC3.</strong> Feeding with hunger=100 returns 422 <code>pet.stat.already_full</code> + emits <code>security.impossible-transition</code>. Verified.</p>\n<p><strong>AC4.</strong> Cooldown enforcement: second feed within 5 min returns 429 with <code>retry_after_sec ≤ 300</code>. Verified by spec test using fake timers.</p>\n<p><strong>AC5.</strong> Idempotency: identical request with same key returns cached response without applying stat twice. Verified by spec test.</p>\n<p><strong>AC6.</strong> Insufficient coins (when FR-ECON-001 wires real ledger) returns 402 <code>insufficient_coins</code>. Verified by spec test with stub ledger throwing.</p>\n<p><strong>AC7.</strong> Rate-limit 60/hour enforced. Verified by spec test pumping 61 requests.</p>\n<p><strong>AC8.</strong> Cross-owner attempt returns 403. Verified by spec test with two seeded users.</p>\n<p><strong>AC9.</strong> Audit row written with correct before/after values. Verified by spec test querying <code>care_actions</code>.</p>\n<p><strong>AC10.</strong> Colyseus broadcast emits <code>pet.fed</code> to all room clients. Verified by integration test with 2 Colyseus clients.</p>\n<p><strong>AC11.</strong> Animation token validated: feeding without token client cannot play <code>eat</code>. Verified by Cocos <code>__tests__/FeedAction.spec.ts</code>.</p>\n<p><strong>AC12.</strong> Stage transition mid-feed (e.g. baby → teen mid-cooldown) does not break cooldown semantics. Verified by spec test.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/care/__tests__/feed.spec.ts\nimport { describe, it, expect, vi } from 'vitest';\nimport { FeedService } from '../feed.service';\n\ndescribe('FR-CARE-001 — feed action', () =&gt; {\n  const svc = new FeedService(/*mocks*/);\n\n  it('feeds baby basic +30 hunger', async () =&gt; {\n    const r = await svc.feed(user('u1'), 'pet-mochi', 'basic', 'k1');\n    expect(r.hunger_after).toBe(80);\n    expect(r.animation_token).toBeTruthy();\n  });\n\n  it('rejects feed at hunger=100', async () =&gt; {\n    await expect(svc.feed(user('u1'), 'pet-full', 'basic', 'k2')).rejects.toMatchObject({ status: 422 });\n  });\n\n  it('enforces cooldown', async () =&gt; {\n    await svc.feed(user('u1'), 'pet-mochi', 'basic', 'k3');\n    await expect(svc.feed(user('u1'), 'pet-mochi', 'basic', 'k4')).rejects.toMatchObject({ status: 429 });\n  });\n\n  it('is idempotent', async () =&gt; {\n    const r1 = await svc.feed(user('u1'), 'pet-mochi', 'basic', 'k5');\n    const r2 = await svc.feed(user('u1'), 'pet-mochi', 'basic', 'k5');\n    expect(r1).toEqual(r2);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/care/cooldown.service.ts\n@Injectable()\nexport class CooldownService {\n  constructor(@Inject('REDIS') private readonly redis: RedisClientType) {}\n  async check(petId: string, foodId: string): Promise&lt;number&gt; {\n    const ttl = await this.redis.ttl(`cd:feed:${petId}:${foodId}`);\n    return ttl &gt; 0 ? ttl : 0;\n  }\n  async start(petId: string, foodId: string, seconds: number): Promise&lt;void&gt; {\n    await this.redis.set(`cd:feed:${petId}:${foodId}`, '1', { EX: seconds });\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Redis (cooldown + idempotency store).</p>\n<p><strong>Internal:</strong> FR-PET-001 (pets table), FR-PET-002 (stage gate), FR-PET-003 (StatDecayService.apply), FR-ART-001 (<code>eat</code> animation), FR-INFRA-002 (Colyseus broadcast), FR-INFRA-003 (care_actions table + RLS).</p>\n<p><strong>Blocks:</strong> FR-CARE-005 (streak system reads care_actions log), FR-VIRAL-002 (Daily Drama event types include feed), FR-B2B-005 (Viettel tenant feed-with-topup uses this contract).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/pets/01HC7QG.../feed\nAuthorization: Bearer eyJ...\nContent-Type: application/json\n{ \"food_id\": \"basic\", \"idempotency_key\": \"01HCFEED7QG...\" }\n\n→ 200 OK\n{ \"pet_id\": \"01HC7QG...\", \"food_id\": \"basic\", \"hunger_after\": 80, \"animation_token\": \"eyJ...\" }</code></pre>\n<pre><code class=\"language-json\">{\n  \"event\": \"care.feed.success\",\n  \"pet_id\": \"01HC7QG...\",\n  \"food_id\": \"basic\",\n  \"hunger_before\": 50,\n  \"hunger_after\": 80,\n  \"source\": \"ui\",\n  \"stage\": \"baby\",\n  \"emitted_at\": \"2026-08-12T14:36:01Z\"\n}</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"cooldown\", \"retry_after_sec\": 240 }</code></pre>\n<pre><code class=\"language-json\">{\n  \"id\": 4242,\n  \"pet_id\": \"01HC7QG...\",\n  \"owner_id\": \"01HC7QGZK4XN8YA1J3WB6XX99\",\n  \"action\": \"feed\",\n  \"food_id\": \"premium\",\n  \"hunger_before\": 40,\n  \"hunger_after\": 90,\n  \"source\": \"ui\",\n  \"occurred_at\": \"2026-08-12T14:36:01Z\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Two foods vs more? → §1.4 — two at P1; FR-ECON-002 expands.</li><li><strong>OQ-2 (resolved):</strong> Per-pet cooldown or per-player? → §1.6 — per-pet-per-food + overall rate limit.</li><li><strong>OQ-3 (resolved):</strong> Animation token vs trust the client? → §1.12 + §2 — token ties animation to real event.</li><li><strong>OQ-4 (resolved):</strong> Where does insufficient_coins come from at P1? → §1.5 — stub ledger; contract preserves rejection shape.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Redis cooldown unreachable</td><td><code>redis.ttl</code> errors</td><td>Cooldown bypass-safe (fail-closed: reject all feeds)</td><td>Surface \"service degraded\" UX; restore Redis</td></tr><tr><td>2</td><td>Idempotency cache miss after restart</td><td>Same key returns 200 twice</td><td>Double feed</td><td>60s cache TTL bounds the blast radius</td></tr><tr><td>3</td><td>Currency ledger stub returns success when real ledger would reject</td><td>E2E test in FR-ECON-001</td><td>Pet over-fed for free</td><td>Acceptable at P1; tightens when ledger lands</td></tr><tr><td>4</td><td>Concurrent feed (owner + co-parent simultaneously)</td><td>Race on <code>pets</code> update</td><td>One feed lost</td><td>Per-pet mutex in PetRoom serialises (FR-PET-003 §10 row 12)</td></tr><tr><td>5</td><td>Cooldown disclaimer + UX hint missing</td><td>Player support tickets</td><td>Frustration</td><td>Surface <code>retry_after_sec</code> to UI countdown</td></tr><tr><td>6</td><td>Premium food shipped without IAP path expectation</td><td>Audit</td><td>Soft-launch confusion</td><td>FR-ECON-001 ledger clarifies — food is Coin-only</td></tr><tr><td>7</td><td>care_actions audit grows unbounded</td><td>Disk pressure</td><td>Migration risk</td><td>90-day retention policy + cold archive</td></tr><tr><td>8</td><td>RLS misconfigured exposing other player's feeds</td><td>Spec test fails</td><td>Privacy issue</td><td>Tighten RLS; audit</td></tr><tr><td>9</td><td>Animation token forged by tampered client</td><td>Token signature fails</td><td>UI lie</td><td>Cocos rejects unsigned tokens</td></tr><tr><td>10</td><td>Stage transition mid-cooldown lingers stale cooldown</td><td>UX confusion</td><td>Player can't feed</td><td>Cooldown is per-pet, stage-agnostic — OK</td></tr><tr><td>11</td><td>Colyseus broadcast lost (room not yet created)</td><td>Co-parent doesn't see feed</td><td>UX desync</td><td>Persistent state in DB authoritative on rejoin</td></tr><tr><td>12</td><td>Rate-limit triggers on legitimate Pet+ owner with 10 pets</td><td>Support ticket</td><td>Frustration</td><td>Raise to 120/hour for Pet+ via FR-SUB-001 entitlement</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 (care loop), plan §PART 4 (server-authoritative), plan §PART 8 (no real-money food).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Coin costs (5/25) tunable via Mixpanel flags. Cooldown durations tunable.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Client-side stat write.</li><li>Real-money food.</li><li>Feeding pets in egg / grandma_house.</li></ul>\n<p><strong>Cross-reference:</strong> FR-CARE-002/003/004 follow this pattern. FR-CARE-005 reads <code>care_actions</code> for streak detection.</p>";
