window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PET-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Pet entity schema (Postgres).</strong> Pets MUST persist in a <code>public.pets</code> table on the appropriate Supabase project (per FR-INFRA-003 §1.1, kids pets live in the kids project, standard pets in the standard project). Columns:</p>\n<pre><code>id            text  primary key check (id ~ '^[0-9A-HJKMNPQRSTVWXYZ]{26}$')\ntenant_id     text  not null default 'mochi' check (tenant_id ~ '^[a-z0-9-]{2,32}$')\nowner_id      uuid  not null references auth.users(id) on delete cascade\nspecies       text  not null check (species in ('mochi','pengu','bao','fluffit','tako'))\ndisplay_name  text  not null check (length(display_name) &lt;= 24)\nstage         text  not null default 'egg'\n              check (stage in ('egg','baby','teen','adult','grandma_house'))\nborn_at       timestamptz not null default now()\nhatched_at    timestamptz\nlast_seen_at  timestamptz not null default now()\npalette_seed  text  not null  -- deterministic palette derivation\naudience_age_gate text not null default '13+' check (audience_age_gate in ('13+','under-13'))\nstatus        text  not null default 'active'\n              check (status in ('active','grandma','tombstoned'))\ncreated_at    timestamptz not null default now()</code></pre>\n<p>Composite indexes on <code>(tenant_id, owner_id)</code> and <code>(tenant_id, status)</code>. RLS policy <code>pets self+co-parent read</code> allows <code>owner_id = auth.uid()</code> OR co-parent join (FR-SOCIAL-002 will extend).</p>\n<p>§1.2  <strong>ULID for pet IDs.</strong> Pet IDs MUST be ULIDs (Universally Unique Lexicographically Sortable Identifiers) — 26 chars Crockford base32. ULIDs are time-encoding (first 10 chars = ms timestamp) which gives a free chronological sort and remains kid-readable for support tickets.</p>\n<p>§1.3  <strong>Colyseus PetState schema.</strong> The Colyseus room state shape (per FR-INFRA-002 §1.11) MUST include a <code>PetState</code> decorated with <code>@type</code> schema fields matching the Postgres columns the client needs to render: <code>id, ownerId, species, stage, displayName, palette_seed, hunger, cleanliness, happiness, energy, lastActionId</code>. Stats (hunger / cleanliness / happiness / energy) live in <code>PetState</code> but their persistence to Postgres is FR-PET-003.</p>\n<p>§1.4  <strong>Hatch flow — initial pet creation.</strong> A new player's first pet MUST be created via a dedicated hatch flow:</p>\n<ul><li><code>POST /v1/pets/hatch</code> with body <code>{ species: 'mochi' }</code> (only 'mochi' at P1 launch);</li><li>Server validates: player has &lt; 3 pets (free-tier cap from FR-PET-005), audience age gate matches, no recent hatch within 1 hour (debounce);</li><li>Server creates the pet row with <code>stage: 'egg'</code>;</li><li>Server returns <code>{ pet_id, hatch_animation_token }</code>;</li><li>Client plays the hatch Spine animation;</li><li>Client posts <code>/v1/pets/{id}/name</code> with the chosen display_name + content-safety token returned by the server.</li></ul>\n<p>§1.5  <strong>Naming content-safety.</strong> Display names MUST pass content-safety BEFORE being persisted:</p>\n<ul><li>Length: 2–24 characters (post-trim); allowed Unicode categories: Letter, Number, Mark, plus space + hyphen + apostrophe;</li><li>VN-specific block list at <code>apps/api/src/pets/naming/vi-blocklist.txt</code>;</li><li>OpenAI Moderation API call (with retry + cache by name hash);</li><li>Under-13 SKU additionally rejects: ANY person's full name pattern (basic regex), any URL pattern, any phone-number pattern (PDPL + COPPA-2025 §1.5 spirit).</li></ul>\n<p>Failed names return 422 <code>{ error: \"name_rejected\", reason: &lt;slug&gt;, suggestion?: &lt;string&gt; }</code>.</p>\n<p>§1.6  <strong>Palette seed.</strong> <code>palette_seed</code> MUST be a deterministic random 16-char string generated server-side at hatch. The Spine palette (per FR-ART-001 §1.4 accessory slots) is derived from the seed via a published palette-generator function <code>derivePalette(seed)</code>. This makes every pet visually unique-ish at hatch, and reproducible (a support ticket can recover what the player saw). For FR-VIRAL-003 (generative pet from prompt/selfie), the palette_seed is overridden by the generator output.</p>\n<p>§1.7  <strong>Co-parent prep.</strong> The schema MUST support a future co-parent relationship (FR-SOCIAL-002). A <code>pet_co_parents</code> join table is provisioned at this FR with columns <code>(pet_id, co_parent_user_id, added_at, status check in ('pending','active','removed'))</code>. The table is empty at P1 but its schema + RLS policy MUST be authored now to avoid migration thrash in P2.</p>\n<p>§1.8  <strong>Audience gate inheritance.</strong> A pet's <code>audience_age_gate</code> MUST inherit from its owner's <code>app_users.audience_age_gate</code> at creation time. The COPPA age-gate middleware (FR-LEGAL-001 §1.10) MUST allow a pet's owner to access their own pet regardless of state (so a child whose parent revoked consent can still see \"your pet has gone to grandma's\" UX during the 30-day deletion grace).</p>\n<p>§1.9  <strong><code>PlayerPetsRoster</code> state.</strong> The Colyseus <code>PlayerPetsRoster</code> schema MUST hold a <code>MapSchema&lt;string, PetState&gt;</code> keyed by pet id, with operations: <code>addPet(pet)</code>, <code>removePet(petId)</code>, <code>evolvePet(petId, newStage)</code>. The roster is loaded from Postgres on player join (FR-INFRA-002 <code>onJoin</code>) and persisted back on idle disposal (FR-INFRA-002 §1.12).</p>\n<p>§1.10  <strong>Hatch animation token.</strong> The <code>hatch_animation_token</code> returned by <code>/v1/pets/hatch</code> is a short-lived (5 min) HMAC token that the client surrenders when posting the name. This prevents a client from skipping the hatch animation + naming immediately, which would create a UX exploit (instant pet hatch + immediate trade) — and also ties analytics events to a confirmed hatch ceremony.</p>\n<p>§1.11  <strong>Last-seen tracking.</strong> <code>last_seen_at</code> MUST be updated on every meaningful interaction (feed, clean, hug, mini-game, AR, name change). Decay timers (FR-PET-003) read from this column; FR-PET-008 (Permadeath-Lite) uses it as the 7-day-neglect signal.</p>\n<p>§1.12  <strong>Pet name uniqueness within owner.</strong> A player's pets MUST have unique display names within their roster (case-insensitive). Cross-player duplicates are allowed (millions of \"Mochi\" pets is fine). Unique index on <code>(owner_id, lower(display_name)) where status = 'active'</code>.</p>\n<p>§1.13  <strong>Hatch rate-limit + debounce.</strong> <code>/v1/pets/hatch</code> MUST: (a) be idempotent given an <code>Idempotency-Key</code> header; (b) be rate-limited at 3 hatches per player per 24 hours; (c) emit <code>pet.hatch.success</code> + <code>pet.hatch.failure { reason }</code> events.</p>\n<p>§1.14  <strong>Pet entity audit.</strong> Every pet state transition (<code>hatch</code>, <code>name_changed</code>, <code>stage_changed</code>, <code>grandma</code>, <code>tombstoned</code>) MUST emit a row in <code>pet_audit_log</code> table (FR-INFRA-003 §1.12). Retention 7 years for kids project (COPPA), 2 years for standard.</p>\n<p>§1.15  <strong>Postgres → Colyseus reconciliation.</strong> On player rejoin of a PetRoom, the server MUST: (a) fetch all <code>active</code> pets for the player from Postgres; (b) populate the Colyseus <code>PlayerPetsRoster</code>; (c) reconcile stat-bar decay (FR-PET-003) by computing time since <code>last_seen_at</code> and applying decay; (d) broadcast updated state to the client.</p>\n<p>§1.16  <strong>Pet deletion on owner deletion.</strong> <code>pets.owner_id</code> has <code>on delete cascade</code>. When a player's account is deleted (FR-AUTH-001 §1.19 or FR-LEGAL-001 §1.9), all their pets are cascade-deleted, AND a tombstone row is added to <code>pet_audit_log</code> recording the deletion reason.</p>\n<p>§1.17  <strong>No public pet listings without privacy review.</strong> A pet's <code>display_name</code> MUST NOT appear in any public/anonymous endpoint without explicit consent (a \"make my pet public\" toggle, deferred to a future FR). Trade window (FR-SOCIAL-003) is in-game and consent-gated.</p>\n<p>§1.18  <strong>Pet quota soft-cap.</strong> Free tier allows 3 active pets per player; Pet+ subscription extends to 10 (FR-PET-005 final). At this FR's P1 scope, the cap is hard-coded at 3 with the entitlement check stubbed (returns \"always free tier\"). FR-SUB-001 wires the real entitlement.</p>\n<p>§1.19  <strong>Tenant partition.</strong> Per FR-INFRA-003 §1.4, the <code>tenant_id</code> column MUST be populated at pet creation. Consumer pets get <code>tenant_id = 'mochi'</code>; PetOS B2B tenants (P4) get their own slug. Cross-tenant pet visibility is forbidden by RLS.</p>\n<p>§1.20  <strong>Cocos PetController component.</strong> A Cocos component <code>PetController.ts</code> MUST attach to the rendered pet node and: (a) subscribe to the Colyseus <code>PetState</code> schema changes; (b) drive <code>SpineLoader.playContractAnimation</code> (FR-ART-001) based on state transitions; (c) emit <code>pet.rendered.frame</code> analytics every 10 seconds for performance monitoring. The component MUST gracefully no-op when state is <code>egg</code> or <code>grandma_house</code>.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why ULIDs over UUIDs.</strong> ULIDs are 26 chars vs UUID-v4 36 chars — 28% shorter, slightly easier on URL bars + support-ticket inputs. Time-encoding gives chronological sort for free (useful in roster UIs). Crockford base32 character set is kid-readable (no <code>I/L/O/U</code>). UUIDv7 is functionally similar but library support is weaker.</p>\n<p><strong>Why two-step hatch (egg → name).</strong> A single <code>POST /v1/pets/hatch</code> that both creates the pet and accepts a name would either (a) require the name BEFORE the hatch animation plays (boring UX — same as filling a form), or (b) let the client decide when to send the name, which lets a sophisticated client farm \"hatch tokens\" + name later. The two-step + 5-min HMAC token forces the hatch ceremony before the name is locked.</p>\n<p><strong>Why content-safety on names.</strong> A pet name is rendered to the owner + co-parent + trade-counterparty + TikTok export. Letting a player name their pet <code>&lt;offensive slur&gt;</code> then sharing a TikTok clip of it is a brand-blast risk + Apple/Google policy hit.</p>\n<p><strong>Why under-13 stricter name rules.</strong> COPPA-2025 spirit: no PII in display names that could enable contact (full names, phone, URL). The basic-regex enforcement catches the common cases; the OpenAI Moderation API catches the rest.</p>\n<p><strong>Why palette_seed not pre-baked palettes.</strong> Pre-baked palettes per species would mean all \"Mochi\" pets look identical. A deterministic palette function derived from a server-side seed makes every pet ever-so-slightly different + reproducible.</p>\n<p><strong>Why pet_co_parents table at P1.</strong> Adding a column or join table later means a schema migration with downtime risk. Empty-table-at-P1 is free and prevents the future churn.</p>\n<p><strong>Why <code>audience_age_gate</code> inherits from owner.</strong> A kid who graduates to 13 (FR-INFRA-003 §1.13 graduation flow) brings their pets forward; the audience_age_gate is re-derived at graduation time. Inheritance at hatch time keeps the model consistent.</p>\n<p><strong>Why hatch idempotency-key.</strong> A flaky network on the hatch tap could cause two <code>/v1/pets/hatch</code> POSTs; without idempotency, the player gets two pets and burns a slot. Idempotency-key is the standard fix.</p>\n<p><strong>Why 5-min HMAC hatch token.</strong> Long enough for the artist's hatch animation (~30 s) + naming UX (~2 min); short enough that a captured token isn't replayable. The HMAC ties the token to a specific player + species so it cannot be reused for a different species/player.</p>\n<p><strong>Why unique pet names within owner.</strong> Multiple \"Mochi\"s in the same roster causes UI ambiguity (\"which Mochi did you feed?\"). Cross-player duplicates are fine — separate rosters never interact at the name layer.</p>\n<p><strong>Why <code>last_seen_at</code> not <code>last_action_at</code>.</strong> \"Seen\" includes passive viewing (open the pet screen, look at AR). FR-PET-003 stat decay should restart on view, not require interaction — viewing is the \"the player checked on the pet\" signal.</p>\n<p><strong>Why <code>on delete cascade</code>.</strong> GDPR Article 17 + COPPA-2025 erasure require full removal. Cascade is the simplest correct behaviour; the audit row preserves the <em>fact</em> of deletion (FR-LEGAL-001 §3.6).</p>\n<p><strong>Why no public pet listings at P1.</strong> Privacy-by-default. A public-pet toggle is a real privacy decision that needs its own FR (probably P2 or later, gated by Trust &amp; Safety review for the kids SKU).</p>\n<p><strong>Why PetController emits perf events.</strong> Cocos's runtime can stutter on lower-end devices; a 10-second cadence performance event lets us detect device-class performance regressions without bloating analytics volume.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Hatch controller</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/pets.controller.ts\n@Controller('v1/pets')\n@UseGuards(SupabaseJwtGuard)\nexport class PetsController {\n  constructor(\n    private readonly hatch: HatchService,\n    private readonly naming: NameContentSafetyService,\n    private readonly pets: PetsService,\n  ) {}\n\n  @Post('hatch')\n  @Header('Idempotency-Key', { required: true })\n  async hatch(@CurrentUser() u: AuthedUser, @Body() body: { species: PetSpecies }, @Headers('idempotency-key') idemp: string) {\n    return this.hatch.requestHatch(u, body.species, idemp);\n  }\n\n  @Post(':id/name')\n  async name(@CurrentUser() u: AuthedUser, @Param('id') id: string,\n             @Body() body: { display_name: string; hatch_animation_token: string }) {\n    await this.naming.assertSafe(body.display_name, { audience: u.audience_age_gate });\n    return this.pets.confirmName(u, id, body);\n  }\n\n  @Get()\n  async list(@CurrentUser() u: AuthedUser) {\n    return this.pets.listOwned(u.id);\n  }\n}</code></pre>\n<h3>3.2 — Colyseus <code>PetState</code> (extended from FR-INFRA-002)</h3>\n<pre><code class=\"language-typescript\">// apps/realtime/src/state/PetState.ts\nimport { Schema, type, MapSchema } from '@colyseus/schema';\n\nexport class PetState extends Schema {\n  @type('string')  id = '';\n  @type('string')  ownerId = '';\n  @type('string')  species = 'mochi';\n  @type('string')  displayName = '';\n  @type('string')  stage = 'egg';\n  @type('string')  paletteSeed = '';\n  @type('number')  hunger = 100;\n  @type('number')  cleanliness = 100;\n  @type('number')  happiness = 100;\n  @type('number')  energy = 100;\n  @type('number')  ageMinutes = 0;\n  @type('string')  lastActionId = '';\n  @type('string')  audienceAgeGate = '13+';\n}\n\nexport class PlayerPetsRoster extends Schema {\n  @type({ map: PetState }) pets = new MapSchema&lt;PetState&gt;();\n}</code></pre>\n<h3>3.3 — Naming content-safety</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/naming/name-content-safety.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { OpenAIModeration } from '../../external/openai-moderation.client';\nimport { viBlocklist } from './vi-blocklist';\n\nconst PERSON_NAME_LIKE = /\\b([A-Z][a-z]{1,}\\s+[A-Z][a-z]{1,})\\b/;\nconst PHONE_LIKE       = /\\d{3,}[- ]?\\d{3,}/;\nconst URL_LIKE         = /(https?:\\/\\/|www\\.|\\.\\w{2,})/i;\n\n@Injectable()\nexport class NameContentSafetyService {\n  constructor(private readonly mod: OpenAIModeration) {}\n\n  async assertSafe(name: string, ctx: { audience: '13+' | 'under-13' }): Promise&lt;void&gt; {\n    const trimmed = name.trim();\n    if (trimmed.length &lt; 2 || trimmed.length &gt; 24) throw new BadRequest({ error: 'name_rejected', reason: 'length' });\n\n    const lower = trimmed.toLowerCase();\n    for (const banned of viBlocklist) if (lower.includes(banned)) throw new BadRequest({ error: 'name_rejected', reason: 'blocklist' });\n\n    if (ctx.audience === 'under-13') {\n      if (PERSON_NAME_LIKE.test(trimmed)) throw new BadRequest({ error: 'name_rejected', reason: 'pii_like_name' });\n      if (PHONE_LIKE.test(trimmed))       throw new BadRequest({ error: 'name_rejected', reason: 'pii_like_phone' });\n      if (URL_LIKE.test(trimmed))         throw new BadRequest({ error: 'name_rejected', reason: 'pii_like_url' });\n    }\n\n    const r = await this.mod.classify(trimmed);\n    if (r.flagged) throw new BadRequest({ error: 'name_rejected', reason: 'moderation_flagged' });\n  }\n}</code></pre>\n<h3>3.4 — Pets migration (standard project)</h3>\n<pre><code class=\"language-sql\">-- infra/supabase/standard/migrations/20260517_006_pets_table.sql\ncreate table public.pets (\n  id text primary key\n     check (id ~ '^[0-9A-HJKMNPQRSTVWXYZ]{26}$'),\n  tenant_id text not null default 'mochi'\n     check (tenant_id ~ '^[a-z0-9-]{2,32}$'),\n  owner_id uuid not null references auth.users(id) on delete cascade,\n  species text not null check (species in ('mochi','pengu','bao','fluffit','tako')),\n  display_name text not null check (length(display_name) &lt;= 24),\n  stage text not null default 'egg'\n     check (stage in ('egg','baby','teen','adult','grandma_house')),\n  born_at timestamptz not null default now(),\n  hatched_at timestamptz,\n  last_seen_at timestamptz not null default now(),\n  palette_seed text not null,\n  audience_age_gate text not null default '13+' check (audience_age_gate in ('13+','under-13')),\n  status text not null default 'active' check (status in ('active','grandma','tombstoned')),\n  created_at timestamptz not null default now()\n);\ncreate index on public.pets (tenant_id, owner_id);\ncreate index on public.pets (tenant_id, status);\ncreate unique index on public.pets (owner_id, lower(display_name)) where status = 'active';\n\nalter table public.pets enable row level security;\ncreate policy \"pets self read\"   on public.pets for select using (owner_id = auth.uid());\ncreate policy \"pets self update\" on public.pets for update using (owner_id = auth.uid());\n\n-- pet_co_parents prep for FR-SOCIAL-002\ncreate table public.pet_co_parents (\n  pet_id text not null references public.pets(id) on delete cascade,\n  co_parent_user_id uuid not null references auth.users(id) on delete cascade,\n  added_at timestamptz not null default now(),\n  status text not null default 'pending' check (status in ('pending','active','removed')),\n  primary key (pet_id, co_parent_user_id)\n);\nalter table public.pet_co_parents enable row level security;</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> <code>POST /v1/pets/hatch</code> with valid auth + <code>Idempotency-Key</code> creates a pet row with <code>stage='egg'</code> AND returns <code>pet_id</code> + <code>hatch_animation_token</code>. Verified by <code>__tests__/hatch.spec.ts</code>.</p>\n<p><strong>AC2.</strong> A second hatch call with the same <code>Idempotency-Key</code> returns the same pet (no duplicate). Verified by spec test.</p>\n<p><strong>AC3.</strong> A 4th hatch within 24 h is rejected with <code>429 Too Many Requests</code>. Verified by spec test pumping requests.</p>\n<p><strong>AC4.</strong> <code>POST /v1/pets/:id/name</code> rejects a name that fails content-safety, returning HTTP 422 + reason. Verified by <code>__tests__/name-content-safety.spec.ts</code> with multiple fixtures (length, blocklist, person-name, phone, URL, moderation).</p>\n<p><strong>AC5.</strong> Under-13 SKU rejects names matching person-name/phone/URL patterns; 13+ SKU allows them (still subject to moderation). Verified by spec test driving both audiences.</p>\n<p><strong>AC6.</strong> Pet ULID format is enforced. Verified by inserting a non-ULID id (e.g. <code>'bad-id'</code>) and asserting Postgres constraint fail.</p>\n<p><strong>AC7.</strong> Unique pet name index enforced — second pet with same <code>display_name</code> under same owner returns HTTP 409. Verified by spec test.</p>\n<p><strong>AC8.</strong> Pet RLS — player A cannot SELECT player B's pets. Verified by <code>__tests__/pets.spec.ts</code> with two seeded users + anon client.</p>\n<p><strong>AC9.</strong> Colyseus PetRoom on player join loads all <code>active</code> pets and broadcasts initial state. Verified by <code>__tests__/PetRoom.spec.ts</code> integration test with seeded pets.</p>\n<p><strong>AC10.</strong> Tenant partition — a pet created via tenant slug <code>techcombank</code> (B2B P4 use case) is invisible to a tenant <code>mochi</code> query. Verified by RLS test driving <code>app.tenant_id</code>.</p>\n<p><strong>AC11.</strong> Cascade delete — deleting an <code>auth.users</code> row removes the player's pets AND adds tombstone audit row. Verified by spec test.</p>\n<p><strong>AC12.</strong> <code>last_seen_at</code> updates on rename. Verified by spec test asserting timestamp diff.</p>\n<p><strong>AC13.</strong> Palette_seed is generated server-side (no client-supplied seed accepted). Verified by spec test attempting client-supplied seed and asserting it's ignored.</p>\n<p><strong>AC14.</strong> <code>pet.hatch.success</code> event emitted with the documented schema. Verified by <code>__tests__/pets.spec.ts</code> + obs schema validation.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 — Hatch flow integration test</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/__tests__/hatch.spec.ts\nimport { describe, it, expect } from 'vitest';\nimport { request } from './_helpers/http';\n\ndescribe('FR-PET-001 — hatch flow', () =&gt; {\n  it('creates a pet with stage egg + returns hatch token', async () =&gt; {\n    const r = await request.post('/v1/pets/hatch')\n      .set('Authorization', `Bearer ${TOKEN}`)\n      .set('Idempotency-Key', 'k1')\n      .send({ species: 'mochi' });\n    expect(r.status).toBe(201);\n    expect(r.body.pet_id).toMatch(/^[0-9A-HJKMNPQRSTVWXYZ]{26}$/);\n    expect(r.body.hatch_animation_token).toBeTruthy();\n    const pet = await db.pets.byId(r.body.pet_id);\n    expect(pet.stage).toBe('egg');\n  });\n\n  it('is idempotent on same key', async () =&gt; {\n    const r1 = await request.post('/v1/pets/hatch').set('Authorization', `Bearer ${TOKEN}`).set('Idempotency-Key', 'k2').send({ species: 'mochi' });\n    const r2 = await request.post('/v1/pets/hatch').set('Authorization', `Bearer ${TOKEN}`).set('Idempotency-Key', 'k2').send({ species: 'mochi' });\n    expect(r1.body.pet_id).toBe(r2.body.pet_id);\n  });\n\n  it('rejects 4th hatch within 24h', async () =&gt; {\n    for (let i = 0; i &lt; 3; i++) {\n      await request.post('/v1/pets/hatch').set('Authorization', `Bearer ${TOKEN}`).set('Idempotency-Key', `k${i+10}`).send({ species: 'mochi' });\n    }\n    const r4 = await request.post('/v1/pets/hatch').set('Authorization', `Bearer ${TOKEN}`).set('Idempotency-Key', 'k99').send({ species: 'mochi' });\n    expect(r4.status).toBe(429);\n  });\n});</code></pre>\n<h3>5.2 — Naming content-safety test</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/naming/__tests__/name-content-safety.spec.ts\nimport { describe, it, expect } from 'vitest';\nimport { NameContentSafetyService } from '../name-content-safety.service';\n\ndescribe('FR-PET-001 §1.5 — name content-safety', () =&gt; {\n  const svc = new NameContentSafetyService(fakeModeration({ flagFor: ['offensive_word'] }));\n\n  it('accepts a clean name', async () =&gt; {\n    await expect(svc.assertSafe('Mochi-chan', { audience: '13+' })).resolves.toBeUndefined();\n  });\n\n  it.each([\n    ['',                  'length'],\n    ['x',                 'length'],\n    ['x'.repeat(25),      'length'],\n    ['offensive_word',    'moderation_flagged'],\n  ])('rejects %s with reason %s', async (name, reason) =&gt; {\n    await expect(svc.assertSafe(name, { audience: '13+' })).rejects.toMatchObject({ reason });\n  });\n\n  it('under-13 rejects person-name pattern', async () =&gt; {\n    await expect(svc.assertSafe('John Smith', { audience: 'under-13' })).rejects.toMatchObject({ reason: 'pii_like_name' });\n  });\n\n  it('under-13 rejects phone-like pattern', async () =&gt; {\n    await expect(svc.assertSafe('call 555-1234', { audience: 'under-13' })).rejects.toMatchObject({ reason: 'pii_like_phone' });\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/hatch.service.ts\n@Injectable()\nexport class HatchService {\n  constructor(\n    private readonly supa: SupabaseClient,\n    private readonly throttle: HatchThrottle,\n    private readonly idemp: IdempotencyStore,\n    private readonly audit: AuditLogService,\n  ) {}\n\n  async requestHatch(u: AuthedUser, species: PetSpecies, idempKey: string) {\n    const prior = await this.idemp.lookup(u.id, idempKey);\n    if (prior) return prior;\n    await this.throttle.assertWithinLimit(u.id);\n    if (await this.supa.from('pets').select('id', { count: 'exact', head: true })\n                      .eq('owner_id', u.id).eq('status', 'active').then(r =&gt; (r.count ?? 0) &gt;= 3)) {\n      throw new HttpException('pet_quota_exceeded', 403);\n    }\n    const id = generateUlid();\n    const paletteSeed = randomBytes(8).toString('hex');\n    await this.supa.from('pets').insert({\n      id, owner_id: u.id, tenant_id: u.tenant_id, species,\n      display_name: 'Egg', stage: 'egg', palette_seed: paletteSeed,\n      audience_age_gate: u.audience_age_gate,\n    });\n    const token = signHatchToken({ pet_id: id, expiresInSec: 300 });\n    await this.audit.write({ who: u.id, what: 'pet.hatch.success', what_keys: { pet_id: id } });\n    const result = { pet_id: id, hatch_animation_token: token };\n    await this.idemp.store(u.id, idempKey, result);\n    return result;\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> OpenAI Moderation API (free tier sufficient at P0/P1 volume); ULID-generation library (npm <code>ulid</code>); Supabase Postgres.</p>\n<p><strong>Internal:</strong> FR-INFRA-003 (Supabase + RLS + tenant partition); FR-INFRA-002 (Colyseus + JWT-verified onJoin); FR-ART-001 (Spine pipeline + contract animations for the hatch ceremony).</p>\n<p><strong>Blocks:</strong> FR-PET-002 (evolution stages — needs <code>stage</code> column + animation hooks); FR-PET-003 (stat-bar decay reads <code>last_seen_at</code>); FR-PET-004 (onboarding flow drives the hatch UX); FR-CARE-001/002/003/004 (care actions mutate stat columns); FR-AR-001 (AR placement renders the same skeleton).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — <code>POST /v1/pets/hatch</code></h3>\n<pre><code class=\"language-http\">POST /v1/pets/hatch\nAuthorization: Bearer eyJ...\nIdempotency-Key: 01HC7QGZK4XN8YA1J3WB6EFR8\nContent-Type: application/json\n{ \"species\": \"mochi\" }\n\n→ 201 Created\n{\n  \"pet_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"hatch_animation_token\": \"eyJhbGciOi...\"\n}</code></pre>\n<h3>8.2 — <code>POST /v1/pets/:id/name</code></h3>\n<pre><code class=\"language-http\">POST /v1/pets/01HC7QGZK4XN8YA1J3WB6EFR8/name\nAuthorization: Bearer eyJ...\nContent-Type: application/json\n{\n  \"display_name\": \"Mochi-chan\",\n  \"hatch_animation_token\": \"eyJhbGciOi...\"\n}\n\n→ 200 OK\n{ \"pet_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\", \"display_name\": \"Mochi-chan\", \"stage\": \"baby\" }</code></pre>\n<h3>8.3 — Name rejection</h3>\n<pre><code class=\"language-json\">{\n  \"error\": \"name_rejected\",\n  \"reason\": \"pii_like_phone\",\n  \"suggestion\": null\n}</code></pre>\n<h3>8.4 — Pet row at rest</h3>\n<pre><code class=\"language-json\">{\n  \"id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"tenant_id\": \"mochi\",\n  \"owner_id\": \"01HC7QGZK4XN8YA1J3WB6XX99\",\n  \"species\": \"mochi\",\n  \"display_name\": \"Mochi-chan\",\n  \"stage\": \"baby\",\n  \"born_at\": \"2026-08-12T14:36:01Z\",\n  \"hatched_at\": \"2026-08-12T14:38:24Z\",\n  \"last_seen_at\": \"2026-08-12T14:38:24Z\",\n  \"palette_seed\": \"9b3e7d2a5c4f1b8e\",\n  \"audience_age_gate\": \"13+\",\n  \"status\": \"active\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> ULID vs UUIDv7? → §2 — ULID, kid-readable + mature library.</li><li><strong>OQ-2 (resolved):</strong> Two-step hatch (egg → name) vs one-step? → §1.4 + §2 — two-step preserves the hatch-ceremony UX + analytics integrity.</li><li><strong>OQ-3 (resolved):</strong> Where lives stat-bar decay logic? → §1.11 + FR-PET-003 — FR-PET-003 owns decay rates; this FR provides the <code>last_seen_at</code> substrate.</li><li><strong>OQ-4 (resolved):</strong> Cross-player name uniqueness? → §1.12 — unique within owner only.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Concurrent hatch race (same Idempotency-Key, two requests)</td><td>Idempotency store conflict</td><td>Same pet returned to both</td><td>Idempotency store wins; second request reads back the first result</td></tr><tr><td>2</td><td>Hatch token expired before naming</td><td><code>pets.hatch_animation_token.expired</code> HTTP 410</td><td>UX: replay hatch ceremony</td><td>Re-issue token via <code>/v1/pets/:id/regenerate-name-token</code></td></tr><tr><td>3</td><td>Content-safety vendor (OpenAI Moderation) outage</td><td>API timeout / 5xx</td><td>Naming blocked</td><td>Fallback to length + blocklist only; emit <code>naming.moderation.fallback</code> Sentry warn</td></tr><tr><td>4</td><td>Bad-words list out of date</td><td>Player support ticket</td><td>Offensive name slips through</td><td>Add to blocklist; audit recent hatches for retroactive review</td></tr><tr><td>5</td><td>Pet ULID collision (astronomically unlikely)</td><td>DB unique constraint error</td><td>One client gets 500</td><td>Regenerate ULID; retry; surface only if persistent</td></tr><tr><td>6</td><td>RLS misconfigured — cross-player visibility</td><td><code>__tests__/pets.spec.ts</code> RLS test fails</td><td>COPPA exposure</td><td>Re-tighten RLS; audit Sentry for affected rows</td></tr><tr><td>7</td><td>Tenant partition forgotten on insert</td><td><code>tenant_id_required</code> trigger fail</td><td>Insert rejected</td><td>Bug in pet creation path; patch caller</td></tr><tr><td>8</td><td>Hatch quota soft-cap of 3 incorrect on Pet+ user</td><td>Entitlement service returns wrong tier</td><td>UX shows wrong message</td><td>FR-PET-005 entitlement integration during P2 fixes; meanwhile manual cap override via DPO</td></tr><tr><td>9</td><td><code>last_seen_at</code> not updated on rename</td><td>Stat-bar decay too aggressive</td><td>UX regression</td><td>Audit code paths; add missing update</td></tr><tr><td>10</td><td>Colyseus state not reconciled on rejoin</td><td>Player sees stale stats</td><td>UX desync</td><td>Force-reload from Postgres; investigate dispose flush</td></tr><tr><td>11</td><td>Cascade delete leaves orphan rows in <code>pet_co_parents</code></td><td>Daily reconciliation Sentry</td><td>Inconsistent data</td><td>Force-cascade trigger on <code>pets</code> delete</td></tr><tr><td>12</td><td>Pet name uniqueness collision in legacy data (P2 migration)</td><td>Migration error</td><td>Migration fails</td><td>DPO-led name-disambiguation pass; resume migration</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 (core loop), plan §PART 4 (anti-cheat — server-authoritative), plan §PART 5 (one pet at launch).</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Final blocklist VN content — owned by Trust &amp; Safety hire (post-P0).</li><li>Palette derivation function spec — locked in <code>apps/api/src/pets/palette/derive-palette.ts</code> annex.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Client-side pet ID generation.</li><li>Free-form display names with no moderation.</li><li>Soft-deletion + hard-delete combinations without an audit row.</li><li>Cross-tenant pet visibility.</li></ul>\n<p><strong>Cross-reference:</strong> This FR is the substrate for the entire pet entity. FR-PET-002/003/004 + every CARE FR + every SOCIAL FR + every ECON FR reads/writes this schema.</p>";
