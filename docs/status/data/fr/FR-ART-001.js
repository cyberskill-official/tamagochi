window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-ART-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Spine 2D engine pin.</strong> Pet skeletons MUST be authored in <strong>Spine 2D 4.2.x</strong> (Esoteric Software). The Spine runtime MUST be <code>@cocos/spine-runtime</code> matching Spine 4.2. Mixing runtime ↔ editor major versions is forbidden.</p>\n<p>§1.2  <strong>20-animation contract.</strong> Every Spine skeleton (one per pet species) MUST implement the contract animation set with these exact names:</p>\n<pre><code>idle_baby | idle_teen | idle_adult\neat | sleep | happy | sad | sick | dance | wave\nhatch | evolve_baby_to_teen | evolve_teen_to_adult\ncare_pet | care_clean | care_feed | care_drink\ncelebrate_co_parent | wedding_pose\nar_greet</code></pre>\n<p>20 animations exactly. Adding a 21st requires an FR amendment (the count is the contract). Missing one breaks the linter.</p>\n<p>§1.3  <strong>Skeleton root contract.</strong> Every skeleton MUST have:</p>\n<ul><li>root bone named <code>root</code>;</li><li>three rig presets: <code>baby</code>, <code>teen</code>, <code>adult</code> (via Spine skin system OR three skeleton states);</li><li>exactly 4 attachment slots: <code>body</code>, <code>face</code>, <code>accessory_head</code>, <code>accessory_body</code>;</li><li>a <code>mood_blink</code> event firing every 2-6 seconds in <code>idle_*</code>.</li></ul>\n<p>§1.4  <strong>Slot taxonomy for cosmetics.</strong> Cosmetic outfits (FR-ECON-002 / FR-ECON-003) MUST swap attachments only on the named <code>accessory_*</code> slots — never on <code>body</code> or <code>face</code> (which would break species identity). The Spine \"Skin\" mechanism MUST be used so that swapping is deterministic and bandwidth-cheap (no full-skeleton reload).</p>\n<p>§1.5  <strong>Atlas + texture limits.</strong> Each species' atlas MUST be: (a) 2048×2048 max single page; (b) PNG-8 if no semi-transparency required, otherwise PNG-32; (c) compressed deterministically with <code>oxipng -o4</code>; (d) ≤ 1.0 MB on disk per species after compression. The <code>compress-textures.mjs</code> script MUST run as a pre-bundle step.</p>\n<p>§1.6  <strong>Bundle budget per species.</strong> Per FR-INFRA-001 §1.5, each pet species' Cocos asset bundle (skeleton JSON + atlas PNG + animation track binary) MUST be ≤ <strong>1.5 MB compressed (br)</strong> per the budget table. Verified by <code>apps/cocos/extensions/asset-budget-per-pet/main.ts</code> Editor extension + the existing <code>assert-bundle-budget.mjs</code> CI check.</p>\n<p>§1.7  <strong>Spine contract linter.</strong> A Cocos Editor extension at <code>apps/cocos/extensions/spine-contract-lint/main.ts</code> MUST: (a) load each Spine JSON; (b) assert all 20 animation names present; (c) assert root bone + skin presets + slot names match §1.3/§1.4; (d) flag any animation longer than 5 seconds (idle exception — capped at 8 s); (e) display a red banner in the Editor on violation. A CLI mirror lives at <code>scripts/art/validate-spine-contract.mjs</code> for CI.</p>\n<p>§1.8  <strong>Cocos <code>SpineLoader.ts</code> wrapper.</strong> A typed wrapper MUST expose:</p>\n<pre><code>loadPetSkeleton(speciesId: string, opts: { tenantSlug?: string }): Promise&lt;sp.Skeleton&gt;\nplayContractAnimation(skel: sp.Skeleton, name: AnimationContractName, opts?: { loop?: boolean; mixDuration?: number }): void</code></pre>\n<p>The type <code>AnimationContractName</code> is the union of the 20 names in §1.2. Calling with a non-contract string is a TypeScript compile error.</p>\n<p>§1.9  <strong>Lottie for UI micro-animation.</strong> UI micro-animations (button-press, level-up burst, currency-gain) MUST use Lottie via <code>lottie-cocos</code>. Lottie JSON files MUST live under <code>apps/cocos/assets/ui/lottie/</code> with a <code>_manifest.json</code> listing each animation's purpose + length. Per-Lottie size budget: ≤ 50 KB JSON + ≤ 200 KB embedded images (use SVG paths where possible).</p>\n<p>§1.10  <strong>Howler.js audio mixer.</strong> Audio playback MUST use Howler.js 2.2.x via the <code>AudioMixer.ts</code> wrapper. Wrapper API:</p>\n<pre><code>AudioMixer.playLoop(name: LoopName, opts?: { fadeInMs?: number }): HowlHandle\nAudioMixer.playOneShot(name: SfxName): void\nAudioMixer.setBus(bus: 'music' | 'sfx' | 'ui', volume: number): void\nAudioMixer.muteAll(): void</code></pre>\n<p>The kids SKU MUST default <code>muteAll()</code> ON at first launch per plan §PART 5 (Apple Kids guideline expectation). Standard SKU defaults music ON, sfx ON, ui ON at 70% / 90% / 80% respectively.</p>\n<p>§1.11  <strong>Audio asset budget.</strong> The launch hero pet (Mochi) ships with: <strong>8 music loops</strong> (3 idle / 2 hatch-celebrate / 1 sleep / 1 sad / 1 sick) + <strong>80 SFX</strong> (≤ 0.5 s each, AAC 96 kbps mono). Total music: ≤ 4 MB; total SFX: ≤ 2 MB. Loops MUST be normalised to -16 LUFS via ffmpeg.</p>\n<p>§1.12  <strong>Sound credit + licensing.</strong> Every audio asset MUST have a license entry in <code>docs/art/audio-license-ledger.md</code> covering: source (composer name OR royalty-free site), license type (full-buyout / CC0 / CC-BY-NC / commissioned), attribution requirement. CC-BY-NC and non-royalty-free assets are forbidden for production. License audit runs in <code>pnpm legal:check</code>.</p>\n<p>§1.13  <strong>HapticsAdapter.</strong> A <code>HapticsAdapter.ts</code> wrapper MUST expose:</p>\n<pre><code>HapticsAdapter.tap(intensity: 'light' | 'medium' | 'heavy'): void\nHapticsAdapter.success(): void\nHapticsAdapter.warning(): void\nHapticsAdapter.error(): void</code></pre>\n<p>On iOS, MUST use CoreHaptics <code>CHHapticEngine</code>; on Android API 26+, MUST use <code>VibrationEffect</code>; on web + older Android, MUST be a no-op (graceful degrade). Haptics MUST respect the user's accessibility \"reduce motion\" preference (per FR-A11Y-001) and the OS-level haptics-off setting.</p>\n<p>§1.14  <strong>Mute-on-default for kids SKU.</strong> Per plan §PART 5 + Apple Kids Category expectation, the kids SKU MUST launch with audio muted; the player MUST tap an explicit \"enable sound\" button before any audio plays. Standard SKU launches with audio ON.</p>\n<p>§1.15  <strong>Spine authoring guide.</strong> A guide at <code>docs/art/spine-authoring-guide.md</code> MUST cover: contract animations checklist; bone-naming conventions; mesh-deformation budget (≤ 200 vertices per skeleton); texture-atlas packing recipe (1-pixel padding); export settings (<code>Cocos Creator preset</code> ticked in Spine export dialog); per-animation FPS (24 fps default).</p>\n<p>§1.16  <strong>Per-pet bundle build script.</strong> <code>scripts/art/build-pet-bundle.mjs</code> MUST: (a) accept <code>--species=&lt;id&gt;</code>; (b) validate Spine contract; (c) compress textures; (d) emit <code>apps/cocos/build/pets/&lt;species&gt;/</code> with skeleton.json + atlas.png + meta.json (size summary); (e) run the asset-budget assert.</p>\n<p>§1.17  <strong>Per-pet tenant override.</strong> A per-tenant theme bundle (FR-B2B-001) MAY override colour palette / accessory presets via Spine's skin mechanism, but MUST NOT override the contract animation set. Tenant overrides live under <code>apps/cocos/assets/tenants/&lt;slug&gt;/pets/&lt;species&gt;/skin.json</code>. The tenant skin path is resolved by <code>SpineLoader.loadPetSkeleton</code> via the <code>tenantSlug</code> option (FR-INFRA-001 §1.6 loader API).</p>\n<p>§1.18  <strong>A11Y reduce-motion compatibility.</strong> When <code>prefers-reduced-motion</code> is set (per FR-A11Y-001), <code>playContractAnimation</code> MUST: (a) replace evolutionary morph cuts with a fade; (b) cap idle loop frequency to 1× per 5 s; (c) skip particle layers attached to the Spine skeleton. The reduce-motion path MUST NOT break the contract — every animation still runs, just gentler.</p>\n<p>§1.19  <strong>Hot-reload during development.</strong> The Cocos Editor MUST hot-reload Spine assets without a full project reload — supported natively by <code>@cocos/spine-runtime</code> but MUST be verified in the authoring-guide test rig.</p>\n<p>§1.20  <strong>Performance budget.</strong> On a baseline VN Android device (1 GB RAM, mid-tier 2024 SoC), playing 3 concurrent Spine skeletons in idle MUST sustain ≥ 50 fps with the Mochi launch skeleton. Verified by a Cocos perf test on a representative device emulator profile.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why Spine 2D over Live2D / frame-by-frame / DragonBones.</strong> Plan §PART 4 + §PART 5 — Spine ($69 essential, $299 pro one-time per artist) is the industry standard with the best Cocos integration. Live2D is heavier license (subscription) and over-tuned for VTuber / anime face — overkill for kawaii pets and 2x the asset cost per pet. Frame-by-frame is 3× the artist budget per plan §PART 5. DragonBones is technically compatible but has a smaller artist community + fewer Cocos integration examples; would silently widen the talent pool to find.</p>\n<p><strong>Why a 20-animation contract.</strong> Without a contract, each new pet species drifts in animation names + counts, which means every UI consumer (Spine player invocation in <code>FR-PET-002</code> evolution flow, <code>FR-CARE-001</code> feed action, <code>FR-AR-001</code> AR-greet, etc.) needs per-species switch cases. With a contract, the consumer code is generic and a new species only requires the Spine artist to fill the contract. The 20 names cover every gameplay-driven animation in P1-P3 — anything new requires an FR amendment so the change is intentional.</p>\n<p><strong>Why three rig presets via skin system.</strong> Spine's skin mechanism is dramatically cheaper at runtime than swapping skeletons. A pet that evolves baby → teen → adult doesn't reload its skeleton; it switches skin. Bundle size drops too (one skeleton instead of three).</p>\n<p><strong>Why slot taxonomy excludes <code>body</code> and <code>face</code>.</strong> Cosmetic swapping on <code>body</code> would let a player buy \"make this pet's species look like a different species\" — the moment that ships, the pet identity collapses and FR-PET-006 rarity tiers become meaningless. <code>accessory_head</code> + <code>accessory_body</code> give cosmetic surface without breaking identity.</p>\n<p><strong>Why 1.5 MB per-species bundle.</strong> Plan §PART 4 — total WebGL initial bundle is 15 MB. The core experience (one pet) sits inside ~3 MB (root engine + UI + Mochi). Adding species without inflating cold-start means cap-per-species at 1.5 MB. Atlas 1.0 MB + skeleton/track binary 0.5 MB = the budget.</p>\n<p><strong>Why deterministic texture compression.</strong> <code>oxipng -o4</code> is the standard reproducible PNG optimiser. Non-deterministic compressors (some node-canvas builds) break FR-INFRA-001 §1.13 reproducible builds.</p>\n<p><strong>Why a Cocos Editor extension AND a CLI for linting.</strong> Editor extension catches violations at design time (red banner is in the artist's face). CLI catches violations when an artist commits direct to git without opening the Editor. Both layers needed.</p>\n<p><strong>Why TypeScript union type for animation names.</strong> Forces compile-time enforcement of the contract in consumer code. A new pet species engineer cannot accidentally play <code>'happey'</code> (typo) — TypeScript errors immediately.</p>\n<p><strong>Why Lottie for UI but Spine for pets.</strong> Lottie is purpose-built for vector UI micro-animation and ships smaller files for short geometric animations. Spine is purpose-built for character rigging + mesh deformation. Using one tool for both is feasible but always optimises for the wrong case on one side.</p>\n<p><strong>Why Howler.js.</strong> Plan §PART 4 — cross-platform audio with consistent API. Cocos's native audio API has documented quirks across iOS Safari WebGL + Android WebView. Howler abstracts them.</p>\n<p><strong>Why mute-on-default for kids SKU.</strong> Apple Kids Category Guideline expectation: kids apps SHOULD launch silently so the device-mute setting is respected. Auto-playing audio in a kids app is a Review-rejection risk.</p>\n<p><strong>Why audio loops at -16 LUFS.</strong> Mobile content broadcast standard since 2019. Loops at louder LUFS clip on cheap VN Android speakers; quieter LUFS makes the game feel \"dead.\"</p>\n<p><strong>Why HapticsAdapter respects accessibility.</strong> Some users find haptics painful (e.g. arthritis); some Android haptics implementations are uncomfortably aggressive. Respecting the OS-level setting is the right user-respect default.</p>\n<p><strong>Why a tenant-skin override exists but cannot override contract animations.</strong> Per FR-B2B-001, tenants want a Pet-of-Techcombank look. Letting them swap the palette is a config; letting them remove animations would break consumer features (every consumer feature relies on the 20 animations existing).</p>\n<p><strong>Why 50 fps on a 1 GB Android baseline.</strong> Plan §PART 4 — VN baseline is 1 GB RAM Android. 50 fps with 3 concurrent skeletons (the owner pet + 1 visiting pet + a UI background pet) is the realistic stress case. Anything below feels stuttery.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Animation contract type</h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/art/AnimationContract.ts\nexport const ANIMATION_CONTRACT = [\n  'idle_baby', 'idle_teen', 'idle_adult',\n  'eat', 'sleep', 'happy', 'sad', 'sick', 'dance', 'wave',\n  'hatch', 'evolve_baby_to_teen', 'evolve_teen_to_adult',\n  'care_pet', 'care_clean', 'care_feed', 'care_drink',\n  'celebrate_co_parent', 'wedding_pose',\n  'ar_greet',\n] as const;\n\nexport type AnimationContractName = (typeof ANIMATION_CONTRACT)[number];\n\nexport const ATTACHMENT_SLOTS = ['body','face','accessory_head','accessory_body'] as const;\nexport type AttachmentSlot = (typeof ATTACHMENT_SLOTS)[number];\n\nexport const SKIN_PRESETS = ['baby','teen','adult'] as const;\nexport type SkinPreset = (typeof SKIN_PRESETS)[number];</code></pre>\n<h3>3.2 — SpineLoader</h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/art/SpineLoader.ts\nimport { sp } from 'cc';\nimport { loadBundle } from '../loaders/AssetBundleLoader';\nimport { AnimationContractName, ANIMATION_CONTRACT } from './AnimationContract';\n\nexport class SpineLoader {\n  async loadPetSkeleton(speciesId: string, opts: { tenantSlug?: string } = {}): Promise&lt;sp.SkeletonData&gt; {\n    const bundle = await loadBundle(`pets/${speciesId}`, {\n      cdnPrefix: process.env.CDN_PREFIX!,\n      tenantSlug: opts.tenantSlug,\n    });\n    const data = await new Promise&lt;sp.SkeletonData&gt;((res, rej) =&gt; {\n      bundle.load(`${speciesId}/skeleton`, sp.SkeletonData, (err, d) =&gt; err ? rej(err) : res(d));\n    });\n    this.assertContract(data);\n    return data;\n  }\n\n  private assertContract(data: sp.SkeletonData): void {\n    const present = new Set(data.animations.map(a =&gt; a.name));\n    for (const required of ANIMATION_CONTRACT) {\n      if (!present.has(required)) {\n        throw new Error(`FR-ART-001 §1.2 — animation '${required}' missing from skeleton`);\n      }\n    }\n  }\n\n  playContractAnimation(\n    skel: sp.Skeleton,\n    name: AnimationContractName,\n    opts: { loop?: boolean; mixDuration?: number } = {},\n  ): void {\n    skel.setAnimation(0, name, opts.loop ?? false);\n    if (opts.mixDuration != null) skel.setMix(name, name, opts.mixDuration);\n  }\n}</code></pre>\n<h3>3.3 — Spine contract linter (CLI)</h3>\n<pre><code class=\"language-javascript\">// scripts/art/validate-spine-contract.mjs\nimport { readFileSync } from 'node:fs';\n\nconst CONTRACT = [\n  'idle_baby','idle_teen','idle_adult','eat','sleep','happy','sad','sick','dance','wave',\n  'hatch','evolve_baby_to_teen','evolve_teen_to_adult','care_pet','care_clean','care_feed','care_drink',\n  'celebrate_co_parent','wedding_pose','ar_greet',\n];\nconst SLOTS = ['body','face','accessory_head','accessory_body'];\nconst SKINS = ['baby','teen','adult'];\n\nconst path = process.argv[2];\nif (!path) { console.error('usage: validate-spine-contract.mjs &lt;skeleton.json&gt;'); process.exit(2); }\n\nconst sk = JSON.parse(readFileSync(path, 'utf8'));\nconst errors = [];\n\nconst animationNames = Object.keys(sk.animations ?? {});\nfor (const required of CONTRACT) if (!animationNames.includes(required)) errors.push(`missing animation: ${required}`);\nif (animationNames.length !== CONTRACT.length) errors.push(`expected exactly ${CONTRACT.length} animations, found ${animationNames.length}`);\n\nconst slotNames = new Set((sk.slots ?? []).map(s =&gt; s.name));\nfor (const slot of SLOTS) if (!slotNames.has(slot)) errors.push(`missing slot: ${slot}`);\n\nconst skinNames = new Set((sk.skins ?? []).map(s =&gt; s.name));\nfor (const skin of SKINS) if (!skinNames.has(skin)) errors.push(`missing skin preset: ${skin}`);\n\nfor (const [name, anim] of Object.entries(sk.animations ?? {})) {\n  const maxDuration = name.startsWith('idle_') ? 8 : 5;\n  const duration = computeAnimDuration(anim);\n  if (duration &gt; maxDuration) errors.push(`animation '${name}' duration ${duration.toFixed(2)}s &gt; ${maxDuration}s`);\n}\n\nif (errors.length) { console.error('FR-ART-001 contract failures:\\n  ' + errors.join('\\n  ')); process.exit(1); }\nconsole.log(`OK: ${path}`);\n\nfunction computeAnimDuration(anim) {\n  let max = 0;\n  for (const tracks of Object.values(anim ?? {})) for (const t of Object.values(tracks ?? {})) {\n    if (Array.isArray(t)) for (const kf of t) if (kf?.time &gt; max) max = kf.time;\n  }\n  return max;\n}</code></pre>\n<h3>3.4 — AudioMixer</h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/art/AudioMixer.ts\nimport { Howl } from 'howler';\nimport { isKidsSku } from '../legal/build-target';\n\ntype Bus = 'music' | 'sfx' | 'ui';\n\nexport class AudioMixer {\n  private static busVolume: Record&lt;Bus, number&gt; =\n    isKidsSku() ? { music: 0, sfx: 0, ui: 0 } : { music: 0.7, sfx: 0.9, ui: 0.8 };\n  private static handles = new Map&lt;string, Howl&gt;();\n\n  static playLoop(name: string, opts: { fadeInMs?: number } = {}): Howl {\n    const h = new Howl({ src: [`audio/loops/${name}.m4a`], loop: true, volume: 0 });\n    this.handles.set(name, h);\n    h.play();\n    h.fade(0, this.busVolume.music, opts.fadeInMs ?? 500);\n    return h;\n  }\n\n  static playOneShot(name: string): void {\n    const h = new Howl({ src: [`audio/sfx/${name}.m4a`], volume: this.busVolume.sfx });\n    h.play();\n  }\n\n  static setBus(bus: Bus, volume: number): void {\n    this.busVolume[bus] = Math.max(0, Math.min(1, volume));\n  }\n\n  static muteAll(): void { for (const h of this.handles.values()) h.mute(true); }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> <code>pnpm art:lint</code> (running <code>validate-spine-contract.mjs</code> over every <code>apps/cocos/assets/pets/*/skeleton.json</code>) exits 0 on the launch hero (Mochi) skeleton AND exits non-zero on a fixture skeleton missing <code>wedding_pose</code>. Verified by CI step + <code>__tests__/AnimationContract.spec.ts</code> fixture set.</p>\n<p><strong>AC2.</strong> TypeScript compile fails when consumer code calls <code>playContractAnimation(skel, 'happey')</code>. Verified by <code>__tests__/AnimationContract.spec.ts</code> with <code>@ts-expect-error</code> lines.</p>\n<p><strong>AC3.</strong> Loading the Mochi pet skeleton via <code>SpineLoader.loadPetSkeleton('mochi')</code> resolves with all 20 animations present. Verified by <code>__tests__/SpineLoader.spec.ts</code>.</p>\n<p><strong>AC4.</strong> A skeleton missing any contract animation throws an error at load time. Verified by spec test with a stripped fixture.</p>\n<p><strong>AC5.</strong> Per-pet bundle (Mochi) is ≤ 1.5 MB brotli-compressed. Verified by <code>assert-bundle-budget.mjs</code> running on the Mochi build artefact.</p>\n<p><strong>AC6.</strong> Audio loop normalisation: every committed <code>.m4a</code> loop measures -16 LUFS ±1 LU. Verified by <code>pnpm art:audit-audio</code> running <code>ffmpeg-normalize</code> in dry-run mode.</p>\n<p><strong>AC7.</strong> Kids SKU launches with bus volumes at 0 (muted). Verified by <code>__tests__/AudioMixer.spec.ts</code> with <code>BUILD_TARGET=kids</code>.</p>\n<p><strong>AC8.</strong> Standard SKU launches with music/sfx/ui at 70/90/80%. Verified similarly.</p>\n<p><strong>AC9.</strong> Lottie animations under <code>apps/cocos/assets/ui/lottie/</code> are each ≤ 50 KB JSON. Verified by a <code>pnpm art:check-lottie-budgets</code> script.</p>\n<p><strong>AC10.</strong> Tenant skin override path resolves correctly via <code>SpineLoader.loadPetSkeleton('mochi', { tenantSlug: 'techcombank' })</code>. Verified by <code>__tests__/SpineLoader.spec.ts</code> against a mocked CDN.</p>\n<p><strong>AC11.</strong> Performance test on a 1 GB Android emulator profile sustains ≥ 50 fps with 3 concurrent Mochi skeletons in idle. Verified by <code>apps/cocos/__tests__/perf-spine.spec.ts</code> running in Cocos Test Player.</p>\n<p><strong>AC12.</strong> Cocos Editor extension shows a red banner when opening a project with a non-contract-conforming Spine asset. Verified manually + by a Playwright-against-Editor smoke test.</p>\n<p><strong>AC13.</strong> Reduce-motion mode is respected — when <code>prefers-reduced-motion: reduce</code> is set (per FR-A11Y-001), <code>playContractAnimation</code> skips particle layers + falls back to fade for evolution morphs. Verified by <code>__tests__/AnimationContract.spec.ts</code>.</p>\n<p><strong>AC14.</strong> Audio license ledger covers every committed asset. Verified by <code>pnpm legal:check</code> which diffs the ledger against <code>apps/cocos/assets/audio/**/*.m4a</code>.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 — Animation contract test</h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/art/__tests__/AnimationContract.spec.ts\nimport { describe, it, expect } from 'vitest';\nimport { ANIMATION_CONTRACT, AnimationContractName } from '../AnimationContract';\nimport { execSync } from 'node:child_process';\nimport { writeFileSync, mkdtempSync } from 'node:fs';\nimport { join } from 'node:path';\nimport { tmpdir } from 'node:os';\n\ndescribe('FR-ART-001 — animation contract', () =&gt; {\n  it('contract has exactly 20 names', () =&gt; {\n    expect(ANIMATION_CONTRACT.length).toBe(20);\n    expect(new Set(ANIMATION_CONTRACT).size).toBe(20);   // no dups\n  });\n\n  it('compile-time rejects unknown animation names', () =&gt; {\n    function play(_name: AnimationContractName) { /* noop */ }\n    play('eat');                                          // ok\n    // @ts-expect-error - 'happey' is not in contract\n    play('happey');\n  });\n\n  it('CLI validator rejects skeleton missing wedding_pose', () =&gt; {\n    const tmp = mkdtempSync(join(tmpdir(), 'spine-'));\n    const stripped = makeFixtureSkeleton({ omit: ['wedding_pose'] });\n    const path = join(tmp, 'bad.json');\n    writeFileSync(path, JSON.stringify(stripped));\n    expect(() =&gt; execSync(`node scripts/art/validate-spine-contract.mjs ${path}`, { stdio: 'pipe' }))\n      .toThrow(/missing animation: wedding_pose/);\n  });\n});\n\nfunction makeFixtureSkeleton(opts: { omit?: string[] } = {}) {\n  const omit = new Set(opts.omit ?? []);\n  return {\n    skins:[{ name:'baby' },{ name:'teen' },{ name:'adult' }],\n    slots:[{ name:'body' },{ name:'face' },{ name:'accessory_head' },{ name:'accessory_body' }],\n    animations: Object.fromEntries(ANIMATION_CONTRACT.filter(n =&gt; !omit.has(n)).map(n =&gt; [n, {}])),\n  };\n}</code></pre>\n<h3>5.2 — AudioMixer kids-default test</h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/art/__tests__/AudioMixer.spec.ts\nimport { describe, it, expect, vi } from 'vitest';\n\ndescribe('FR-ART-001 §1.10/§1.14 — AudioMixer defaults', () =&gt; {\n  it('kids SKU launches muted', async () =&gt; {\n    (globalThis as any).__BUILD_TARGET__ = 'kids';\n    vi.resetModules();\n    const { AudioMixer } = await import('../AudioMixer');\n    expect((AudioMixer as any).busVolume.music).toBe(0);\n    expect((AudioMixer as any).busVolume.sfx).toBe(0);\n    expect((AudioMixer as any).busVolume.ui).toBe(0);\n  });\n\n  it('standard SKU launches with 70/90/80', async () =&gt; {\n    (globalThis as any).__BUILD_TARGET__ = 'standard';\n    vi.resetModules();\n    const { AudioMixer } = await import('../AudioMixer');\n    expect((AudioMixer as any).busVolume.music).toBeCloseTo(0.7);\n    expect((AudioMixer as any).busVolume.sfx).toBeCloseTo(0.9);\n    expect((AudioMixer as any).busVolume.ui).toBeCloseTo(0.8);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/art/HapticsAdapter.ts\nimport { sys } from 'cc';\nimport { prefersReducedMotion } from '../a11y/preferences';\n\ndeclare const window: any;\n\nexport class HapticsAdapter {\n  static tap(intensity: 'light' | 'medium' | 'heavy'): void {\n    if (prefersReducedMotion()) return;\n    if (sys.os === sys.OS.IOS &amp;&amp; (window as any).__iosHaptics?.tap) {\n      (window as any).__iosHaptics.tap(intensity);\n    } else if (sys.os === sys.OS.ANDROID &amp;&amp; (window as any).__androidVibrate) {\n      const ms = intensity === 'light' ? 10 : intensity === 'medium' ? 25 : 60;\n      (window as any).__androidVibrate(ms);\n    }\n    // web + older Android: no-op\n  }\n\n  static success(): void { this.tap('light'); this.tap('light'); }\n  static warning(): void { this.tap('medium'); }\n  static error():   void { this.tap('heavy'); this.tap('heavy'); }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Spine 2D editor license (per artist); <code>@cocos/spine-runtime</code> matching Spine 4.2; <code>lottie-cocos</code> plugin; Howler.js 2.2.x npm package; CoreHaptics (iOS SDK); VibrationEffect (Android SDK 26+); oxipng + ffmpeg for build pipeline; commissioned composer for 8 loops + 80 SFX (~$3-5K per plan §PART 5).</p>\n<p><strong>Internal:</strong> FR-INFRA-001 (Cocos scaffold + asset-bundle loader + bundle-budget script).</p>\n<p><strong>Blocks:</strong> FR-PET-001 (pet entity needs the skeleton runtime), FR-PET-002 (evolution stages use the skin presets), FR-PET-006 (5 launch species all conform to contract), FR-CARE-001/002/003 (each care action triggers a contract animation), FR-AR-001 (AR-greet animation), FR-VIRAL-001 (TikTok export captures contract animations), FR-ECON-003 (UGC Pet Couture uses accessory slots).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — Spine contract linter PASS</h3>\n<pre><code class=\"language-text\">$ pnpm art:lint\nOK: apps/cocos/assets/pets/mochi/skeleton.json\nOK: apps/cocos/assets/pets/pengu/skeleton.json</code></pre>\n<h3>8.2 — Spine contract linter FAIL</h3>\n<pre><code class=\"language-text\">$ pnpm art:lint\nFR-ART-001 contract failures:\n  missing animation: wedding_pose\n  animation 'idle_baby' duration 9.20s &gt; 8s\napps/cocos/assets/pets/bao/skeleton.json\nexit 1</code></pre>\n<h3>8.3 — Bundle-budget output for Mochi species</h3>\n<pre><code class=\"language-text\">$ node scripts/art/build-pet-bundle.mjs --species=mochi\n[1/4] validating spine contract ... OK\n[2/4] compressing textures via oxipng -o4 ... 1.0 MB → 786 KB\n[3/4] building bundle ... apps/cocos/build/pets/mochi/ (1.18 MB br)\n[4/4] asserting per-species budget (1.5 MB) ... PASS</code></pre>\n<h3>8.4 — Audio license ledger entry</h3>\n<pre><code class=\"language-yaml\">- file: audio/loops/idle-baby.m4a\n  source: composer \"Hoa Tran (commissioned)\"\n  license: full-buyout\n  contract: docs/legal/composer-engagement-letter.pdf\n  added: 2026-05-17</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Spine Essential vs Pro? → §<code>allowed_tools</code> — Essential ($69) sufficient for launch; Pro ($299) only if mesh skinning becomes needed for premium pets (deferred FR).</li><li><strong>OQ-2 (resolved):</strong> Live2D for the hero pet? → §<code>disallowed_tools</code> + §2 — Live2D permitted only for premium-species pets in a future FR; launch hero uses Spine.</li><li><strong>OQ-3 (resolved):</strong> Contract size — 20 vs 25 animations? → §1.2 + §2 — 20 covers every P1-P3 gameplay-driven animation; new ones require FR amendment.</li><li><strong>OQ-4 (resolved):</strong> Audio format — AAC vs MP3 vs Opus? → §<code>disallowed_tools</code> — AAC primary (iOS Safari WebGL friendly); MP3 fallback. Opus deferred.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>New species ships missing a contract animation</td><td><code>pnpm art:lint</code> CI step</td><td>PR blocked</td><td>Artist fills the missing animation; re-PR</td></tr><tr><td>2</td><td>Texture atlas exceeds 1 MB after compression</td><td><code>assert-bundle-budget</code> fails</td><td>Build blocked</td><td>Artist re-packs or drops mesh detail; or split into 2 atlas pages</td></tr><tr><td>3</td><td>A consumer ships <code>playContractAnimation('typo')</code></td><td>TypeScript compile error</td><td>PR blocked</td><td>Use canonical contract name</td></tr><tr><td>4</td><td>Spine 4.2 → 4.3 major upgrade breaks runtime</td><td>Cocos plugin regression</td><td>Pet rendering breaks</td><td>Pin Spine + runtime; upgrade FR with regression pass</td></tr><tr><td>5</td><td>Lottie file inflates with embedded raster art</td><td><code>art:check-lottie-budgets</code> fails</td><td>Build blocked</td><td>Vector-only refactor; or move to Spine</td></tr><tr><td>6</td><td>Audio loop measured at -10 LUFS (too loud)</td><td><code>art:audit-audio</code> fails</td><td>Build blocked</td><td>Re-normalise; re-commit</td></tr><tr><td>7</td><td>Composer asset uploaded with CC-BY-NC license</td><td><code>pnpm legal:check</code> rejects</td><td>Build blocked</td><td>Either commission full-buyout or replace asset</td></tr><tr><td>8</td><td>iOS CoreHaptics permission denied</td><td>Runtime detection</td><td>Haptic no-op</td><td>Document; user can re-enable in Settings</td></tr><tr><td>9</td><td>Tenant skin override breaks contract (e.g. removes a slot)</td><td>Linter on tenant manifest</td><td>Tenant theme rejected</td><td>Tenant uploads compliant override; re-deploy</td></tr><tr><td>10</td><td>Performance test fails on 1 GB Android emulator</td><td><code>__tests__/perf-spine.spec.ts</code> fails</td><td>PR blocked</td><td>Optimise skeleton bone count; reduce mesh deformation; or relax target with explicit FR amendment</td></tr><tr><td>11</td><td>Spine artist commits with non-default Cocos export preset</td><td>Linter checks JSON shape; emits warning</td><td>Asset may load but suboptimal</td><td>Artist re-exports with correct preset</td></tr><tr><td>12</td><td>Reduce-motion toggle off-by-one (animation skipped when not needed)</td><td><code>__tests__/AnimationContract.spec.ts</code> covers branch</td><td>UX regression for non-A11Y users</td><td>Fix conditional; redeploy</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 5 (art direction &amp; UX) — Spine 2D, 20 base animations per pet, Lottie UI, Howler.js audio, ~$3-5K composer budget, mute-on default for kids per Apple Kids guideline.</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Spine Pro vs Essential per artist — Essential default; Pro purchase requires a budget memo.</li><li>Composer engagement letter template — locked in <code>docs/legal/composer-engagement-letter-template.pdf</code>.</li><li>Final 8 loops + 80 SFX music briefs — owned by art lead post-P0.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Frame-by-frame animation for pets.</li><li>Cosmetic slot overlap on <code>body</code> / <code>face</code>.</li><li>Live2D on the launch hero (cost + license risk).</li><li>Per-species custom animation names outside the contract.</li><li>Audio assets without a license ledger entry.</li></ul>\n<p><strong>Cross-reference:</strong> This FR is the substrate for every visual gameplay FR in P1. Without it, FR-PET-001 cannot load a renderable pet; FR-CARE-001 has nothing to animate; FR-VIRAL-001 has no export-able motion. FR-B2B-001 (tenant themes) extends this without breaking the contract.</p>";
