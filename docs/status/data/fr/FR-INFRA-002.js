window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-INFRA-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Colyseus pin.</strong> The real-time server MUST use <strong>Colyseus 0.16.x</strong> (locked to a specific patch in <code>apps/realtime/package.json</code>). Colyseus is chosen over raw Socket.IO + Redis because its schema-based delta sync + room lifecycle + presence integration significantly reduce boilerplate and supply built-in anti-cheat seams.</p>\n<p>§1.2  <strong>Node + TypeScript pin.</strong> Runtime MUST be <strong>Node 22 LTS</strong> with <strong>TypeScript 5.4.x</strong> matching <code>apps/cocos/</code>. <code>strict: true</code> and <code>noUncheckedIndexedAccess: true</code> apply identically to this app.</p>\n<p>§1.3  <strong>Deployment target.</strong> Production MUST deploy to <strong>AWS Singapore (ap-southeast-1)</strong> behind an <strong>Application Load Balancer with sticky sessions</strong> (target group cookie + <code>lb_cookie</code> stickiness). Deployment runtime SHOULD be <strong>ECS Fargate</strong> (managed, ARM Graviton) for P0–P2 scale; the Terraform module MUST expose a switch to EC2 ARM Graviton autoscaling group at P3+ once concurrent rooms exceed Fargate's per-task socket limits.</p>\n<p>§1.4  <strong>Presence in Redis.</strong> Player presence (online / offline / which room) MUST live in <strong>Upstash global Redis 7.x</strong> (free tier sufficient through P1). Colyseus's built-in <code>RedisPresence</code> adapter MUST be wired, not a homegrown adapter.</p>\n<p>§1.5  <strong>JWT verification on every join.</strong> Every Colyseus <code>onAuth</code> hook MUST verify a Supabase-issued JWT against the project's JWKS URL (<code>https://&lt;supabase-project&gt;.supabase.co/auth/v1/.well-known/jwks.json</code>). Tokens MUST be verified for: signature, expiry, audience (<code>authenticated</code>), and <code>sub</code> claim shape. Unauthenticated joins MUST be rejected with <code>4001 unauthenticated</code>.</p>\n<p>§1.6  <strong>Tenant-id partition.</strong> Every room handler MUST extract <code>tenant_id</code> from the JWT claims (FR-B2B-002 will populate this in P4; consumer tenant <code>mochi</code> is the default at P0). The room MUST refuse a join whose <code>tenant_id</code> does not match the room's tenant. A cross-tenant join attempt MUST emit a <code>security.cross-tenant.join.refused</code> Sentry event.</p>\n<p>§1.7  <strong>Signed messages.</strong> Every client→server message MUST carry an HMAC signature over <code>{message_id, room_id, message_body, session_seq}</code> using a session secret negotiated at join time. The <code>messageSigner.ts</code> module MUST: (a) generate session secret at <code>onAuth</code> (32 random bytes); (b) verify each incoming message's HMAC before dispatching; (c) reject invalid signatures with <code>4002 signature_invalid</code> and record to <code>security.signature.invalid</code> metric.</p>\n<p>§1.8  <strong>Rate limiting.</strong> Per-account + per-IP rate limits MUST be enforced server-side. Defaults: 60 messages/minute per account; 600 messages/minute per IP; burst of 10 in 1 second. Per-message-type granular limits MUST be supported via a <code>limits.yaml</code> config. Exceeding limits MUST emit <code>security.rate-limit.exceeded</code> event and disconnect with <code>4003 rate_limit_exceeded</code>.</p>\n<p>§1.9  <strong>Impossible-state-transition ban.</strong> When a message would transition state in a way that's impossible given current state (e.g. feeding a pet that's already at 100% hunger; trading with a non-friend; spawning a 6th pet on a 3-slot free tier), the room MUST: (a) refuse the transition; (b) emit <code>security.impossible-transition</code> Sentry event with the player_id + attempted transition; (c) flag the account for review; (d) after 3 such events in 24h, auto-ban the account pending manual review.</p>\n<p>§1.10  <strong>Server-authoritative care actions.</strong> Every state-affecting action (feed, clean, hug, mini-game payout, breed, trade-confirm, wedding-vow, daily-drama-roll) MUST be executed server-side. The client MUST only render server-broadcast state. The client MAY perform predictive rendering for ≤ 100ms before server reconciliation but MUST roll back visual state to server truth on conflict.</p>\n<p>§1.11  <strong>Schema-based state sync.</strong> All synced state MUST use Colyseus's <code>@colyseus/schema</code> decorators. <code>PetState</code>, <code>PlayerState</code>, <code>RoomState</code>, and tenant-extension shapes MUST live under <code>apps/realtime/src/state/</code> and be re-exportable to the Cocos client via a published <code>@cyberskill/tamagochi-shared-state</code> package.</p>\n<p>§1.12  <strong>Room lifecycle.</strong> The base room class <code>_BaseTenantRoom.ts</code> MUST: (a) validate JWT in <code>onAuth</code>; (b) enforce tenant partition in <code>onJoin</code>; (c) start an idle disposal timer (15 min of zero clients → dispose); (d) on <code>onDispose</code>, flush any unsaved state to Supabase Postgres; (e) wire Sentry breadcrumbs on every lifecycle transition.</p>\n<p>§1.13  <strong>Health probe.</strong> A <code>GET /healthz</code> endpoint MUST return 200 with payload <code>{ ok: true, rooms_active: &lt;int&gt;, presence_connected: &lt;bool&gt;, ts: &lt;ISO&gt; }</code>. ALB health checks point at this. A <code>GET /readyz</code> MUST additionally verify Redis presence + JWKS reachability.</p>\n<p>§1.14  <strong>Graceful shutdown.</strong> On <code>SIGTERM</code>, the server MUST: (a) stop accepting new joins; (b) broadcast a <code>server.shutdown.imminent</code> message to all rooms with <code>expected_seconds: 30</code>; (c) wait up to 30 s for rooms to dispose naturally; (d) force-dispose remaining rooms; (e) flush state. ECS / Kubernetes drain hooks MUST be configured to allow 60 s total.</p>\n<p>§1.15  <strong>Per-tenant rate-limit budget.</strong> Per FR-B2B-002, each tenant MUST have its own rate-limit budget; consumer tenant <code>mochi</code> budget separate from any B2B tenant. Budgets configurable via <code>limits.yaml</code> at deploy time.</p>\n<p>§1.16  <strong>Observability.</strong> Sentry Node SDK + OpenTelemetry / Prometheus metrics MUST emit: room count, concurrent players, message-per-second rate, p95 message-handle latency, error count, JWT-rejection count, rate-limit-exceed count, impossible-transition count.</p>\n<p>§1.17  <strong>No <code>Math.random()</code> for state outcomes.</strong> Per FR-LEGAL-002, all randomised state-affecting outcomes MUST use <code>crypto.randomBytes()</code> seeded calls routed through the <code>DropRateService</code> (FR-LEGAL-002 §6 implementation). A custom ESLint rule MUST flag <code>Math.random()</code> in <code>apps/realtime/src/rooms/</code> + <code>apps/realtime/src/state/</code>.</p>\n<p>§1.18  <strong>Reproducible Docker image.</strong> <code>apps/realtime/Dockerfile</code> MUST produce a reproducible image: <code>npm ci --omit=dev</code> with a frozen lockfile; deterministic copy order; no wall-clock metadata leaking; multi-stage build for slim final image. CI MUST tag images by both <code>git rev</code> and a content hash, and the two MUST be stable across rebuilds.</p>\n<p>§1.19  <strong>Terraform infra-as-code.</strong> All AWS resources (ECS service, ALB, target group, security groups, Redis allowlist) MUST be declared in <code>infra/terraform/realtime/main.tf</code>. Manual console edits are forbidden post-launch — drift is detected via <code>terraform plan</code> in CI weekly.</p>\n<p>§1.20  <strong>Local development parity.</strong> A <code>docker compose up</code> recipe at <code>apps/realtime/docker-compose.yml</code> MUST start the realtime server + a local Redis + a JWKS mock, so developers can run end-to-end without AWS.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why Colyseus over Nakama / PlayFab / raw Socket.IO.</strong> Plan §PART 4 lays out the case: Colyseus shares the language (TypeScript) with the Cocos client and the rest of the stack; it has a focused room-based model that maps directly to pet-care sessions, co-parent rooms, and trade windows; its schema-based delta sync is more bandwidth-efficient than custom JSON shapes; and migration to Nakama is documented as the P3+ option if needed. Nakama is overkill for P0 (it ships chat, matchmaking, leaderboards we don't need yet). PlayFab is Microsoft-stack lock-in we don't want.</p>\n<p><strong>Why AWS Singapore (ap-southeast-1).</strong> Latency to VN / PH / ID is sub-30ms; latency to Tokyo / Seoul is sub-80ms; latency to US West is ~180ms (acceptable for soft launch). Singapore is also where the Supabase US region is colocated for the backend.</p>\n<p><strong>Why ECS Fargate at P0.</strong> Fargate eliminates ops overhead for a 6-8 person team. The per-task socket limit (~1000 sockets per task) is fine at P0/P1 scale. The switch to EC2 ARM Graviton ASG at P3+ is documented but not required at P0.</p>\n<p><strong>Why JWT verification against Supabase JWKS rather than a shared secret.</strong> A shared secret means the realtime server stores the Supabase service-role key, which is over-broad and a compromise vector. JWKS verification is read-only and decouples key rotation from this codebase.</p>\n<p><strong>Why HMAC-signed messages.</strong> A pure JWT-at-join model is vulnerable to mid-session message tampering (e.g. a client modifies its outgoing JSON before the WS frame). The HMAC per-message check tightens this surface and is cheap (HMAC-SHA256 of a small payload is sub-microsecond).</p>\n<p><strong>Why rate-limit + impossible-state-transition + signature checks as three layers.</strong> Anti-cheat is defence-in-depth: the rate limiter catches volume attacks; the signature check catches tampering; the impossible-transition check catches logic-level cheats. A breach of any single layer is contained.</p>\n<p><strong>Why disposal at 15 min idle.</strong> Fargate task-per-room consumes resources even with zero clients. 15 min idle balances \"don't churn rooms when a player closes their app for 5 min\" against \"don't pay for ghost rooms.\"</p>\n<p><strong>Why Sentry breadcrumbs on every lifecycle.</strong> Debugging room state divergence post-incident requires the full lifecycle trace. Breadcrumbs are cheap (&lt;1 KB per room) and pay off enormously on the first incident.</p>\n<p><strong>Why <code>crypto.randomBytes()</code> not <code>Math.random()</code>.</strong> <code>Math.random()</code> is not cryptographically random and on Node 16+ the PRNG seed is shared across worker threads. For randomised state outcomes that affect monetised inventory (per FR-LEGAL-002 audit trail), <code>crypto.randomBytes()</code> is the only acceptable source.</p>\n<p><strong>Why a published shared-state package.</strong> Sharing <code>PetState</code> etc. between the Cocos client and the server through file-system imports breaks Cocos's bundler. A published npm package (<code>@cyberskill/tamagochi-shared-state</code>) gives both clients a clean version-pinned dependency.</p>\n<p><strong>Why Terraform for IaC.</strong> AWS console edits drift. Terraform produces a <code>plan</code> diff that's reviewable in a PR. Weekly drift detection catches manual interventions.</p>\n<p><strong>Why graceful shutdown windows.</strong> ECS / K8s send SIGTERM with a 30-second drain by default. Rooms need to flush state to Supabase before disposal, which can take 5–15 seconds on a Postgres hiccup. The 30 s broadcast + 30 s shutdown gives players a visible warning (\"game saving — back in a moment\") and avoids lost progress.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Server entrypoint</h3>\n<pre><code class=\"language-typescript\">// apps/realtime/src/index.ts\nimport { Server } from 'colyseus';\nimport { createServer } from 'node:http';\nimport express from 'express';\nimport { RedisPresence } from '@colyseus/redis-presence';\nimport { PetRoom } from './rooms/PetRoom';\nimport { initSentry } from './obs/sentry';\nimport { livenessRouter } from './health/liveness';\n\ninitSentry(process.env.SENTRY_DSN!);\n\nconst app = express();\napp.use(livenessRouter);\n\nconst server = new Server({\n  server: createServer(app),\n  presence: new RedisPresence({ url: process.env.REDIS_URL! }),\n});\nserver.define('pet-room', PetRoom);\n\nserver.listen(parseInt(process.env.PORT ?? '2567', 10));</code></pre>\n<h3>3.2 — Base tenant room</h3>\n<pre><code class=\"language-typescript\">// apps/realtime/src/rooms/_BaseTenantRoom.ts\nimport { Room, Client } from 'colyseus';\nimport { verifyJwt } from '../auth/jwtVerifier';\nimport { newSessionSecret, verifyHmac } from '../anticheat/messageSigner';\nimport { withinLimit } from '../anticheat/rateLimiter';\n\nexport abstract class _BaseTenantRoom&lt;State extends object = any&gt; extends Room&lt;State&gt; {\n  protected tenantId!: string;\n  protected sessionSecrets = new Map&lt;string, Buffer&gt;();\n\n  override async onAuth(_client: Client, options: { token?: string }) {\n    const payload = await verifyJwt(options.token);\n    if (!payload) throw new Error('4001 unauthenticated');\n    return payload;\n  }\n\n  override async onJoin(client: Client, _options: unknown, payload: { sub: string; tenant_id?: string }) {\n    const tenant = payload.tenant_id ?? 'mochi';\n    if (this.tenantId &amp;&amp; tenant !== this.tenantId) {\n      throw new Error('4004 cross_tenant');\n    }\n    this.tenantId = tenant;\n    this.sessionSecrets.set(client.sessionId, newSessionSecret());\n  }\n\n  override async onMessage&lt;T&gt;(client: Client, type: string, msg: T) {\n    if (!withinLimit({ accountId: (client as any).userId, ip: (client as any).address })) {\n      throw new Error('4003 rate_limit_exceeded');\n    }\n    if (!verifyHmac(msg, this.sessionSecrets.get(client.sessionId)!)) {\n      throw new Error('4002 signature_invalid');\n    }\n    return super.onMessage(client, type, msg);\n  }\n}</code></pre>\n<h3>3.3 — PetState schema</h3>\n<pre><code class=\"language-typescript\">// apps/realtime/src/state/PetState.ts\nimport { Schema, type } from '@colyseus/schema';\n\nexport class PetState extends Schema {\n  @type('string')  id           = '';\n  @type('string')  ownerId      = '';\n  @type('string')  species      = 'mochi';\n  @type('string')  stage        = 'baby';   // egg | baby | teen | adult\n  @type('number')  hunger       = 100;\n  @type('number')  cleanliness  = 100;\n  @type('number')  happiness    = 100;\n  @type('number')  energy       = 100;\n  @type('number')  ageMinutes   = 0;\n  @type('string')  lastActionId = '';      // server-authoritative; client uses for reconciliation\n}</code></pre>\n<h3>3.4 — Anti-cheat rate limiter</h3>\n<pre><code class=\"language-typescript\">// apps/realtime/src/anticheat/rateLimiter.ts\nimport { createClient } from 'redis';\n\nconst client = createClient({ url: process.env.REDIS_URL });\nclient.connect();\n\nconst LIMITS = {\n  perAccountPerMinute: 60,\n  perIpPerMinute: 600,\n  burstPerSecond: 10,\n};\n\nexport async function withinLimit(ctx: { accountId: string; ip: string }): Promise&lt;boolean&gt; {\n  const minute = Math.floor(Date.now() / 60_000);\n  const second = Math.floor(Date.now() / 1_000);\n  const [acct, ip, burst] = await Promise.all([\n    client.incr(`rl:acct:${ctx.accountId}:${minute}`),\n    client.incr(`rl:ip:${ctx.ip}:${minute}`),\n    client.incr(`rl:burst:${ctx.accountId}:${second}`),\n  ]);\n  client.expire(`rl:acct:${ctx.accountId}:${minute}`, 90);\n  client.expire(`rl:ip:${ctx.ip}:${minute}`, 90);\n  client.expire(`rl:burst:${ctx.accountId}:${second}`, 5);\n  return acct &lt;= LIMITS.perAccountPerMinute\n      &amp;&amp; ip   &lt;= LIMITS.perIpPerMinute\n      &amp;&amp; burst &lt;= LIMITS.burstPerSecond;\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> <code>pnpm realtime:dev</code> starts the server locally with Redis + JWKS mock; a Colyseus client can connect and join a <code>pet-room</code>. Verified by <code>apps/realtime/src/__tests__/PetRoom.spec.ts</code>.</p>\n<p><strong>AC2.</strong> Joining without a JWT returns Colyseus error code <code>4001 unauthenticated</code>. Verified by integration test fixture.</p>\n<p><strong>AC3.</strong> Joining with a JWT whose <code>tenant_id != room.tenantId</code> returns <code>4004 cross_tenant</code> AND emits <code>security.cross-tenant.join.refused</code> Sentry event. Verified by <code>__tests__/PetRoom.spec.ts</code>.</p>\n<p><strong>AC4.</strong> A tampered message (HMAC fails to verify) is rejected with <code>4002 signature_invalid</code> AND increments <code>security.signature.invalid</code> metric. Verified by <code>__tests__/messageSigner.spec.ts</code>.</p>\n<p><strong>AC5.</strong> Exceeding rate limit returns <code>4003 rate_limit_exceeded</code> AND emits metric. Verified by <code>__tests__/rateLimiter.spec.ts</code> driving Redis-backed counters.</p>\n<p><strong>AC6.</strong> Impossible state transition (<code>feed</code> a pet at hunger=100) is refused, emits <code>security.impossible-transition</code> Sentry event, and increments the account's flag counter. Three such events in 24h trigger auto-ban. Verified by <code>__tests__/PetRoom.spec.ts</code>.</p>\n<p><strong>AC7.</strong> Idle room disposes after 15 min of zero clients. State is flushed to Supabase Postgres. Verified by a fast-forward test (<code>vi.useFakeTimers()</code>).</p>\n<p><strong>AC8.</strong> Graceful shutdown — on SIGTERM, server broadcasts <code>server.shutdown.imminent</code>, waits ≤ 30 s, force-disposes, exits 0. Verified by a Node child-process test that sends <code>SIGTERM</code> and asserts the lifecycle.</p>\n<p><strong>AC9.</strong> <code>GET /healthz</code> returns 200 with the documented payload. <code>GET /readyz</code> returns 503 when Redis is unreachable. Verified by integration test.</p>\n<p><strong>AC10.</strong> Dockerfile produces a reproducible image — two <code>docker build</code> runs against the same git rev produce identical <code>docker images --digests</code> SHA. Verified by <code>__tests__/dockerfile-reproducible.spec.ts</code>.</p>\n<p><strong>AC11.</strong> <code>terraform plan -detailed-exitcode</code> in <code>infra/terraform/realtime/</code> returns exit code 0 (no drift) after deployment, AND a weekly GitHub Action re-runs <code>terraform plan</code> and alerts Slack on exit 2 (drift detected).</p>\n<p><strong>AC12.</strong> Custom ESLint rule <code>no-math-random-in-rooms</code> flags <code>Math.random()</code> usage in <code>apps/realtime/src/rooms/</code> and <code>apps/realtime/src/state/</code>. Verified by ESLint fixture suite.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 — PetRoom integration test</h3>\n<pre><code class=\"language-typescript\">// apps/realtime/src/__tests__/PetRoom.spec.ts\nimport { describe, it, expect, beforeAll, afterAll } from 'vitest';\nimport { Server } from 'colyseus';\nimport { Client } from 'colyseus.js';\nimport { PetRoom } from '../rooms/PetRoom';\nimport { startMockJwks, mintJwt } from './_helpers/jwks';\n\ndescribe('FR-INFRA-002 — PetRoom', () =&gt; {\n  let server: Server;\n  let port: number;\n\n  beforeAll(async () =&gt; {\n    port = await getFreePort();\n    server = new Server({ presence: undefined as any });\n    server.define('pet-room', PetRoom);\n    await server.listen(port);\n    await startMockJwks();\n  });\n\n  afterAll(async () =&gt; { await server.gracefullyShutdown(false); });\n\n  it('rejects unauthenticated joins with 4001', async () =&gt; {\n    const client = new Client(`ws://localhost:${port}`);\n    await expect(client.joinOrCreate('pet-room')).rejects.toThrow(/4001/);\n  });\n\n  it('refuses cross-tenant joins with 4004', async () =&gt; {\n    const tokenA = await mintJwt({ sub: 'u1', tenant_id: 'mochi' });\n    const tokenB = await mintJwt({ sub: 'u2', tenant_id: 'techcombank' });\n    const client = new Client(`ws://localhost:${port}`);\n    const roomA = await client.joinOrCreate('pet-room', { token: tokenA });\n    await expect(client.joinById(roomA.id, { token: tokenB }))\n      .rejects.toThrow(/4004/);\n  });\n\n  it('rejects impossible state transition (feed at hunger=100)', async () =&gt; {\n    const token = await mintJwt({ sub: 'u1', tenant_id: 'mochi' });\n    const client = new Client(`ws://localhost:${port}`);\n    const room = await client.joinOrCreate('pet-room', { token });\n    // Force state into hunger=100 (test seam).\n    (room as any).setStateForTest({ hunger: 100 });\n    const beforeFlag = await getImpossibleTransitionFlag('u1');\n    room.send('feed', { signedPayload: signHmac({ message_id: '1', body: {} }) });\n    await waitMs(50);\n    const afterFlag = await getImpossibleTransitionFlag('u1');\n    expect(afterFlag).toBe(beforeFlag + 1);\n  });\n});</code></pre>\n<h3>5.2 — Rate-limiter test</h3>\n<pre><code class=\"language-typescript\">// apps/realtime/src/__tests__/rateLimiter.spec.ts\nimport { describe, it, expect, beforeEach } from 'vitest';\nimport { withinLimit } from '../anticheat/rateLimiter';\nimport { flushRedis } from './_helpers/redis';\n\ndescribe('FR-INFRA-002 §1.8 — rate limiter', () =&gt; {\n  beforeEach(flushRedis);\n\n  it('allows up to 60 per account per minute', async () =&gt; {\n    for (let i = 0; i &lt; 60; i++) {\n      expect(await withinLimit({ accountId: 'a', ip: '1.1.1.1' })).toBe(true);\n    }\n    expect(await withinLimit({ accountId: 'a', ip: '1.1.1.1' })).toBe(false);\n  });\n\n  it('blocks burst &gt;10 in 1 sec', async () =&gt; {\n    for (let i = 0; i &lt; 10; i++) {\n      expect(await withinLimit({ accountId: 'a', ip: '1.1.1.1' })).toBe(true);\n    }\n    expect(await withinLimit({ accountId: 'a', ip: '1.1.1.1' })).toBe(false);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/realtime/src/rooms/PetRoom.ts\nimport { _BaseTenantRoom } from './_BaseTenantRoom';\nimport { PetState } from '../state/PetState';\nimport { PlayerState } from '../state/PlayerState';\nimport { RoomState } from '../state/RoomState';\nimport { recordImpossibleTransition } from '../anticheat/impossibleTransition';\n\nexport class PetRoom extends _BaseTenantRoom&lt;RoomState&gt; {\n  maxClients = 2;        // 1 owner + 1 co-parent per FR-SOCIAL-002\n\n  override async onCreate(_opts: { tenantId?: string }) {\n    this.setState(new RoomState());\n    this.onMessage('feed', (client, payload: { signedPayload: string }) =&gt; this.handleFeed(client, payload));\n  }\n\n  private async handleFeed(client: any, _payload: any) {\n    const pet = this.state.pets.get(client.userId);\n    if (!pet) return;\n    if (pet.hunger &gt;= 100) {\n      recordImpossibleTransition({ playerId: client.userId, kind: 'feed-at-max' });\n      return;\n    }\n    pet.hunger = Math.min(100, pet.hunger + 20);\n    pet.lastActionId = `feed:${Date.now()}`;\n    // Persist via Supabase in the dispose flush (§1.12).\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong></p>\n<ul><li>Upstash global Redis (free tier OK at P0/P1; paid tier at P2+).</li><li>AWS Singapore (ap-southeast-1) — ECS Fargate cluster + ALB + ECR.</li><li>Supabase JWKS endpoint reachability.</li><li>Sentry Node SDK + OpenTelemetry collector (P1+).</li></ul>\n<p><strong>Internal:</strong></p>\n<ul><li>FR-INFRA-001 (TurboRepo + TypeScript baseline).</li><li>FR-INFRA-003 (Supabase Postgres for state flush, lands next).</li></ul>\n<p><strong>Blocks:</strong> FR-PET-001, FR-CARE-001..005, FR-SOCIAL-001..004, FR-AI-001, FR-VIRAL-002, FR-OBS-002.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — Successful join handshake</h3>\n<pre><code class=\"language-text\">C → S  WS upgrade Authorization: Bearer eyJ...   (Supabase JWT)\nS → C  4xx if invalid OR connect + emit {sessionSecret, roomId}</code></pre>\n<h3>8.2 — Signed message</h3>\n<pre><code class=\"language-json\">{\n  \"type\": \"feed\",\n  \"signedPayload\": {\n    \"body\": { \"petId\": \"01HC7QG...\" },\n    \"message_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n    \"session_seq\": 42,\n    \"hmac\": \"9b3e7d2a5c4f1b8e6d3a2c9f7b5d3a1c9e1f8b6d4c2a3e5f9b7d5a3c1e9f7b5d\"\n  }\n}</code></pre>\n<h3>8.3 — Cross-tenant refusal Sentry event</h3>\n<pre><code class=\"language-json\">{\n  \"event\": \"security.cross-tenant.join.refused\",\n  \"player_id\": \"01HC7QG...\",\n  \"attempted_tenant\": \"techcombank\",\n  \"room_tenant\": \"mochi\",\n  \"occurred_at\": \"2026-08-12T14:36:01Z\"\n}</code></pre>\n<h3>8.4 — Healthz payload</h3>\n<pre><code class=\"language-json\">{\n  \"ok\": true,\n  \"rooms_active\": 142,\n  \"presence_connected\": true,\n  \"ts\": \"2026-08-12T14:36:01.234Z\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Fargate vs EC2 ASG at P0? → §1.3 — Fargate at P0/P1, ASG at P3+; switch documented in Terraform.</li><li><strong>OQ-2 (resolved):</strong> Redis Upstash global vs regional? → §1.4 — global for cross-region future tenants; free tier acceptable through P1.</li><li><strong>OQ-3 (resolved):</strong> Per-message HMAC or per-batch? → §1.7 + §2 — per-message; the latency cost is negligible.</li><li><strong>OQ-4 (resolved):</strong> What's a \"session\"? → bound to a single Colyseus client; secret rotates on rejoin.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Supabase JWKS endpoint down</td><td><code>onAuth</code> fails for all joins; Sentry alert burst</td><td>New users can't join</td><td>Cache JWKS at server start with 10-min TTL; raise alert; fall back to last-known JWKS for the TTL window</td></tr><tr><td>2</td><td>Redis Upstash quota hit</td><td>Presence ops error; <code>RedisPresence</code> falls back to in-memory (single-instance only)</td><td>Multi-instance presence stale</td><td>Auto-scale Upstash tier; emit <code>infra.presence.fallback</code> alert</td></tr><tr><td>3</td><td>ALB sticky-session cookie not honoured by old Cocos client builds</td><td>Players bounced between Fargate tasks; rooms desync</td><td>UX broken</td><td>Cocos client minimum-version gate; force-update flow</td></tr><tr><td>4</td><td>HMAC session secret leaks via Sentry breadcrumb</td><td>Sentry security review</td><td>Anti-cheat bypassed</td><td>Strip <code>sessionSecret</code> from breadcrumbs via PII scrubber; rotate all secrets on detection</td></tr><tr><td>5</td><td>Impossible-transition auto-ban triggers on legitimate edge case</td><td>Player support ticket spike</td><td>False positives</td><td>Tunable threshold via <code>limits.yaml</code>; manual review queue; ban reversible</td></tr><tr><td>6</td><td>Fargate task hits 1000-socket limit</td><td>ECS metric <code>ActiveConnectionCount</code></td><td>New joins refused</td><td>Switch to ASG (Terraform variable); scale horizontally</td></tr><tr><td>7</td><td>Colyseus 0.16.x → 0.17.x major upgrade breaks schema</td><td>Vendor changelog</td><td>Future tech debt</td><td>Upgrade FR with regression test; pin until then</td></tr><tr><td>8</td><td>Graceful-shutdown drain time exceeded</td><td><code>server.shutdown.timeout</code> Sentry event</td><td>Players see disconnect without warning</td><td>Lengthen drain window; investigate slow Postgres</td></tr><tr><td>9</td><td>Tenant-id partition forgotten on a new room class</td><td>Lint rule <code>tenant-id-required-on-room</code></td><td>Cross-tenant data leak</td><td>ESLint rule blocks PR; runtime check in <code>_BaseTenantRoom</code> catches the test fixture</td></tr><tr><td>10</td><td>Redis cluster fail-over splits room state</td><td>Sentry + presence-state drift detected</td><td>Players see ghost-pet state</td><td>Auto-rebalance on rejoin; persist last-known-good state to Postgres every 30 s</td></tr><tr><td>11</td><td><code>Math.random()</code> slips into a new room class</td><td>ESLint rule <code>no-math-random-in-rooms</code></td><td>PR blocked</td><td>Refactor to <code>crypto.randomBytes()</code> via DropRateService</td></tr><tr><td>12</td><td>Terraform drift discovered weekly</td><td><code>terraform plan</code> exit code 2</td><td>Manual console edit happened</td><td>Slack alert; identify operator; either revert or codify the change</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 4 — Colyseus reasoning (same language as client, room-based, schema delta sync, AWS Singapore latency, Nakama as P3+ upgrade path).</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>ECS task CPU/memory sizing — start at 0.5 vCPU / 1 GB; tune after first soft-launch load test.</li><li>ALB target-group cookie name + duration — locked in Terraform.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Client → client direct WebSocket (everything routes through Colyseus).</li><li>Trust-the-client state for monetised outcomes.</li><li><code>Math.random()</code> in <code>rooms/</code> or <code>state/</code>.</li><li>AWS console edits without Terraform reconciliation.</li><li>Shipping Colyseus 0.17.x mid-phase without a regression pass.</li></ul>\n<p><strong>Cross-reference:</strong> This FR is the substrate for every multi-actor gameplay FR. FR-SOCIAL-002 (PetPair co-parent), FR-SOCIAL-003 (trade), FR-SOCIAL-004 (wedding) all require this room scaffold.</p>";
