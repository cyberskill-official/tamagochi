window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SUB-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Pricing.</strong></p>\n<ul><li>Monthly: $4.99 globally ($4.99 USD = ~₫119K but VN priced at <strong>₫99K/mo</strong> to match local elasticity).</li><li>Annual: $39.99/yr (VN ₫799K/yr) — ~33% savings vs monthly.</li></ul>\n<p>§1.2  <strong>Pet+ benefits.</strong></p>\n<ul><li>10 active pet slots (vs 3 free) — TASK-PET-005.</li><li>50% breeding cooldown reduction — TASK-PET-007.</li><li>Premium AI persona quota raise (60 calls/pet/day vs 30 free) — TASK-AI-001.</li><li>+1 forgiveness token monthly — TASK-CARE-005.</li><li>Monthly 100 Hearts grant.</li><li>Exclusive seasonal pet (rotates monthly).</li><li>Cloud save priority (faster cross-device sync).</li><li>Ad-free (no rewarded video required).</li></ul>\n<p>§1.3  <strong>Pet+ subscription products.</strong> Two StoreKit 2 + Play Billing products:</p>\n<ul><li><code>mochi.subscription.monthly</code> — $4.99/mo auto-renew.</li><li><code>mochi.subscription.annual</code> — $39.99/yr auto-renew.</li></ul>\n<p>§1.4  <strong>Subscription state.</strong> Per user, stored in <code>user_subscriptions</code> table: <code>(user_id pk, tier 'free'|'pet_plus', product_id, current_period_end, status, vendor, original_transaction_id, grace_period_until, tenant_id)</code>.</p>\n<p>§1.5  <strong>Entitlement service.</strong> Replaces the stub at TASK-PET-005 §3.3. Returns <code>tier</code> based on <code>user_subscriptions.status</code> + <code>current_period_end</code>. Cached for 60 seconds.</p>\n<p>§1.6  <strong>Grace period.</strong> When a subscription fails to renew (Apple/Google billing retry), Pet+ entitlement MUST persist for <strong>16 days</strong> (Apple's recommended grace). During grace, surface \"Update billing info\" UX. After grace expires, downgrade to free tier.</p>\n<p>§1.7  <strong>Restore purchase.</strong> Users on a new device MUST tap \"Restore purchase\" → triggers Apple StoreKit <code>Transaction.currentEntitlements</code> / Google <code>BillingClient.queryPurchasesAsync</code> → re-validates active subscription server-side. No double-charging.</p>\n<p>§1.8  <strong>Webhook integration.</strong> Apple App Store Server Notifications V2 + Google Real-Time Developer Notifications (RTDN) MUST be wired. Each event type handled:</p>\n<ul><li><code>SUBSCRIBED</code> / <code>DID_RENEW</code> → ensure entitlement active.</li><li><code>DID_FAIL_TO_RENEW</code> → start grace.</li><li><code>EXPIRED</code> → downgrade.</li><li><code>REFUND</code> → revoke entitlement.</li></ul>\n<p>§1.9  <strong>Annual proration on cancel.</strong> Users who cancel annual mid-cycle KEEP Pet+ until period end (no early refund; Apple/Google handle their own refund policy).</p>\n<p>§1.10  <strong>Family Sharing.</strong> Apple Family Sharing + Google Family Library entitlements honoured. Per TASK-LEGAL-003 §1.9.</p>\n<p>§1.11  <strong>Trial — none at P3.</strong> Plan §PART 6 — defer trial to P4+ after baseline conversion data.</p>\n<p>§1.12  <strong>VN pricing rationale.</strong> Plan §PART 6 — local elasticity is steeper than 1:1 USD. ₫99K &lt; ₫119K matches consumer expectations.</p>\n<p>§1.13  <strong>Monthly Hearts grant.</strong> On each renewal, the user is granted 100 Hearts via TASK-ECON-001 <code>iap_source</code> ledger entry. <code>ref = 'sub.monthly_grant:&lt;period&gt;'</code>.</p>\n<p>§1.14  <strong>Seasonal pet rotation.</strong> Each month, Pet+ subscribers can claim a unique seasonal pet (e.g. \"April Mochi\", \"May Pengu\"). Per TASK-PET-006 — special-tier outside the 5 launch species (added to catalogue per-season).</p>\n<p>§1.15  <strong>Kids SKU + Family tier.</strong> Pet+ on the kids SKU is bridged through TASK-SUB-002 Family tier — kids cannot independently subscribe. Per TASK-LEGAL-003.</p>\n<p>§1.16  <strong>Entitlement RLS.</strong> Subscription state RLS — only user can read own row + DPO for support.</p>\n<p>§1.17  <strong>Audit retention.</strong> 7 years.</p>\n<p>§1.18  <strong>Webhook replay protection.</strong> Apple/Google webhooks include event IDs; persist + dedupe to prevent re-grant on retried delivery.</p>\n<p>§1.19  <strong>Pricing change procedure.</strong> Plan §PART 6 + Apple/Google rules — pricing changes require user opt-in for existing subscribers + 30-day notice. task amendment required.</p>\n<p>§1.20  <strong>Analytics.</strong> <code>sub.subscribed</code>, <code>sub.renewed</code>, <code>sub.grace_started</code>, <code>sub.expired</code>, <code>sub.refunded</code>, <code>sub.restored</code>, <code>sub.cancelled</code> per TASK-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why $4.99/mo.</strong> Plan §PART 6 — industry-standard f2p sub price. Duolingo Super, Calm, etc. land here. Above $4.99 conversion drops sharply.</p>\n<p><strong>Why ₫99K VN.</strong> Plan §PART 6 — local elasticity research. Vietnamese players accept ₫99K (≈$4.00) more readily than ₫119K (≈$4.99 direct conversion).</p>\n<p><strong>Why annual at $39.99.</strong> 33% discount vs 12 × $4.99 = $59.88. Standard sub-discount ratio.</p>\n<p><strong>Why 16-day grace.</strong> Apple's recommended billing retry window. Without grace, players see immediate-downgrade UX which feels punitive.</p>\n<p><strong>Why no Stripe/Antom for subscriptions at P3.</strong> Apple/Google require subscriptions be processed through their stores for in-app digital content. Antom for one-off IAP is allowed; subscription must stay in-store.</p>\n<p><strong>Why no trial at P3.</strong> Plan §PART 6 — establish baseline conversion first; trial adds churn risk.</p>\n<p><strong>Why webhook replay protection.</strong> Apple/Google retry webhooks aggressively; double-grant is a common bug.</p>\n<p><strong>Why Family Sharing.</strong> Apple Guideline 3.1.3(a) — Family Sharing must be honoured.</p>\n<p><strong>Why no kids direct subscribe.</strong> COPPA + TASK-LEGAL-001 — parental consent required.</p>\n<p><strong>Why monthly Hearts grant.</strong> Plan §PART 6 — perceived value bump beyond unlocks; creates \"I've got 100 Hearts this month\" feel.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/sub/entitlement.service.ts\n@Injectable()\nexport class EntitlementService {\n  private cache = new Map&lt;string, { tier: 'free' | 'pet_plus'; ts: number }&gt;();\n\n  async tier(userId: string): Promise&lt;'free' | 'pet_plus'&gt; {\n    const cached = this.cache.get(userId);\n    if (cached &amp;&amp; Date.now() - cached.ts &lt; 60_000) return cached.tier;\n    const { data } = await this.supa.from('user_subscriptions').select('*').eq('user_id', userId).maybeSingle();\n    let tier: 'free' | 'pet_plus' = 'free';\n    if (data) {\n      const inPeriod = new Date(data.current_period_end).getTime() &gt; Date.now();\n      const inGrace = data.grace_period_until &amp;&amp; new Date(data.grace_period_until).getTime() &gt; Date.now();\n      if ((data.status === 'active' &amp;&amp; inPeriod) || (data.status === 'in_grace' &amp;&amp; inGrace)) tier = 'pet_plus';\n    }\n    this.cache.set(userId, { tier, ts: Date.now() });\n    return tier;\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/sub/pet-plus.service.ts (excerpt)\nasync handleAppleWebhook(event: AppleServerNotification) {\n  await this.webhookDedupe.assertNotSeen(event.notificationUUID);\n  const userId = await this.resolveUserId(event.data.signedTransactionInfo);\n  switch (event.notificationType) {\n    case 'SUBSCRIBED':\n    case 'DID_RENEW':\n      await this.upsertSubscription(userId, event);\n      if (event.notificationType === 'DID_RENEW') await this.ledger.grant(userId, 'hearts', 100, `sub.monthly_grant:${event.subscriptionGroupIdentifier}`, 'mochi', 'iap_source');\n      break;\n    case 'DID_FAIL_TO_RENEW':\n      await this.startGrace(userId, event);\n      break;\n    case 'EXPIRED':\n      await this.expire(userId);\n      break;\n    case 'REFUND':\n      await this.refund(userId, event);\n      break;\n  }\n  this.entitlement.invalidateCache(userId);\n}</code></pre>\n<pre><code class=\"language-sql\">-- migration\ncreate table public.user_subscriptions (\n  user_id uuid primary key references auth.users(id) on delete cascade,\n  tier text not null default 'free' check (tier in ('free','pet_plus')),\n  product_id text,\n  vendor text check (vendor in ('apple','google')),\n  original_transaction_id text,\n  current_period_start timestamptz,\n  current_period_end timestamptz,\n  status text not null default 'active' check (status in ('active','in_grace','expired','refunded','cancelled')),\n  grace_period_until timestamptz,\n  tenant_id text not null default 'mochi',\n  updated_at timestamptz not null default now()\n);\nalter table public.user_subscriptions enable row level security;\ncreate policy \"sub self\" on public.user_subscriptions for select using (user_id = auth.uid());\n\ncreate table public.subscription_webhook_dedupe (\n  event_id text primary key,\n  vendor text not null,\n  received_at timestamptz not null default now()\n);</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Apple subscribe → entitlement active. Verified. <strong>AC2.</strong> Renewal → 100 Hearts granted via ledger. Verified. <strong>AC3.</strong> Fail-to-renew → grace started for 16 days. Verified. <strong>AC4.</strong> After grace → downgrade. Verified. <strong>AC5.</strong> Restore purchase on new device re-validates. Verified. <strong>AC6.</strong> Webhook replay no double-grant. Verified. <strong>AC7.</strong> Pet+ tier unlocks 10 slots in TASK-PET-005. Verified. <strong>AC8.</strong> Pet+ tier reduces breeding cooldown 50%. Verified. <strong>AC9.</strong> Pet+ tier raises AI quota to 60/pet/day. Verified. <strong>AC10.</strong> Pet+ tier +1 forgiveness token. Verified. <strong>AC11.</strong> Family Sharing honoured. Verified. <strong>AC12.</strong> Kids cannot subscribe directly. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('TASK-SUB-001 — Pet+', () =&gt; {\n  it('grace period 16 days', async () =&gt; {\n    await mockSubscribe('u1');\n    await mockFailToRenew('u1');\n    const tier1 = await entitlement.tier('u1');\n    expect(tier1).toBe('pet_plus');  // still in grace\n    await advanceClockDays(17);\n    const tier2 = await entitlement.tier('u1');\n    expect(tier2).toBe('free');\n  });\n\n  it('webhook replay no double-grant', async () =&gt; {\n    const event = mockAppleRenewEvent('uuid-1');\n    await svc.handleAppleWebhook(event);\n    await svc.handleAppleWebhook(event);   // replay\n    expect(await ledger.balance('u1', 'hearts')).toBe(100); // not 200\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/sub/grace-period.service.ts\n@Injectable()\nexport class GracePeriodService {\n  async startGrace(userId: string): Promise&lt;void&gt; {\n    const until = new Date(Date.now() + 16 * 24 * 3600 * 1000).toISOString();\n    await this.supa.from('user_subscriptions').update({ status: 'in_grace', grace_period_until: until }).eq('user_id', userId);\n    await this.notify.sendBillingUpdateRequired(userId);\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Apple StoreKit 2 + ASSN V2 webhooks; Google Play Billing 6 + RTDN webhooks. <strong>Internal:</strong> TASK-ECON-001 ledger, TASK-ECON-002 IAP base, TASK-PET-005/007, TASK-AI-001 cost cap, TASK-CARE-005 forgiveness token, TASK-AUTH-001 (Family Sharing tie-in). <strong>Blocks:</strong> TASK-SUB-002 (Family tier extends this).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/sub/restore\n→ 200 { \"tier\": \"pet_plus\", \"expires_at\": \"2026-09-12T14:36:01Z\", \"vendor\": \"apple\" }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"sub.renewed\", \"user_id\": \"01HU...\", \"product_id\": \"mochi.subscription.monthly\", \"hearts_granted\": 100, \"next_renewal_at\": \"...\" }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"sub.grace_started\", \"user_id\": \"01HU...\", \"grace_until\": \"2026-08-28T14:36:01Z\" }</code></pre>\n<pre><code class=\"language-json\">{\n  \"user_id\": \"01HU...\",\n  \"tier\": \"pet_plus\",\n  \"product_id\": \"mochi.subscription.monthly\",\n  \"current_period_end\": \"2026-09-12T14:36:01Z\",\n  \"status\": \"active\",\n  \"vendor\": \"apple\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Trial at P3? → §1.11 — no.</li><li><strong>OQ-2 (resolved):</strong> VN local payment? → §<code>disallowed_tools</code> — Apple/Google only.</li><li><strong>OQ-3 (resolved):</strong> Grace period length? → §1.6 — 16 days (Apple).</li><li><strong>OQ-4 (resolved):</strong> Hearts grant on renewal? → §1.13 — 100/mo.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Webhook never received</td><td>Polling fallback</td><td>Grace prevents instant downgrade</td><td>Periodic state reconciliation</td></tr><tr><td>2</td><td>Webhook replay double-grant</td><td>Dedupe table</td><td>None</td><td>Verified</td></tr><tr><td>3</td><td>Restore-purchase fails on new device</td><td>Vendor SDK error</td><td>UX retry</td><td>Surface \"contact support\"</td></tr><tr><td>4</td><td>Grace period state machine race</td><td>Per-user mutex</td><td>None</td><td>Serialised updates</td></tr><tr><td>5</td><td>Family Sharing partial entitlement</td><td>Vendor handling</td><td>Honoured</td><td>Verified</td></tr><tr><td>6</td><td>Refund clawback consumed Hearts</td><td>DPO review</td><td>No clawback</td><td>Per §1.13</td></tr><tr><td>7</td><td>Pricing change without opt-in</td><td>Apple/Google reject</td><td>Submission blocked</td><td>task amendment</td></tr><tr><td>8</td><td>Cache stale</td><td>60s TTL</td><td>Brief misstate</td><td>Invalidate on webhook</td></tr><tr><td>9</td><td>Cross-tenant subscription</td><td>RLS</td><td>Privacy</td><td>Enforced</td></tr><tr><td>10</td><td>Audit growth</td><td>Disk</td><td>7-yr retention</td><td>OK</td></tr><tr><td>11</td><td>Trial accidentally enabled in store config</td><td>Apple Connect review</td><td>Catch</td><td>Manual sync</td></tr><tr><td>12</td><td>Annual mid-cycle cancel UX confusion</td><td>Player support</td><td>Education</td><td>UX clarifies \"stays until X\"</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 6 (Pet+ benefits + pricing).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Seasonal pet rotation per-month curated.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>VN MoR subscriptions (must be in-store).</li><li>Trial at P3.</li><li>Pricing change without opt-in.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-SUB-002 Family tier; TASK-PET-005/007 entitlement-consuming.</p>";
