window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SOCIAL-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Wedding definition.</strong> A ceremony involving 2 adult pets owned by 2 different players who are friends. Wedding produces: a permanent \"married\" cosmetic badge on both pets + a 12-second auto-rendered share clip.</p>\n<p>§1.2  <strong>Eligibility.</strong> Both pets <code>stage='adult'</code>; both players friends (TASK-SOCIAL-001); both standard SKU; neither pet currently in another wedding; cross-tenant blocked.</p>\n<p>§1.3  <strong>Naming.</strong> Ceremony can be \"Pet Wedding\" (romantic) or \"Best Friend Ceremony\" (platonic). Player selects framing on proposal. Both produce same mechanics; locale-tuned copy.</p>\n<p>§1.4  <strong>Proposal flow.</strong> Player A's pet \"proposes\" to player B's pet via <code>POST /v1/weddings/propose</code>. Player B receives invite + 24h to accept.</p>\n<p>§1.5  <strong>Synchronous ceremony.</strong> When both accept, a dedicated Colyseus <code>WeddingRoom</code> is created. Both players must be online + present in the room. Ceremony lasts 90 seconds with synchronized Spine <code>wedding_pose</code> animation + Lottie sparkle overlay.</p>\n<p>§1.6  <strong>Ceremony script.</strong> Pre-rendered 90s sequence: pets meet → exchange \"vows\" (scripted text or AI-generated if both pets are 13+ owner consented) → kiss/high-five → fade to married badge reveal. Synchronized across both clients via Colyseus state.</p>\n<p>§1.7  <strong>Auto-rendered share clip.</strong> During ceremony, the server renders a 12-second highlight reel. Per TASK-VIRAL-001, exported as 1080×1920 H.264 with watermark + locale-aware hashtag prefill <code>#mochiwedding #virtualpet</code>.</p>\n<p>§1.8  <strong>Married badge.</strong> Both pets receive a permanent <code>married_to</code> field referencing the other pet. Visible as a small badge in roster + AR.</p>\n<p>§1.9  <strong>Divorce.</strong> Either party can <code>POST /v1/weddings/:weddingId/end</code> to dissolve. Badge removed. Audit row.</p>\n<p>§1.10  <strong>One marriage per pet.</strong> A pet currently <code>married_to</code> another cannot enter a new wedding. Must divorce first.</p>\n<p>§1.11  <strong>No real-money wedding fee.</strong> Per TASK-LEGAL-002 §1.1. Cost is 100 Coins (earned only).</p>\n<p>§1.12  <strong>Wedding cooldown.</strong> 30-day cooldown between weddings for the same pet (prevents shotgun-wedding spam).</p>\n<p>§1.13  <strong>AI vows (optional opt-in).</strong> If both players consent, TASK-AI-001 LLM generates personalized vows. Goes through TASK-AI-002 safety. Both pets' personas referenced. Kids SKU forbidden per TASK-AI-001 §1.1 (but kids SKU already blocked from wedding entirely).</p>\n<p>§1.14  <strong>Asynchronous fallback.</strong> If one party disconnects mid-ceremony, the other can wait up to 5 minutes; if reconnection fails, ceremony is paused + can resume within 24h.</p>\n<p>§1.15  <strong>Spine wedding pose.</strong> Uses TASK-ART-001 contract <code>wedding_pose</code> animation. Both pets visible in same scene.</p>\n<p>§1.16  <strong>Cocos UI.</strong> <code>WeddingCeremonyScene.ts</code> displays both pets, animated camera, ambient music (Howler.js), confetti particles (reduce-motion respected).</p>\n<p>§1.17  <strong>Cross-tenant + cross-SKU locked.</strong> Per TASK-INFRA-003 + TASK-AUTH-003.</p>\n<p>§1.18  <strong>Audit.</strong> <code>social.wedding.proposed</code>, <code>social.wedding.accepted</code>, <code>social.wedding.completed { wedding_id }</code>, <code>social.wedding.ended { trigger }</code> per TASK-OBS-001.</p>\n<p>§1.19  <strong>DSR + delete cascade.</strong> Wedding rows cascade-delete on account deletion. Married badge removed from surviving pet.</p>\n<p>§1.20  <strong>Wedding gallery.</strong> Player can view their pets' wedding history in <code>Settings → Wedding gallery</code>.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why romantic + platonic framing.</strong> Plan §PART 3 — viral content + kid-appropriate language gradient. Different player audiences want different framings.</p>\n<p><strong>Why synchronous.</strong> Plan §PART 3 viral hook #7 — co-presence is the share moment. Async would feel like a stat update.</p>\n<p><strong>Why 90s ceremony length.</strong> Long enough to feel like an event; short enough not to drag.</p>\n<p><strong>Why 12s share clip.</strong> TikTok-native short-form (vs TASK-VIRAL-001 6-default). Wedding warrants longer.</p>\n<p><strong>Why optional AI vows.</strong> Plan §PART 3 #3 + personalisation pinnacle. Opt-in respects TASK-AI-002 cost/safety.</p>\n<p><strong>Why 30-day cooldown.</strong> Prevents marriage-spam exploiting share clips.</p>\n<p><strong>Why one marriage per pet.</strong> Identity preservation. Polygamous would confuse semantic of \"married_to\".</p>\n<p><strong>Why pause-resume on disconnect.</strong> Plan §PART 7 emerging-market flaky-network reality.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/social/wedding/wedding.service.ts (excerpt)\nasync propose(u: AuthedUser, myPetId: string, partnerPetId: string, framing: 'romantic' | 'platonic') {\n  await this.assertEligibility(u, myPetId, partnerPetId);\n  await this.coins.spend(u.id, 100);\n  const weddingId = generateUlid();\n  await this.supa.from('pet_weddings').insert({\n    id: weddingId, proposer_pet_id: myPetId, recipient_pet_id: partnerPetId,\n    framing, state: 'proposed', expires_at: new Date(Date.now() + 24 * 3600 * 1000).toISOString(),\n  });\n  await this.notify.sendWeddingInvite(partnerPetId, weddingId, framing);\n  return { wedding_id: weddingId, state: 'proposed' };\n}\n\nasync startCeremony(weddingId: string) {\n  const wedding = await this.supa.from('pet_weddings').select('*').eq('id', weddingId).single();\n  // Create WeddingRoom in Colyseus\n  const roomId = await this.realtime.matchMaker.createRoom('wedding-room', { wedding_id: weddingId });\n  await this.supa.from('pet_weddings').update({ state: 'ceremony_active', room_id: roomId }).eq('id', weddingId);\n  return { room_id: roomId };\n}\n\nasync completeCeremony(weddingId: string, shareClipUri: string) {\n  const wedding = await this.fetchWedding(weddingId);\n  await this.supa.rpc('apply_wedding_badges', { _wedding_id: weddingId });\n  await this.audit.emit('social.wedding.completed', { wedding_id: weddingId });\n  return { wedding_id: weddingId, share_clip_uri: shareClipUri };\n}</code></pre>\n<pre><code class=\"language-sql\">create table public.pet_weddings (\n  id text primary key,\n  proposer_pet_id text not null references public.pets(id) on delete cascade,\n  recipient_pet_id text not null references public.pets(id) on delete cascade,\n  framing text not null check (framing in ('romantic','platonic')),\n  state text not null check (state in ('proposed','accepted','ceremony_active','completed','ended','expired')),\n  proposed_at timestamptz not null default now(),\n  accepted_at timestamptz,\n  completed_at timestamptz,\n  ended_at timestamptz,\n  expires_at timestamptz not null,\n  room_id text,\n  share_clip_uri text,\n  tenant_id text not null default 'mochi'\n);\n\nalter table public.pets add column married_to text references public.pets(id) on delete set null;\ncreate index on public.pets (married_to);</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Propose → accept → ceremony → complete end-to-end. Verified. <strong>AC2.</strong> Both pets receive married_to. Verified. <strong>AC3.</strong> 12s share clip rendered with watermark + hashtag. Verified. <strong>AC4.</strong> Cross-SKU rejected. Verified. <strong>AC5.</strong> Adult-only enforced. Verified. <strong>AC6.</strong> 30-day cooldown enforced. Verified. <strong>AC7.</strong> Disconnect mid-ceremony pauses; resumable within 24h. Verified. <strong>AC8.</strong> Divorce removes badges. Verified. <strong>AC9.</strong> No real-money fee. Verified. <strong>AC10.</strong> AI vows opt-in respects TASK-AI-002. Verified. <strong>AC11.</strong> Reduce-motion mode caps confetti. Verified. <strong>AC12.</strong> Audit + analytics emit. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('TASK-SOCIAL-004 — wedding', () =&gt; {\n  it('completes with both pets married_to each other', async () =&gt; {\n    await proposeAndAccept('petA', 'petB');\n    await runCeremony('petA', 'petB');\n    expect((await db.pets.byId('petA')).married_to).toBe('petB');\n    expect((await db.pets.byId('petB')).married_to).toBe('petA');\n  });\n\n  it('blocks polygamy', async () =&gt; {\n    await marry('petA', 'petB');\n    await expect(svc.propose(user('u1'), 'petA', 'petC', 'romantic')).rejects.toThrow(/already_married/);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/realtime/src/rooms/WeddingRoom.ts\nexport class WeddingRoom extends _BaseTenantRoom {\n  maxClients = 2;\n  override async onCreate(opts: { wedding_id: string }) {\n    this.setState(new WeddingCeremonyState());\n    this.clock.setInterval(() =&gt; this.advanceScript(), 1000);\n  }\n  private advanceScript() {\n    this.state.elapsed_sec += 1;\n    if (this.state.elapsed_sec === 90) this.complete();\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> None new. <strong>Internal:</strong> TASK-SOCIAL-002 (presence model), TASK-VIRAL-001 (export pipeline), TASK-ART-001 (wedding_pose), TASK-AI-001+002 (optional vows). <strong>Blocks:</strong> none.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/weddings/propose\n{ \"my_pet_id\": \"petA\", \"partner_pet_id\": \"petB\", \"framing\": \"romantic\" }\n→ 201 { \"wedding_id\": \"01HWED...\", \"state\": \"proposed\", \"expires_at\": \"...\" }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"social.wedding.completed\", \"wedding_id\": \"01HWED...\", \"petA\": \"...\", \"petB\": \"...\", \"framing\": \"romantic\", \"share_clip_uri\": \"...\" }</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"wedding.already_married\", \"pet_id\": \"petA\", \"married_to\": \"petX\" }</code></pre>\n<pre><code class=\"language-json\">{ \"id\": \"01HWED...\", \"framing\": \"platonic\", \"state\": \"ceremony_active\", \"room_id\": \"wd-...\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Romantic vs platonic? → §1.3 — both.</li><li><strong>OQ-2 (resolved):</strong> AI vows? → §1.13 — opt-in.</li><li><strong>OQ-3 (resolved):</strong> Cooldown? → §1.12 — 30 days.</li><li><strong>OQ-4 (resolved):</strong> Cost? → §1.11 — 100 Coins.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Disconnect mid-ceremony</td><td>Colyseus detects</td><td>Pause + resume</td><td>24h grace</td></tr><tr><td>2</td><td>Share clip render fails</td><td>Server error</td><td>Wedding still completes</td><td>UX retry on share</td></tr><tr><td>3</td><td>Both proposals to same pet</td><td>Concurrency</td><td>First wins</td><td>Locked</td></tr><tr><td>4</td><td>Cross-SKU bypass</td><td>Audit</td><td>Blocked</td><td>Validation</td></tr><tr><td>5</td><td>Divorce race</td><td>Atomic SQL</td><td>Last wins</td><td>OK</td></tr><tr><td>6</td><td>AI vows safety block</td><td>TASK-AI-002</td><td>Fallback scripted</td><td>Standard vows</td></tr><tr><td>7</td><td>Married_to cascade orphans</td><td>TASK-LEGAL-001 cascade</td><td>Auto-null</td><td>Set null on delete</td></tr><tr><td>8</td><td>Confetti perf on low-end</td><td>Reduce-motion</td><td>Capped</td><td>OK</td></tr><tr><td>9</td><td>24h grace pause expires</td><td>Auto-cancel</td><td>Refund coins</td><td>Compensating tx</td></tr><tr><td>10</td><td>Cross-tenant attempted</td><td>RLS</td><td>Blocked</td><td>Audit</td></tr><tr><td>11</td><td>Polygamy via race</td><td>First-wins SQL</td><td>Blocked</td><td>Lock</td></tr><tr><td>12</td><td>Spamming proposals</td><td>Rate limit</td><td>Throttled</td><td>Per-day cap</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 viral hook #7 (Pet Wedding / Best Friend Ceremony).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Locale-tuned ceremony scripts DPO-reviewed.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Real-money fee.</li><li>Polygamy.</li><li>Cross-SKU.</li><li>Wedding of grandma pets.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-VIRAL-001 share path. Plan §PART 3 viral hooks.</p>";
