window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SUB-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Family tier pricing.</strong> $9.99/mo (VN ₫199K/mo). 2× Pet+ price for up to 5 child profiles.</p>\n<p>§1.2  <strong>Family tier benefits.</strong></p>\n<ul><li>All Pet+ benefits for up to 5 child profiles + 1 parent profile.</li><li>Parental dashboard for monitoring + controlling.</li><li>Screen-time caps per child (configurable 15min–unlimited).</li><li>Spend caps per child (configurable 0–unlimited Hearts/mo).</li><li>Content filter override (parent can disable AI dialogue per TASK-AI-001 even for 13+ children).</li><li>Friend-request approval queue (parent must approve adds).</li></ul>\n<p>§1.3  <strong>Family tier purchasable by adults only.</strong> Verified via <code>audience_age_gate = '13+'</code>. Apple Family Sharing membership confirms adult role.</p>\n<p>§1.4  <strong>Child profile creation.</strong> Parent in dashboard taps \"Add child profile\" → flow:</p>\n<ul><li>Enter child's display name (filtered per TASK-PET-001 §1.5).</li><li>Enter child's birth year (used to set <code>audience_age_gate</code> correctly).</li><li>Triggers TASK-AUTH-003 parental consent via Safe Harbor vendor — even though the parent IS the parent, COPPA-2025 requires VPC per child.</li><li>On consent grant: child profile created on <strong>kids SKU's Supabase project</strong>.</li><li>Parent's account on standard SKU is linked via <code>family_links</code> table.</li></ul>\n<p>§1.5  <strong>Parental dashboard.</strong> Browser + in-app view showing per child:</p>\n<ul><li>Time spent today / this week / this month.</li><li>Coins + Hearts spent.</li><li>Friend list + pending requests.</li><li>AI dialogue logs (sanitised — DPO review).</li><li>\"Pause this child\" button (instant suspend).</li><li>\"Delete this child profile\" → DSR-erasure path.</li></ul>\n<p>§1.6  <strong>Screen-time caps.</strong> Configurable per child by parent. When cap exceeded, child's app launch returns \"Time's up — see you tomorrow!\" UX. Resets at child's local midnight.</p>\n<p>§1.7  <strong>Spend caps.</strong> Per child / per month. Cap defaults to 0 (no real-money spend without parental approval). Each IAP attempt by child triggers a parental-approval push to parent. Approval = transient (15 min).</p>\n<p>§1.8  <strong>Content filter override.</strong> Parent can: disable AI dialogue (forces scripted only); restrict friend graph (invite-code only OR fully off); restrict push notifications; restrict ad rewarded-video.</p>\n<p>§1.9  <strong>Endpoint — family state.</strong> <code>GET /v1/sub/family/state</code> returns full family + per-child summaries.</p>\n<p>§1.10  <strong>Endpoint — child action.</strong> <code>POST /v1/sub/family/child/:childId/pause</code>, <code>/unpause</code>, <code>/delete</code>, <code>/set-cap</code>.</p>\n<p>§1.11  <strong>Endpoint — approve spend.</strong> <code>POST /v1/sub/family/spend-approval/:requestId/approve</code> or <code>/reject</code>.</p>\n<p>§1.12  <strong>Family Sharing OS integration.</strong> Apple Family Sharing / Google Family Library — Apple/Google handle the IAP family approval flow. Our system pre-emptively gates AT the in-app surface (per §1.7) so OS prompt is the second layer.</p>\n<p>§1.13  <strong>Cross-SKU family link.</strong> Parent on standard SKU + children on kids SKU. The <code>family_links</code> table joins across the 2 Supabase projects. Each parent action MUST authorise across both projects (signed cross-project token).</p>\n<p>§1.14  <strong>DSR cascade.</strong> Per TASK-LEGAL-001 §3.6 — deleting parent's account triggers child profile review (parent confirms child's data also deletes). Child data deletion follows kids-SKU 30-day retention rules.</p>\n<p>§1.15  <strong>Family tier audit retention.</strong> 7 years (kids).</p>\n<p>§1.16  <strong>Family tier vs Pet+ upgrade path.</strong> A Pet+ subscriber can upgrade to Family tier via in-app upgrade. Apple/Google handle proration. Downgrade allowed; existing child profiles enter read-only mode if family tier ends + are deleted within 30 days.</p>\n<p>§1.17  <strong>Locale parental copy.</strong> All parental UI strings reviewed by DPO + localised per TASK-I18N-001.</p>\n<p>§1.18  <strong>Push notifications to parent.</strong> Spend approvals + friend approvals send push to parent — independent of children's notifications.</p>\n<p>§1.19  <strong>No content moderation outsource.</strong> AI dialogue logs reviewed by parent dashboard. T&amp;S team for offensive UGC submitted by child.</p>\n<p>§1.20  <strong>Analytics.</strong> <code>sub.family.subscribed</code>, <code>sub.family.child_added</code>, <code>sub.family.spend_approved</code>, <code>sub.family.spend_rejected</code>, <code>sub.family.screen_cap_hit</code>, <code>sub.family.child_paused</code> per TASK-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why $9.99 / ~2× Pet+.</strong> Plan §PART 6 — family tiers across competitors land at 2× single-user.</p>\n<p><strong>Why 5 child profiles.</strong> Plan §PART 6 — typical household size + Apple/Google Family Sharing limits.</p>\n<p><strong>Why parental dashboard mandatory.</strong> COPPA-2025 §1.5 + TASK-AUTH-003. Without it, kids SKU has no parental oversight surface beyond Apple/Google level.</p>\n<p><strong>Why VPC per child.</strong> COPPA — even with adult Family Sharing membership, each child needs explicit parental consent (the parent IS the consenter, but legally each child requires its own).</p>\n<p><strong>Why spend cap defaults to 0.</strong> Default-safe. Parent must actively raise.</p>\n<p><strong>Why content filter override.</strong> Plan §PART 8 — some parents want stricter than default (no AI dialogue even for 13+).</p>\n<p><strong>Why pre-emptive approval gate.</strong> Apple's Family Sharing approval is OS-level + sometimes slow. Our pre-emptive approval pushes the decision to parent's notification in our app + OS Family Sharing as second layer.</p>\n<p><strong>Why cross-SKU family link.</strong> Parent is on standard SKU (Apple/Google adult account); children on kids SKU (separate Supabase project). The link table bridges.</p>\n<p><strong>Why DSR cascade.</strong> Parental account deletion typically wants child data also gone.</p>\n<p><strong>Why no content moderation outsource.</strong> Plan §PART 8 — Family tier is too kid-adjacent to outsource trust decisions.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/sub/family/family.controller.ts\n@Controller('v1/sub/family')\n@UseGuards(SupabaseJwtGuard)\nexport class FamilyController {\n  @Get('state')\n  async state(@CurrentUser() u: AuthedUser) { return this.svc.state(u.id); }\n\n  @Post('child')\n  async addChild(@CurrentUser() u: AuthedUser, @Body() body: { display_name: string; birth_year: number }) {\n    return this.svc.addChild(u, body.display_name, body.birth_year);\n  }\n\n  @Post('child/:childId/pause')\n  async pause(@CurrentUser() u: AuthedUser, @Param('childId') childId: string) {\n    return this.svc.pauseChild(u.id, childId);\n  }\n\n  @Post('spend-approval/:requestId/:decision')\n  async approve(@CurrentUser() u: AuthedUser, @Param('requestId') reqId: string, @Param('decision') decision: 'approve' | 'reject') {\n    return this.svc.handleSpendApproval(u.id, reqId, decision);\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/sub/family/family.service.ts (excerpt)\nasync addChild(u: AuthedUser, displayName: string, birthYear: number) {\n  if (await this.entitlement.tier(u.id) !== 'family') throw new HttpException('family.tier.required', 402);\n  if (await this.countChildren(u.id) &gt;= 5) throw new HttpException('family.child_limit', 422);\n  const age = new Date().getFullYear() - birthYear;\n  const audience = age &lt; 13 ? 'under-13' : '13+';\n  // Trigger TASK-AUTH-003 parental consent flow.\n  const consentId = await this.parentalConsent.startNewConsentFlow({\n    childDisplayName: displayName,\n    parentEmail: u.email,\n    triggeredByFamilyTier: true,\n  });\n  // Cross-SKU: child row created on kids Supabase project once consent granted (webhook).\n  await this.supa.from('family_links').insert({\n    parent_user_id: u.id, child_pending_consent_id: consentId, child_audience: audience, tenant_id: u.tenant_id,\n  });\n  return { consent_id: consentId, status: 'pending_consent' };\n}</code></pre>\n<pre><code class=\"language-sql\">-- migration\ncreate table public.family_links (\n  parent_user_id uuid not null references auth.users(id) on delete cascade,\n  child_user_id uuid,                            -- nullable until consent grants\n  child_pending_consent_id text,\n  child_audience text check (child_audience in ('under-13', '13+')),\n  screen_time_minutes_per_day int,\n  monthly_hearts_spend_cap int default 0,\n  ai_dialogue_disabled boolean default false,\n  friend_requests_require_approval boolean default true,\n  ads_disabled boolean default true,\n  paused_at timestamptz,\n  tenant_id text not null default 'mochi',\n  added_at timestamptz not null default now(),\n  primary key (parent_user_id, coalesce(child_user_id, child_pending_consent_id))\n);\nalter table public.family_links enable row level security;\ncreate policy \"family self\" on public.family_links for select using (parent_user_id = auth.uid());\n\ncreate table public.spend_approval_requests (\n  id text primary key,\n  parent_user_id uuid not null,\n  child_user_id uuid not null,\n  product_id text not null,\n  price_hearts int not null,\n  status text not null default 'pending' check (status in ('pending','approved','rejected','expired')),\n  expires_at timestamptz not null default (now() + interval '15 minutes'),\n  created_at timestamptz not null default now()\n);</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Family tier subscription ($9.99/mo) via Apple/Google works. Verified. <strong>AC2.</strong> Adding a child triggers TASK-AUTH-003 consent flow. Verified. <strong>AC3.</strong> Up to 5 children; 6th rejected. Verified. <strong>AC4.</strong> Screen-time cap enforced — over-cap launch shows \"time's up\". Verified. <strong>AC5.</strong> Spend cap default 0 — IAP attempt triggers parent approval push. Verified. <strong>AC6.</strong> Content filter (AI dialogue disabled) honoured even for 13+ child. Verified. <strong>AC7.</strong> Parental dashboard shows per-child summary. Verified. <strong>AC8.</strong> Pause child instant-suspends app access. Verified. <strong>AC9.</strong> Delete child triggers DSR-erasure. Verified. <strong>AC10.</strong> Friend-request approval queue functions. Verified. <strong>AC11.</strong> Cross-SKU family link works across Supabase projects. Verified. <strong>AC12.</strong> Spend approval 15-min expiry. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('TASK-SUB-002 — Family tier', () =&gt; {\n  it('enforces 5-child limit', async () =&gt; {\n    for (let i = 0; i &lt; 5; i++) await svc.addChild(parent, `child${i}`, 2018);\n    await expect(svc.addChild(parent, 'sixth', 2018)).rejects.toThrow(/child_limit/);\n  });\n\n  it('rejects child IAP without approval', async () =&gt; {\n    await mockChild('c1', { spend_cap_hearts: 0 });\n    await expect(iap.validateAndGrant(user('c1'), 'apple', 'tx-hearts')).rejects.toThrow(/parental_approval_required/);\n  });\n\n  it('content filter disables AI dialogue', async () =&gt; {\n    await mockChild('c1', { ai_dialogue_disabled: true, audience: '13+' });\n    const r = await ai.speak('petX', user('c1').id, 'idle');\n    expect(r.source_model).toBe('scripted_fallback');\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/sub/family/parental-dashboard.service.ts\n@Injectable()\nexport class ParentalDashboardService {\n  async childSummary(parentId: string, childId: string) {\n    const today = todayInUserRegion(parentId);\n    const [timeSpent, spend, friends, dialogueCount] = await Promise.all([\n      this.timeSpentMinutes(childId, today),\n      this.heartsSpent(childId, currentMonth()),\n      this.friendList(childId),\n      this.aiDialogueCount(childId, today),\n    ]);\n    return { childId, time_spent_today_min: timeSpent, hearts_spent_this_month: spend, friend_count: friends.length, ai_dialogues_today: dialogueCount };\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Apple Family Sharing API; Google Family Library API; TASK-AUTH-003 Safe Harbor vendor. <strong>Internal:</strong> TASK-SUB-001 (Pet+ baseline), TASK-AUTH-003 (parental consent), TASK-AI-001/002 (content filter override), TASK-LEGAL-001 (DSR + DPO). <strong>Blocks:</strong> none.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">GET /v1/sub/family/state\n→ 200\n{\n  \"tier\": \"family\",\n  \"children\": [\n    { \"id\": \"01HU...\", \"display_name\": \"Anh\", \"audience\": \"under-13\", \"screen_time_today_min\": 14, \"hearts_spent_this_month\": 0, \"spend_cap\": 0, \"ai_dialogue_disabled\": false, \"paused\": false },\n    ...\n  ]\n}</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"sub.family.child_added\", \"parent_id\": \"01HU...\", \"child_pending_consent_id\": \"01HCNST...\", \"audience\": \"under-13\" }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"sub.family.spend_approved\", \"parent_id\": \"01HU...\", \"child_id\": \"01HU...\", \"product_id\": \"mochi.hearts.30\", \"price_hearts\": 30 }</code></pre>\n<pre><code class=\"language-http\">POST /v1/sub/family/spend-approval/01HSPND.../approve\n→ 200 { \"request_id\": \"01HSPND...\", \"status\": \"approved\", \"valid_until\": \"...\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> 5 vs more children? → §1.1 — 5 + Apple/Google limits.</li><li><strong>OQ-2 (resolved):</strong> Default spend cap? → §1.7 — 0 (default-safe).</li><li><strong>OQ-3 (resolved):</strong> AI dialogue override for 13+? → §1.8 — yes, parent's call.</li><li><strong>OQ-4 (resolved):</strong> Cross-SKU family link? → §1.13 — yes.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Parental consent flow times out</td><td>Apple webhook delay</td><td>Pending state</td><td>Surface \"Resend consent email\"</td></tr><tr><td>2</td><td>Cross-SKU token forge</td><td>RLS + token sign</td><td>Privacy</td><td>Tighten signing</td></tr><tr><td>3</td><td>Spend approval push not received</td><td>Polling fallback</td><td>Approval pending</td><td>UX surfaces \"Check your other device\"</td></tr><tr><td>4</td><td>Screen-time cap miscounts via timezone</td><td>Region-aware</td><td>Wrong cut-off</td><td>TZ-locked</td></tr><tr><td>5</td><td>Cascade delete on family unsubscribe leaves orphans</td><td>Audit</td><td>Data lingers</td><td>30-day delete grace</td></tr><tr><td>6</td><td>Family tier downgrade with active children</td><td>30-day grace</td><td>Children read-only</td><td>UX clarifies</td></tr><tr><td>7</td><td>AI dialogue filter bypass via direct API</td><td>RLS + override check</td><td>Blocked</td><td>Verified</td></tr><tr><td>8</td><td>DSR cascade child without parent confirm</td><td>Manual confirm UX</td><td>Avoids accidental</td><td>Pre-deletion warning</td></tr><tr><td>9</td><td>Friend approval queue grows unbounded</td><td>Cap notifications</td><td>Backlog</td><td>Per-child cap</td></tr><tr><td>10</td><td>Apple Family Sharing approval skipped</td><td>OS-level</td><td>Our pre-emptive catches</td><td>Two-layer</td></tr><tr><td>11</td><td>Audit retention</td><td>DPO</td><td>7-yr</td><td>Configured</td></tr><tr><td>12</td><td>Locale missing parental copy</td><td>EN fallback</td><td>Mild</td><td>i18n batch</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 6 (Family tier), plan §PART 8 (COPPA + parental dashboard).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Specific dashboard UI design + DPO copy review.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Family tier without per-child consent.</li><li>Real-money child spend without approval.</li><li>Cross-tenant family.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-SUB-001 baseline; TASK-AUTH-003 consent; TASK-AI-002 filter; TASK-VIRAL-005 push.</p>";
