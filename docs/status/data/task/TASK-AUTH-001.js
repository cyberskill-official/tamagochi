window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AUTH-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Apple Sign-In on iOS.</strong> The iOS app MUST integrate Apple Sign-In via the <code>ASAuthorizationAppleIDProvider</code> API (Authentication Services framework). The native Swift bridge at <code>apps/cocos/native/ios/AppleSignInBridge.swift</code> MUST: (a) request <code>[.fullName, .email]</code> scopes on first sign-in; (b) deliver the Apple ID token to Cocos via the documented native-to-JS bridge; (c) handle the \"Hide my email\" private relay path correctly (treat relay emails as the canonical email).</p>\n<p>§1.2  <strong>Google Sign-In on Android.</strong> The Android app MUST integrate Google Sign-In via <code>com.google.android.gms:play-services-auth</code> v21+. The native Kotlin bridge at <code>apps/cocos/native/android/GoogleSignInBridge.kt</code> MUST: (a) request <code>email</code> + <code>profile</code> scopes; (b) return the Google ID token to Cocos via the bridge; (c) handle credential-fail / cancellation cleanly.</p>\n<p>§1.3  <strong>Cross-platform sign-in on web.</strong> The WebGL build MUST integrate <strong>Google Identity Services JS</strong> (<code>google.accounts.id</code>) for Google sign-in. Apple Sign-In on web MUST be available via the JS SDK (Apple's <code>appleid.auth.js</code>). Both flows MUST return ID tokens to Cocos via a documented <code>WebSocialSignIn.ts</code> adapter.</p>\n<p>§1.4  <strong>Supabase ID-token exchange.</strong> The Cocos client MUST exchange the provider ID token for a Supabase session via <code>supabase.auth.signInWithIdToken({ provider, token, nonce })</code>. The exchange MUST happen client-side (Supabase Auth handles the verification + session issuance). The resulting Supabase JWT MUST be persisted in the Cocos <code>localStorage</code> (web) or platform-secure storage (iOS Keychain / Android EncryptedSharedPreferences).</p>\n<p>§1.5  <strong>Nonce + replay protection.</strong> Apple's ID token includes a <code>nonce</code> claim; the client MUST generate a CSPRNG nonce, pass it to Apple's sign-in call, and pass it to Supabase's <code>signInWithIdToken</code>. Supabase Auth verifies the nonce server-side.</p>\n<p>§1.6  <strong>Region of record derivation.</strong> Per TASK-LEGAL-001 §1.11, on first successful sign-in the API MUST derive <code>region_of_record</code> using the deterministic chain: explicit user choice → Apple/Google account region (from the ID token's <code>country</code> claim where available) → fallback IP geo → \"confirm your country\" gate. The derived region MUST be persisted to <code>public.app_users.region_of_record</code>.</p>\n<p>§1.7  <strong>Policy version stamp.</strong> On first successful sign-in (and on every subsequent sign-in if the policy version has changed), the API MUST stamp <code>public.app_users.policy_version</code> with the current <code>tamagochi-privacy-en-v&lt;X.Y.Z&gt;</code> git-tagged version. A version bump on a material change MUST trigger consent re-confirmation per TASK-LEGAL-001 AC11.</p>\n<p>§1.8  <strong><code>audience_age_gate</code> resolution at sign-in time.</strong> The API MUST resolve <code>audience_age_gate</code> based on the SKU the sign-in is happening through: if the request is from the kids SKU bundle ID (verified via the JWT custom claim <code>sku_origin</code> set by build-target injection per TASK-LEGAL-003 §1.3), <code>audience_age_gate = \"under-13\"</code>; otherwise <code>audience_age_gate = \"13+\"</code>.</p>\n<p>§1.9  <strong>No account commingling across SKUs.</strong> If a user signs in on the standard SKU with an Apple/Google identity that has previously signed into the kids SKU, the API MUST refuse the sign-in with a clear error message (\"This account is registered as a children's account. Please use the Tamagochi Kids app to sign in.\") and surface the graduation flow (TASK-INFRA-003 §1.13) as the only path between SKUs.</p>\n<p>§1.10  <strong>Sign-out flow.</strong> A <code>POST /v1/auth/sign-out</code> endpoint MUST revoke the Supabase session AND emit an audit-log row. The Cocos client MUST clear the locally persisted JWT.</p>\n<p>§1.11  <strong>Token refresh handling.</strong> Supabase issues access tokens with default 1-hour lifetime + refresh tokens. The Cocos client's <code>AuthService.ts</code> MUST call <code>supabase.auth.refreshSession()</code> proactively when the access token is within 5 minutes of expiry. On refresh failure (refresh token revoked or expired), the client MUST gracefully return to the sign-in screen with state preserved.</p>\n<p>§1.12  <strong>Native-bridge security.</strong> Tokens MUST NEVER cross the native-bridge boundary as plain string. The Swift / Kotlin bridge MUST serialise tokens as a JSON object with a <code>bridge_version</code> field; Cocos MUST validate the version before consuming. A mismatched bridge version triggers a forced app update prompt.</p>\n<p>§1.13  <strong>Disable email/password for consumer SKU.</strong> Supabase's email/password sign-in MUST be disabled in both project configs (<code>config.toml</code> <code>[auth.email] enable_signup = false</code>). Magic-link fallback (for desktop test rigs) MAY be enabled for the standard project only via service-role API in development; production MUST disable it.</p>\n<p>§1.14  <strong>Observability.</strong> Every sign-in attempt MUST emit <code>auth.signin.attempt { provider, sku, outcome }</code>. Failures emit <code>auth.signin.failure { provider, sku, error_class }</code>. Successful sign-ins write to <code>auth_audit_log</code> table.</p>\n<p>§1.15  <strong>Apple Sign-In privacy compliance.</strong> Apple Sign-In is REQUIRED by Apple App Store Review if any third-party social sign-in is offered. The standard SKU offers Google + Apple; the kids SKU offers neither directly (parental consent flow per TASK-AUTH-003 is the only entry).</p>\n<p>§1.16  <strong>Edge case: Apple \"Hide my email.\"</strong> When Apple returns a relay email (<code>xyz@privaterelay.appleid.com</code>), the API MUST persist it as the canonical email and respect Apple's relay deliverability rules (only send via the registered sender domain). The relay email MUST NEVER be merged with a non-relay email for the same user.</p>\n<p>§1.17  <strong>Cocos native bridge test seam.</strong> The <code>AuthService.ts</code> MUST have a <code>setNativeBridgeForTest()</code> seam so unit tests can mock the bridge. Production builds reject this seam via <code>process.env.NODE_ENV !== 'production'</code> check.</p>\n<p>§1.18  <strong>Sign-in latency budget.</strong> P95 sign-in latency (tap \"Sign in with Apple/Google\" → app-ready state) MUST be ≤ 3 seconds on a mid-tier device + 4G network. Sentry transactions tag <code>auth.signin.latency_ms</code>.</p>\n<p>§1.19  <strong>Account-deletion path.</strong> Per Apple App Store policy (Guideline 5.1.1(v)) any app offering account creation MUST offer in-app account deletion. A <code>POST /v1/auth/delete-account</code> endpoint MUST: (a) verify the user; (b) open a 30-day grace window (configurable); (c) emit an audit row; (d) at grace expiry, hard-delete via DSR-erasure flow (TASK-LEGAL-001 §1.9).</p>\n<p>§1.20  <strong>Multi-device session sharing.</strong> A user signing in on two devices MUST see consistent pet state via the Colyseus presence layer (TASK-INFRA-002 §1.4). Multi-device sessions are NOT mutually exclusive — both can play concurrently, with Colyseus reconciliation.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why Supabase Auth's <code>signInWithIdToken</code> flow.</strong> Supabase Auth handles ID-token verification against Apple/Google JWKS endpoints + issues its own Supabase JWT in one round-trip. Building this ourselves would duplicate verification logic and add a security-review surface. The flow is OAuth-compliant.</p>\n<p><strong>Why platform-native sign-in instead of OAuth web redirect.</strong> Apple Guideline 4.8 requires the system Apple Sign-In sheet (not a webview) when offered. Google's recommendation is the Identity Services SDK on mobile (better consent UX, faster). The web build uses the JS SDK which is the canonical web flow.</p>\n<p><strong>Why disable email/password.</strong> Passwordless authentication is the modern UX standard for consumer mobile apps. Email/password adds breach-blast-radius (every leak is a credential leak), reset flows, password-hint UX, and CAPTCHA pressure. Apple Sign-In + Google + Zalo (TASK-AUTH-002) cover ~99% of the target VN/global audience.</p>\n<p><strong>Why a CSPRNG nonce for Apple.</strong> Apple's ID token replay protection requires the relying party to seed a nonce. Skipping it is technically valid for Apple but breaks Supabase's verification requirements.</p>\n<p><strong>Why \"Hide my email\" handled explicitly.</strong> Apple relay emails (<code>*@privaterelay.appleid.com</code>) are first-class user identities under Apple's privacy framework. Treating them as second-class causes UX confusion (a re-sign-in maps to a new account) and Apple Review rejection.</p>\n<p><strong>Why no SKU commingling.</strong> Per TASK-LEGAL-001 + TASK-INFRA-003 §1.1, two databases means a user's identity is partitioned by SKU. The explicit refusal message + graduation flow prevents silent data spread.</p>\n<p><strong>Why proactive token refresh.</strong> A token expiring mid-feed (in the middle of a Colyseus session) drops the WebSocket and resets state. Refreshing 5 min ahead of expiry keeps sessions stable.</p>\n<p><strong>Why native-bridge versioning.</strong> Native bridges drift when the app is reinstalled at a different version. A version mismatch is a real cause of \"infinite loading\" support tickets.</p>\n<p><strong>Why account-deletion in-app required.</strong> Apple Guideline 5.1.1(v) enforced since June 2022 — non-compliance is a hard rejection.</p>\n<p><strong>Why ≤ 3 s P95 sign-in.</strong> Mobile-game soft-launch retention is sensitive to sign-in friction. Above 3 s users drop. Sentry transactions catch regressions.</p>\n<p><strong>Why multi-device sessions are concurrent.</strong> PetPair (TASK-SOCIAL-002) explicitly requires co-parent multi-device. Forcing single-device login would block PetPair. Colyseus presence + room reconciliation handles consistency.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Cocos AuthService</h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/auth/AuthService.ts\nimport { createClient, SupabaseClient } from '@supabase/supabase-js';\n\nexport class AuthService {\n  private supa: SupabaseClient;\n  constructor(opts: { supabaseUrl: string; anonKey: string }) {\n    this.supa = createClient(opts.supabaseUrl, opts.anonKey, {\n      auth: { persistSession: true, storageKey: 'tamagochi.auth' },\n    });\n  }\n\n  async signInWithApple(token: string, nonce: string): Promise&lt;void&gt; {\n    const { data, error } = await this.supa.auth.signInWithIdToken({\n      provider: 'apple', token, nonce,\n    });\n    if (error) throw error;\n    await this.afterSignIn(data.session!.access_token);\n  }\n\n  async signInWithGoogle(token: string): Promise&lt;void&gt; {\n    const { data, error } = await this.supa.auth.signInWithIdToken({\n      provider: 'google', token,\n    });\n    if (error) throw error;\n    await this.afterSignIn(data.session!.access_token);\n  }\n\n  private async afterSignIn(jwt: string): Promise&lt;void&gt; {\n    await fetch(`${process.env.API_URL}/v1/auth/after-signin`, {\n      method: 'POST',\n      headers: { Authorization: `Bearer ${jwt}`, 'content-type': 'application/json' },\n      body: JSON.stringify({ sku_origin: __BUILD_TARGET__ }),\n    });\n  }\n\n  async refreshIfStale(): Promise&lt;void&gt; {\n    const { data: { session } } = await this.supa.auth.getSession();\n    if (!session) return;\n    const expiresInSec = session.expires_at! - Math.floor(Date.now() / 1000);\n    if (expiresInSec &lt; 300) await this.supa.auth.refreshSession();\n  }\n}</code></pre>\n<h3>3.2 — Apple Swift bridge</h3>\n<pre><code class=\"language-swift\">// apps/cocos/native/ios/AppleSignInBridge.swift\nimport AuthenticationServices\n\n@objc class AppleSignInBridge: NSObject, ASAuthorizationControllerDelegate {\n  static let shared = AppleSignInBridge()\n  private var pendingNonce: String?\n\n  @objc func signIn(_ nonce: String) {\n    self.pendingNonce = nonce\n    let req = ASAuthorizationAppleIDProvider().createRequest()\n    req.requestedScopes = [.fullName, .email]\n    req.nonce = sha256(nonce)\n    let ctrl = ASAuthorizationController(authorizationRequests: [req])\n    ctrl.delegate = self\n    ctrl.performRequests()\n  }\n\n  func authorizationController(controller: ASAuthorizationController,\n    didCompleteWithAuthorization auth: ASAuthorization) {\n    guard let cred = auth.credential as? ASAuthorizationAppleIDCredential,\n          let tokenData = cred.identityToken,\n          let token = String(data: tokenData, encoding: .utf8) else { return }\n    CocosBridge.emit(\"apple.signin.success\", [\n      \"bridge_version\": \"1\",\n      \"token\": token,\n      \"nonce\": pendingNonce ?? \"\",\n    ])\n  }\n}</code></pre>\n<h3>3.3 — Auth controller (Nest)</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/auth/auth.controller.ts\n@Controller('v1/auth')\nexport class AuthController {\n  constructor(private readonly svc: AuthService) {}\n\n  @UseGuards(SupabaseJwtGuard)\n  @Post('after-signin')\n  async afterSignIn(@CurrentUser() u: AuthedUser, @Body() body: { sku_origin: 'kids' | 'standard' }) {\n    return this.svc.afterSignIn(u, body.sku_origin);\n  }\n\n  @UseGuards(SupabaseJwtGuard)\n  @Post('sign-out')\n  async signOut(@CurrentUser() u: AuthedUser) {\n    return this.svc.signOut(u);\n  }\n\n  @UseGuards(SupabaseJwtGuard)\n  @Post('delete-account')\n  async deleteAccount(@CurrentUser() u: AuthedUser) {\n    return this.svc.openAccountDeletion(u);\n  }\n}</code></pre>\n<h3>3.4 — Region derivation service</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/auth/region-derivation.service.ts\n@Injectable()\nexport class RegionDerivationService {\n  async derive(ctx: {\n    explicit?: string;\n    appleCountry?: string;\n    googleCountry?: string;\n    zaloCountry?: string;\n    ip?: string;\n  }): Promise&lt;{ region: string; source: string; needsConfirmation: boolean }&gt; {\n    if (ctx.explicit)      return { region: ctx.explicit,      source: 'explicit',  needsConfirmation: false };\n    if (ctx.appleCountry)  return { region: ctx.appleCountry,  source: 'apple',     needsConfirmation: false };\n    if (ctx.googleCountry) return { region: ctx.googleCountry, source: 'google',    needsConfirmation: false };\n    if (ctx.zaloCountry)   return { region: ctx.zaloCountry,   source: 'zalo',      needsConfirmation: false };\n    if (ctx.ip) {\n      const geo = await this.geoFromIp(ctx.ip);\n      return { region: geo, source: 'ip', needsConfirmation: true };\n    }\n    return { region: 'unknown', source: 'fallback', needsConfirmation: true };\n  }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Apple Sign-In on iOS produces a Supabase session. Verified by Detox UI test on iOS Simulator with the Apple sandbox account.</p>\n<p><strong>AC2.</strong> Google Sign-In on Android produces a Supabase session. Verified by Detox UI test with a test Google account.</p>\n<p><strong>AC3.</strong> Web Google Sign-In produces a Supabase session in the WebGL build. Verified by Playwright headed test.</p>\n<p><strong>AC4.</strong> Apple relay emails are persisted as canonical email; a subsequent Apple sign-in with the same relay does not create a new account. Verified by <code>__tests__/auth.spec.ts</code>.</p>\n<p><strong>AC5.</strong> Sign-in on the standard SKU with an identity registered in the kids SKU returns HTTP 409 with the graduation message. Verified by <code>__tests__/auth.spec.ts</code> with a pre-seeded kids account.</p>\n<p><strong>AC6.</strong> Token refresh fires when &lt; 5 min remaining. Verified by <code>__tests__/AuthService.spec.ts</code> with fake timers.</p>\n<p><strong>AC7.</strong> Account-deletion endpoint opens a 30-day grace window and emits an audit row. Verified by <code>__tests__/auth.spec.ts</code>.</p>\n<p><strong>AC8.</strong> Region-of-record derivation chain returns the highest-priority signal. Verified by <code>__tests__/region-derivation.spec.ts</code> with each branch.</p>\n<p><strong>AC9.</strong> Policy-version stamp updates on sign-in when version has changed since last sign-in. Verified by <code>__tests__/auth.spec.ts</code> rotating policy_version mid-test.</p>\n<p><strong>AC10.</strong> P95 sign-in latency on iOS Simulator + Android Emulator ≤ 3 s. Verified by Sentry transaction sampling on the test cohort.</p>\n<p><strong>AC11.</strong> Bridge-version mismatch triggers a force-update prompt. Verified by <code>__tests__/AuthService.spec.ts</code> with a mocked bridge emitting <code>bridge_version: \"999\"</code>.</p>\n<p><strong>AC12.</strong> Email/password sign-up is disabled in both Supabase project configs. Verified by reading <code>config.toml</code> <code>enable_signup = false</code>.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 — AuthService unit test (token refresh)</h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/auth/__tests__/AuthService.spec.ts\nimport { describe, it, expect, vi } from 'vitest';\nimport { AuthService } from '../AuthService';\n\ndescribe('TASK-AUTH-001 §1.11 — proactive token refresh', () =&gt; {\n  it('refreshes when &lt; 5 min remaining', async () =&gt; {\n    const svc = new AuthService({ supabaseUrl: 'http://x', anonKey: 'y' });\n    const expiresAt = Math.floor(Date.now() / 1000) + 200;  // 200 sec\n    vi.spyOn(svc['supa'].auth, 'getSession').mockResolvedValue({ data: { session: { expires_at: expiresAt } } } as any);\n    const spy = vi.spyOn(svc['supa'].auth, 'refreshSession').mockResolvedValue({} as any);\n    await svc.refreshIfStale();\n    expect(spy).toHaveBeenCalled();\n  });\n});</code></pre>\n<h3>5.2 — Region derivation unit test</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/auth/__tests__/region-derivation.spec.ts\nimport { describe, it, expect } from 'vitest';\nimport { RegionDerivationService } from '../region-derivation.service';\n\ndescribe('TASK-AUTH-001 §1.6 — region derivation', () =&gt; {\n  const svc = new RegionDerivationService();\n\n  it.each([\n    { explicit: 'VN',                   expected: 'VN',  source: 'explicit' },\n    { appleCountry: 'US',                expected: 'US',  source: 'apple' },\n    { googleCountry: 'PH',               expected: 'PH',  source: 'google' },\n    { zaloCountry: 'VN',                 expected: 'VN',  source: 'zalo' },\n  ])('returns $expected from $source signal', async (ctx) =&gt; {\n    const r = await svc.derive(ctx as any);\n    expect(r.region).toBe(ctx.expected);\n    expect(r.source).toBe(ctx.source);\n  });\n\n  it('falls back to IP geo with needsConfirmation', async () =&gt; {\n    const r = await svc.derive({ ip: '203.0.113.5' });\n    expect(r.needsConfirmation).toBe(true);\n    expect(r.source).toBe('ip');\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/auth/auth.service.ts\n@Injectable()\nexport class AuthService {\n  constructor(\n    private readonly users: AppUsersRepo,\n    private readonly region: RegionDerivationService,\n    private readonly policy: PolicyVersionService,\n    private readonly audit: AuditLogService,\n  ) {}\n\n  async afterSignIn(u: AuthedUser, skuOrigin: 'kids' | 'standard') {\n    const expectedSku = u.tenant_id === 'mochi' &amp;&amp; skuOrigin === 'kids' ? 'kids' : 'standard';\n    const existing = await this.users.findByAppleOrGoogleId(u.providerId);\n    if (existing &amp;&amp; existing.audience_age_gate === 'under-13' &amp;&amp; skuOrigin !== 'kids') {\n      throw new HttpException({\n        error: 'account_registered_as_child', graduation_url: '/graduation',\n      }, HttpStatus.CONFLICT);\n    }\n    const region = await this.region.derive({\n      appleCountry: u.appleCountry, googleCountry: u.googleCountry, ip: u.ip,\n    });\n    await this.users.upsert({\n      id: u.id,\n      tenant_id: 'mochi',\n      display_name: u.displayName ?? `Player-${u.id.slice(0, 6)}`,\n      region_of_record: region.region,\n      audience_age_gate: skuOrigin === 'kids' ? 'under-13' : '13+',\n      policy_version: await this.policy.current(),\n    });\n    await this.audit.write({ who: u.id, what: 'auth.signin.success', source: skuOrigin });\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Apple Developer account (Sign in with Apple capability enabled). Google Cloud OAuth client (Android + Web client IDs). Supabase Auth (both projects).</p>\n<p><strong>Internal:</strong> TASK-INFRA-003 (Supabase + RLS templates).</p>\n<p><strong>Blocks:</strong> TASK-AUTH-002 (Zalo follows the same shape), TASK-AUTH-003 (kid invite-code uses the consent flow which is keyed off this task's account creation), TASK-SUB-001 (Pet+ subscription is tied to the user id created here), TASK-B2B-003 (PetOS console SSO bridge piggybacks on this scaffold).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — <code>POST /v1/auth/after-signin</code></h3>\n<pre><code class=\"language-http\">POST /v1/auth/after-signin\nAuthorization: Bearer eyJ... (Supabase JWT)\nContent-Type: application/json\n\n{ \"sku_origin\": \"standard\" }\n\n→ 200 OK\n{\n  \"user_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"region_of_record\": \"VN\",\n  \"region_source\": \"apple\",\n  \"audience_age_gate\": \"13+\",\n  \"needs_region_confirmation\": false\n}</code></pre>\n<h3>8.2 — Cross-SKU sign-in refusal</h3>\n<pre><code class=\"language-http\">→ 409 Conflict\n{\n  \"error\": \"account_registered_as_child\",\n  \"message\": \"This account is registered as a children's account. Please use the Tamagochi Kids app to sign in.\",\n  \"graduation_url\": \"https://tamagochi.app/graduation\"\n}</code></pre>\n<h3>8.3 — Apple bridge payload</h3>\n<pre><code class=\"language-json\">{\n  \"bridge_version\": \"1\",\n  \"token\": \"eyJraWQiOiJlNkVN... (Apple ID token)\",\n  \"nonce\": \"8b3c5d7f2a1e9b6d4c2a3e5f9b7d5a3c\"\n}</code></pre>\n<h3>8.4 — Account-deletion request</h3>\n<pre><code class=\"language-http\">POST /v1/auth/delete-account\nAuthorization: Bearer eyJ...\n→ 202 Accepted\n{\n  \"delete_at\": \"2026-09-12T14:36:01Z\",\n  \"grace_days\": 30,\n  \"audit_ref\": \"01HC7QG...\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Email/password fallback for desktop test rigs? → §1.13 — magic-link in dev only.</li><li><strong>OQ-2 (resolved):</strong> Facebook / Twitter Sign-In? → §<code>disallowed_tools</code> — deferred.</li><li><strong>OQ-3 (resolved):</strong> Single-session-only model? → §1.20 — multi-device concurrent.</li><li><strong>OQ-4 (resolved):</strong> Apple relay-email handling? → §1.16 + §2 — treat as canonical email.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Apple Sign-In service outage</td><td><code>auth.signin.failure { provider: apple }</code> spike</td><td>iOS users blocked</td><td>Surface fallback to Google sign-in; in VN, fallback to Zalo (TASK-AUTH-002)</td></tr><tr><td>2</td><td>Google Sign-In service outage</td><td>failure metric spike</td><td>Android users blocked</td><td>Surface alternative provider; status page update</td></tr><tr><td>3</td><td>Supabase Auth outage</td><td><code>auth.signinWithIdToken</code> errors</td><td>All sign-ins blocked</td><td>Maintain in-memory session cache for already-signed-in users; surface \"maintenance\" splash</td></tr><tr><td>4</td><td>Native bridge version mismatch</td><td>Bridge emits unknown <code>bridge_version</code></td><td>Force-update prompt</td><td>Reinstall flow; clear local Auth state on next launch</td></tr><tr><td>5</td><td>Token refresh fails (refresh token revoked)</td><td><code>auth.refreshSession</code> error</td><td>User returned to sign-in screen</td><td>Preserve in-flight game state via Colyseus reconciliation</td></tr><tr><td>6</td><td>Cross-SKU sign-in attempt</td><td>409 response code</td><td>User shown graduation page</td><td>Surface graduation flow with parental approval link</td></tr><tr><td>7</td><td>Apple relay email leak / merge</td><td>UI test detects merged account</td><td>Wrong user data exposed</td><td>Re-partition account; manual DPO escalation</td></tr><tr><td>8</td><td>Account deletion not completing at 30-day grace</td><td>Daily cron checks</td><td>Stale deletion ticket</td><td>Manual hard-delete via DSR-erasure path</td></tr><tr><td>9</td><td>Region of record indeterminate (no Apple/Google/Zalo/IP)</td><td><code>region.indeterminate</code> event</td><td>Forced \"confirm country\" gate</td><td>Block app features until user confirms; UX path documented</td></tr><tr><td>10</td><td>Policy version git tag missing</td><td>Sign-in fails with <code>policy.version.missing</code></td><td>Sign-in pipeline breaks</td><td>DPO escalation; tag the missing version; release blocker</td></tr><tr><td>11</td><td>Sign-in latency &gt;3s P95</td><td>Sentry transaction metric</td><td>UX regression</td><td>Investigate Apple/Google ID-token verification slowdown; cache JWKS aggressively</td></tr><tr><td>12</td><td>Apple Sign-In privacy policy URL drift</td><td>Apple Review rejection</td><td>Submission blocked</td><td>Update policy URL; re-submit</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 4 — auth providers (Apple + Google + Zalo + under-13 invite-code-only); plan §PART 8 — COPPA-2025 separate SKU + region of record.</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Apple sign-in service ID + Google OAuth client IDs locked in <code>infra/supabase/*/config.toml</code> annex.</li><li>Account-deletion grace-window length: 30 days default; tunable for legal-counsel jurisdictional advice.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Webview-based Apple Sign-In (Apple Guideline 4.8).</li><li>Embedding service_role key client-side.</li><li>Single-session-only model (would break PetPair co-parent in P2).</li><li>Merging Apple relay email with non-relay email for same user.</li></ul>\n<p><strong>Cross-reference:</strong> This task is the consumer-SKU authentication keystone. TASK-AUTH-002 (Zalo) follows the same shape with Zalo-specific quirks. TASK-AUTH-003 (kid invite-code) layers parental consent on top.</p>";
