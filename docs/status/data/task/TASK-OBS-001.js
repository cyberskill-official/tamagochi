window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OBS-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Standard-SKU SDK stack.</strong> The standard SKU MUST integrate: (a) <strong>GameAnalytics</strong> for f2p funnel + economy; (b) <strong>Mixpanel</strong> for product analytics + feature flags (Amplitude is a deferred alternative); (c) <strong>AppsFlyer</strong> for UA attribution (Adjust deferred alternative); (d) <strong>Sentry</strong> for client + server errors; (e) <strong>Better Stack</strong> for uptime monitoring + structured log aggregation.</p>\n<p>§1.2  <strong>Kids-SKU SDK stack — radically reduced.</strong> The kids SKU MUST integrate ONLY: (a) <strong>GameAnalytics</strong> with <code>consent_mode: \"kids_safe\"</code> (no behavioural-ID tracking); (b) <strong>Sentry</strong> for crash reporting (Sentry's <code>sendDefaultPii: false</code> mode). No Mixpanel / Amplitude / AppsFlyer / Adjust / Firebase. Verified at binary level by TASK-LEGAL-003 §1.10 inspection script.</p>\n<p>§1.3  <strong>SDK allow-list config.</strong> <code>apps/cocos/src/__sdk-allowlist/kids.json</code> and <code>apps/cocos/src/__sdk-allowlist/standard.json</code> MUST enumerate the SDK packages and their version pins. The build script (TASK-INFRA-001 §1.4) MUST consume the SKU's allow-list and reject the build if any imported package is NOT on the allow-list for that SKU.</p>\n<p>§1.4  <strong>Event taxonomy.</strong> A canonical event taxonomy MUST live at <code>docs/obs/event-taxonomy.md</code> covering: <code>auth.*</code>, <code>pet.*</code>, <code>care.*</code>, <code>ai.*</code>, <code>ar.*</code>, <code>viral.*</code>, <code>social.*</code>, <code>econ.*</code>, <code>sub.*</code>, <code>ad.*</code>, <code>legal.*</code>, <code>security.*</code>, <code>infra.*</code>. Every event name MUST follow <code>&lt;module&gt;.&lt;action&gt;.&lt;outcome&gt;</code> snake-case format. New event names require an task amendment.</p>\n<p>§1.5  <strong>Schema validation.</strong> Every emitted event payload MUST validate against a schema in <code>apps/api/src/obs/event-schemas/&lt;event-name&gt;.zod.ts</code>. Server-side, the API's <code>EventBus.emit</code> MUST run schema validation; failures MUST emit <code>obs.event.schema.invalid</code> Sentry event and drop the event payload.</p>\n<p>§1.6  <strong>Server-side first, client mirror second.</strong> Where a state-changing event happens both client and server (e.g. <code>care.feed.completed</code>), the server-emitted event is the source of truth. The client emits an analytics event for UX-funnel purposes but MUST tag it with a <code>client_emitted_at</code> so the analytics pipeline can dedupe against the server <code>emitted_at</code>.</p>\n<p>§1.7  <strong>PII-scrubbing.</strong> Sentry MUST be configured with explicit PII scrubbers stripping: <code>parent_email</code>, <code>parental_consent.*</code> rows, <code>display_name</code> (unless <code>display_name_consent: true</code>), <code>device_id</code> (under-13). The scrubber config MUST be loaded from <code>apps/api/src/obs/sentry-pii-scrub.ts</code> and unit-tested.</p>\n<p>§1.8  <strong>Sentry release tagging.</strong> Every release MUST tag Sentry with the <code>git rev</code> + a human-readable version (e.g. <code>tamagochi@1.0.3-rc.2+abc1234</code>). Source maps for the web build MUST be uploaded to Sentry on every CI deploy (per TASK-INFRA-001 §1.17).</p>\n<p>§1.9  <strong>Sentry transaction sampling.</strong> Backend transactions sample at 5% by default; auth-flow transactions sample at 50% (lower volume); error transactions sample at 100%. Per-tenant tail sampling (B2B in P4) is deferred to TASK-OBS-002.</p>\n<p>§1.10  <strong>Mixpanel feature flags.</strong> Mixpanel's feature-flag service MUST gate every non-trivial change to gameplay (e.g. mini-game payout adjustments, AI personality tuning). Feature flags MUST be referenced via a <code>useFeatureFlag(flagName, defaultValue)</code> Cocos hook (or <code>serverFeatureFlag.value(name, defaultValue, userId)</code> server-side). All flags MUST have a default value that allows rollout without flag-service availability.</p>\n<p>§1.11  <strong>AppsFlyer install attribution.</strong> AppsFlyer SDK MUST be initialised on first launch (standard SKU only); install events MUST tag the campaign ID + media-source + agency. Postback to AppsFlyer's API for in-app events (sub_start, iap_complete) MUST be server-driven (not client) to prevent fraud.</p>\n<p>§1.12  <strong>GameAnalytics economy events.</strong> GameAnalytics MUST receive <code>BusinessEvent</code>, <code>ResourceEvent</code>, <code>ProgressionEvent</code>, <code>DesignEvent</code> per its documented model. The economy taxonomy MUST cover all currency sources + sinks defined in TASK-ECON-001. The kids SKU sends only <code>DesignEvent</code> (no business — no IAP in kids SKU directly).</p>\n<p>§1.13  <strong>Better Stack uptime + log aggregation.</strong> Better Stack MUST monitor every public endpoint (<code>/healthz</code>, <code>/readyz</code>, <code>/v1/auth/*</code>, <code>/v1/dsr</code>) with 30-second checks. Logs from Nest API + Colyseus + Edge Functions MUST stream to Better Stack with structured JSON; PII-scrubbed before send.</p>\n<p>§1.14  <strong>Self-host PostHog as alternative.</strong> PostHog (self-host on AWS Singapore) MUST be set up as a Mixpanel alternative for situations where Mixpanel's pricing breaks the budget. The standard-SKU <code>Analytics.ts</code> MUST be dual-provider-compatible (config-driven). PostHog is the GameAnalytics analog if both Mixpanel and Amplitude become cost-prohibitive.</p>\n<p>§1.15  <strong>Cross-SKU workspace separation.</strong> Each SKU (kids + standard) AND each B2B tenant (P4) MUST have its own GameAnalytics + Mixpanel + AppsFlyer workspace. Workspace IDs MUST be stored in <code>infra/supabase/&lt;sku&gt;/config.toml</code>. No cross-workspace event leakage is permitted.</p>\n<p>§1.16  <strong><code>Analytics.ts</code> API surface.</strong> The Cocos client analytics surface MUST expose:</p>\n<pre><code>Analytics.track(eventName: string, props: Record&lt;string, unknown&gt;): void\nAnalytics.identify(userId: string, traits: Record&lt;string, unknown&gt;): void\nAnalytics.setSuperProperties(props: Record&lt;string, unknown&gt;): void\nAnalytics.reset(): void</code></pre>\n<p>On the kids SKU, <code>identify</code> MUST be a no-op when <code>parental_consent_state != \"granted\"</code>.</p>\n<p>§1.17  <strong>PII consent before identify.</strong> On the standard SKU, <code>Analytics.identify</code> MUST be deferred until: (a) user accepts the privacy-policy version; (b) a <code>policy_version</code> audit row is recorded. Identify call carries the <code>policy_version</code> as a super-property.</p>\n<p>§1.18  <strong>Grafana dashboards.</strong> Two Grafana dashboards MUST exist at scaffold time: <code>pet-care-funnel.json</code> (hatch → first-feed → 7-day-streak retention) and <code>auth-funnel.json</code> (signin → after-signin → first-pet-hatch). Both MUST be JSON-versioned in <code>infra/grafana/dashboards/</code> and re-importable on a fresh Grafana instance.</p>\n<p>§1.19  <strong>Critical-incident alerts.</strong> Sentry + Better Stack MUST page on:</p>\n<ul><li>error rate &gt; 1% on <code>/v1/auth/*</code> for 5 min;</li><li>error rate &gt; 5% anywhere for 2 min;</li><li>5xx on <code>/healthz</code> ≥ 3 in 5 min;</li><li>AppsFlyer attribution failure (no installs reported) for 1 hour;</li><li>COPPA-relevant events: <code>coppa.gate.bypass.attempt</code>, <code>coppa.consent.transition</code> (info only, manual review queue).</li></ul>\n<p>§1.20  <strong>Quarterly observability review.</strong> A scheduled task MUST fire quarterly to: (a) audit the event-taxonomy doc for orphaned events; (b) re-verify SDK allow-list parity (no new behavioural SDK leaked into kids); (c) verify Sentry PII scrubbers still active; (d) verify Grafana dashboards still rendering.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why three product-analytics tools (GameAnalytics + Mixpanel + AppsFlyer).</strong> Plan §PART 4 lays out the canonical f2p stack: GameAnalytics for economy + funnel (their schema is opinionated for games), Mixpanel for ad-hoc product questions (cohorts + funnels with arbitrary filters), AppsFlyer for UA attribution (no other vendor has comparable VN/SEA media-source coverage). Each addresses a different question; replacing one with another would lose value.</p>\n<p><strong>Why radically reduced SDK stack on kids SKU.</strong> Plan §PART 8 + TASK-LEGAL-001 §1.5 — COPPA-2025 redefines \"personal information\" to include persistent identifiers used for engagement. Behavioural analytics SDKs (Mixpanel, AppsFlyer, Adjust) all rely on persistent identifiers. The compliant kids analytics is GameAnalytics with <code>kids_safe</code> mode + Sentry with <code>sendDefaultPii: false</code>. Any other SDK on the kids binary is a regulator surface.</p>\n<p><strong>Why SDK allow-list as a JSON config + build-time enforcement.</strong> A policy in markdown is a recommendation; a JSON allow-list referenced by the build script is a CI-survivable rule. The kids SKU's allow-list is the only place we encode \"this is the list of SDKs that won't get us in trouble.\"</p>\n<p><strong>Why event taxonomy is canonical.</strong> Custom event naming + ad-hoc properties = analytics chaos within 6 months. A taxonomy doc + schema validation forces the discipline.</p>\n<p><strong>Why server-emitted events are source of truth.</strong> Client events can be lost (offline, app-killed mid-emit), tampered with (anti-cheat bypass), or duplicated (network retry). Server events run in a trusted environment with at-most-once + at-least-once semantics depending on the queue. For business-critical events (purchase, currency change), server is authoritative.</p>\n<p><strong>Why PII scrubbers are unit-tested.</strong> Sentry's default PII handling has historically had regressions (per Sentry's CHANGELOG). Unit-testing the scrubber config catches drift on Sentry SDK upgrade.</p>\n<p><strong>Why feature flags from day one.</strong> A f2p game tunes economy + retention via small experiments. Feature flags let the team try changes without rebuilds. Mixpanel is a single-vendor solution; PostHog is the self-host fallback.</p>\n<p><strong>Why Better Stack rather than Datadog.</strong> Datadog is great but expensive ($23+/host/month). Better Stack is dramatically cheaper for the soft-launch volume, has a free tier covering P0/P1, and matches Sentry's UX. Datadog can be added later if needed.</p>\n<p><strong>Why each B2B tenant gets its own analytics workspace.</strong> PetOS tenants (banks, telcos, FMCG) want their own analytics dashboards isolated from each other and from the consumer game. Workspace separation is a B2B sales requirement.</p>\n<p><strong>Why server-side AppsFlyer postback for in-app events.</strong> Client-side postback can be spoofed (a fraud farm makes fake \"iap_complete\" events to attribute fake installs). Server-side postback ties the event to a verified IAP receipt.</p>\n<p><strong>Why critical-incident alerts on auth &gt;1% specifically.</strong> Authentication is a leading indicator of broader outage. A 1% threshold catches partial-outage before player-complaint volume spikes.</p>\n<p><strong>Why quarterly observability review.</strong> Drift is the silent killer of observability. A scheduled task + checklist keeps the system honest.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Cocos <code>Analytics.ts</code></h3>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/obs/Analytics.ts\nimport { isKidsSku } from '../legal/build-target';\nimport { parentalConsentState } from '../legal/parental-consent';\n\nexport class Analytics {\n  static track(name: string, props: Record&lt;string, unknown&gt;): void {\n    if (isKidsSku() &amp;&amp; !this.isKidsAllowed(name)) return;   // §1.2 enforcement\n    gameAnalytics.send(name, props);\n    if (!isKidsSku()) mixpanel.track(name, props);\n  }\n\n  static identify(userId: string, traits: Record&lt;string, unknown&gt;): void {\n    if (isKidsSku() &amp;&amp; parentalConsentState() !== 'granted') return;   // §1.16\n    gameAnalytics.identify(userId);\n    if (!isKidsSku()) mixpanel.identify(userId);\n    if (!isKidsSku()) appsFlyer.setCustomerUserID(userId);\n  }\n\n  private static isKidsAllowed(name: string): boolean {\n    return name.startsWith('pet.') || name.startsWith('care.')\n        || name.startsWith('design.') || name.startsWith('auth.kids.');\n  }\n}</code></pre>\n<h3>3.2 — Event-schema example</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/obs/event-schemas/care.feed.completed.zod.ts\nimport { z } from 'zod';\n\nexport const CareFeedCompletedSchema = z.object({\n  pet_id: z.string().uuid(),\n  player_id: z.string().uuid(),\n  hunger_before: z.number().int().min(0).max(100),\n  hunger_after:  z.number().int().min(0).max(100),\n  food_item: z.enum(['basic','premium','seasonal']),\n  source: z.enum(['ui','co_parent_remote']),\n  emitted_at: z.string().datetime(),\n  client_emitted_at: z.string().datetime().optional(),\n});</code></pre>\n<h3>3.3 — Sentry PII scrub</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/obs/sentry-pii-scrub.ts\nimport * as Sentry from '@sentry/node';\n\nexport function configureSentry(opts: { dsn: string; release: string }) {\n  Sentry.init({\n    dsn: opts.dsn, release: opts.release, sendDefaultPii: false,\n    beforeSend(event) {\n      delete event.user?.email;\n      delete event.user?.ip_address;\n      if (event.extra) {\n        delete (event.extra as any).parent_email;\n        delete (event.extra as any).parental_consent;\n        delete (event.extra as any).device_id;\n      }\n      return event;\n    },\n    tracesSampleRate: 0.05,\n  });\n}</code></pre>\n<h3>3.4 — SDK allow-list shape</h3>\n<pre><code class=\"language-jsonc\">// apps/cocos/src/__sdk-allowlist/kids.json\n{\n  \"sku\": \"kids\",\n  \"allow\": {\n    \"gameanalytics\": { \"version\": \"5.x\" },\n    \"@sentry/react-native\": { \"version\": \"5.x\" }\n  },\n  \"deny\": [\n    \"mixpanel-react-native\", \"appsflyer-react-native-plugin\", \"@firebase/analytics\",\n    \"@firebase/messaging\", \"@react-native-firebase/crashlytics\",\n    \"@adjustcom/adjust-react-native\", \"react-native-applovin-max\",\n    \"react-native-google-mobile-ads\"\n  ]\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Standard SKU build links GameAnalytics + Mixpanel + AppsFlyer + Sentry. Verified by <code>__tests__/Analytics.spec.ts</code> + binary inspection.</p>\n<p><strong>AC2.</strong> Kids SKU build links ONLY GameAnalytics + Sentry. Verified by TASK-LEGAL-003 §1.10 binary inspection — <code>strings &lt;kids.ipa&gt; | grep -iE \"(Mixpanel|AppsFlyer|Adjust|AppLovin|IronSource|Firebase)\"</code> returns empty.</p>\n<p><strong>AC3.</strong> Server-side event schema validation rejects malformed events. Verified by <code>__tests__/event-schema.spec.ts</code> with a malformed payload.</p>\n<p><strong>AC4.</strong> Sentry PII scrub removes <code>parent_email</code>, <code>parental_consent</code>, <code>device_id</code> (under-13) from emitted events. Verified by <code>__tests__/sentry-pii-scrub.spec.ts</code> driving sample events.</p>\n<p><strong>AC5.</strong> <code>Analytics.identify</code> is a no-op on kids SKU when <code>parental_consent_state != \"granted\"</code>. Verified by <code>__tests__/Analytics.spec.ts</code>.</p>\n<p><strong>AC6.</strong> Sentry release tagging is correct: <code>release</code> field equals <code>tamagochi@&lt;semver&gt;+&lt;git-rev&gt;</code>. Verified by inspecting a Sentry test event.</p>\n<p><strong>AC7.</strong> AppsFlyer in-app event postback comes from the server, not the client. Verified by inspecting the AppsFlyer dashboard's source attribution.</p>\n<p><strong>AC8.</strong> Grafana dashboards <code>pet-care-funnel.json</code> + <code>auth-funnel.json</code> are JSON-versioned and re-importable. Verified by <code>pnpm grafana:import</code> against a test Grafana instance.</p>\n<p><strong>AC9.</strong> Critical-incident alerts (§1.19) fire in a synthetic chaos test. Verified by <code>__tests__/chaos/auth-error-spike.spec.ts</code>.</p>\n<p><strong>AC10.</strong> Feature-flag default returns when Mixpanel is unreachable. Verified by <code>__tests__/feature-flag-fallback.spec.ts</code>.</p>\n<p><strong>AC11.</strong> Quarterly observability-review scheduled task is created on 2026-08-17, 2026-11-17, etc. Verified by inspecting scheduled-task list.</p>\n<p><strong>AC12.</strong> Cross-SKU event isolation — events emitted by the kids SKU never appear in the standard SKU Mixpanel workspace. Verified by reviewing workspace event volumes after a soft-launch dry run.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/obs/__tests__/kids-sdk-allowlist.spec.ts\nimport { describe, it, expect } from 'vitest';\nimport allowlist from '../../__sdk-allowlist/kids.json';\nimport { readFileSync, readdirSync } from 'node:fs';\nimport { join } from 'node:path';\n\ndescribe('TASK-OBS-001 §1.3 — kids SDK allow-list', () =&gt; {\n  it('built kids bundle contains zero denied SDK strings', () =&gt; {\n    const bundleDir = 'build/kids/web-mobile';\n    const files = readdirSync(bundleDir, { recursive: true }) as string[];\n    const jsFiles = files.filter(f =&gt; f.endsWith('.js'));\n    for (const f of jsFiles) {\n      const content = readFileSync(join(bundleDir, f), 'utf8');\n      for (const denied of allowlist.deny) {\n        expect(content.includes(denied)).toBe(false);\n      }\n    }\n  });\n\n  it('lists all expected denied SDKs', () =&gt; {\n    const denied = new Set(allowlist.deny);\n    for (const must of ['mixpanel-react-native','appsflyer-react-native-plugin','@firebase/analytics']) {\n      expect(denied.has(must)).toBe(true);\n    }\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/obs/sentry.ts\nimport { configureSentry } from './sentry-pii-scrub';\nconfigureSentry({ dsn: process.env.SENTRY_DSN!, release: process.env.RELEASE! });\n\n// apps/api/src/obs/metrics.ts\nimport { register, Counter, Histogram } from 'prom-client';\nexport const authSigninCounter = new Counter({\n  name: 'auth_signin_total', help: 'signin attempts', labelNames: ['provider','sku','outcome'],\n});\nexport const careActionLatency = new Histogram({\n  name: 'care_action_latency_ms', help: 'care action latency', labelNames: ['action','sku'],\n  buckets: [10, 50, 100, 250, 500, 1000, 2500, 5000],\n});</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> GameAnalytics account; Mixpanel account; AppsFlyer account; Sentry org; Better Stack; Cloudflare R2 (for self-host PostHog if chosen).</p>\n<p><strong>Internal:</strong> TASK-INFRA-001 (Cocos scaffold); TASK-INFRA-003 (Supabase for event audit); TASK-LEGAL-001 (PII scrub rules); TASK-LEGAL-003 (binary inspection for SDK allow-list); TASK-AUTH-001..003 (the <code>identify</code> call hooks into auth state).</p>\n<p><strong>Blocks:</strong> TASK-PET-001 (will emit <code>pet.hatched</code>); TASK-CARE-001..005 (care events); TASK-OBS-002 (anti-cheat builds on this metrics scaffold).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — <code>auth.signin.success</code> event</h3>\n<pre><code class=\"language-json\">{\n  \"event\": \"auth.signin.success\",\n  \"provider\": \"apple\",\n  \"sku\": \"standard\",\n  \"outcome\": \"success\",\n  \"user_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"region_of_record\": \"VN\",\n  \"policy_version\": \"tamagochi-privacy-en-v1.3.0\",\n  \"emitted_at\": \"2026-08-12T14:36:01Z\"\n}</code></pre>\n<h3>8.2 — Sentry PII-scrubbed error</h3>\n<pre><code class=\"language-json\">{\n  \"release\": \"tamagochi@1.0.3-rc.2+abc1234\",\n  \"level\": \"error\",\n  \"tags\": { \"sku\": \"standard\", \"tenant_id\": \"mochi\" },\n  \"extra\": { \"request_id\": \"01HC7QG...\" },\n  \"user\": { \"id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\" }\n  /* no email, no ip_address, no parent_email, no device_id */\n}</code></pre>\n<h3>8.3 — GameAnalytics business event</h3>\n<pre><code class=\"language-typescript\">GameAnalytics.addBusinessEvent('USD', 4.99, 'cosmetics', 'mochi-skin-a', 'iap-pack-a');</code></pre>\n<h3>8.4 — Critical alert payload (Sentry rule)</h3>\n<pre><code class=\"language-yaml\">- name: auth-error-rate-high\n  condition: error_rate &gt; 1% on path ~ '/v1/auth/*' over 5 min\n  action:\n    - sentry.notify(channel: '#oncall', severity: 'page')\n    - pagerduty.escalate(routing_key: tamagochi-prod)</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Mixpanel vs Amplitude? → §1.1 + §<code>allowed_tools</code> — Mixpanel default; Amplitude documented as alternative; vendor swap is config.</li><li><strong>OQ-2 (resolved):</strong> AppsFlyer vs Adjust? → §1.1 — AppsFlyer primary (VN/SEA media-source coverage); Adjust alternative.</li><li><strong>OQ-3 (resolved):</strong> PostHog self-host? → §1.14 — only if Mixpanel cost breaks budget.</li><li><strong>OQ-4 (resolved):</strong> Tail sampling per tenant? → §1.9 + deferred to TASK-OBS-002 in P4.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>A denied SDK leaks into kids binary</td><td>TASK-LEGAL-003 §1.10 + <code>__tests__/kids-sdk-allowlist.spec.ts</code></td><td>Build blocked</td><td>Identify transitive dep; replace or vendor-isolate</td></tr><tr><td>2</td><td>Sentry PII scrubber regression after SDK upgrade</td><td><code>__tests__/sentry-pii-scrub.spec.ts</code> fails</td><td>PR blocked</td><td>Update scrubber; pin SDK</td></tr><tr><td>3</td><td>Mixpanel feature-flag service down</td><td><code>feature-flag.fallback.used</code> metric spike</td><td>Default values used</td><td>Use defaults until service recovers; investigate</td></tr><tr><td>4</td><td>AppsFlyer attribution outage</td><td>Postback failure metric</td><td>UA mis-attributed</td><td>Manual UA reconciliation; vendor support</td></tr><tr><td>5</td><td>GameAnalytics quota exceeded</td><td>Vendor email + ingestion failures</td><td>Loss of f2p funnel data</td><td>Upgrade plan; restore ingestion</td></tr><tr><td>6</td><td>Sentry release tagging breaks (CI doesn't set RELEASE env)</td><td>Events untagged in Sentry</td><td>Hard to correlate by version</td><td>Add CI assertion; fix script</td></tr><tr><td>7</td><td>Grafana dashboard JSON drifts</td><td><code>grafana:import</code> test fails</td><td>Dashboards stale</td><td>Re-export from running Grafana; commit fresh JSON</td></tr><tr><td>8</td><td>Quarterly review missed</td><td>Scheduled task alerts</td><td>Drift accumulates</td><td>Manual review; tighten taxonomy</td></tr><tr><td>9</td><td>Cross-SKU event leakage</td><td>Mixpanel workspace event spike from \"wrong\" SKU</td><td>COPPA exposure</td><td>Identify leak source; rotate workspace tokens</td></tr><tr><td>10</td><td>Server-side event-schema validation rejects valid events</td><td><code>obs.event.schema.invalid</code> spike</td><td>Analytics data missing</td><td>Update schema; emergency permissive mode while fix lands</td></tr><tr><td>11</td><td>Critical alert false-positive flood</td><td>Pager fatigue</td><td>On-call ignores real alerts</td><td>Tune thresholds; rate-limit pages</td></tr><tr><td>12</td><td>Better Stack uptime check rate-limited</td><td><code>infra.uptime.alert.missing</code> synthetic check</td><td>Outage undetected</td><td>Tune check frequency; multi-vendor uptime cross-check</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 4 — analytics &amp; ops stack (Mixpanel + GameAnalytics + AppsFlyer + Sentry + Better Stack). PostHog mentioned as self-host alternative.</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Mixpanel vs Amplitude — locked in <code>infra/supabase/*/config.toml</code> annex with cost rationale.</li><li>Sentry transaction sampling rates — tunable; defaults in §1.9.</li><li>Better Stack vs Datadog — Better Stack default; Datadog upgrade documented.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Mixpanel/Amplitude/AppsFlyer SDK in kids binary.</li><li><code>display_name</code> in Sentry events without consent.</li><li>Client-side AppsFlyer postback for IAP.</li><li>Custom event name outside the taxonomy.</li><li>Shipping a feature without a feature flag.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-OBS-001 is the observability substrate. TASK-OBS-002 (P4 anti-cheat audit) builds on top. Every gameplay task (TASK-PET-001 onward) emits events into this scaffold.</p>";
