window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-VIRAL-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Generative pet on adoption.</strong> Only at the moment of first hatching a NEW pet (per TASK-PET-001 §1.4), 13+ players have an optional path to provide a text prompt OR selfie to influence the pet's appearance.</p>\n<p>§1.2  <strong>Standard SKU only.</strong> Per TASK-AUTH-003 + plan §PART 8, kids SKU pets use the default palette derivation per TASK-PET-001 §1.6 (server-seeded). Generative path is disabled.</p>\n<p>§1.3  <strong>Two input modes.</strong></p>\n<ul><li><strong>Text prompt</strong> (max 100 chars): e.g. \"sunset over the ocean\" — palette derived from sentiment + theme.</li><li><strong>Selfie</strong>: player uploads a photo — palette derived from dominant colours (NOT photo itself; only the 3-5 dominant RGB values).</li></ul>\n<p>§1.4  <strong>No image storage server-side.</strong> Per privacy: selfies are processed in-memory; only the extracted dominant-colour vector (≤ 30 bytes) is persisted. Original image discarded immediately.</p>\n<p>§1.5  <strong>Prompt safety.</strong> Text prompt MUST pass TASK-AI-002 content safety. Selfie MUST pass an OPTIONAL vision-API safety classifier (faces detected → flag for review but not blocked; NSFW → blocked).</p>\n<p>§1.6  <strong>Palette derivation.</strong> The dominant colours combine with the species' base palette via a deterministic blend function:</p>\n<pre><code>finalPalette = blend(speciesBase, userInput, weight=0.4)</code></pre>\n<p>The blend is computed server-side; same inputs → same output.</p>\n<p>§1.7  <strong>One-of-one uniqueness.</strong> Combined with TASK-PET-001 §1.6 server-side <code>palette_seed</code>, the result is a unique 16-byte palette. Two players with identical prompts get slightly different results due to the seed.</p>\n<p>§1.8  <strong>Spine palette swap.</strong> Cocos <code>SpineLoader</code> (TASK-ART-001 §1.4) applies the palette via Spine's slot-recolour mechanism. The 4 attachment slots (<code>body</code>, <code>face</code>, <code>accessory_head</code>, <code>accessory_body</code>) MUST respect the palette.</p>\n<p>§1.9  <strong>Endpoint — adopt with prompt.</strong> <code>POST /v1/pets/hatch</code> (existing TASK-PET-001 endpoint, extended) — body adds optional <code>{ generative_input: { mode: 'text'|'selfie', prompt?: string, dominant_colors?: string[] } }</code>.</p>\n<p>§1.10  <strong>Cost.</strong> Free at P2 (encourage adoption). TASK-ECON-001 may extend with cosmetic re-generation options in P3+.</p>\n<p>§1.11  <strong>One generative input per pet.</strong> Once hatched, the palette is locked. Re-roll is not available; player must buy a \"Pet Couture\" cosmetic to change appearance (TASK-ECON-003).</p>\n<p>§1.12  <strong>Audit + analytics.</strong> <code>viral.generative.adopted { pet_id, mode, palette_hash }</code>. Records the mode used but NEVER the prompt text or selfie reference. 7-year retention for kids-adjacent audit.</p>\n<p>§1.13  <strong>Prompt sanitisation.</strong> Text prompt is stripped of any PII regex patterns (email/phone/URL) before being passed to TASK-AI-002 — defense in depth.</p>\n<p>§1.14  <strong>Vision API fail-closed.</strong> If vision API down, selfie mode is blocked + text prompt mode offered. Don't ship pet with unvalidated selfie.</p>\n<p>§1.15  <strong>Deterministic blend.</strong> Implementation MUST be a pure function. Property-test verified.</p>\n<p>§1.16  <strong>Cocos <code>GenerativePetAdoptionUi.ts</code>.</strong> UI offers 3 paths: \"Surprise me\" (default palette per TASK-PET-001), \"Describe a feeling\" (text prompt), \"Use a photo\" (selfie). 90-second budget per onboarding (TASK-PET-004).</p>\n<p>§1.17  <strong>Tenant override blocks generative.</strong> B2B tenants (TASK-B2B-001) may disable the generative path — their themes are pre-curated.</p>\n<p>§1.18  <strong>Locale.</strong> Prompt input + UX copy localised per TASK-I18N-001.</p>\n<p>§1.19  <strong>AR compatibility.</strong> Generative palette renders correctly in AR (TASK-AR-001) — palette is per-skeleton, not per-scene.</p>\n<p>§1.20  <strong>Sharing the unique pet.</strong> First-hatch share via TASK-VIRAL-001 includes \"Generated from: &lt;prompt or 'a photo'&gt;\" caption attribution (text-mode only — selfie mode never reveals).</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why prompts not text-to-image.</strong> Plan §PART 4 + cost. Full image generation is expensive + risks copyright/likeness issues. Palette derivation is safe + cheap.</p>\n<p><strong>Why no selfie storage.</strong> Privacy first. Plan §PART 11 trust model.</p>\n<p><strong>Why kids SKU disabled.</strong> Plan §PART 8 + TASK-AI-002 — kid generative is regulator-toxic.</p>\n<p><strong>Why one-of-one + locked.</strong> Plan §PART 3 — uniqueness drives share-worthiness. Re-rolling defeats the moment.</p>\n<p><strong>Why deterministic blend.</strong> Auditability + repeatability.</p>\n<p><strong>Why optional rather than default.</strong> Plan §PART 5 onboarding ≤90s — generative adds time. Optional respects pace.</p>\n<p><strong>Why vision API fail-closed.</strong> Better to disable a feature than ship unvalidated content.</p>\n<p><strong>Why no external generative API.</strong> Cost cap + safety simplicity + IP cleanliness.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/viral/generative-pet/palette-generator.ts\nexport function generatePalette(input: {\n  species: Species;\n  baseSeed: Buffer;        // TASK-PET-001 server-side\n  dominantColors?: string[];  // selfie mode\n  prompt?: string;          // text mode (already sanitised + safety-passed)\n}): { paletteSeed: string; paletteHash: string } {\n  const speciesPalette = SPECIES_PALETTES[input.species];\n  let blendVector: number[];\n\n  if (input.dominantColors) {\n    blendVector = input.dominantColors.flatMap(c =&gt; hexToRgb(c));\n  } else if (input.prompt) {\n    blendVector = themeToVector(input.prompt);  // designer-defined theme→RGB table\n  } else {\n    blendVector = randomVectorFromSeed(input.baseSeed);\n  }\n\n  const final = Buffer.alloc(16);\n  for (let i = 0; i &lt; 16; i++) {\n    final[i] = Math.round((speciesPalette[i] * 0.6 + blendVector[i % blendVector.length] * 0.4)) &amp; 0xff;\n    final[i] ^= input.baseSeed[i % input.baseSeed.length];  // re-randomise via seed\n  }\n\n  return {\n    paletteSeed: final.toString('hex'),\n    paletteHash: sha256(final).digest('hex'),\n  };\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/viral/generative-pet/prompt-safety.ts\n@Injectable()\nexport class PromptSafetyService {\n  constructor(private readonly mod: ContentSafetyService) {}\n\n  async sanitize(prompt: string): Promise&lt;string&gt; {\n    if (prompt.length &gt; 100) throw new HttpException('prompt.too_long', 422);\n    const stripped = prompt\n      .replace(/[\\w.-]+@[\\w.-]+\\.\\w{2,}/g, '&lt;email&gt;')\n      .replace(/\\d{3,}[- ]?\\d{3,}/g, '&lt;phone&gt;')\n      .replace(/(https?:\\/\\/|www\\.)\\S+/g, '&lt;url&gt;');\n    const r = await this.mod.assertSafe(stripped, { audience: '13+' });\n    if (!r.ok) throw new HttpException({ error: 'prompt.unsafe', reason: r.reason }, 422);\n    return stripped;\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/viral/generative-pet/generative-pet.service.ts (excerpt)\nasync generateAdoption(u: AuthedUser, species: Species, input?: GenerativeInput) {\n  if (await this.userSkuFor(u.id) === 'kids') {\n    // Force default palette\n    input = undefined;\n  }\n  let palette: { paletteSeed: string; paletteHash: string };\n  if (input?.mode === 'selfie') {\n    if (!await this.vision.checkSelfie(input.dominant_colors_source)) {\n      throw new HttpException('selfie.unsafe', 422);\n    }\n    palette = generatePalette({ species, baseSeed: randomBytes(16), dominantColors: input.dominant_colors });\n  } else if (input?.mode === 'text') {\n    const cleaned = await this.promptSafety.sanitize(input.prompt!);\n    palette = generatePalette({ species, baseSeed: randomBytes(16), prompt: cleaned });\n  } else {\n    palette = generatePalette({ species, baseSeed: randomBytes(16) });\n  }\n  await this.audit.emit('viral.generative.adopted', { user_id: u.id, mode: input?.mode ?? 'default', palette_hash: palette.paletteHash });\n  return palette;\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Text-prompt adoption returns palette unique per (species, seed, prompt). Verified. <strong>AC2.</strong> Selfie input never stored — only dominant_colors extracted. Verified. <strong>AC3.</strong> Kids SKU forces default palette regardless of input. Verified. <strong>AC4.</strong> Unsafe prompt rejected with reason. Verified. <strong>AC5.</strong> PII regex stripped before moderation. Verified. <strong>AC6.</strong> Vision API down → selfie mode blocked. Verified. <strong>AC7.</strong> Deterministic blend (same inputs → same output). Verified by property test. <strong>AC8.</strong> Palette locked after hatch (no re-roll). Verified. <strong>AC9.</strong> AR renders palette correctly. Verified. <strong>AC10.</strong> Share caption attributes prompt mode only (text). Verified. <strong>AC11.</strong> Tenant override disables path. Verified. <strong>AC12.</strong> Audit emits without storing prompt text. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('TASK-VIRAL-003 — generative palette', () =&gt; {\n  it('is deterministic per input', () =&gt; {\n    const a = generatePalette({ species: 'mochi', baseSeed: Buffer.from('00'.repeat(16), 'hex'), prompt: 'ocean' });\n    const b = generatePalette({ species: 'mochi', baseSeed: Buffer.from('00'.repeat(16), 'hex'), prompt: 'ocean' });\n    expect(a.paletteSeed).toBe(b.paletteSeed);\n  });\n\n  it('differs across seeds (uniqueness)', () =&gt; {\n    const a = generatePalette({ species: 'mochi', baseSeed: Buffer.from('00'.repeat(16), 'hex'), prompt: 'ocean' });\n    const b = generatePalette({ species: 'mochi', baseSeed: Buffer.from('FF'.repeat(16), 'hex'), prompt: 'ocean' });\n    expect(a.paletteSeed).not.toBe(b.paletteSeed);\n  });\n\n  it('rejects unsafe prompts', async () =&gt; {\n    await expect(prompt.sanitize('VISIT example.com FOR FREE COINS')).rejects.toThrow(/url/i);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/viral/GenerativePetAdoptionUi.ts (excerpt)\n@ccclass('GenerativePetAdoptionUi')\nexport class GenerativePetAdoptionUi extends Component {\n  onTapSurpriseMe() { this.proceed(undefined); }\n  async onTapTextPrompt() {\n    const text = await this.promptInput.text;\n    this.proceed({ mode: 'text', prompt: text });\n  }\n  async onTapSelfie() {\n    const photoUri = await CameraBridge.takePhoto();\n    const colors = await CameraBridge.extractDominantColors(photoUri, 5);  // in-memory\n    this.proceed({ mode: 'selfie', dominant_colors: colors });\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Optional vision-API (Google Vision / AWS Rekognition). <strong>Internal:</strong> TASK-PET-006 (species + tier), TASK-AI-002 (content safety), TASK-ART-001 (Spine palette). <strong>Blocks:</strong> none.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/pets/hatch\n{ \"species\": \"mochi\", \"generative_input\": { \"mode\": \"text\", \"prompt\": \"sunset over the ocean\" } }\n→ 201 { \"pet_id\": \"01HC...\", \"palette_seed\": \"9b3e7d2a5c4f1b8e6d3a2c9f7b5d3a1c\", \"palette_hash\": \"...\" }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"viral.generative.adopted\", \"user_id\": \"01HU...\", \"pet_id\": \"01HC...\", \"mode\": \"selfie\", \"palette_hash\": \"...\" }</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"prompt.unsafe\", \"reason\": \"moderation_flagged\" }</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"selfie.vision_unavailable\", \"fallback\": \"use_text_prompt_or_default\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Palette-only vs image gen? → §<code>disallowed_tools</code> + §2.</li><li><strong>OQ-2 (resolved):</strong> Kids generative? → §1.2 — disabled.</li><li><strong>OQ-3 (resolved):</strong> Selfie storage? → §1.4 — never.</li><li><strong>OQ-4 (resolved):</strong> Re-roll? → §1.11 — Pet Couture path only.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Vision API down</td><td>Synthetic</td><td>Selfie mode disabled</td><td>Text/default fallback</td></tr><tr><td>2</td><td>Prompt evades moderation</td><td>DPO review</td><td>Off-palette pet</td><td>Tighten classifier</td></tr><tr><td>3</td><td>Selfie image accidentally stored</td><td>Audit</td><td>Privacy breach</td><td>Code review + audit log scan</td></tr><tr><td>4</td><td>Palette blend produces black/white only</td><td>Stat</td><td>Boring pet</td><td>Force minimum saturation</td></tr><tr><td>5</td><td>Re-roll requested (UX glitch)</td><td>Server reject</td><td>UX hang</td><td>Surface Pet Couture path</td></tr><tr><td>6</td><td>Cross-tenant disabled gen</td><td>B2B config</td><td>OK</td><td>Tenant manifest read</td></tr><tr><td>7</td><td>Spine palette swap missing slot</td><td>Cocos error</td><td>Default colors</td><td>Slot guard</td></tr><tr><td>8</td><td>Audit text leak (prompt stored)</td><td>DPO</td><td>Privacy issue</td><td>Schema lint</td></tr><tr><td>9</td><td>AR render off-palette</td><td>Cocos</td><td>Visual bug</td><td>Recompute palette in AR scene</td></tr><tr><td>10</td><td>Locale missing prompt copy</td><td>EN fallback</td><td>OK</td><td>i18n batch</td></tr><tr><td>11</td><td>Selfie crash on iOS Photos limit</td><td>OS error</td><td>UX error</td><td>Surface \"try again\"</td></tr><tr><td>12</td><td>Theme-to-vector mapping missing key</td><td>Default mid-gray</td><td>Bland</td><td>Designer table review</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 viral hook #8 (generative-at-adoption), plan §PART 11 privacy.</p>\n<p><strong>Sub-decisions punted to ops:</strong> Theme→RGB table designer-curated.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Image storage.</li><li>Kids generative.</li><li>External text-to-image.</li><li>Re-roll path.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-PET-006 species + tier carry forward; TASK-VIRAL-001 share attribution.</p>";
