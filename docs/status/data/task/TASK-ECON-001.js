window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-ECON-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Two currencies.</strong> Tamagochi has exactly TWO currencies at P3:</p>\n<ul><li><strong>Coins</strong> (soft) — earned via gameplay (mini-games, streaks, daily ritual, ad-rewarded video). Used for: food, soap, breeding, surprise eggs, cosmetic re-styling.</li><li><strong>Hearts</strong> (premium) — purchasable via real-money IAP only (TASK-ECON-002). Used for: premium species direct purchase, battle pass premium track unlock, cosmetic bundles. <strong>Hearts MUST NOT randomly outcome anything</strong> per TASK-LEGAL-002.</li></ul>\n<p>§1.2  <strong>Double-entry ledger.</strong> Every currency mutation MUST be a paired row in <code>currency_ledger</code> table: one debit + one credit, summing to 0 net per transaction. Account types: <code>user_wallet</code>, <code>system_source</code> (rewards), <code>system_sink</code> (consumed in-game), <code>iap_source</code> (Hearts purchased), <code>creator_revshare</code> (TASK-ECON-003 UGC).</p>\n<p>§1.3  <strong>Balance derivation.</strong> A user's balance MUST be computed by SUM over the ledger filtered by <code>user_id</code> + <code>currency</code>. Never stored as a single column updated in place.</p>\n<p>§1.4  <strong>Performance — cached balance.</strong> A <code>user_currency_balance</code> materialised summary table MAY cache the result per (user_id, currency) for read-perf. It MUST be invalidated on every ledger insert + recomputed asynchronously via trigger. Reads of the cache MUST tolerate up to 5 seconds of staleness — never authoritative.</p>\n<p>§1.5  <strong>Server-authoritative.</strong> All ledger writes happen server-side. The Cocos client MUST NEVER write balance directly. Per TASK-INFRA-002 §1.10.</p>\n<p>§1.6  <strong>No overdraft.</strong> Spend operations MUST check balance via the ledger SUM (NOT the cached summary) within the same SERIALIZABLE transaction:</p>\n<pre><code class=\"language-sql\">BEGIN ISOLATION LEVEL SERIALIZABLE;\nSELECT COALESCE(SUM(amount), 0) AS balance FROM currency_ledger WHERE user_id = $u AND currency = 'coins';\n-- if balance &gt;= cost:\nINSERT INTO currency_ledger (user_id, currency, amount, account_type, ref) VALUES ($u, 'coins', -$cost, 'user_wallet', $ref);\nINSERT INTO currency_ledger (user_id, currency, amount, account_type, ref) VALUES (NULL, 'coins', $cost, 'system_sink', $ref);\nCOMMIT;</code></pre>\n<p>If <code>balance &lt; cost</code>, raise <code>insufficient_balance</code> and ROLLBACK.</p>\n<p>§1.7  <strong>Per-user mutex.</strong> To prevent concurrent double-spend, every transaction MUST acquire a Postgres advisory lock on <code>(hash(user_id))</code> for the transaction's duration. Releases on COMMIT/ROLLBACK.</p>\n<p>§1.8  <strong>Endpoint — balance.</strong> <code>GET /v1/wallet/balance</code> returns <code>{ coins: number, hearts: number, last_synced_at: ISO }</code>.</p>\n<p>§1.9  <strong>Endpoint — history.</strong> <code>GET /v1/wallet/history?currency=coins&amp;limit=50</code> returns recent ledger rows visible to the player (their own, not system rows). DSR-compliant.</p>\n<p>§1.10  <strong>Reference tagging.</strong> Every ledger entry MUST have a <code>ref</code> field tying it to the originating action: <code>'care.feed:&lt;feed_id&gt;'</code>, <code>'mini-game:&lt;session_id&gt;'</code>, <code>'breeding:&lt;event_id&gt;'</code>, <code>'iap:&lt;purchase_id&gt;'</code>, <code>'streak.milestone:&lt;streak_id&gt;'</code>. Audit-traceable.</p>\n<p>§1.11  <strong>Anti-cheat ban.</strong> Per TASK-INFRA-002 §1.9, any attempt to mutate balance via an invalid path (e.g. negative spend, cross-tenant move) → <code>security.impossible-transition</code> event + DPO review.</p>\n<p>§1.12  <strong>Daily reconciliation.</strong> A scheduled function MUST run nightly and: (a) recompute the cached balance for every user; (b) compare ledger SUM against expected source-sink balance (system_source - system_sink = total user_wallet for soft currency); (c) alert on drift &gt; 0.1%.</p>\n<p>§1.13  <strong>No Coins ↔ Hearts conversion.</strong> Premium-to-soft conversion is forbidden — it would create a real-money-to-soft pathway (TASK-LEGAL-002 randomisation concerns).</p>\n<p>§1.14  <strong>Free-tier daily cap on Coin earn.</strong> Per TASK-CARE-004 mini-game caps + TASK-CARE-005 streak — cumulative Coin earn capped at 500/day for free tier, 1500/day for Pet+ subscribers. Anti-grind.</p>\n<p>§1.15  <strong>Hearts entry path.</strong> Hearts arrive ONLY from TASK-ECON-002 IAP. No earning path. No conversion.</p>\n<p>§1.16  <strong>Tenant scoping.</strong> All ledger rows tagged <code>tenant_id</code>. Per TASK-INFRA-003 §1.4. Cross-tenant ledger reads + writes forbidden.</p>\n<p>§1.17  <strong>Audit retention.</strong> 7 years for kids-adjacent + 2 years for standard. Per TASK-LEGAL-001.</p>\n<p>§1.18  <strong>DSR support.</strong> On account deletion, ledger rows are tombstoned (not deleted) per TASK-LEGAL-001 §3.6. Player can request export via <code>GET /v1/wallet/history?export=csv</code>.</p>\n<p>§1.19  <strong>Real-money compliance.</strong> Hearts IAP receipts MUST be validated server-side via TASK-ECON-002 receipt-validation. Untrusted client IAP claims are ALWAYS rejected.</p>\n<p>§1.20  <strong>Analytics.</strong> <code>econ.ledger.entry { currency, amount, account_type, ref_type }</code>, <code>econ.balance.queried</code>, <code>econ.spend.rejected { reason }</code>, <code>econ.reconciliation.drift { drift_pct }</code> per TASK-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why two currencies.</strong> Plan §PART 6 — soft + premium is the f2p standard. Pou / Talking Tom / Adopt Me all use it. Single currency conflates engagement reward with monetization.</p>\n<p><strong>Why double-entry ledger.</strong> Accounting standard. Sum-to-zero invariant catches bugs (single-entry can silently lose currency). Reconciliation works.</p>\n<p><strong>Why balance is derived not stored.</strong> Plan §PART 4 anti-cheat — derived state cannot be tampered. Cached summary is for performance; never authoritative.</p>\n<p><strong>Why SERIALIZABLE + advisory lock.</strong> Plan §PART 10 #7 Adopt Me scam precedent + general concurrency. Without it, two simultaneous spends can both pass the balance check before either commits.</p>\n<p><strong>Why no Coins↔Hearts conversion.</strong> Plan §PART 8 + TASK-LEGAL-002 — real-money ↔ soft pathways trigger gambling-regulation scrutiny in BE/NL.</p>\n<p><strong>Why daily Coin cap.</strong> Plan §PART 6 economy balance — uncapped grinding devalues the IAP path.</p>\n<p><strong>Why ref tagging.</strong> Plan §PART 4 — auditability is a regulator + ops requirement.</p>\n<p><strong>Why no Hearts earn path.</strong> Hearts = monetization. Earning Hearts would let players bypass payment. (Different from giving free Hearts on promotion — that's an IAP-source row.)</p>\n<p><strong>Why daily reconciliation.</strong> Plan §PART 10 — silent bugs compound. Nightly drift check catches them within 24h.</p>\n<p><strong>Why ledger rows tombstoned on DSR.</strong> GDPR Article 17 + TASK-LEGAL-001 §3.6 — accounting evidence retained; PII redacted.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/econ/ledger/ledger.service.ts\n@Injectable()\nexport class LedgerService {\n  constructor(private readonly supa: SupabaseClient, private readonly audit: AuditLogService) {}\n\n  async spend(userId: string, currency: 'coins' | 'hearts', amount: number, ref: string, tenantId: string): Promise&lt;{ balance_after: number }&gt; {\n    if (amount &lt;= 0) throw new Error('econ.invalid_amount');\n    return this.supa.rpc('econ_spend', {\n      _user_id: userId, _currency: currency, _amount: amount, _ref: ref, _tenant_id: tenantId,\n    });\n  }\n\n  async grant(userId: string, currency: 'coins' | 'hearts', amount: number, ref: string, tenantId: string, source: 'system_source' | 'iap_source'): Promise&lt;{ balance_after: number }&gt; {\n    if (amount &lt;= 0) throw new Error('econ.invalid_amount');\n    if (currency === 'hearts' &amp;&amp; source !== 'iap_source') throw new Error('econ.hearts_only_via_iap');\n    return this.supa.rpc('econ_grant', {\n      _user_id: userId, _currency: currency, _amount: amount, _ref: ref, _tenant_id: tenantId, _source: source,\n    });\n  }\n\n  async balance(userId: string, currency: 'coins' | 'hearts'): Promise&lt;number&gt; {\n    const { data } = await this.supa.rpc('econ_balance', { _user_id: userId, _currency: currency });\n    return data ?? 0;\n  }\n}</code></pre>\n<pre><code class=\"language-sql\">-- migration\ncreate table public.currency_ledger (\n  id bigserial primary key,\n  user_id uuid references auth.users(id) on delete restrict,\n  currency text not null check (currency in ('coins','hearts')),\n  amount bigint not null,\n  account_type text not null check (account_type in ('user_wallet','system_source','system_sink','iap_source','creator_revshare')),\n  ref text not null,\n  tenant_id text not null default 'mochi',\n  occurred_at timestamptz not null default now(),\n  check (amount != 0)\n);\ncreate index on public.currency_ledger (user_id, currency, occurred_at desc);\ncreate index on public.currency_ledger (ref);\nalter table public.currency_ledger enable row level security;\ncreate policy \"ledger self\" on public.currency_ledger for select using (user_id = auth.uid() or account_type in ('user_wallet') and user_id = auth.uid());\n\ncreate table public.user_currency_balance (\n  user_id uuid not null,\n  currency text not null,\n  balance bigint not null default 0,\n  last_synced_at timestamptz not null default now(),\n  primary key (user_id, currency)\n);\n\ncreate or replace function econ_spend(_user_id uuid, _currency text, _amount bigint, _ref text, _tenant_id text)\n  returns json security definer language plpgsql as $$\ndeclare\n  current_balance bigint;\nbegin\n  perform pg_advisory_xact_lock(hashtext(_user_id::text));\n  select coalesce(sum(amount), 0) into current_balance from public.currency_ledger where user_id = _user_id and currency = _currency;\n  if current_balance &lt; _amount then raise exception 'econ.insufficient_balance'; end if;\n  insert into public.currency_ledger (user_id, currency, amount, account_type, ref, tenant_id)\n    values (_user_id, _currency, -_amount, 'user_wallet', _ref, _tenant_id);\n  insert into public.currency_ledger (user_id, currency, amount, account_type, ref, tenant_id)\n    values (null, _currency, _amount, 'system_sink', _ref, _tenant_id);\n  insert into public.user_currency_balance as ucb (user_id, currency, balance, last_synced_at)\n    values (_user_id, _currency, current_balance - _amount, now())\n    on conflict (user_id, currency) do update set balance = ucb.balance - _amount, last_synced_at = now();\n  return json_build_object('balance_after', current_balance - _amount);\nend;\n$$;</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Spend reduces balance by exact amount + writes 2 ledger rows. Verified. <strong>AC2.</strong> Concurrent spend (parallel calls) cannot overdraw. Verified by chaos test. <strong>AC3.</strong> Insufficient balance raises <code>econ.insufficient_balance</code>. Verified. <strong>AC4.</strong> Coins ↔ Hearts conversion blocked. Verified. <strong>AC5.</strong> Hearts grant via non-IAP source rejected. Verified. <strong>AC6.</strong> Cross-tenant ledger query returns 0 rows (RLS). Verified. <strong>AC7.</strong> Daily Coin earn cap (500 free / 1500 Pet+) enforced. Verified. <strong>AC8.</strong> Daily reconciliation detects 0.1% drift. Verified by chaos test injecting drift. <strong>AC9.</strong> Negative amount rejected. Verified. <strong>AC10.</strong> Cached balance staleness ≤ 5 sec. Verified by latency test. <strong>AC11.</strong> History endpoint returns DSR-exportable CSV. Verified. <strong>AC12.</strong> Audit retention 7-year kid / 2-year standard. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/econ/ledger/__tests__/ledger.spec.ts\ndescribe('TASK-ECON-001 — ledger', () =&gt; {\n  it('blocks overdraft', async () =&gt; {\n    await ledger.grant('u1', 'coins', 100, 'test', 'mochi', 'system_source');\n    await expect(ledger.spend('u1', 'coins', 150, 'test:spend', 'mochi')).rejects.toThrow(/insufficient_balance/);\n    expect(await ledger.balance('u1', 'coins')).toBe(100);\n  });\n\n  it('handles concurrent spend correctly (no double-spend)', async () =&gt; {\n    await ledger.grant('u1', 'coins', 100, 'test', 'mochi', 'system_source');\n    const results = await Promise.allSettled([\n      ledger.spend('u1', 'coins', 60, 'test:a', 'mochi'),\n      ledger.spend('u1', 'coins', 60, 'test:b', 'mochi'),\n    ]);\n    const succeeded = results.filter(r =&gt; r.status === 'fulfilled').length;\n    expect(succeeded).toBe(1);\n    expect(await ledger.balance('u1', 'coins')).toBe(40);\n  });\n\n  it('blocks Hearts grant from non-IAP source', async () =&gt; {\n    await expect(ledger.grant('u1', 'hearts', 100, 'test', 'mochi', 'system_source')).rejects.toThrow(/hearts_only_via_iap/);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/econ/ledger/reconciliation.service.ts\n@Injectable()\nexport class ReconciliationService {\n  async runDaily() {\n    const { data: drift } = await this.supa.rpc('econ_compute_drift');\n    if (drift.drift_pct &gt; 0.001) {\n      this.audit.emit('econ.reconciliation.drift', drift);\n      this.sentry.capture(new Error(`Ledger drift ${drift.drift_pct * 100}% — investigate`));\n    }\n    await this.supa.rpc('econ_rebuild_balance_cache');\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Postgres SERIALIZABLE + advisory locks. <strong>Internal:</strong> TASK-INFRA-003 (Supabase + tenant), TASK-INFRA-002 (anti-cheat path). <strong>Blocks:</strong> TASK-ECON-002 IAP (grants Hearts), TASK-ECON-003 UGC rev-share, TASK-SUB-001 (Pet+ subscription perks), TASK-ADS-001 (rewarded video payout), TASK-VIRAL-004 (battle pass).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">GET /v1/wallet/balance\n→ 200 { \"coins\": 1420, \"hearts\": 12, \"last_synced_at\": \"2026-08-12T14:36:01Z\" }</code></pre>\n<pre><code class=\"language-http\">GET /v1/wallet/history?currency=coins&amp;limit=5\n→ 200 [\n  { \"id\": 4242, \"amount\": -5, \"ref\": \"care.feed:01HC...\", \"occurred_at\": \"2026-08-12T14:35:55Z\" },\n  { \"id\": 4241, \"amount\": +30, \"ref\": \"mini-game:01HCMGS...\", \"occurred_at\": \"2026-08-12T14:30:01Z\" }\n]</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"econ.insufficient_balance\", \"required\": 25, \"have\": 12 }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"econ.reconciliation.drift\", \"drift_pct\": 0.0023, \"expected_total\": 12345678, \"actual_total\": 12317284 }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Single vs two currencies? → §1.1.</li><li><strong>OQ-2 (resolved):</strong> Single-entry vs double-entry? → §1.2.</li><li><strong>OQ-3 (resolved):</strong> Stored balance? → §1.3 — derived.</li><li><strong>OQ-4 (resolved):</strong> Hearts earnable? → §1.15 — no.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Concurrent spend race</td><td>Advisory lock prevents</td><td>None</td><td>OK</td></tr><tr><td>2</td><td>Daily reconciliation detects drift</td><td>Sentry alert</td><td>Drift logged</td><td>Investigate; possible compensating tx</td></tr><tr><td>3</td><td>SERIALIZABLE deadlock spike</td><td>DB metrics</td><td>Latency</td><td>Retry with backoff</td></tr><tr><td>4</td><td>Cached balance stale &gt; 5s</td><td>Test fails</td><td>Stale display</td><td>Hard-refresh; not authoritative</td></tr><tr><td>5</td><td>Negative amount slips through</td><td>Constraint check</td><td>DB rejects</td><td>Verified</td></tr><tr><td>6</td><td>Cross-tenant via RLS misconfig</td><td>Spec test</td><td>Privacy</td><td>Tighten RLS</td></tr><tr><td>7</td><td>Hearts grant from non-IAP source</td><td>Code check</td><td>Rejected</td><td>Verified</td></tr><tr><td>8</td><td>Daily Coin cap evades via timezone shift</td><td>Audit</td><td>Possible exploit</td><td>Lock day boundary to user region</td></tr><tr><td>9</td><td>IAP receipt forgery → Hearts grant</td><td>TASK-ECON-002 validation</td><td>Rejected</td><td>Verified via Apple/Google</td></tr><tr><td>10</td><td>Ledger row deletion (DSR) leaves orphan ref</td><td>Tombstone retains row</td><td>OK</td><td>Designed</td></tr><tr><td>11</td><td>Cascade delete on user_id</td><td>Restrict prevents</td><td>OK</td><td>Tombstone instead</td></tr><tr><td>12</td><td>Audit growth</td><td>Disk</td><td>Retention enforced</td><td>2/7 yr</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 6 (Pet+ + currency), plan §PART 4 anti-cheat.</p>\n<p><strong>Sub-decisions punted to ops:</strong> Daily Coin cap Mixpanel-tunable.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Client balance write.</li><li>Stored balance updated in place.</li><li>Coins ↔ Hearts conversion.</li><li>Real-money convertibility from Coins.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-ECON-002 IAP grants Hearts; TASK-CARE-001/002/004/005 + TASK-PET-007 spend via ledger; TASK-OBS-002 anti-cheat reads ledger.</p>";
