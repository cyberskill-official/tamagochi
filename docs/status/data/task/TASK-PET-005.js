window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PET-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Slot counts.</strong> A free-tier player MUST be limited to <strong>3</strong> active pets. A Pet+ subscriber MUST be limited to <strong>10</strong> active pets. (TASK-SUB-001 implements the subscription; this task's <code>SlotEntitlementService</code> stubs the entitlement check until then — returning \"free tier always\" at P1/P2.)</p>\n<p>§1.2  <strong>Active vs total.</strong> \"Active\" = <code>pets.status = 'active'</code>. Pets in <code>grandma_house</code> (Permadeath-Lite per TASK-PET-008) DO NOT count against the slot limit; the player can have unlimited grandma-house pets but only the slot count's worth of active ones.</p>\n<p>§1.3  <strong><code>tombstoned</code> pets MUST NOT count</strong> against the slot quota either. Tombstoned pets are post-DSR-erasure markers; they are effectively soft-deleted.</p>\n<p>§1.4  <strong>Endpoint — list roster.</strong> <code>GET /v1/pets/roster</code> MUST return <code>{ slot_limit, active_count, grandma_count, pets: PetSummary[] }</code>. Players see their full roster grouped by status.</p>\n<p>§1.5  <strong>Endpoint — slot quota check.</strong> <code>GET /v1/pets/slot-quota</code> MUST return <code>{ slot_limit, current_active, remaining, tier: 'free' | 'pet_plus' }</code> so the client can render UX hints (\"You have 2 slots left\").</p>\n<p>§1.6  <strong>Hatch enforcement.</strong> <code>POST /v1/pets/hatch</code> (TASK-PET-001 §1.4) MUST check slot quota via this task's <code>InventoryService.canHatchNew(userId)</code>. On exceeded: HTTP 402 <code>{ error: 'pet_slot_full', current_count, slot_limit, suggest_upgrade: true }</code>.</p>\n<p>§1.7  <strong>Pet+ downgrade handling.</strong> If a Pet+ subscriber lapses to free tier WHILE having more than 3 active pets, the over-quota pets MUST NOT be automatically deleted. Instead: (a) the player can keep all current pets; (b) cannot hatch new ones until count drops below 3; (c) on next hatch attempt, surface a \"Choose your free-tier 3 pets\" UX where the player explicitly archives extras to <code>grandma_house</code> (which then cannot be rescued until they re-subscribe).</p>\n<p>§1.8  <strong>Audit row on slot change.</strong> Slot-quota changes (subscribe / unsubscribe / hatch / archive) MUST emit <code>pet.slot.change</code> events: <code>{user_id, prev_active_count, new_active_count, tier_change?, source}</code>.</p>\n<p>§1.9  <strong>Colyseus broadcast.</strong> When a player's roster changes (hatch, archive, rescue), broadcast <code>roster.changed</code> to all of their connected sessions (multi-device sync).</p>\n<p>§1.10  <strong>Real-time slot-counter widget.</strong> Cocos <code>PetRosterUi.ts</code> displays the slot counter \"X / Y pets\" with a colour-coded indicator (green if remaining ≥ 1, yellow if 0/full).</p>\n<p>§1.11  <strong>No silent slot expansion.</strong> A bug that mistakenly issues more slots than the tier allows MUST be caught by a Postgres CHECK constraint: <code>count(pets where status='active' group by owner_id) &lt;= 10</code> enforced via a TRIGGER (not a column constraint — Postgres doesn't support cross-row CHECKs).</p>\n<p>§1.12  <strong>Migration of legacy single-pet players.</strong> Existing P1 players who have exactly 1 pet at the time TASK-PET-005 ships MUST be migrated to the new model:</p>\n<ul><li><code>pets.status = 'active'</code> confirmed for their single pet;</li><li>Slot quota established at 3 (free tier by default).</li></ul>\n<p>A migration script <code>scripts/migrations/p1-to-p2-roster.mjs</code> MUST run idempotently.</p>\n<p>§1.13  <strong>Rate limit for hatch.</strong> Per TASK-PET-001 §1.13, 3 hatches per 24h per player. This rule remains regardless of slot quota — even Pet+ players with empty slots can only hatch 3/day.</p>\n<p>§1.14  <strong>Inventory query performance.</strong> <code>GET /v1/pets/roster</code> MUST return in P95 ≤ 200ms. Composite index <code>(owner_id, status)</code> on <code>pets</code> table ensures this.</p>\n<p>§1.15  <strong>Cross-tenant scoping.</strong> Inventory queries MUST respect <code>tenant_id</code> per TASK-INFRA-003 §1.4 — a Mochi (consumer) player's roster MUST NOT include B2B tenant pets, and vice versa. RLS policy <code>pets self read</code> (TASK-PET-001 §3.4) handles this when <code>app.tenant_id</code> is set.</p>\n<p>§1.16  <strong>Trade impact.</strong> TASK-SOCIAL-003 (trade) MUST validate the receiving player has slot capacity BEFORE accepting a trade. The trade handler will call <code>canHatchNew(receiverId)</code> equivalent.</p>\n<p>§1.17  <strong>Breeding impact.</strong> TASK-PET-007 (breeding) produces offspring pets. The parent's player MUST have slot capacity for the offspring; on hatching (post-24h incubation per TASK-PET-007), if slots are full, the egg is held in a \"pending hatch\" queue (24h grace period) before being lost.</p>\n<p>§1.18  <strong>Pet+ marketing surface — non-FOMO.</strong> When a free-tier player tries to hatch beyond 3, the UX MUST: surface \"Upgrade to Pet+ for 10 slots + premium AI + cloud save\" in a non-shaming way (no urgency, no countdown). Plan §PART 6 + TASK-CARE-005 ethical retention principles.</p>\n<p>§1.19  <strong>Analytics.</strong> <code>pet.slot.hatch_blocked</code>, <code>pet.slot.archive_to_grandma</code>, <code>pet.slot.upgrade_prompt_shown</code> per TASK-OBS-001.</p>\n<p>§1.20  <strong>DPO transparency.</strong> <code>GET /v1/me/data</code> (a future DSR feature) MUST include slot history. Implementation slot here for TASK-LEGAL-001 §1.9 integration.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 3 / 10 split.</strong> Plan §PART 6 — free tier offers enough to engage; Pet+ offers enough to feel substantively expanded. Industry comparison: Adopt Me! offers ~6 active pets free; Pou is single-pet only; Talking Tom is single-pet. 3 free / 10 Pet+ is differentiated.</p>\n<p><strong>Why grandma_house doesn't count.</strong> Plan §PART 3 — Permadeath-Lite is a \"your pet is parked, you can rescue them\" path, not deletion. Punishing slot quota on grandma'd pets would discourage rescue and create FOMO.</p>\n<p><strong>Why downgrade keeps existing pets but blocks new.</strong> Apple/Google subscription guidelines strongly discourage \"loss of access on downgrade\" patterns. The compromise (keep what you have but can't grow) is standard f2p practice.</p>\n<p><strong>Why no individual-slot IAP.</strong> Plan §PART 6 + TASK-LEGAL-002 — randomised or atomic IAP undermines the subscription value proposition + creates regulator-questioned \"pay to expand\" surfaces. Slots come bundled with Pet+ only.</p>\n<p><strong>Why server-side enforcement via TRIGGER.</strong> Plan §PART 4 — defense in depth. Application-layer checks can have bugs; a DB trigger is the final line.</p>\n<p><strong>Why offspring egg has 24h grace period when slot full.</strong> Plan §PART 3 ethical retention — losing your breed offspring instantly to \"no slot\" is punishing.</p>\n<p><strong>Why no real-time slot counter on multi-device.</strong> Plan §PART 3 PetPair co-parent — multi-device IS the canonical case. Broadcast roster changes via Colyseus is correct architecture.</p>\n<p><strong>Why no FOMO upgrade prompt.</strong> Plan §PART 3 ethical retention + plan §PART 8 ICO AADC compliance for kids. Same principles apply to all users (consistent UX); shame doesn't differentiate by audience.</p>\n<p><strong>Why hatch rate-limit independent of slots.</strong> Plan §PART 3 — 3 hatches/day is anti-spam, separate from quota. A Pet+ player with 10 empty slots still rate-limited to 3/day so they engage with each new pet meaningfully.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Inventory controller</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/inventory/inventory.controller.ts\n@Controller('v1/pets')\n@UseGuards(SupabaseJwtGuard)\nexport class InventoryController {\n  constructor(private readonly svc: InventoryService) {}\n\n  @Get('roster')\n  async roster(@CurrentUser() u: AuthedUser) {\n    return this.svc.roster(u.id);\n  }\n\n  @Get('slot-quota')\n  async slotQuota(@CurrentUser() u: AuthedUser) {\n    return this.svc.slotQuota(u.id);\n  }\n}</code></pre>\n<h3>3.2 — Inventory service</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/inventory/inventory.service.ts\n@Injectable()\nexport class InventoryService {\n  constructor(\n    private readonly supa: SupabaseClient,\n    private readonly entitlement: SlotEntitlementService,\n    private readonly audit: AuditLogService,\n  ) {}\n\n  async slotQuota(userId: string) {\n    const tier = await this.entitlement.tier(userId);\n    const slotLimit = tier === 'pet_plus' ? 10 : 3;\n    const { count } = await this.supa.from('pets')\n      .select('id', { count: 'exact', head: true })\n      .eq('owner_id', userId).eq('status', 'active');\n    return { slot_limit: slotLimit, current_active: count ?? 0, remaining: slotLimit - (count ?? 0), tier };\n  }\n\n  async canHatchNew(userId: string): Promise&lt;{ allowed: boolean; reason?: string; slot_info: SlotInfo }&gt; {\n    const q = await this.slotQuota(userId);\n    if (q.remaining &lt;= 0) {\n      this.audit.emit('pet.slot.hatch_blocked', { userId, ...q });\n      return { allowed: false, reason: 'pet_slot_full', slot_info: q };\n    }\n    return { allowed: true, slot_info: q };\n  }\n\n  async roster(userId: string) {\n    const [active, grandma] = await Promise.all([\n      this.supa.from('pets').select('id, species, display_name, stage').eq('owner_id', userId).eq('status', 'active'),\n      this.supa.from('pets').select('id, species, display_name, stage').eq('owner_id', userId).eq('status', 'grandma'),\n    ]);\n    const tier = await this.entitlement.tier(userId);\n    return {\n      slot_limit: tier === 'pet_plus' ? 10 : 3,\n      active_count: active.data?.length ?? 0,\n      grandma_count: grandma.data?.length ?? 0,\n      tier,\n      pets: { active: active.data, grandma: grandma.data },\n    };\n  }\n}</code></pre>\n<h3>3.3 — Slot entitlement (stub at P2)</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/inventory/slot-entitlement.service.ts\n@Injectable()\nexport class SlotEntitlementService {\n  // P2 stub: always returns 'free'. TASK-SUB-001 (P3) wires real subscription state.\n  async tier(_userId: string): Promise&lt;'free' | 'pet_plus'&gt; {\n    if (process.env.SLOT_ENTITLEMENT_STUB === 'pet_plus') return 'pet_plus';\n    return 'free';\n  }\n}</code></pre>\n<h3>3.4 — DB trigger</h3>\n<pre><code class=\"language-sql\">-- migration excerpt\ncreate or replace function enforce_slot_quota() returns trigger as $$\ndeclare\n  active_count int;\n  slot_limit int := 10; -- conservative cap; app enforces actual tier\nbegin\n  select count(*) into active_count from public.pets where owner_id = new.owner_id and status = 'active';\n  if active_count &gt; slot_limit then\n    raise exception 'TASK-PET-005 §1.11 slot quota exceeded for owner %', new.owner_id;\n  end if;\n  return new;\nend;\n$$ language plpgsql;\ncreate trigger trg_slot_quota after insert or update of status on public.pets\n  for each row execute procedure enforce_slot_quota();</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Free-tier player with 0 active pets has <code>slot_limit=3, remaining=3</code>. Verified. <strong>AC2.</strong> Free-tier player with 3 active pets hits 402 on 4th hatch. Verified. <strong>AC3.</strong> Pet+ stub player can have 10 active pets. Verified. <strong>AC4.</strong> Grandma pet does NOT count against slot. Verified. <strong>AC5.</strong> Downgrade from Pet+ with 7 pets does NOT auto-delete; player blocked from hatching until count drops. Verified. <strong>AC6.</strong> Trigger rejects bypassing 10 cap at DB layer. Verified by direct SQL test. <strong>AC7.</strong> <code>GET /v1/pets/roster</code> returns grouped by status. Verified. <strong>AC8.</strong> Roster query P95 ≤ 200ms with seeded 10-pet roster. Verified. <strong>AC9.</strong> Multi-device <code>roster.changed</code> broadcast fires on hatch. Verified. <strong>AC10.</strong> Non-FOMO upgrade prompt copy verified by content lint. <strong>AC11.</strong> Migration script idempotent + handles 0/1/2/3 pet starting states. Verified. <strong>AC12.</strong> Cross-tenant pet not visible in roster. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/inventory/__tests__/inventory.spec.ts\ndescribe('TASK-PET-005 — inventory', () =&gt; {\n  it('blocks 4th hatch on free tier', async () =&gt; {\n    await seedPets('u1', 3);\n    const r = await svc.canHatchNew('u1');\n    expect(r.allowed).toBe(false);\n    expect(r.reason).toBe('pet_slot_full');\n  });\n\n  it('grandma does not count', async () =&gt; {\n    await seedPets('u1', 3);\n    await archiveToGrandma('u1', 'p1');\n    const r = await svc.canHatchNew('u1');\n    expect(r.allowed).toBe(true);\n  });\n\n  it('downgrade keeps over-quota pets', async () =&gt; {\n    await mockTier('u1', 'pet_plus');\n    await seedPets('u1', 7);\n    await mockTier('u1', 'free');\n    const roster = await svc.roster('u1');\n    expect(roster.active_count).toBe(7);\n    const r = await svc.canHatchNew('u1');\n    expect(r.allowed).toBe(false);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/pets/PetRosterUi.ts\n@ccclass('PetRosterUi')\nexport class PetRosterUi extends Component {\n  @property(Label) slotCounterLabel!: Label;\n\n  async refresh() {\n    const q = await api.get('/v1/pets/slot-quota');\n    this.slotCounterLabel.string = `${q.current_active} / ${q.slot_limit}`;\n    this.slotCounterLabel.color = q.remaining &gt; 0 ? GREEN : YELLOW;\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> none new. <strong>Internal:</strong> TASK-PET-001 (pets table), TASK-PET-003 (status field), TASK-SUB-001 (entitlement — stubbed here), TASK-INFRA-002 (broadcast), TASK-INFRA-003 (Postgres + RLS + tenant). <strong>Blocks:</strong> TASK-PET-006 (5 species hatching), TASK-PET-007 (breeding offspring slot check), TASK-SOCIAL-003 (trade slot check).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">GET /v1/pets/slot-quota\n→ 200 { \"slot_limit\": 3, \"current_active\": 2, \"remaining\": 1, \"tier\": \"free\" }</code></pre>\n<pre><code class=\"language-http\">GET /v1/pets/roster\n→ 200 {\n  \"slot_limit\": 3, \"active_count\": 2, \"grandma_count\": 1, \"tier\": \"free\",\n  \"pets\": {\n    \"active\": [{ \"id\": \"01HC...\", \"species\": \"mochi\", \"display_name\": \"Mochi\", \"stage\": \"adult\" }, ...],\n    \"grandma\": [{ \"id\": \"01HC...\", \"species\": \"pengu\", \"display_name\": \"Pingu\", \"stage\": \"grandma_house\" }]\n  }\n}</code></pre>\n<pre><code class=\"language-http\">POST /v1/pets/hatch (4th on free tier)\n→ 402 { \"error\": \"pet_slot_full\", \"current_count\": 3, \"slot_limit\": 3, \"suggest_upgrade\": true }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"pet.slot.hatch_blocked\", \"user_id\": \"01HC...\", \"current_active\": 3, \"slot_limit\": 3, \"tier\": \"free\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> 3/10 vs other split? → §1.1 + §2.</li><li><strong>OQ-2 (resolved):</strong> Grandma count? → §1.2 — no.</li><li><strong>OQ-3 (resolved):</strong> Downgrade behaviour? → §1.7 — keep + block.</li><li><strong>OQ-4 (resolved):</strong> Individual-slot IAP? → §<code>disallowed_tools</code> — never.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>DB trigger fires on legitimate Pet+ user (race with entitlement)</td><td>Player support</td><td>Hatch blocked</td><td>Trigger uses 10 cap (conservative); app-layer reconciles</td></tr><tr><td>2</td><td>Migration script fails idempotency on retry</td><td>CI test</td><td>Migration broken</td><td>Script reads existing state; retries safe</td></tr><tr><td>3</td><td>Stub entitlement returns wrong tier</td><td>Player report</td><td>Slots wrong</td><td>TASK-SUB-001 lands; until then env var override</td></tr><tr><td>4</td><td>Tombstoned pets accidentally counted</td><td>Audit</td><td>Slot quota wrong</td><td>Status check explicit in queries</td></tr><tr><td>5</td><td>Cross-tenant pet visible in roster</td><td>RLS test</td><td>Privacy issue</td><td>Tighten RLS; audit recent reads</td></tr><tr><td>6</td><td>Roster query &gt;200ms P95</td><td>Sentry metric</td><td>UX latency</td><td>Composite index check; query plan review</td></tr><tr><td>7</td><td>Downgrade race with hatch attempt</td><td>Concurrent ops</td><td>Inconsistent state</td><td>Per-user mutex on slot ops</td></tr><tr><td>8</td><td>Offspring egg lost despite grace period</td><td>TASK-PET-007 audit</td><td>Player frustration</td><td>Cron job moves pending eggs</td></tr><tr><td>9</td><td>Multi-device roster sync lag</td><td>UX desync</td><td>Stale data</td><td>Authoritative refresh on focus</td></tr><tr><td>10</td><td>Upgrade prompt deemed manipulative</td><td>DPO review</td><td>Copy revision</td><td>Pre-approved copy library</td></tr><tr><td>11</td><td>Slot trigger overhead on bulk migration</td><td>DB perf</td><td>Slow migration</td><td>Disable trigger during bulk insert; verify after</td></tr><tr><td>12</td><td>Player accumulates many grandma pets</td><td>DB growth</td><td>Disk pressure</td><td>Quarterly DPO sweep + bulk archive</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 (multi-pet), plan §PART 6 (Pet+ value props).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Slot counts (3/10) tunable via Mixpanel flag.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Real-money individual slot purchase.</li><li>Auto-delete on downgrade.</li><li>FOMO upgrade copy.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-PET-006 (5 species), TASK-PET-007 (breeding offspring slot check), TASK-PET-008 (grandma_house archival), TASK-SOCIAL-003 (trade slot check), TASK-SUB-001 (entitlement).</p>";
