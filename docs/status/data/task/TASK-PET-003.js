window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PET-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Four stats.</strong> Every pet MUST track exactly <strong>four</strong> stat-bars: <code>hunger</code>, <code>cleanliness</code>, <code>happiness</code>, <code>energy</code>. All integers 0–100. Plan §PART 3 — these map to feed / clean / hug+mini-game / sleep care actions.</p>\n<p>§1.2  <strong>Decay rates by stage (units per hour).</strong> Decay rate MUST be stage-aware. Defaults:</p>\n<table><thead><tr><th>Stat</th><th>egg</th><th>baby</th><th>teen</th><th>adult</th></tr></thead><tbody><tr><td>hunger</td><td>0</td><td>4.0</td><td>3.0</td><td>2.0</td></tr><tr><td>cleanliness</td><td>0</td><td>3.0</td><td>2.5</td><td>2.0</td></tr><tr><td>happiness</td><td>0</td><td>2.5</td><td>2.0</td><td>1.5</td></tr><tr><td>energy</td><td>0</td><td>3.5</td><td>3.0</td><td>2.5</td></tr></tbody></table>\n<p>Rates configurable via Mixpanel feature flags (per TASK-OBS-001 §1.10). <code>egg</code> decays at 0 (pre-hatch — pet has no needs yet). <code>grandma_house</code> decays at 0 (pet is frozen in the rescue path).</p>\n<p>§1.3  <strong>Decay is monotonic linear.</strong> The decay function MUST be <code>clamp_0_100(current - rate * hours_since_last_seen)</code>. No non-linear curves. No stat goes below 0; no stat goes above 100.</p>\n<p>§1.4  <strong>Care action effects.</strong> Care actions mutate stats by these amounts (delta from current, clamped 0–100):</p>\n<table><thead><tr><th>Action</th><th>hunger</th><th>cleanliness</th><th>happiness</th><th>energy</th></tr></thead><tbody><tr><td>feed (basic food)</td><td>+30</td><td>0</td><td>0</td><td>0</td></tr><tr><td>feed (premium food)</td><td>+50</td><td>0</td><td>+5</td><td>0</td></tr><tr><td>clean</td><td>0</td><td>+60</td><td>0</td><td>-5</td></tr><tr><td>hug</td><td>0</td><td>0</td><td>+25</td><td>0</td></tr><tr><td>mini-game (win)</td><td>0</td><td>-3</td><td>+20</td><td>-10</td></tr><tr><td>mini-game (loss)</td><td>0</td><td>-2</td><td>+5</td><td>-8</td></tr><tr><td>sleep (8h passive)</td><td>0</td><td>-5</td><td>0</td><td>+90</td></tr></tbody></table>\n<p>Exact values configurable per TASK-OBS-001 feature flag. The cleanliness penalty on mini-game / sleep models real-world wear-and-tear.</p>\n<p>§1.5  <strong>Server-authoritative computation.</strong> Every stat value MUST be computed server-side. The client never decides \"your hunger is 75 now\" — it asks the server, which holds authoritative state in Colyseus <code>PetState</code> + persists to Postgres on idle disposal (TASK-INFRA-002 §1.12).</p>\n<p>§1.6  <strong>In-room tick.</strong> Colyseus <code>PetRoom</code> MUST run a <code>stat-tick.handler.ts</code> every <strong>60 seconds</strong> while the room is non-idle. Each tick: (a) computes elapsed minutes since <code>pets.last_seen_at</code>; (b) applies decay to <code>PetState</code>; (c) broadcasts diff via Colyseus schema sync. The tick is a no-op during <code>egg</code> and <code>grandma_house</code> stages.</p>\n<p>§1.7  <strong>Offline reconciliation.</strong> On player rejoin (Colyseus <code>onJoin</code>), the server MUST: (a) compute total minutes offline; (b) cap effective decay at 24 hours (a player gone 5 days does NOT find an empty pet at 0 — caps preserve emotional retention); (c) apply capped decay; (d) emit <code>pet.offline.reconciled</code> event with diff stats.</p>\n<p>§1.8  <strong>Care prerequisite snapshot.</strong> Every minute the in-room tick MUST write a row to <code>pet_stat_history</code> (TimescaleDB hypertable): <code>(pet_id, stat, value, observed_at)</code>. This is the substrate for TASK-PET-002 §1.5 care-gated stall detection (queries last 24/72 h history for hours-below-30). Retention 90 days (compressed after 7 days).</p>\n<p>§1.9  <strong>Stat warnings.</strong> When any stat crosses 30 → 29 (downward), Colyseus MUST broadcast <code>pet.stat.warn { stat, value }</code> to the player. The Cocos client renders a soft \"Mochi looks &lt;hungry / dirty / sad / tired&gt;\" inline hint. When stat crosses 10 → 9, broadcast <code>pet.stat.critical</code> triggering a stronger UI cue + a deferred push notification (subject to sleep-hours per TASK-VIRAL-005).</p>\n<p>§1.10  <strong>Stat full-state on join.</strong> Colyseus <code>onJoin</code> MUST broadcast the full current stat snapshot before the client renders; the client MUST display the stat-bar at server-truth values, NOT extrapolate.</p>\n<p>§1.11  <strong>Stat-bar rendering — accessibility.</strong> The Cocos <code>StatBar.ts</code> component MUST: (a) render each bar with a colour-blind-safe palette (defaults to the TASK-A11Y-001 colour-blind preset when enabled); (b) show numeric value on long-press for screen-reader users; (c) animate transitions at ≤ 200 ms (capped at 50 ms under reduce-motion). Bar fill colours: green ≥ 50; yellow 30-49; orange 20-29; red &lt; 20.</p>\n<p>§1.12  <strong>Persistence cadence.</strong> <code>PetState</code> stat updates are persisted to <code>public.pets</code> table on: (a) every care action (immediate); (b) every 5 min if the room is active; (c) on <code>onDispose</code> (room idle 15 min, TASK-INFRA-002 §1.12). The Postgres columns MUST be added in the migration: <code>hunger int not null default 100, cleanliness int not null default 100, happiness int not null default 100, energy int not null default 100</code>.</p>\n<p>§1.13  <strong>Persistence is best-effort with eventual consistency guarantees.</strong> If Postgres is briefly unavailable, in-room state remains correct (Colyseus is authoritative for the active session). The reconciliation on next disposal flush picks up missed writes via the room's \"dirty since last flush\" tracker.</p>\n<p>§1.14  <strong>Stat-clamp at care-action handlers.</strong> Every care-action handler MUST clamp resulting stat values to [0, 100]. Negative or &gt; 100 values MUST be impossible — handler-level clamping is enforced by a shared <code>clampStat()</code> utility + asserted in tests.</p>\n<p>§1.15  <strong>Stat decay anti-cheat.</strong> Direct client writes to <code>PetState.hunger</code> etc. MUST be impossible — Colyseus schema is server-only. Care actions go through server handlers per TASK-INFRA-002 §1.10. Impossible state transitions (e.g. negative stat values) emit <code>security.impossible-transition</code> Sentry event (TASK-INFRA-002 §1.9).</p>\n<p>§1.16  <strong>Energy + sleep — passive recovery.</strong> Unlike the other three stats, <code>energy</code> recovers passively during the \"sleep\" period defined as <code>local 22:00–07:00</code> (per player's region). During this window, <code>energy</code> recovers at +12/hr (so 8 hours sleep restores 96 points, capped at 100). Hunger / cleanliness / happiness still decay during sleep (slower at the stage-rate × 0.5).</p>\n<p>§1.17  <strong>AI prompt context.</strong> The current stats (and 24 h history) MUST be exposable to the AI personality engine (TASK-AI-001) as part of the persona-context payload. AI replies are aware of pet emotional state (\"Mochi is feeling really hungry right now ...\").</p>\n<p>§1.18  <strong>Co-parent visibility.</strong> When TASK-SOCIAL-002 (co-parent) ships, the co-parent MUST see live stat-bars + care-action history. The schema MUST be co-parent-aware from day one (RLS extension is the path).</p>\n<p>§1.19  <strong>Feature-flag tunability.</strong> All decay rates + care-action effects MUST be readable via Mixpanel feature flags. Defaults baked into code (TASK-OBS-001 §10 row 3 fallback).</p>\n<p>§1.20  <strong>Analytics + observability.</strong> <code>pet.stat.changed { stat, prev, new, source }</code>, <code>pet.stat.warn</code>, <code>pet.stat.critical</code>, <code>pet.offline.reconciled</code> MUST be emitted per TASK-OBS-001 §1.4 taxonomy with schema validation. The mini-game / care-action source MUST be tagged so funnel analysis can identify which care actions drive retention.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why four stats.</strong> Plan §PART 3 — Tamagotchi-style is canonically a multi-stat care loop. Fewer stats trivialise the loop (one stat = \"fill this bar\"); more stats overwhelm casual players. Pou uses 4; Talking Tom uses ~4 with simplified UX. Four stats with distinct care actions is the proven design.</p>\n<p><strong>Why monotonic linear decay.</strong> Players form intuition about a linear \"10 points per hour\" rule fast. Non-linear curves (e.g. exponential — decays slow at full, fast when low) feel buggy (\"I just fed them why are they hungry already?\"). Linear decay also makes the math straightforward for the player + the QA team.</p>\n<p><strong>Why stage-aware decay rates.</strong> A baby pet decaying as slow as an adult feels static. A baby decaying faster simulates \"babies need more care\" intuition + ramps up engagement during the early stage where retention is most fragile.</p>\n<p><strong>Why cap offline decay at 24 hours.</strong> A player offline 5 days returning to a 0-stat pet is a churn trigger. The cap softens this — they return to a slightly-needy pet, not a crisis. Plan §PART 3 retention mechanics emphasise non-punitive design.</p>\n<p><strong>Why TimescaleDB hypertable for stat-history.</strong> Stat history is high-write-volume time-series (one row per pet per minute). TimescaleDB compresses + auto-partitions this efficiently. 90-day retention with compression after 7 days gives TASK-PET-002 care-stall detection ample window without runaway storage. Same TimescaleDB pattern used in sale-noti project for price history — but as per the project-isolation rule, we don't cite that, we just adopt the proven pattern.</p>\n<p><strong>Why broadcast warnings at 30 → 29 and 10 → 9.</strong> These are the inflection points where a player should care + intervene. 30 is the TASK-PET-002 care-gated-stall threshold (warning gives the player a chance to act before the stall actually triggers). 10 is critical territory before TASK-PET-008 Permadeath-Lite kicks in.</p>\n<p><strong>Why best-effort persistence + Colyseus-authoritative for active session.</strong> Postgres latency / availability is fine for periodic flushes; in-room state needs to be instantaneous. Colyseus + in-memory + 5-min flush is the canonical pattern. Worst case (Postgres down for 30 min, room disposes mid-outage): we lose ≤ 30 min of stat progression, recoverable from <code>pet_stat_history</code> reconciliation on next room create.</p>\n<p><strong>Why passive energy recovery during sleep hours.</strong> Plan §PART 3 — \"respect sleep hours.\" Energy recovering only via active gameplay would punish players who do close the app at night (good behaviour). Passive recovery rewards healthy real-world habits.</p>\n<p><strong>Why all care-action effects in one table.</strong> Single source of truth simplifies live-ops tuning + task review. The Mixpanel feature-flag bindings consume the same table.</p>\n<p><strong>Why colour-blind-safe palette default.</strong> Plan §PART 5 accessibility. Green/red is the canonical pitfall — using yellow/orange in the middle band gives 4-tier readability for protan/deutan colour-blind users without a setting toggle.</p>\n<p><strong>Why feature-flag tunability over hardcoded values.</strong> Soft-launch in VN will demand 5-10 tuning iterations on decay rates (too aggressive = churn; too gentle = no urgency). Without flag-driven tuning, every adjustment is a redeploy.</p>\n<p><strong>Why source-tagging in stat events.</strong> Funnel analysis: \"do players who feed Premium food retain better than Basic feeders?\" requires knowing which action source moved the stat. Without it the analytics team is blind to which care primitive drives retention.</p>\n<p><strong>Why co-parent visibility designed in day one.</strong> Plan §PART 3 + TASK-SOCIAL-002 — PetPair is the viral wedge. If stat-bar visibility is owner-only at P1, P2 needs to retrofit; designing the RLS for co-parent at P1 is essentially free.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Stat decay service</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/stats/stat-decay.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { STAT_CONFIG } from './stat-config';\n\nexport type StatName = 'hunger' | 'cleanliness' | 'happiness' | 'energy';\nexport type Stats = Record&lt;StatName, number&gt;;\nexport type Stage = 'egg' | 'baby' | 'teen' | 'adult' | 'grandma_house';\n\n@Injectable()\nexport class StatDecayService {\n  decay(stats: Stats, stage: Stage, minutesElapsed: number, opts: { isSleepWindow: boolean } = { isSleepWindow: false }): Stats {\n    if (stage === 'egg' || stage === 'grandma_house') return { ...stats };\n    const hours = Math.max(0, minutesElapsed / 60);\n    const cappedHours = Math.min(hours, 24);              // §1.7 offline cap\n    const sleepMult = opts.isSleepWindow ? 0.5 : 1.0;     // §1.16\n    const result: Stats = { ...stats };\n    for (const k of ['hunger','cleanliness','happiness'] as const) {\n      const rate = STAT_CONFIG.decay[stage][k] * sleepMult;\n      result[k] = clamp(result[k] - rate * cappedHours);\n    }\n    if (opts.isSleepWindow) {\n      result.energy = clamp(result.energy + STAT_CONFIG.energy_sleep_recovery_per_h * cappedHours);\n    } else {\n      result.energy = clamp(result.energy - STAT_CONFIG.decay[stage].energy * cappedHours);\n    }\n    return result;\n  }\n\n  apply(stats: Stats, action: keyof typeof STAT_CONFIG.actions, outcome: 'win' | 'loss' = 'win'): Stats {\n    const effects = STAT_CONFIG.actions[action]?.[outcome] ?? STAT_CONFIG.actions[action];\n    const result: Stats = { ...stats };\n    for (const [k, delta] of Object.entries(effects ?? {})) {\n      result[k as StatName] = clamp(result[k as StatName] + (delta as number));\n    }\n    return result;\n  }\n}\n\nfunction clamp(v: number): number { return Math.max(0, Math.min(100, Math.round(v))); }</code></pre>\n<h3>3.2 — Stat config</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/stats/stat-config.ts\nexport const STAT_CONFIG = {\n  decay: {\n    egg:           { hunger: 0,   cleanliness: 0,   happiness: 0,   energy: 0   },\n    baby:          { hunger: 4.0, cleanliness: 3.0, happiness: 2.5, energy: 3.5 },\n    teen:          { hunger: 3.0, cleanliness: 2.5, happiness: 2.0, energy: 3.0 },\n    adult:         { hunger: 2.0, cleanliness: 2.0, happiness: 1.5, energy: 2.5 },\n    grandma_house: { hunger: 0,   cleanliness: 0,   happiness: 0,   energy: 0   },\n  },\n  energy_sleep_recovery_per_h: 12,\n  actions: {\n    feed_basic:   { hunger: +30, cleanliness: 0,   happiness: 0,   energy: 0   },\n    feed_premium: { hunger: +50, cleanliness: 0,   happiness: +5,  energy: 0   },\n    clean:        { hunger: 0,   cleanliness: +60, happiness: 0,   energy: -5  },\n    hug:          { hunger: 0,   cleanliness: 0,   happiness: +25, energy: 0   },\n    mini_game: {\n      win:        { hunger: 0,   cleanliness: -3,  happiness: +20, energy: -10 },\n      loss:       { hunger: 0,   cleanliness: -2,  happiness: +5,  energy: -8  },\n    },\n  },\n} as const;</code></pre>\n<h3>3.3 — Stat history migration (TimescaleDB hypertable)</h3>\n<pre><code class=\"language-sql\">-- infra/supabase/standard/migrations/20260517_008_pet_stat_history.sql\ncreate extension if not exists timescaledb;\n\ncreate table public.pet_stat_history (\n  pet_id text not null references public.pets(id) on delete cascade,\n  observed_at timestamptz not null default now(),\n  hunger int not null,\n  cleanliness int not null,\n  happiness int not null,\n  energy int not null,\n  primary key (pet_id, observed_at)\n);\nselect create_hypertable('public.pet_stat_history', 'observed_at', if_not_exists =&gt; true);\nselect add_retention_policy('public.pet_stat_history', interval '90 days', if_not_exists =&gt; true);\nselect add_compression_policy('public.pet_stat_history', interval '7 days', if_not_exists =&gt; true);\n\nalter table public.pet_stat_history enable row level security;\ncreate policy \"pet_stat_history self\" on public.pet_stat_history for select\n  using (pet_id in (select id from public.pets where owner_id = auth.uid()));</code></pre>\n<h3>3.4 — Stat tick handler (Colyseus)</h3>\n<pre><code class=\"language-typescript\">// apps/realtime/src/rooms/handlers/stat-tick.handler.ts\nexport function installStatTick(room: any, deps: { decay: StatDecayService; history: StatHistoryService }) {\n  room.clock.setInterval(async () =&gt; {\n    const now = Date.now();\n    for (const [, pet] of room.state.pets) {\n      if (pet.stage === 'egg' || pet.stage === 'grandma_house') continue;\n      const sinceMin = (now - pet.lastSeenMs) / 60_000;\n      const isSleep = isLocalSleepWindow(pet.ownerRegion);\n      const decayed = deps.decay.decay(snapshot(pet), pet.stage, sinceMin, { isSleepWindow: isSleep });\n      applyToColyseus(pet, decayed);\n      pet.lastSeenMs = now;\n      maybeBroadcastWarnings(room, pet, snapshot(pet), decayed);\n      deps.history.recordTick(pet.id, decayed);\n    }\n  }, 60_000);\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> <code>StatDecayService.decay({...all 100}, 'baby', 60, {isSleepWindow:false})</code> returns approximately <code>{hunger:96, cleanliness:97, happiness:97.5, energy:96.5}</code> (within 1 unit). Verified by <code>__tests__/stat-decay.spec.ts</code>.</p>\n<p><strong>AC2.</strong> <code>StatDecayService.decay({...all 100}, 'egg', 60, {})</code> returns unchanged stats. Verified by spec test.</p>\n<p><strong>AC3.</strong> <code>StatDecayService.decay({...all 100}, 'baby', 2880, {})</code> (48 h offline) returns stats reflecting only 24 h of decay (cap). Verified by spec test.</p>\n<p><strong>AC4.</strong> Sleep-window energy recovery: <code>decay({...energy: 50}, 'adult', 60, {isSleepWindow:true})</code> returns energy ≈ 62. Verified by spec test.</p>\n<p><strong>AC5.</strong> <code>StatDecayService.apply</code> clamps stat values: applying <code>clean</code> (+60) to cleanliness=70 returns 100, not 130. Verified by spec test.</p>\n<p><strong>AC6.</strong> Negative stat values are impossible — applying <code>feed_basic</code> to hunger=-5 (invalid input) clamps to 0+30=30. Verified by spec test.</p>\n<p><strong>AC7.</strong> Colyseus tick broadcasts diff via schema sync. Verified by <code>__tests__/PetRoom.spec.ts</code> integration test.</p>\n<p><strong>AC8.</strong> Stat warning at 30→29 boundary broadcasts <code>pet.stat.warn</code>. Verified by spec test stepping decay across boundary.</p>\n<p><strong>AC9.</strong> <code>pet_stat_history</code> hypertable accepts inserts and respects 90-day retention. Verified by migration test on a shadow Supabase.</p>\n<p><strong>AC10.</strong> Offline reconciliation: a pet whose last_seen was 48h ago + stats {hunger:80, ...} on rejoin sees correct decayed stats (24h cap applied). Verified by integration test.</p>\n<p><strong>AC11.</strong> Co-parent visibility: a co-parent (TASK-SOCIAL-002 will activate) cannot read another player's stat-history at the schema level — verified by RLS test (even if TASK-SOCIAL-002 not yet shipped, the policy denies until the co-parent join row exists).</p>\n<p><strong>AC12.</strong> Stat-bar Cocos component renders in colour-blind-safe palette by default. Verified by Playwright UI test snapshotting the bar colours.</p>\n<p><strong>AC13.</strong> Mini-game win effects: applying <code>mini_game.win</code> to {happiness:60, cleanliness:80, energy:60} returns {happiness:80, cleanliness:77, energy:50}. Verified by spec test.</p>\n<p><strong>AC14.</strong> Feature-flag override: setting <code>pet.stat.decay.baby.hunger=8</code> via Mixpanel flag doubles the baby hunger decay rate at next tick. Verified by spec test mocking flag service.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 — Stat decay tests</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/stats/__tests__/stat-decay.spec.ts\nimport { describe, it, expect } from 'vitest';\nimport { StatDecayService } from '../stat-decay.service';\n\ndescribe('TASK-PET-003 — stat decay', () =&gt; {\n  const svc = new StatDecayService();\n  const full = { hunger: 100, cleanliness: 100, happiness: 100, energy: 100 };\n\n  it('1 hour baby decay matches table', () =&gt; {\n    const r = svc.decay(full, 'baby', 60, { isSleepWindow: false });\n    expect(r).toEqual({ hunger: 96, cleanliness: 97, happiness: 98, energy: 96 });\n  });\n\n  it('egg does not decay', () =&gt; {\n    expect(svc.decay(full, 'egg', 600, {})).toEqual(full);\n  });\n\n  it('48h offline caps at 24h', () =&gt; {\n    const r48 = svc.decay(full, 'baby', 2880, {});\n    const r24 = svc.decay(full, 'baby', 1440, {});\n    expect(r48).toEqual(r24);\n  });\n\n  it('sleep recovers energy', () =&gt; {\n    const r = svc.decay({ ...full, energy: 50 }, 'adult', 60, { isSleepWindow: true });\n    expect(r.energy).toBeGreaterThanOrEqual(61);\n    expect(r.energy).toBeLessThanOrEqual(63);\n  });\n\n  it('clamps at 0..100', () =&gt; {\n    expect(svc.apply({ hunger: 70, cleanliness: 100, happiness: 0, energy: 0 }, 'clean')).toMatchObject({ cleanliness: 100 });\n    expect(svc.apply({ hunger: 0, cleanliness: 0, happiness: 0, energy: 0 }, 'hug')).toMatchObject({ happiness: 25 });\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/pets/stats/stat-reconciliation.service.ts\n@Injectable()\nexport class StatReconciliationService {\n  constructor(private readonly decay: StatDecayService, private readonly supa: SupabaseClient) {}\n\n  async reconcileOnRejoin(petId: string): Promise&lt;{ deltaStats: Partial&lt;Stats&gt;; reconciled: boolean }&gt; {\n    const { data: pet } = await this.supa.from('pets').select('*').eq('id', petId).single();\n    if (!pet || pet.stage === 'egg' || pet.stage === 'grandma_house') return { deltaStats: {}, reconciled: false };\n    const now = Date.now();\n    const sinceMin = (now - new Date(pet.last_seen_at).getTime()) / 60_000;\n    if (sinceMin &lt; 1) return { deltaStats: {}, reconciled: false };\n    const next = this.decay.decay(\n      { hunger: pet.hunger, cleanliness: pet.cleanliness, happiness: pet.happiness, energy: pet.energy },\n      pet.stage as any, sinceMin, { isSleepWindow: false /* TODO: derive */ },\n    );\n    await this.supa.from('pets').update({ ...next, last_seen_at: new Date().toISOString() }).eq('id', petId);\n    return { deltaStats: next, reconciled: true };\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Supabase TimescaleDB extension enabled.</p>\n<p><strong>Internal:</strong> TASK-PET-001 (<code>pets</code> table + last_seen_at + cascade), TASK-PET-002 (<code>stage</code> column drives decay rate selection).</p>\n<p><strong>Blocks:</strong> TASK-CARE-001/002/003/004 (each care action calls <code>StatDecayService.apply</code>), TASK-CARE-005 (streak system reads stat history), TASK-PET-008 (Permadeath-Lite reads 7-day stat history).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — <code>pet.stat.changed</code> event</h3>\n<pre><code class=\"language-json\">{\n  \"event\": \"pet.stat.changed\",\n  \"pet_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"stat\": \"hunger\",\n  \"prev\": 95,\n  \"new\": 99,\n  \"source\": \"care.feed_basic\",\n  \"stage\": \"baby\",\n  \"tenant_id\": \"mochi\",\n  \"emitted_at\": \"2026-08-12T14:36:01Z\"\n}</code></pre>\n<h3>8.2 — <code>pet.stat.warn</code></h3>\n<pre><code class=\"language-json\">{\n  \"event\": \"pet.stat.warn\",\n  \"pet_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"stat\": \"happiness\",\n  \"value\": 29,\n  \"stage\": \"baby\"\n}</code></pre>\n<h3>8.3 — <code>pet.offline.reconciled</code></h3>\n<pre><code class=\"language-json\">{\n  \"event\": \"pet.offline.reconciled\",\n  \"pet_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"offline_minutes\": 1820,\n  \"offline_minutes_capped\": 1440,\n  \"delta\": { \"hunger\": -96, \"cleanliness\": -72, \"happiness\": -60, \"energy\": -84 }\n}</code></pre>\n<h3>8.4 — Stat-history row</h3>\n<pre><code class=\"language-json\">{ \"pet_id\": \"01HC7QG...\", \"observed_at\": \"2026-08-12T14:36:01Z\", \"hunger\": 76, \"cleanliness\": 82, \"happiness\": 71, \"energy\": 55 }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> 4 stats vs 5/6? → §1.1 — 4 matches plan + Pou + Talking Tom precedent.</li><li><strong>OQ-2 (resolved):</strong> Linear vs exponential decay? → §1.3 + §2 — linear; intuitable.</li><li><strong>OQ-3 (resolved):</strong> Offline cap — 24 h or 48 h? → §1.7 — 24 h, retention-protective.</li><li><strong>OQ-4 (resolved):</strong> Should energy decay at all? → §1.16 — yes during waking hours, recovers in sleep window.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Tick handler stops firing (Colyseus clock bug)</td><td>Synthetic check on <code>pet.stat.tick</code> event rate</td><td>Stats freeze</td><td>Restart room; investigate clock</td></tr><tr><td>2</td><td>TimescaleDB extension unavailable</td><td>Migration fails</td><td>History writes fail</td><td>Fallback to plain table; compress manually; or pin to compatible Supabase tier</td></tr><tr><td>3</td><td>Mixpanel feature flag returns NaN value</td><td><code>STAT_CONFIG</code> fallback hit</td><td>Defaults apply</td><td>Flag service alerts; investigate flag JSON</td></tr><tr><td>4</td><td>Stat clamp bypassed somewhere (negative value reaches DB)</td><td>Postgres <code>check</code> constraint rejects</td><td>Insert fails</td><td>Audit caller path; add unit test for that handler</td></tr><tr><td>5</td><td>Sleep-window timezone wrong (DST transition)</td><td>Stat decay timing off-by-1-hour</td><td>Confused player UX</td><td>Use IANA TZ data; recompute on TZ change</td></tr><tr><td>6</td><td>Offline reconciliation infinite loop (last_seen_at not updated)</td><td>Memory + CPU spike</td><td>Server hot</td><td>Set last_seen_at unconditionally on reconcile</td></tr><tr><td>7</td><td>RLS missing on <code>pet_stat_history</code> exposing other players' history</td><td>Spec test fails</td><td>Privacy exposure</td><td>Add RLS policy; audit Sentry for affected rows</td></tr><tr><td>8</td><td>Stat warning event flood</td><td>OBS alert</td><td>Notification spam</td><td>Hysteresis (only fire on crossing, not repeated)</td></tr><tr><td>9</td><td>Premium-food handler accidentally applies twice (idempotency miss)</td><td>Stat unexpected</td><td>Anti-cheat triggers</td><td>Per-handler idempotency keys</td></tr><tr><td>10</td><td>Stat-history retention policy fails</td><td>Disk grows</td><td>Storage cost spike</td><td>Manual <code>add_retention_policy</code>; audit Supabase</td></tr><tr><td>11</td><td>Co-parent stat broadcast leaks to non-co-parent</td><td>RLS test fails</td><td>Privacy issue</td><td>Tighten RLS; audit Sentry</td></tr><tr><td>12</td><td>Concurrent care actions mutate stats simultaneously</td><td>Optimistic concurrency conflict</td><td>Race</td><td>Server serialises via per-pet mutex in Colyseus room</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 (core loop — feed/clean/hug + stat bars), plan §PART 4 (anti-cheat — server-authoritative), plan §PART 5 (accessibility — colour-blind palette).</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Final stat decay rates (Mixpanel feature flag) — tuned in soft launch.</li><li>Sleep-window time-zone resolution — uses player's <code>region_of_record</code> + IANA tz mapping.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Client-side stat value (anti-cheat).</li><li>Non-linear decay curves.</li><li>Decay during <code>egg</code> or <code>grandma_house</code>.</li><li>Stat values outside [0, 100].</li></ul>\n<p><strong>Cross-reference:</strong> TASK-CARE-001/002/003/004/005 + TASK-PET-008 all depend on this model. TASK-AI-001 reads stat state for personality dialogue. TASK-PET-002 reads 24/72 h history for care-gated stall detection.</p>";
