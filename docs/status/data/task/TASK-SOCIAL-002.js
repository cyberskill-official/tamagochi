window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SOCIAL-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>PetPair definition.</strong> A PetPair is a pet co-parented by <strong>exactly 2 players</strong>: the original owner + 1 co-parent. Both can feed/clean/hug/play; both see live state.</p>\n<p>§1.2  <strong>Eligibility.</strong> Initial owner must be at <code>stage ∈ {teen, adult}</code> per TASK-PET-002 §1.6. Both players must be <strong>friends</strong> per TASK-SOCIAL-001. Both must be on the standard SKU (no kid co-parenting at P1/P2; parental-dashboard-controlled in P3 via TASK-SUB-002).</p>\n<p>§1.3  <strong>Invite flow.</strong> Owner taps \"Invite a co-parent\" → selects a friend → API <code>POST /v1/pets/:petId/coparent/invite</code> body <code>{ friend_user_id: string }</code>. Creates a row in <code>pet_co_parents</code> with <code>status='pending'</code>.</p>\n<p>§1.4  <strong>Accept flow.</strong> Friend receives push + in-app card \"X wants to co-parent Mochi with you!\". Friend taps Accept → <code>POST /v1/pets/:petId/coparent/:friendUserId/accept</code>. Row transitions to <code>active</code>.</p>\n<p>§1.5  <strong>Co-parent permissions.</strong> Active co-parent can: feed/clean/hug/play; trigger AI dialogue; place in AR (their own device's AR session); take photos. CANNOT: rename, breed, trade, archive to grandma, remove the original owner.</p>\n<p>§1.6  <strong>Owner privileges.</strong> Owner can remove co-parent at any time via <code>DELETE /v1/pets/:petId/coparent/:userId</code>. Co-parent receives gentle notification.</p>\n<p>§1.7  <strong>Co-parent leaves.</strong> Co-parent can leave voluntarily via <code>DELETE /v1/pets/:petId/coparent/self</code>. Owner notified.</p>\n<p>§1.8  <strong>One co-parent per pet at P2.</strong> Multi-co-parent (3+) is deferred to P3+.</p>\n<p>§1.9  <strong>Pets per player as co-parent.</strong> A player can be co-parent on up to <strong>3 pets</strong> (free) or <strong>10 pets</strong> (Pet+). Owned pets + co-parented pets count separately against their respective limits.</p>\n<p>§1.10  <strong>Receipt push.</strong> When a co-parent performs a care action, the OTHER party receives a push notification (TASK-VIRAL-005 sleep-hour respected): \"Linh fed Mochi while you were asleep ❤️\" — copy locked in <code>apps/cocos/assets/i18n/&lt;locale&gt;/petpair.json</code>.</p>\n<p>§1.11  <strong>Receipt push throttling.</strong> Max 4 receipt pushes per pet per day per recipient. Beyond → aggregated daily digest at next non-quiet-hour.</p>\n<p>§1.12  <strong>Live presence in PetRoom.</strong> Colyseus PetRoom <code>maxClients = 2</code> (owner + co-parent). When both connected simultaneously, see real-time care actions. When one disconnected, the other plays solo + receives receipts.</p>\n<p>§1.13  <strong>Source attribution.</strong> Care actions performed by co-parent tagged <code>source='co_parent_remote'</code> per TASK-CARE-001 §1.17. Audit + analytics distinguishable.</p>\n<p>§1.14  <strong>Break-up screen.</strong> A <code>breakup-watch.service.ts</code> MUST monitor: when ONLY ONE party has performed care for <strong>3 consecutive days</strong>, surface \"break-up screen\" UX to BOTH parties: \"Linh hasn't fed Mochi in 3 days. Are you still co-parenting together?\". CTAs: \"Yes, keep going\" / \"End PetPair\" / \"Talk to Linh\".</p>\n<p>§1.15  <strong>End PetPair on choose.</strong> If either chooses \"End PetPair\", co-parent row transitions to <code>removed</code>. Both notified.</p>\n<p>§1.16  <strong>Pet stays with owner.</strong> When PetPair ends (any path), the pet remains with the original owner. Co-parent loses access. (Designed-in non-disputable ownership.)</p>\n<p>§1.17  <strong>Cross-SKU + cross-tenant locked.</strong> Cross-SKU forbidden per §1.2. Cross-tenant forbidden per TASK-INFRA-003.</p>\n<p>§1.18  <strong>Audit + analytics.</strong> <code>social.coparent.invite_sent</code>, <code>social.coparent.accepted</code>, <code>social.coparent.declined</code>, <code>social.coparent.action { source }</code>, <code>social.coparent.breakup_warned</code>, <code>social.coparent.ended { trigger: 'owner' | 'coparent' | 'breakup' }</code> per TASK-OBS-001.</p>\n<p>§1.19  <strong>TikTok share moments.</strong> Per TASK-VIRAL-001, breakup-warning moment is a documented share-worthy beat (sad/dramatic). Opt-in share button surfaces.</p>\n<p>§1.20  <strong>Pet-pair limit per owner.</strong> An owner can have at most <strong>1 active co-parent per pet</strong> at P2. Trying to add a 2nd → 409 <code>coparent.already_active</code>.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why exactly 2 at P2.</strong> Plan §PART 3 Widgetable/Pengu pattern — co-parent is 1-to-1 intimacy. 3+ dilutes the emotional thread.</p>\n<p><strong>Why limited co-parent permissions.</strong> Owner retains identity. Renaming / breeding / trading affect long-term inventory; co-parent only does daily care.</p>\n<p><strong>Why owner can remove unilaterally.</strong> Anti-abuse — prevents extortion (\"give me Coins or I'll grief your pet\").</p>\n<p><strong>Why both standard SKU only at P2.</strong> TASK-AUTH-003 + COPPA — kids social interactions go through parental dashboard. P3 TASK-SUB-002 wires the parental gate.</p>\n<p><strong>Why 4 daily receipt pushes max.</strong> Plan §PART 3 retention + plan §PART 5 ethical UX — anti-spam on notification.</p>\n<p><strong>Why break-up screen on 3-day asymmetry.</strong> Plan §PART 3 — \"creates Reddit drama screenshots.\" Designed-in tension.</p>\n<p><strong>Why pet stays with owner on end.</strong> Conflict avoidance. Disputed-ownership scenarios collapse to \"owner wins.\"</p>\n<p><strong>Why share break-up moment.</strong> Plan §PART 3 — sad-relatable beats are organically viral.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/social/coparent/coparent.controller.ts\n@Controller('v1/pets')\n@UseGuards(SupabaseJwtGuard)\nexport class CoparentController {\n  @Post(':petId/coparent/invite')\n  async invite(@CurrentUser() u: AuthedUser, @Param('petId') petId: string, @Body() body: { friend_user_id: string }) {\n    return this.svc.invite(u, petId, body.friend_user_id);\n  }\n  @Post(':petId/coparent/:friendUserId/accept')\n  async accept(@CurrentUser() u: AuthedUser, @Param('petId') petId: string, @Param('friendUserId') friendId: string) {\n    return this.svc.accept(u, petId, friendId);\n  }\n  @Delete(':petId/coparent/:userId')\n  async remove(@CurrentUser() u: AuthedUser, @Param('petId') petId: string, @Param('userId') uid: string) {\n    return this.svc.remove(u, petId, uid);\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/social/coparent/coparent.service.ts (excerpt)\nasync invite(u: AuthedUser, petId: string, friendId: string) {\n  const pet = await this.pets.byId(petId);\n  if (pet.owner_id !== u.id) throw new HttpException('forbidden', 403);\n  if (!['teen','adult'].includes(pet.stage)) throw new HttpException('pet.stage.locked', 422);\n  if (!await this.friends.isAccepted(u.id, friendId)) throw new HttpException('coparent.not_friend', 422);\n  if (await this.crossSku(u.id, friendId)) throw new HttpException('coparent.cross_sku', 403);\n  const existing = await this.supa.from('pet_co_parents').select('status').eq('pet_id', petId).eq('status', 'active').maybeSingle();\n  if (existing.data) throw new HttpException('coparent.already_active', 409);\n  await this.supa.from('pet_co_parents').insert({ pet_id: petId, co_parent_user_id: friendId, status: 'pending' });\n  await this.notify.sendCoparentInvite(friendId, pet.display_name, u.id);\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/social/coparent/breakup-watch.service.ts\n@Injectable()\nexport class BreakupWatchService {\n  async runDailyCheck() {\n    const { data: pairs } = await this.supa.from('pet_co_parents').select('*').eq('status', 'active');\n    for (const pair of pairs ?? []) {\n      const since = daysAgo(3);\n      const { data: actions } = await this.supa.from('care_actions')\n        .select('owner_id, source').eq('pet_id', pair.pet_id).gte('occurred_at', since);\n      const distinctActors = new Set(actions?.map(a =&gt; a.source === 'co_parent_remote' ? pair.co_parent_user_id : a.owner_id));\n      if (distinctActors.size === 1 &amp;&amp; actions &amp;&amp; actions.length &gt; 0) {\n        await this.notify.sendBreakupWarning(pair, [...distinctActors][0]);\n        await this.audit.emit('social.coparent.breakup_warned', { pet_id: pair.pet_id });\n      }\n    }\n  }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Invite → accept flow end-to-end. Verified. <strong>AC2.</strong> Cross-SKU invite returns 403. Verified. <strong>AC3.</strong> Co-parent feed action emits <code>source=co_parent_remote</code>. Verified. <strong>AC4.</strong> Both clients receive receipt push (sleep-respected). Verified. <strong>AC5.</strong> Break-up screen surfaces on 3-day one-sided care. Verified by fake-clock. <strong>AC6.</strong> Owner remove unilateral. Verified. <strong>AC7.</strong> Pet stays with owner on end. Verified. <strong>AC8.</strong> Co-parent cannot rename/breed/trade. Verified. <strong>AC9.</strong> Receipt push 4/day cap → digest. Verified. <strong>AC10.</strong> PetRoom maxClients=2 enforced. Verified. <strong>AC11.</strong> Cross-tenant rejected. Verified. <strong>AC12.</strong> TikTok share button surfaces on break-up moment. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/social/coparent/__tests__/breakup-watch.spec.ts\ndescribe('TASK-SOCIAL-002 — breakup watch', () =&gt; {\n  it('warns on 3-day one-sided care', async () =&gt; {\n    await setupCoPair('owner', 'coparent', 'pet1');\n    for (let i = 0; i &lt; 3; i++) {\n      await advanceClockDay();\n      await seedCareAction('pet1', 'owner', 'ui');\n    }\n    await svc.runDailyCheck();\n    expect(await notifications.lastFor('owner')).toMatch(/breakup_warning/);\n    expect(await notifications.lastFor('coparent')).toMatch(/breakup_warning/);\n  });\n\n  it('does not warn when both have acted', async () =&gt; {\n    await setupCoPair('owner', 'coparent', 'pet1');\n    for (let i = 0; i &lt; 3; i++) {\n      await seedCareAction('pet1', i % 2 === 0 ? 'owner' : 'coparent', i % 2 === 0 ? 'ui' : 'co_parent_remote');\n      await advanceClockDay();\n    }\n    await svc.runDailyCheck();\n    expect(await notifications.lastFor('owner')).toBeUndefined();\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/social/BreakupScreenUi.ts\n@ccclass('BreakupScreenUi')\nexport class BreakupScreenUi extends Component {\n  async show(pairInfo: { other_party_name: string; pet_name: string; days_silent: number }) {\n    this.titleLabel.string = `${pairInfo.other_party_name} hasn't fed ${pairInfo.pet_name} in ${pairInfo.days_silent} days`;\n    this.continueBtn.onClick = () =&gt; this.dismiss();\n    this.endBtn.onClick = () =&gt; this.endPair();\n    this.talkBtn.onClick = () =&gt; this.openShareIntent({ source: 'breakup', message: 'Hey, are we still co-parenting Mochi?' });\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Push notification system (TASK-VIRAL-005). <strong>Internal:</strong> TASK-SOCIAL-001 (friend graph), TASK-CARE-001/002/003 (care actions with source attribution), TASK-INFRA-002 (Colyseus PetRoom maxClients=2), TASK-VIRAL-001 (break-up share moment). <strong>Blocks:</strong> TASK-SOCIAL-003 (trade needs co-parent presence model), TASK-SOCIAL-004 (wedding builds on co-parent).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/pets/01HC.../coparent/invite\n{ \"friend_user_id\": \"01HU...\" }\n→ 200 { \"pet_id\": \"01HC...\", \"co_parent_user_id\": \"01HU...\", \"status\": \"pending\" }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"social.coparent.action\", \"pet_id\": \"01HC...\", \"actor\": \"co_parent_remote\", \"kind\": \"feed\" }</code></pre>\n<pre><code class=\"language-json\">{\n  \"type\": \"push.coparent.receipt\",\n  \"title\": \"Linh fed Mochi while you were asleep ❤️\",\n  \"body\": \"Mochi's hunger is back up to 80%\"\n}</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"social.coparent.breakup_warned\", \"pet_id\": \"01HC...\", \"owner_id\": \"01HU...\", \"co_parent_user_id\": \"01HU...\", \"days_silent\": 3 }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> 1 vs N co-parents at P2? → §1.8 — 1.</li><li><strong>OQ-2 (resolved):</strong> Co-parent permissions scope? → §1.5 + §2.</li><li><strong>OQ-3 (resolved):</strong> Dispute resolution? → §1.16 — owner wins.</li><li><strong>OQ-4 (resolved):</strong> Kids co-parent? → §1.2 — defer.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Concurrent care from owner + co-parent</td><td>Per-pet mutex</td><td>None</td><td>Already covered by TASK-PET-003</td></tr><tr><td>2</td><td>Receipt push during sleep hours</td><td>TASK-VIRAL-005</td><td>Suppressed</td><td>OK</td></tr><tr><td>3</td><td>Co-parent count miscounts on cancellation</td><td>Audit</td><td>Limit wrong</td><td>Status-aware query</td></tr><tr><td>4</td><td>Receipt spam 5+/day</td><td>Throttle catches</td><td>Digest only</td><td>Verified by test</td></tr><tr><td>5</td><td>Break-up warning misfires (timezone off)</td><td>Spec test</td><td>False positive</td><td>TZ-aware day computation</td></tr><tr><td>6</td><td>Cross-SKU through bypass</td><td>Spec test</td><td>COPPA risk</td><td>Validation enforced</td></tr><tr><td>7</td><td>Owner removes during invite acceptance race</td><td>Atomic SQL</td><td>Last-write wins</td><td>Conservative reject</td></tr><tr><td>8</td><td>Co-parent disconnects mid-action</td><td>Colyseus tolerates</td><td>Action persists</td><td>Eventual consistency</td></tr><tr><td>9</td><td>TikTok share break-up creates negativity loops</td><td>DPO review</td><td>UX risk</td><td>Optional share + locale-tuned copy</td></tr><tr><td>10</td><td>PetPair across tenant via misconfig</td><td>RLS</td><td>Privacy</td><td>Enforced</td></tr><tr><td>11</td><td>Push spam to recipient (4+/day)</td><td>Aggregation</td><td>Digest</td><td>OK</td></tr><tr><td>12</td><td>Audit retention COPPA gap</td><td>DPO</td><td>7-yr kept</td><td>Configured</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 viral hook #1 (PetPair co-parent), plan §PART 3 break-up screen / Reddit drama.</p>\n<p><strong>Sub-decisions punted to ops:</strong> Receipt copy + break-up copy DPO-reviewed.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>3+ co-parents.</li><li>Real-money slot expansion.</li><li>Co-parent renaming/breeding/trading.</li><li>Permanent ownership transfer.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-SOCIAL-003 trade window uses presence model. TASK-SOCIAL-004 wedding builds on co-parent intimacy.</p>";
