window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LEGAL-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>This task establishes the compliance baseline that <strong>all kid-touching tasks depend on</strong>. It MUST be in place before any task that processes user-personal data ships, and it gates every later phase.</p>\n<p>§1.1  <strong>DPO appointment.</strong> The tamagochi-operating legal entity MUST appoint a Data Protection Officer per Vietnam PDPL Law 91/2025/QH15 Article 28 within 30 days of starting data processing. The DPO MAY be an internal employee OR an external personnel contracted via Tilleke / Rouse / EY-Vietnam. The DPO's name, work email, mobile phone, and Vietnamese national-ID number MUST be recorded in <code>docs/legal/DPO-appointment.md</code> and registered with Ministry of Public Security (A05) within 60 days.</p>\n<p>§1.2  <strong>DPIA (Data Processing Impact Assessment).</strong> A DPIA MUST be authored at <code>docs/legal/DPIA-2026-05.md</code> covering: (a) categories of personal data processed (display name, email, parental email, device persistent identifier, in-game purchase history, persona seed text); (b) purposes of processing (account auth, save-game persistence, IAP receipt validation, anti-cheat); (c) lawful basis (consent for adults, parental consent for under-13/under-16, contract performance for paid users); (d) cross-border-transfer destinations (AWS Singapore for stateful state, Supabase US/EU for metagame, Claude Anthropic US for LLM, Sentry Germany for errors); (e) risk register with mitigations; (f) data-subject rights surfaces (access / rectification / erasure / portability). The DPIA MUST be filed with A05 within 60 days of start of processing.</p>\n<p>§1.3  <strong>Cross-border Transfer Impact Assessment (TIA).</strong> A separate TIA at <code>docs/legal/cross-border-transfer-impact-assessment.md</code> MUST justify every cross-border transfer per PDPL Decree 356/2025/ND-CP. The TIA MUST enumerate: destination country, vendor name + DPA reference, data minimisation applied, supplementary measures (encryption-in-transit + encryption-at-rest + access controls), and the legitimate-interest balancing test. The TIA MUST be re-filed when a new cross-border vendor is added.</p>\n<p>§1.4  <strong>A05 breach-notification automation.</strong> A breach-notification template at <code>docs/legal/A05-breach-notification-template.md</code> AND a CA05-form generator script at <code>scripts/legal/generate-ca05-form.mjs</code> MUST be wired such that any breach affecting Vietnamese users triggers the 72-hour notification window per PDPL Article 24. The generator MUST output a printable PDF with: breach detection timestamp, root cause summary, scope (rows affected, columns affected, jurisdictions affected), mitigation status, DPO contact. A <code>breach-notification.service.ts</code> Nest provider MUST emit on the <code>breach-detected</code> event and persist the notification draft within 60 minutes of detection.</p>\n<p>§1.5  <strong>COPPA-2025 separate-SKU policy.</strong> Under-13 users MUST be served via a <strong>separate Apple App Store SKU and Google Play package</strong> (<code>world.cyberskill.tamagochi.kids</code>) distinct from the 13+ SKU (<code>world.cyberskill.tamagochi</code>). The kids SKU MUST: (a) disable all behavioural analytics SDKs (only GameAnalytics + Sentry permitted per TASK-OBS-001); (b) disable generative LLM dialogue (scripted trees only per TASK-AI-002); (c) restrict friend-graph features to invite-code-only — no name search (per TASK-SOCIAL-001); (d) restrict ads to SuperAwesome kWS contextual-only (per TASK-ADS-002); (e) restrict push notifications to a curated engagement-safe template set with the under-13 quiet-hours window expanded to local 20:00-08:00 (per TASK-VIRAL-005); (f) require parental consent before any data persistence beyond a session-scoped device ID.</p>\n<p>§1.6  <strong>Safe Harbor vendor.</strong> The compliance program MUST be enrolled in <strong>PRIVO Kids Privacy Assured (US-focused)</strong> OR <strong>SuperAwesome kidSAFE / kWS (US + UK + EU)</strong> before any kids-SKU submission to Apple or Google. The decision memo at <code>docs/legal/safe-harbor-vendor-decision-memo.md</code> MUST justify the choice. The chosen vendor's parental-consent flow MUST be wired such that:</p>\n<ul><li>a child's first attempt to register triggers <code>parental-consent-request</code> → Safe Harbor email-to-parent;</li><li>consent state is persisted in Supabase <code>parental_consent</code> table with columns <code>child_id, parent_email_hash, consent_state, consent_method, evidence_blob_url, expires_at</code>;</li><li>consent can be revoked by the parent via a unique URL within ≤ 1 business day of request, with automatic deletion of all child data within 30 days.</li></ul>\n<p>§1.7  <strong>Parental consent flow.</strong> Implementation MUST follow <code>docs/legal/parental-consent-flow.md</code> which mirrors COPPA-2025 (\"verifiable parental consent\" — VPC). The minimum acceptable VPC method is <strong>email-plus-payment-card-verification</strong> (Safe Harbor vendor handles the card hold + immediate release). The flow MUST be re-runnable when the child's <code>region</code> field updates (e.g. family relocates from VN to US triggers COPPA gate).</p>\n<p>§1.8  <strong>Privacy policies.</strong> Privacy policies MUST be authored in both English (<code>docs/legal/privacy-policy-en.md</code>) and Vietnamese (<code>docs/legal/privacy-policy-vi.md</code>) and published at <code>https://tamagochi.app/privacy/en</code> and <code>https://tamagochi.app/privacy/vi</code> BEFORE any production user account is created. The policies MUST cover the COPPA-2025 categories AND the PDPL Article 14 categories AND the GDPR Article 13/14 categories. The policies MUST be versioned in git and a <code>policy_version</code> audit field MUST be recorded on every consent event.</p>\n<p>§1.9  <strong>Data subject request (DSR) controller.</strong> A <code>data-subject-request.controller.ts</code> Nest controller MUST expose <code>POST /v1/dsr</code> accepting <code>{ kind: \"access\" | \"rectification\" | \"erasure\" | \"portability\", subject_email }</code> and respond with a ticket id. The SLAs MUST be: 30 days for access/portability (GDPR Article 12), immediate for under-13 erasure per COPPA, 30 days for adult erasure per PDPL Article 16. Tickets MUST be persisted in a <code>dsr_tickets</code> Supabase table.</p>\n<p>§1.10  <strong>COPPA age-gate middleware.</strong> A <code>coppa-age-gate.middleware.ts</code> Nest middleware MUST run before every authenticated API handler on the kids SKU. It MUST: (a) verify <code>parental_consent_state == \"granted\"</code> for the requesting <code>user_id</code>; (b) on <code>not-granted</code> return HTTP 451 with body <code>{ error: \"parental_consent_required\", consent_url }</code>; (c) on <code>revoked</code> or <code>expired</code> return HTTP 451 and trigger account-deletion job within 30 days; (d) log every age-gate hit to OBS with <code>event=coppa.gate.hit, state=&lt;state&gt;</code>.</p>\n<p>§1.11  <strong>Region-of-record determination.</strong> Every user's <code>region</code> field MUST be derived from a deterministic priority chain: (1) explicit user selection at first sign-in; (2) Apple/Google account region; (3) Zalo declared region (VN-only); (4) IP-derived geolocation (fallback only, MUST surface a \"confirm your country\" gate). The <code>region</code> MUST be persisted with version + source, and changes MUST trigger re-evaluation of the applicable compliance regime (COPPA / PDPL / GDPR-K / UK AADC).</p>\n<p>§1.12  <strong>No commingling of kids-SKU and 13+-SKU databases.</strong> The kids SKU MUST use a <strong>dedicated Supabase project</strong> (<code>tamagochi-kids</code>) with its own Postgres instance, its own Storage bucket, its own Auth users table. Cross-SKU data sharing is forbidden except via the limited \"graduation\" flow (child turns 13 → parent approves migration → one-shot import row in 13+ SKU).</p>\n<p>§1.13  <strong>Audit reviewability.</strong> All compliance artefacts (DPIA, TIA, DPO appointment, breach drafts, DSR tickets, consent evidence blobs) MUST be deterministically exportable via <code>pnpm legal:export &lt;out.zip&gt;</code> for regulator review on demand. The export MUST be byte-identical across runs.</p>\n<p>§1.14  <strong>Compliance program lifecycle.</strong> The Founder MUST review this task's artefacts annually and re-author the DPIA + TIA when any of: (a) a new cross-border vendor is added; (b) a new data category is collected; (c) a regulator updates its guidance materially (e.g. EU Digital Fairness Act publication, COPPA-2025 sub-regulation, PDPL Decree update); (d) the user-base crosses 100K users (PDPL small-business grace-period exit threshold per <code>docs/legal/coppa-2025-readiness-checklist.md</code> §3).</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why a separate kids SKU instead of a <code>is_minor</code> flag.</strong> Apple Kids Category and Google Play Families both require store-submission declarations that cannot be conditionally enabled per-user. The HoYoverse FTC $20M settlement (Jan 2025) and Jam City $1.4M (2025) both stemmed from \"minor-detection-as-flag\" architectures that failed under regulator scrutiny. Two SKUs is operationally costly (~20% extra QA effort) but is the only architecture that survives a COPPA-2025 audit.</p>\n<p><strong>Why PRIVO + SuperAwesome are listed as alternatives.</strong> PRIVO has stronger US Federal Trade Commission Safe Harbor coverage; SuperAwesome's kWS has stronger pan-EU coverage and a more mature SDK. The decision memo at <code>safe-harbor-vendor-decision-memo.md</code> resolves the choice by weighing $/MAU costs against the markets where tamagochi launches first (per plan §PART 7: VN + PH + ID soft launch, then global). If global launch starts in EU, kWS wins; if US-first, PRIVO wins; VN-first allows either.</p>\n<p><strong>Why DPO can be external.</strong> PDPL Article 28 explicitly permits external DPO via contracted personnel; Vietnamese in-house counsel salaries (~$40K/year) plus the 1.5x burden multiplier make a contracted DPO (~$15-30K/year for the engagement scope) the cheaper path for a 6-8 person team.</p>\n<p><strong>Why 60-minute breach-draft generation.</strong> PDPL Article 24 mandates 72-hour notification but does not specify a draft generation window. The 60-minute SLA leaves time for legal review + DPO sign-off + escalation to executives within the regulator window. The CA05 form is the printable artefact A05 (Bộ Công An — Ministry of Public Security) accepts.</p>\n<p><strong>Why we forbid behavioural SDKs in the kids SKU outright.</strong> COPPA-2025 amends 16 CFR Part 312 to include \"persistent identifiers\" + \"device IDs used for behavioural advertising\" as personal information. SDK vendors that do not contractually disable behavioural use (which most do not in their free tiers) are unsuitable. SuperAwesome kWS is explicitly designed for under-13 contextual-only — the only sustainable choice.</p>\n<p><strong>Why no generative LLM chat to under-13.</strong> The Snap My AI / Replika regulatory precedent (FTC inquiry 2024, EU AI Act high-risk categorisation for under-18 emotional companions) classifies generative chat to children as a regulatory landmine. Scripted dialogue trees (TASK-AI-002) preserve the \"AI personality\" hook while side-stepping that category. This is opinionated and deliberate.</p>\n<p><strong>Why region-of-record is deterministic.</strong> A user spoofing region to avoid an SKU is a known abuse vector. Apple/Google account region is the strongest signal because it requires payment-method verification. IP fallback exists only because a user with no Apple/Google identity (Zalo-only, common in VN) needs a path — but the \"confirm your country\" gate prevents silent mis-classification.</p>\n<p><strong>Why no commingled databases.</strong> Compliance regimes differ enough between kids and 13+ that data minimisation is best served at the database boundary. The migration cost (~$2K/yr to operate two Supabase projects) is trivial compared to a regulator-mandated database-split-under-deadline.</p>\n<p><strong>Why annual review with vendor / regulator triggers.</strong> PDPL + COPPA + DFA are all evolving. The \"if any of (a-d) triggers, re-author DPIA + TIA\" rule prevents drift between authored policy and shipping reality.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<h3>3.1 — Data subject request controller (<code>apps/api/src/legal/data-subject-request.controller.ts</code>)</h3>\n<pre><code class=\"language-typescript\">import { Body, Controller, HttpCode, Post, UseGuards } from '@nestjs/common';\nimport { z } from 'zod';\n\nconst DsrRequestSchema = z.object({\n  kind: z.enum(['access', 'rectification', 'erasure', 'portability']),\n  subject_email: z.string().email(),\n  is_under_13: z.boolean().default(false),\n  regulator_jurisdiction: z.enum(['US-COPPA', 'EU-GDPR', 'UK-AADC', 'VN-PDPL']),\n  evidence_attachment_url: z.string().url().optional(),\n});\n\ntype DsrRequest = z.infer&lt;typeof DsrRequestSchema&gt;;\n\n@Controller('v1/dsr')\nexport class DataSubjectRequestController {\n  constructor(private readonly svc: DataSubjectRequestService) {}\n\n  @Post()\n  @HttpCode(202)\n  async createDsr(@Body() raw: unknown): Promise&lt;{ ticket_id: string; sla_due_at: string }&gt; {\n    const req = DsrRequestSchema.parse(raw);\n    return this.svc.openTicket(req);\n  }\n}</code></pre>\n<h3>3.2 — Breach notification service (<code>apps/api/src/legal/breach-notification.service.ts</code>)</h3>\n<pre><code class=\"language-typescript\">import { Injectable } from '@nestjs/common';\nimport { EventBus } from '@nestjs/cqrs';\nimport { z } from 'zod';\n\nconst BreachDetectedSchema = z.object({\n  detected_at: z.string().datetime(),\n  scope_summary: z.string().min(20),\n  rows_affected_estimate: z.number().int().nonneg(),\n  jurisdictions_affected: z.array(z.enum(['US', 'EU', 'UK', 'VN'])).min(1),\n  data_categories: z.array(z.string()).min(1),\n  root_cause_hypothesis: z.string(),\n});\n\n@Injectable()\nexport class BreachNotificationService {\n  // MUST persist the CA05 draft within 60 min of detection.\n  async draftCa05Form(payload: z.infer&lt;typeof BreachDetectedSchema&gt;): Promise&lt;{ draft_url: string }&gt; {\n    /* implementation in §6 skeleton */\n    return { draft_url: '' };\n  }\n}</code></pre>\n<h3>3.3 — COPPA age-gate middleware (<code>apps/api/src/legal/coppa-age-gate.middleware.ts</code>)</h3>\n<pre><code class=\"language-typescript\">import { Injectable, NestMiddleware, HttpStatus } from '@nestjs/common';\nimport { Request, Response, NextFunction } from 'express';\nimport type { ParentalConsentRepo } from './parental-consent.repo';\n\n@Injectable()\nexport class CoppaAgeGateMiddleware implements NestMiddleware {\n  constructor(private readonly consent: ParentalConsentRepo) {}\n\n  async use(req: Request, res: Response, next: NextFunction) {\n    const userId = (req as any).user?.id;\n    if (!userId) return next();\n    const audience = (req as any).sku?.audience as 'kids' | 'standard';\n    if (audience !== 'kids') return next();\n\n    const c = await this.consent.byUserId(userId);\n    if (c?.state === 'granted' &amp;&amp; !this.isExpired(c)) return next();\n\n    res.status(HttpStatus.UNAVAILABLE_FOR_LEGAL_REASONS).json({\n      error: 'parental_consent_required',\n      consent_url: this.consent.consentUrlFor(userId),\n    });\n  }\n\n  private isExpired(c: { expires_at: string | null }): boolean {\n    return c.expires_at !== null &amp;&amp; new Date(c.expires_at).getTime() &lt; Date.now();\n  }\n}</code></pre>\n<h3>3.4 — Postgres schema (Supabase)</h3>\n<pre><code class=\"language-sql\">-- Per TASK-LEGAL-001 §1.6 and §1.12 — kids SKU lives in its own Supabase project.\ncreate table parental_consent (\n  id            uuid primary key default gen_random_uuid(),\n  child_id      uuid not null references auth.users(id) on delete cascade,\n  parent_email_hash text not null,                       -- sha256, no PII at rest\n  consent_state text not null\n                check (consent_state in ('pending','granted','revoked','expired')),\n  consent_method text not null\n                check (consent_method in ('email_plus_card','knowledge_based','government_id')),\n  evidence_blob_url text,\n  granted_at    timestamptz,\n  expires_at    timestamptz,\n  revoked_at    timestamptz,\n  vendor        text not null check (vendor in ('privo','superawesome_kws')),\n  policy_version text not null,\n  created_at    timestamptz not null default now(),\n  unique (child_id)\n);\ncreate index on parental_consent (consent_state);\n\ncreate table dsr_tickets (\n  id            uuid primary key default gen_random_uuid(),\n  kind          text not null check (kind in ('access','rectification','erasure','portability')),\n  subject_email text not null,\n  regulator_jurisdiction text not null check (regulator_jurisdiction in ('US-COPPA','EU-GDPR','UK-AADC','VN-PDPL')),\n  is_under_13   boolean not null default false,\n  status        text not null default 'open'\n                check (status in ('open','in_progress','completed','rejected')),\n  sla_due_at    timestamptz not null,\n  evidence_url  text,\n  created_at    timestamptz not null default now(),\n  closed_at     timestamptz\n);\n\ncreate table coppa_age_gate_log (\n  id            bigserial primary key,\n  user_id       uuid not null,\n  decision      text not null check (decision in ('pass','block','expired','revoked')),\n  occurred_at   timestamptz not null default now(),\n  request_path  text not null\n);\n\n-- RLS: only the DPO role can read DSR + consent + age-gate tables.\nalter table parental_consent enable row level security;\nalter table dsr_tickets       enable row level security;\nalter table coppa_age_gate_log enable row level security;\ncreate policy \"dpo-only-read\" on parental_consent  for select using (auth.role() = 'dpo');\ncreate policy \"dpo-only-read\" on dsr_tickets       for select using (auth.role() = 'dpo');\ncreate policy \"dpo-only-read\" on coppa_age_gate_log for select using (auth.role() = 'dpo');</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> <code>docs/legal/DPO-appointment.md</code> is committed and references a named individual or contracted entity with valid Vietnamese tax number, work email, and mobile phone. Verified by <code>pnpm legal:check</code> script which parses the file and asserts non-empty fields.</p>\n<p><strong>AC2.</strong> <code>docs/legal/DPIA-2026-05.md</code> enumerates ≥ 6 data categories, ≥ 3 cross-border destinations, ≥ 8 risk-register rows, and includes the A05 filing confirmation number once received. Verified by <code>pnpm legal:check</code> regex over section headings.</p>\n<p><strong>AC3.</strong> <code>docs/legal/cross-border-transfer-impact-assessment.md</code> lists every external vendor in <code>apps/api/package.json</code> + <code>apps/web/package.json</code> that processes personal data and provides a DPA reference URL. Verified by a Vitest test that diff-checks vendor list against an allowlist.</p>\n<p><strong>AC4.</strong> <code>scripts/legal/generate-ca05-form.mjs</code> produces a deterministic PDF when fed the example payload in <code>__tests__/breach-notification.spec.ts</code>. Output PDF SHA-256 is recorded as a Vitest snapshot. The script accepts <code>--locale=vi</code> and <code>--locale=en</code> and produces the correct A05 form-field labels in each.</p>\n<p><strong>AC5.</strong> A simulated breach event (<code>pnpm legal:simulate-breach</code>) writes a draft to Supabase <code>breach_drafts</code> table within 60 seconds (P95) and emits a Slack alert to <code>#legal-breach</code> channel.</p>\n<p><strong>AC6.</strong> The kids SKU and standard SKU build into <strong>two separate Apple App Store records and two separate Google Play packages</strong> with distinct bundle IDs. Verified by reviewing <code>apps/cocos/fastlane/Fastfile</code> or <code>apps/cocos/gradle.properties</code> per build target.</p>\n<p><strong>AC7.</strong> The Safe Harbor vendor (PRIVO OR SuperAwesome kWS) is enrolled with a signed contract on file at <code>docs/legal/safe-harbor-vendor-decision-memo.md</code> ANNEX A. Verified by manual inspection of the annex (PDF attachment SHA-256 recorded in the decision memo).</p>\n<p><strong>AC8.</strong> The parental consent flow end-to-end test (<code>tests/integration/parental-consent.spec.ts</code>) covers: register-child → trigger parent email → click consent link → state=granted → child can access app. The reverse flow (parent revokes → state=revoked → child blocked within 1 business day → data deleted within 30 days) is also covered.</p>\n<p><strong>AC9.</strong> <code>POST /v1/dsr</code> returns HTTP 202 with a <code>ticket_id</code> and a <code>sla_due_at</code> ISO timestamp matching the jurisdiction (immediate for COPPA under-13 erasure; 30 days for adult erasure under PDPL/GDPR). Verified by <code>tests/integration/dsr.spec.ts</code>.</p>\n<p><strong>AC10.</strong> The COPPA age-gate middleware returns HTTP 451 with <code>error: \"parental_consent_required\"</code> when consent is missing on the kids SKU. Verified by <code>apps/api/src/legal/__tests__/coppa-age-gate.spec.ts</code>.</p>\n<p><strong>AC11.</strong> Privacy policies in both languages are served at the canonical URLs AND their git SHA matches a <code>policy_version</code> audit field on every <code>parental_consent</code> row created since their publication. Verified by a daily reconciliation job emitting <code>policy.version.mismatch</code> Sentry alerts.</p>\n<p><strong>AC12.</strong> Annual review reminder: a scheduled task (<code>mcp__scheduled-tasks__create_scheduled_task</code>) fires on 2027-05-17 with the prompt \"Annual review of TASK-LEGAL-001 artefacts due — re-author DPIA + TIA if any trigger condition (a-d) has occurred.\" Verified by inspecting the scheduled-task list.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 — Vitest integration test (parental consent flow)</h3>\n<pre><code class=\"language-typescript\">// tests/integration/parental-consent.spec.ts\nimport { describe, it, expect } from 'vitest';\nimport { request } from './_helpers/http';\nimport { mockSafeHarbor } from './_helpers/safe-harbor';\n\ndescribe('TASK-LEGAL-001 — parental consent flow', () =&gt; {\n  it('blocks the kids SKU until parental consent is granted', async () =&gt; {\n    const vendor = mockSafeHarbor('superawesome_kws');\n    const { body: child } = await request\n      .post('/v1/auth/register-child')\n      .set('x-sku', 'kids')\n      .send({ display_name: 'Mochi', parent_email: 'parent@example.com' });\n\n    expect(child.consent_state).toBe('pending');\n\n    // Child tries to play before consent — gate returns 451.\n    const r1 = await request.get('/v1/pet').set('x-sku', 'kids').set('Authorization', `Bearer ${child.token}`);\n    expect(r1.status).toBe(451);\n    expect(r1.body.error).toBe('parental_consent_required');\n\n    // Parent clicks vendor's verified link.\n    await vendor.simulateParentConsent(child.id, { method: 'email_plus_card' });\n\n    const r2 = await request.get('/v1/pet').set('x-sku', 'kids').set('Authorization', `Bearer ${child.token}`);\n    expect(r2.status).toBe(200);\n  });\n\n  it('revokes consent within 1 business day and triggers data deletion within 30 days', async () =&gt; {\n    const vendor = mockSafeHarbor('superawesome_kws');\n    const child = await vendor.registerAndConsent('parent@example.com');\n\n    await vendor.simulateParentRevoke(child.id);\n    const r1 = await request.get('/v1/pet').set('x-sku', 'kids').set('Authorization', `Bearer ${child.token}`);\n    expect(r1.status).toBe(451);\n\n    await vendor.advanceTimeDays(30);\n    const child2 = await request.get(`/v1/users/${child.id}/exists`);\n    expect(child2.body.exists).toBe(false);\n  });\n});</code></pre>\n<h3>5.2 — Vitest unit test (CA05 form generator determinism)</h3>\n<pre><code class=\"language-typescript\">// scripts/legal/__tests__/generate-ca05-form.spec.ts\nimport { describe, it, expect } from 'vitest';\nimport { generateCa05 } from '../generate-ca05-form.mjs';\nimport { createHash } from 'node:crypto';\n\ndescribe('TASK-LEGAL-001 — CA05 form generator', () =&gt; {\n  const payload = {\n    detected_at: '2026-06-15T03:21:09Z',\n    scope_summary: 'Test breach for snapshot.',\n    rows_affected_estimate: 1234,\n    jurisdictions_affected: ['VN'],\n    data_categories: ['email'],\n    root_cause_hypothesis: 'Test hypothesis.',\n  };\n\n  it('produces a byte-identical PDF across two runs (Vietnamese)', async () =&gt; {\n    const pdf1 = await generateCa05(payload, { locale: 'vi' });\n    const pdf2 = await generateCa05(payload, { locale: 'vi' });\n    expect(createHash('sha256').update(pdf1).digest('hex'))\n      .toBe(createHash('sha256').update(pdf2).digest('hex'));\n  });\n\n  it('matches the locked-in snapshot hash', async () =&gt; {\n    const pdf = await generateCa05(payload, { locale: 'vi' });\n    expect(createHash('sha256').update(pdf).digest('hex'))\n      .toMatchInlineSnapshot('\"7a1c3e9b5d2f4a8c0e6b3a1d9f7c2e4b8a6d5c3e1f9b7d5a3c1e9f7b5d3a1c9e\"');\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<h3>6.1 — Breach-notification service implementation</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/legal/breach-notification.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { SupabaseClient } from '@supabase/supabase-js';\nimport { renderCa05Pdf } from '../../../scripts/legal/generate-ca05-form.mjs';\n\n@Injectable()\nexport class BreachNotificationService {\n  private readonly log = new Logger(BreachNotificationService.name);\n  constructor(private readonly supa: SupabaseClient) {}\n\n  async draftCa05Form(payload: BreachDetected): Promise&lt;{ draft_url: string }&gt; {\n    const t0 = Date.now();\n    const pdf  = await renderCa05Pdf(payload, { locale: 'vi' });\n    const path = `breach-drafts/${payload.detected_at}-${randomBytes(4).toString('hex')}.pdf`;\n    const { error } = await this.supa.storage.from('legal').upload(path, pdf, {\n      contentType: 'application/pdf', upsert: false,\n    });\n    if (error) throw error;\n    const { data: row } = await this.supa.from('breach_drafts').insert({\n      pdf_path: path, payload, draft_generated_at: new Date().toISOString(),\n    }).select('id').single();\n    const elapsed = Date.now() - t0;\n    if (elapsed &gt; 60_000) this.log.warn(`breach draft exceeded 60s SLA (took ${elapsed}ms)`);\n    await this.notifySlack(`#legal-breach`, `🚨 breach draft ${row.id} in ${elapsed}ms`);\n    return { draft_url: `${process.env.STORAGE_PUBLIC_BASE}/${path}` };\n  }\n\n  private async notifySlack(channel: string, text: string) { /* slack webhook */ }\n}</code></pre>\n<h3>6.2 — Region-of-record resolver</h3>\n<pre><code class=\"language-typescript\">// apps/api/src/legal/region-of-record.service.ts\n@Injectable()\nexport class RegionOfRecordService {\n  async resolve(ctx: { explicit?: string; apple?: string; google?: string; zalo?: string; ip?: string }): Promise&lt;string&gt; {\n    if (ctx.explicit) return ctx.explicit;       // §1.11 priority 1\n    if (ctx.apple)    return ctx.apple;          //         priority 2\n    if (ctx.google)   return ctx.google;\n    if (ctx.zalo)     return ctx.zalo;           //         priority 3\n    if (ctx.ip)       return this.geoFromIp(ctx.ip); // priority 4 fallback\n    throw new Error('region indeterminate — surface \"confirm country\" gate');\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External</strong></p>\n<ul><li>Vietnam Ministry of Public Security A05 — DPIA filing form (paper, in-person at HCM City office, or via the upcoming online portal once it launches).</li><li>PRIVO Kids Privacy Assured (US) OR SuperAwesome kidSAFE / kWS (US + UK + EU). Pick one in the decision memo.</li><li>Resend (transactional email for parental consent + DSR ticket replies).</li><li>Cloudflare Turnstile (CAPTCHA for parental email anti-bot).</li><li>AWS Singapore region for stateful state (cross-border declared in TIA).</li><li>Supabase US/EU region for metagame (cross-border declared in TIA).</li><li>Anthropic Claude US for LLM (cross-border declared in TIA; under-13 disabled per TASK-AI-002).</li><li>Sentry Germany region for errors (chosen specifically for EU customer assurance).</li></ul>\n<p><strong>Internal</strong></p>\n<ul><li>None — this is the root task with <code>depends_on: []</code>.</li></ul>\n<p><strong>Blocks</strong> (the tasks that cannot start until this lands)</p>\n<ul><li>TASK-LEGAL-002 (loot-box policy depends on this baseline)</li><li>TASK-LEGAL-003 (store-category declarations depend on the SKU split here)</li><li>TASK-AUTH-003 (invite-code flow depends on parental consent service)</li><li>TASK-AI-002 (kids LLM gate)</li><li>TASK-ADS-002 (kWS contextual gate)</li><li>TASK-OBS-001 (analytics SDK allow/deny list)</li><li>TASK-VIRAL-005 (push notification kid quiet-hours)</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — Breach detected event (input to CA05 generator)</h3>\n<pre><code class=\"language-json\">{\n  \"detected_at\": \"2026-08-12T14:33:21Z\",\n  \"scope_summary\": \"Internal Slack misconfiguration exposed a CSV of 412 child display_name + parent_email_hash pairs to a public Slack channel for 47 minutes.\",\n  \"rows_affected_estimate\": 412,\n  \"jurisdictions_affected\": [\"VN\", \"US\"],\n  \"data_categories\": [\"display_name\", \"parent_email_hash\"],\n  \"root_cause_hypothesis\": \"Misconfigured Slack channel privacy when a new joiner was added; revoked at 15:20:14Z.\",\n  \"mitigation_status\": \"channel-privatised; CSV deleted from Slack; Slack admin audit log preserved\",\n  \"dpo_contact\": \"dpo@cyberskill.world\"\n}</code></pre>\n<h3>8.2 — DSR ticket creation</h3>\n<pre><code class=\"language-json\">POST /v1/dsr\nContent-Type: application/json\n\n{\n  \"kind\": \"erasure\",\n  \"subject_email\": \"parent@example.com\",\n  \"is_under_13\": true,\n  \"regulator_jurisdiction\": \"US-COPPA\",\n  \"evidence_attachment_url\": \"https://safe-harbor.example.com/evidence/abc123\"\n}\n\n→ 202 Accepted\n{\n  \"ticket_id\": \"01HC7QG2EFR8XK4ZN8YA1J3WB6\",\n  \"sla_due_at\": \"2026-08-12T15:33:21Z\"\n}</code></pre>\n<h3>8.3 — COPPA age-gate block (HTTP 451)</h3>\n<pre><code class=\"language-http\">HTTP/1.1 451 Unavailable For Legal Reasons\nContent-Type: application/json\n{\n  \"error\": \"parental_consent_required\",\n  \"consent_url\": \"https://safe-harbor.example.com/consent?child=01HC7QG2EFR8XK4ZN8YA1J3WB6\"\n}</code></pre>\n<h3>8.4 — Parental consent row at rest</h3>\n<pre><code class=\"language-json\">{\n  \"id\": \"01HC7QG2EFR8XK4ZN8YA1J3WB6\",\n  \"child_id\": \"01HC7QGZK4XN8YA1J3WB6EFR8\",\n  \"parent_email_hash\": \"9b3e7d2a5c4f1b8e6d3a2c9f7b5d3a1c9e1f8b6d4c2a3e5f9b7d5a3c1e9f7b5d\",\n  \"consent_state\": \"granted\",\n  \"consent_method\": \"email_plus_card\",\n  \"evidence_blob_url\": \"https://kws-evidence.example.com/9b3e7d2a/abc123.json\",\n  \"granted_at\": \"2026-08-12T14:36:01Z\",\n  \"expires_at\": null,\n  \"vendor\": \"superawesome_kws\",\n  \"policy_version\": \"tamagochi-privacy-en-v1.3.0\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All open questions resolved at authoring time:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> PRIVO vs SuperAwesome kWS? → Decision deferred to <code>safe-harbor-vendor-decision-memo.md</code> ANNEX A (mandatory pre-launch decision). Both options spec'd in §1.6 so neither blocks downstream tasks.</li><li><strong>OQ-2 (resolved):</strong> DPO in-house or external? → §2 settles this: external via Tilleke / Rouse / EY-VN for the 6-8 person team scale.</li><li><strong>OQ-3 (resolved):</strong> Two SKUs or <code>is_minor</code> flag? → §1.5 + §2 lock two SKUs. Operational cost accepted.</li><li><strong>OQ-4 (resolved):</strong> Region-of-record on conflict? → §1.11 deterministic chain.</li><li><strong>OQ-5 (resolved):</strong> Kids SKU LLM allowed? → §1.5(b) forbids; scripted trees in TASK-AI-002.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>DPO appointment not registered with A05 within 60 days</td><td>Annual review reminder + scheduled <code>legal:check</code> script</td><td>Fines up to VND 3B per PDPL Article 24</td><td>Emergency engagement with Tilleke/Rouse to file within 7 days</td></tr><tr><td>2</td><td>DPIA filing rejected by A05 (incomplete categories)</td><td>A05 reply letter</td><td>30-day cure window, then enforcement risk</td><td>Re-author DPIA, re-file with cure-letter response</td></tr><tr><td>3</td><td>Cross-border TIA missing a new vendor</td><td><code>pnpm legal:check</code> diffs vendor list vs TIA</td><td>Cross-border-transfer enforcement risk (5% revenue cap)</td><td>Halt new vendor traffic; re-author TIA; re-file with A05 within 30 days</td></tr><tr><td>4</td><td>Breach draft not generated within 60 min</td><td>Sentry alert <code>breach.draft.slow</code></td><td>72h window jeopardised</td><td>Manual draft by DPO using paper template; fix automation post-incident</td></tr><tr><td>5</td><td>Safe Harbor vendor outage prevents parental consent</td><td>Synthetic monitor <code>kws.healthcheck.fail</code></td><td>Kids signups blocked</td><td>Surface \"we're working on it\" page; queue requests; retry on vendor recovery</td></tr><tr><td>6</td><td>Parental consent revoked but child data not deleted within 30 days</td><td>Daily reconciliation job <code>parental_consent.revoked.expired</code></td><td>COPPA enforcement risk</td><td>DPO escalation; manual deletion within 24h; root-cause in job pipeline</td></tr><tr><td>7</td><td>Region-of-record indeterminate (no Apple/Google/Zalo/IP signal)</td><td><code>region.indeterminate</code> event</td><td>User cannot be classified</td><td>Surface \"confirm your country\" mandatory gate before any data persists</td></tr><tr><td>8</td><td>Kids SKU and 13+ SKU databases commingled by accident</td><td>Schema linter detects shared FK</td><td>Compliance violation + audit failure</td><td>Halt deploy; restore from backup pre-incident; quarantine commingled rows</td></tr><tr><td>9</td><td>Privacy policy git SHA drifts from <code>policy_version</code> audit field</td><td>Daily reconciliation Sentry alert</td><td>Auditability fails</td><td>Re-publish policy; replay consent re-confirmation for affected users</td></tr><tr><td>10</td><td>DSR ticket SLA exceeded</td><td><code>dsr.sla.exceeded</code> Sentry alert per ticket</td><td>Regulator complaint risk</td><td>DPO escalation; pause new DSR intake until backlog cleared</td></tr><tr><td>11</td><td>Annual review missed (no DPIA refresh after vendor change)</td><td>Scheduled task fires on anniversary; on-failure escalation to Founder Slack DM</td><td>Regulator finds outdated DPIA</td><td>Block new feature deploys until DPIA re-filed</td></tr><tr><td>12</td><td>Cross-border destination changes silently (e.g. Sentry moves Germany → US)</td><td><code>pnpm legal:check</code> quarterly diff of vendor data-region declarations</td><td>TIA mismatch</td><td>Re-author TIA; pause traffic; escalate DPO</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 8 (Legal &amp; Compliance) is the authoritative source; specific anchors — COPPA-2025 effective April 22 2026; Vietnam PDPL Law 91/2025/QH15 + Decree 356/2025/ND-CP effective Jan 1 2026; Belgium 2018 loot box ban; NL Antwerp 2025 ruling; EU Digital Fairness Act draft late-2025/early-2026; HoYoverse $20M FTC settlement Jan 2025; Jam City $1.4M (2025).</p>\n<p><strong>Regulator inbox:</strong></p>\n<ul><li>A05 (Bộ Công An, Ministry of Public Security, Hanoi): paper filing and the upcoming online portal at <code>https://pdpl.bocongan.gov.vn</code> (URL placeholder until launched).</li><li>FTC (US) Children's Privacy Bureau: <code>coppahelp@ftc.gov</code>.</li><li>ICO (UK) AADC team: <code>casework@ico.org.uk</code>.</li><li>EU EDPB cooperation desk: per the lead-supervisor-authority rule once an EU establishment is chosen.</li></ul>\n<p><strong>Vendor cost snapshot (2026-05-17):</strong></p>\n<ul><li>PRIVO: ~$3,000/year base + $0.05/verified-parent. Best for US-first.</li><li>SuperAwesome kWS: ~£15,000/year base + tier pricing per MAU. Best for EU/UK.</li><li>Tilleke &amp; Gibbins Vietnam DPO retainer: ~$18,000-25,000/year for the engagement scope.</li><li>Resend transactional: $20/month + $0.0004 per email at the consent-flow volume.</li></ul>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Specific A05 in-person filing logistics (which HCM City office, what hours) — deferred to the legal counsel engagement.</li><li>Cross-jurisdictional escalation procedure when a DSR spans VN + US + EU — deferred to the DPO operational manual (post-P0 deliverable).</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>\"We'll declare TBD as the DPO at first then update later\" — A05 rejects this.</li><li>\"Kids SKU is just the standard SKU with one flag\" — fails COPPA-2025 audit per §2.</li><li>\"Behavioural analytics in kids SKU is fine if we anonymise\" — COPPA-2025 redefines persistent identifier broadly enough that anonymisation is insufficient.</li><li>\"LLM dialogue to under-13 is fine with safety filter\" — Snap My AI / Replika precedent says otherwise; scripted trees only.</li><li>\"Region from IP is enough\" — spoofing is trivial; deterministic chain in §1.11 is required.</li></ul>\n<p><strong>Risk register cross-reference:</strong> This task is the mitigation for plan §PART 10 risks #1 (regulator action under COPPA / PDPL) and the foundation for #4 (Apple/Google store policy changes for kids category).</p>";
