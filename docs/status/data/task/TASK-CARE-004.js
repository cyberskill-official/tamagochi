window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CARE-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Mini-game contract.</strong> A mini-game MUST implement the <code>MiniGameContract</code> interface:</p>\n<pre><code class=\"language-typescript\">interface MiniGameContract {\n  id: 'tap-the-mochi'|'memory-match'|'catch-snacks'|'rhythm-tap';\n  startWithSeed(seed: string, opts: { difficulty: 1|2|3 }): Promise&lt;void&gt;;\n  collectInputEvents(): InputEvent[];          // ordered list, server-replayable\n  durationMs: number;                          // fixed, e.g. 30000 for Tap-the-Mochi\n}</code></pre>\n<p>New mini-games (P3+) MUST conform to this contract.</p>\n<p>§1.2  <strong>Per-game asset bundle.</strong> Each mini-game lives in its own Cocos asset bundle (per TASK-INFRA-001 §1.5) loaded on demand via <code>MiniGameLoader</code>. Bundle budget ≤ 2 MB per game (per TASK-INFRA-001 §1.5 budget table).</p>\n<p>§1.3  <strong>Server-seeded RNG.</strong> When the player starts a mini-game, the <strong>server</strong> generates a CSPRNG seed (<code>crypto.randomBytes(16)</code>) returned in <code>POST /v1/mini-games/start</code>. The Cocos game seeds its RNG from this server seed (deterministic per session). The server replays the same seed when validating scores.</p>\n<p>§1.4  <strong>Input event collection.</strong> During a mini-game session, the Cocos client records every meaningful input event (tap timestamp + position; gesture metadata) into an ordered <code>InputEvent[]</code>. On finish, the client POSTs the events to <code>POST /v1/mini-games/finish</code> for server-side replay scoring.</p>\n<p>§1.5  <strong>Server-side scoring.</strong> The server replays the session: starts from the same seed → replays input events → computes the score. If the client-claimed score does not match the server-computed score (within a tolerance for floating-point variance ≤ 1%), reject + record <code>security.mini-game.score-mismatch</code> Sentry event.</p>\n<p>§1.6  <strong>Payout caps.</strong> Each mini-game has a per-session payout cap + a per-day per-pet aggregate cap:</p>\n<ul><li>Tap-the-Mochi: max 30 Coins/session, 90 Coins/pet/day.</li><li>Memory Match: max 50 Coins/session, 150 Coins/pet/day.</li><li>Catch Snacks: max 40 Coins/session, 120 Coins/pet/day.</li><li>Rhythm Tap: max 60 Coins/session, 180 Coins/pet/day.</li></ul>\n<p>§1.7  <strong>Stat side-effects.</strong> Per TASK-PET-003 §1.4, mini-game outcomes affect stats:</p>\n<ul><li>Win: cleanliness -3, happiness +20, energy -10.</li><li>Loss: cleanliness -2, happiness +5, energy -8.</li></ul>\n<p>Stats clamped 0-100.</p>\n<p>§1.8  <strong>Stage gate.</strong> Mini-games require <code>baby|teen|adult</code>. (<code>baby</code> is the gating moment for the first mini-game per TASK-PET-002 §1.6.)</p>\n<p>§1.9  <strong>Anti-replay.</strong> Each session has a unique <code>session_id</code> (ULID). A session id MUST be <code>finish</code>-able at most once. Subsequent finish attempts → 409 <code>session.already_finished</code>.</p>\n<p>§1.10  <strong>Timeout.</strong> A <code>start</code>ed session that does not <code>finish</code> within <code>durationMs + 30s</code> MUST be auto-finished with score 0 + recorded as <code>incomplete</code>. Prevents leaving sessions open to retry later with a better score.</p>\n<p>§1.11  <strong>Difficulty scaling.</strong> Each mini-game offers difficulty 1/2/3 (selected by player). Higher difficulty = higher max payout. The server-side scoring rule encodes the per-difficulty multiplier.</p>\n<p>§1.12  <strong>Audit row.</strong> <code>mini_game_sessions</code> table: <code>(session_id, pet_id, owner_id, game_id, difficulty, seed, started_at, finished_at, raw_input_events_url, server_score, coin_payout, status, tenant_id)</code>.</p>\n<p>§1.13  <strong>Per-session perf monitoring.</strong> Each game session opens a Sentry transaction tagged with <code>game_id</code> + <code>device_class</code>. Frame-rate samples emit <code>mini_game.frame_rate</code> metric.</p>\n<p>§1.14  <strong>No real-money outcome.</strong> Per TASK-LEGAL-002 §1.1, mini-game payouts are earned-currency only. No mini-game outcome MAY: (a) determine an IAP-purchasable item; (b) consume an IAP-purchased entry ticket; (c) gate a randomised inventory pull.</p>\n<p>§1.15  <strong>Co-parent visibility.</strong> Mini-game completion broadcasts <code>pet.mini_game.played</code> to PetRoom (co-parent sees the activity but does not share payout — payout goes to the player who played).</p>\n<p>§1.16  <strong>Kids-SKU restrictions.</strong> No leaderboards or social-comparison surfaces on kids SKU. No in-game chat. Standard SKU may surface per-game personal best.</p>\n<p>§1.17  <strong>A11Y.</strong> Each mini-game scene MUST: (a) support reduce-motion (slower animations); (b) provide colour-blind-safe palette; (c) be playable with a single tap target (no requirement for multi-touch fine-motor input); (d) include audio cues for primary feedback (alongside visuals).</p>\n<p>§1.18  <strong>Cooldown between sessions.</strong> 60 seconds between any two <code>start</code> calls on the same pet (prevents replay-spam exploiting the per-session cap multiple times in quick succession).</p>\n<p>§1.19  <strong>Idempotent finish.</strong> <code>finish</code> accepts an <code>idempotency_key</code>; identical retry returns the cached response.</p>\n<p>§1.20  <strong>Analytics taxonomy.</strong> <code>mini_game.start { game_id, difficulty }</code>, <code>mini_game.finish { game_id, server_score, coin_payout, status: 'win'|'loss'|'incomplete' }</code>, <code>mini_game.score_mismatch</code> per TASK-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why a contract interface.</strong> New mini-games (P3+) need a stable seam; without a contract each new game forks the framework.</p>\n<p><strong>Why server-seeded RNG + replay.</strong> Plan §PART 4 anti-cheat. Client-side RNG + client-claimed score = trivially spoofable. Server-seed + server replay = deterministic auditability.</p>\n<p><strong>Why per-game asset bundle.</strong> Mini-games are heavy assets (~1-2 MB each). Bundling each separately keeps cold-start fast (TASK-INFRA-001 §1.5 budget) — load only the chosen game.</p>\n<p><strong>Why payout caps.</strong> Without caps, a player could grind one mini-game indefinitely for unlimited Coins. Caps preserve economy integrity + force engagement variety.</p>\n<p><strong>Why 60s session cooldown.</strong> A player could <code>start</code> then deliberately fail to test the game without consuming a payout slot. The cooldown prevents excessive testing-as-grinding.</p>\n<p><strong>Why timeout on incomplete sessions.</strong> Prevents abandoning a low-scoring session + retrying later. Plus releases server state.</p>\n<p><strong>Why difficulty 1/2/3.</strong> Three tiers covers low-effort / medium / high; granularity beyond 3 confuses players + complicates payout math.</p>\n<p><strong>Why no leaderboard on kids SKU.</strong> COPPA-2025 + social-comparison concerns. Plan §PART 8.</p>\n<p><strong>Why audio cues required.</strong> WCAG-AA compliance + Apple Kids Category accessibility. Players who can't see well still get feedback.</p>\n<p><strong>Why per-session Sentry transaction.</strong> Mini-games are perf-critical (frame-rate sensitive on low-end VN Android). Per-session telemetry lets us spot regressions per-device-class.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/care/mini-game/mini-game.controller.ts\n@Controller('v1/mini-games')\n@UseGuards(SupabaseJwtGuard)\nexport class MiniGameController {\n  constructor(private readonly svc: MiniGameService) {}\n\n  @Post('start')\n  async start(@CurrentUser() u: AuthedUser, @Body() body: { pet_id: string; game_id: 'tap-the-mochi'|'memory-match'|'catch-snacks'|'rhythm-tap'; difficulty: 1|2|3 }) {\n    return this.svc.start(u, body.pet_id, body.game_id, body.difficulty);\n  }\n\n  @Post('finish')\n  async finish(@CurrentUser() u: AuthedUser,\n               @Body() body: { session_id: string; client_score: number; input_events: any[]; idempotency_key: string }) {\n    return this.svc.finish(u, body);\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/mini-games/_framework/MiniGameContract.ts\nexport interface MiniGameContract {\n  readonly id: 'tap-the-mochi'|'memory-match'|'catch-snacks'|'rhythm-tap';\n  readonly durationMs: number;\n  startWithSeed(seed: string, opts: { difficulty: 1|2|3 }): Promise&lt;void&gt;;\n  collectInputEvents(): InputEvent[];\n}\n\nexport interface InputEvent {\n  type: 'tap'|'gesture'|'frame';\n  t_ms: number;       // ms since session start\n  x?: number; y?: number;\n  meta?: Record&lt;string, unknown&gt;;\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/care/mini-game/scoring-rules.ts\nexport const SCORING = {\n  'tap-the-mochi':  { perCorrectTap: 1, missPenalty: 0, capPerSession: 30, capPerDay: 90 },\n  'memory-match':   { perMatch: 10, missPenalty: 2, capPerSession: 50, capPerDay: 150 },\n  'catch-snacks':   { perCatch: 3, missPenalty: 1, capPerSession: 40, capPerDay: 120 },\n  'rhythm-tap':     { perOnBeat: 4, perPerfect: 6, capPerSession: 60, capPerDay: 180 },\n} as const;</code></pre>\n<pre><code class=\"language-sql\">-- infra/supabase/standard/migrations/20260517_011_mini_game_sessions.sql\ncreate table public.mini_game_sessions (\n  session_id text primary key check (session_id ~ '^[0-9A-HJKMNPQRSTVWXYZ]{26}$'),\n  pet_id text not null references public.pets(id) on delete cascade,\n  owner_id uuid not null,\n  game_id text not null check (game_id in ('tap-the-mochi','memory-match','catch-snacks','rhythm-tap')),\n  difficulty int not null check (difficulty between 1 and 3),\n  seed text not null,\n  raw_input_events_url text,\n  server_score int,\n  coin_payout int,\n  status text not null default 'started' check (status in ('started','finished','incomplete','rejected_score_mismatch')),\n  tenant_id text not null default 'mochi',\n  started_at timestamptz not null default now(),\n  finished_at timestamptz\n);\ncreate index on public.mini_game_sessions (pet_id, started_at desc);\nalter table public.mini_game_sessions enable row level security;\ncreate policy \"mini_game_sessions self\" on public.mini_game_sessions for select using (owner_id = auth.uid());</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> <code>POST /v1/mini-games/start</code> returns <code>session_id</code> + <code>seed</code> + <code>started_at</code>. Verified. <strong>AC2.</strong> <code>POST /v1/mini-games/finish</code> with valid replay matches client_score → 200 with <code>coin_payout</code>. Verified. <strong>AC3.</strong> Score mismatch (client claims 100 but server replay computes 50) → 422 + <code>security.mini-game.score-mismatch</code> event. Verified. <strong>AC4.</strong> Per-session cap enforced — client claims 50 Coins on Tap-the-Mochi (cap 30) → payout capped at 30. Verified. <strong>AC5.</strong> Per-day per-pet cap enforced (90 Coins/day on Tap-the-Mochi). Verified. <strong>AC6.</strong> Stage-locked: mini-game on egg/grandma → 403. Verified. <strong>AC7.</strong> Re-finish of same session_id → 409. Verified. <strong>AC8.</strong> Timeout: session not finished within durationMs+30s → auto-finished with score 0. Verified. <strong>AC9.</strong> Stat effects: win → cleanliness -3, happiness +20, energy -10; loss → cleanliness -2, happiness +5, energy -8. Verified. <strong>AC10.</strong> Cooldown 60s between starts. Verified. <strong>AC11.</strong> Per-game bundle ≤ 2 MB. Verified via <code>assert-bundle-budget</code>. <strong>AC12.</strong> Co-parent broadcast <code>pet.mini_game.played</code> works. Verified. <strong>AC13.</strong> Kids SKU has no leaderboard surface. Verified by Playwright. <strong>AC14.</strong> A11Y: each game playable with single-tap + audio cues. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/care/mini-game/__tests__/mini-game.spec.ts\ndescribe('TASK-CARE-004 — mini-game framework', () =&gt; {\n  it('rejects client_score mismatching server replay', async () =&gt; {\n    const { session_id, seed } = await svc.start(user('u1'), 'pet', 'tap-the-mochi', 2);\n    const fakeEvents = [{ type: 'tap', t_ms: 100, x: 50, y: 50 }];\n    await expect(svc.finish(user('u1'), {\n      session_id, client_score: 999, input_events: fakeEvents, idempotency_key: 'k1',\n    })).rejects.toMatchObject({ status: 422 });\n  });\n\n  it('caps payout at session cap', async () =&gt; {\n    const { session_id } = await svc.start(user('u1'), 'pet', 'tap-the-mochi', 3);\n    const events = generatePerfectTapEvents(60);\n    const r = await svc.finish(user('u1'), { session_id, client_score: 60, input_events: events, idempotency_key: 'k2' });\n    expect(r.coin_payout).toBe(30);  // capped\n  });\n\n  it('respects per-day cap', async () =&gt; {\n    for (let i = 0; i &lt; 3; i++) {\n      const { session_id } = await svc.start(user('u1'), 'pet', 'tap-the-mochi', 2);\n      await svc.finish(user('u1'), { session_id, client_score: 30, input_events: events30, idempotency_key: `kp${i}` });\n    }\n    const { session_id } = await svc.start(user('u1'), 'pet', 'tap-the-mochi', 2);\n    const r4 = await svc.finish(user('u1'), { session_id, client_score: 30, input_events: events30, idempotency_key: 'kp4' });\n    expect(r4.coin_payout).toBe(0);\n    expect(r4.daily_capped).toBe(true);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/care/mini-game/anti-cheat.service.ts\n@Injectable()\nexport class MiniGameAntiCheat {\n  replayScore(seed: string, gameId: string, difficulty: number, events: InputEvent[]): number {\n    const rng = new SeededRng(seed);\n    const rules = SCORING[gameId];\n    let score = 0;\n    let frame = 0;\n    for (const ev of events) {\n      if (ev.type === 'tap' &amp;&amp; this.isCorrectTap(rng, frame, ev)) score += rules.perCorrectTap ?? rules.perMatch ?? rules.perCatch ?? rules.perOnBeat ?? 0;\n      frame = ev.t_ms;\n    }\n    return Math.min(score, rules.capPerSession);\n  }\n\n  private isCorrectTap(rng: SeededRng, _t: number, _ev: InputEvent): boolean {\n    return rng.next() &gt; 0.3; // simplified scoring rule; per-game implementations override\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Cocos asset bundle infra (TASK-INFRA-001). <strong>Internal:</strong> TASK-INFRA-002 (Colyseus broadcast), TASK-PET-003 (stat apply for win/loss), TASK-ART-001 (per-game assets follow art contract). <strong>Blocks:</strong> TASK-CARE-005 (streak system reads mini-game wins), TASK-VIRAL-002 (Daily Drama micro-events), TASK-B2B-004 (Techcombank financial-literacy mini-game extends framework).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/mini-games/start\n{ \"pet_id\": \"01HC...\", \"game_id\": \"tap-the-mochi\", \"difficulty\": 2 }\n→ 200\n{ \"session_id\": \"01HCMGS...\", \"seed\": \"9b3e7d2a5c4f1b8e\", \"started_at\": \"2026-08-12T14:36:01Z\", \"duration_ms\": 30000 }</code></pre>\n<pre><code class=\"language-http\">POST /v1/mini-games/finish\n{ \"session_id\": \"01HCMGS...\", \"client_score\": 25, \"input_events\": [...], \"idempotency_key\": \"01HCMG...\" }\n→ 200\n{ \"server_score\": 25, \"coin_payout\": 25, \"status\": \"win\", \"daily_capped\": false, \"stat_after\": { \"happiness\": 95, \"energy\": 80, \"cleanliness\": 75 } }</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"score_mismatch\", \"expected_max\": 25, \"client_claimed\": 999 }</code></pre>\n<pre><code class=\"language-json\">{ \"session_id\": \"01HCMGS...\", \"status\": \"incomplete\", \"reason\": \"timeout\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> 4 games at P1? → §1.1 — 4 covers variety without bloating launch scope.</li><li><strong>OQ-2 (resolved):</strong> Replay all events vs sample? → §1.5 — full replay; events compress well + audit-ready.</li><li><strong>OQ-3 (resolved):</strong> Difficulty tunable by player? → §1.11 — yes, with higher cap at higher difficulty.</li><li><strong>OQ-4 (resolved):</strong> Per-game multi-touch? → §1.17 — single-tap minimum for A11Y; multi-touch optional bonus.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Client RNG diverges from server (event-loop timing)</td><td>Score-mismatch</td><td>Payout rejected</td><td>Tighten replay determinism + tolerance</td></tr><tr><td>2</td><td>Input event stream corrupted</td><td>Replay errors</td><td>Session marked rejected</td><td>Sentry alert; investigate Cocos serialisation</td></tr><tr><td>3</td><td>Session not finished within timeout</td><td>Server cron</td><td>Auto-finish 0</td><td>Player can start a new session immediately</td></tr><tr><td>4</td><td>Per-session cap accidentally bypassed in scoring</td><td>Replay test fails</td><td>Inflation</td><td>Recompute affected sessions; ledger refund</td></tr><tr><td>5</td><td>Anti-cheat false-positive</td><td>Player support</td><td>Frustrated player</td><td>Re-score manually; lower tolerance</td></tr><tr><td>6</td><td>Per-game bundle exceeds 2 MB</td><td>Bundle-budget CI</td><td>Build blocked</td><td>Compress assets; or split game further</td></tr><tr><td>7</td><td>Mini-game crashes mid-session</td><td>Cocos error</td><td>Session lost</td><td>Auto-finish on next session start; analytics</td></tr><tr><td>8</td><td>Daily cap counter Redis loss</td><td>Cap resets</td><td>Possible exploit</td><td>Fail-closed: hard-reject when counter unavailable</td></tr><tr><td>9</td><td>Co-parent broadcast lost</td><td>UX desync</td><td>Tolerable</td><td>Persistent state authoritative</td></tr><tr><td>10</td><td>Mini-game scene asset missing on CDN</td><td>Cocos load error</td><td>Player blocked</td><td>Bundle-budget CI catches; fallback to a different game UX</td></tr><tr><td>11</td><td>A11Y audio cue missing for a primary feedback</td><td>Audit</td><td>A11Y regression</td><td>Audio license ledger gate + manual review</td></tr><tr><td>12</td><td>Score-mismatch event flood (auto-cheat ring)</td><td>Sentry alert</td><td>Coordinated exploit</td><td>Tighten anti-cheat; investigate source IPs</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 (mini-games as soft-currency source), plan §PART 4 (server-authoritative).</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Per-game-specific scoring rules — locked in <code>scoring-rules.ts</code> per-game implementation.</li><li>Difficulty multipliers — Mixpanel-tunable.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Client-decided score.</li><li>Mini-game gated by IAP entry.</li><li>Randomised inventory pull as mini-game reward (per TASK-LEGAL-002).</li><li>Mini-games on grandma/egg pets.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-CARE-005 reads mini-game wins for streak detection. TASK-B2B-004 Techcombank tenant extends framework with savings-quest mini-game.</p>";
