window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-VIRAL-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Vertical 9:16 video format.</strong> Exported videos MUST be 1080×1920 H.264 at 30 fps. AAC mono audio at 96 kbps. Container MP4.</p>\n<p>§1.2  <strong>6-second default.</strong> Auto-generated clips (Daily Drama, evolution) default to 6 seconds. AR Bedroom Cam clips (TASK-AR-001) extend up to 15 seconds. Configurable per export call.</p>\n<p>§1.3  <strong>Watermark.</strong> Lower-right corner \"tamagochi.app\" overlay — 12pt, semi-transparent, included at encode time. Indelible.</p>\n<p>§1.4  <strong>Hashtag prefill on share.</strong> When user taps share, the system MUST pre-fill text: <code>\"My pet Mochi! #mochilife #virtualpet\"</code> (locale-adapted). User can edit before posting.</p>\n<p>§1.5  <strong>Trending audio metadata.</strong> The exported MP4 SHOULD include an <code>iTunes</code> metadata tag suggesting trending audio (a curated list per region rotates monthly). Actual audio mix is the in-game soundtrack; the metadata hints at TikTok's audio-pairing UX.</p>\n<p>§1.6  <strong>Share intent — one tap.</strong> A single \"Share\" button invokes the OS share sheet:</p>\n<ul><li>iOS: <code>UIActivityViewController</code> with the video file URI.</li><li>Android: <code>Intent(Intent.ACTION_SEND)</code> with <code>mime=video/mp4</code>.</li></ul>\n<p>The OS share sheet handles the routing to TikTok/Reels/messaging/etc.</p>\n<p>§1.7  <strong>Per-share telemetry.</strong> When the user invokes share, emit <code>viral.share.invoked { source: 'ar' | 'daily_drama' | 'evolution' | 'wedding', video_duration_sec }</code>. We CANNOT detect which app the user picked (OS-level privacy); but we record the invocation.</p>\n<p>§1.8  <strong>Share-tracking link.</strong> A short-link <code>https://tamagochi.app/s/&lt;id&gt;</code> MAY be appended to the share caption. When a recipient taps the link, server logs the click via the <code>share_tracking</code> table for k-factor analytics.</p>\n<p>§1.9  <strong>Kids-SKU restriction.</strong> Per TASK-LEGAL-003 + TASK-AUTH-003, the Share button MUST be hidden on the kids SKU. Kids cannot share externally without parental approval; P3+ feature flag enables parental-approved sharing.</p>\n<p>§1.10  <strong>Stage gate.</strong> Sharing requires <code>stage ∈ {teen, adult}</code> for AR; Daily Drama clips can share from baby+. Evolution clips share at the moment of advancement.</p>\n<p>§1.11  <strong>Privacy — no biometric capture.</strong> AR clips MUST NOT capture identifiable people in frame. Pre-recording UX hint advises \"make sure other people aren't in frame.\" No client-side face detection at P1; P3+ adds it.</p>\n<p>§1.12  <strong>Server-side share log retention.</strong> <code>share_tracking</code> rows retained 90 days for analytics + privacy. Player can delete their share history via TASK-LEGAL-001 DSR.</p>\n<p>§1.13  <strong>No automated posts.</strong> Plan §PART 11 — no automated TikTok account integration at P1. Sharing is purely user-driven via OS share sheet.</p>\n<p>§1.14  <strong>A11Y.</strong> Share button labelled for screen readers (\"Share video of your pet\"). Reduce-motion does not affect share output (video unaffected).</p>\n<p>§1.15  <strong>Caption locale.</strong> Hashtag list MUST localise per TASK-I18N-001:</p>\n<ul><li>en: <code>#mochilife #virtualpet</code></li><li>vi: <code>#mochilife #thúảo</code></li><li>(P4: ja <code>#モチライフ</code>, ko <code>#모치라이프</code>, etc.)</li></ul>\n<p>§1.16  <strong>Trending audio tag rotation.</strong> Monthly review of trending audio in TikTok per region. Locked in <code>apps/cocos/assets/viral/trending-audio.json</code> (Mixpanel-tunable for fast iteration).</p>\n<p>§1.17  <strong>Performance budget.</strong> Export encoding MUST complete in ≤ 3 seconds for a 6-second clip on baseline VN Android. Beyond → cancel + retry with lower bitrate.</p>\n<p>§1.18  <strong>Captioning at P1 → none.</strong> Auto-generated captions (\"Mochi feeling happy!\") are deferred to P2+ once Content Safety classifier (TASK-AI-002) approves caption variety.</p>\n<p>§1.19  <strong>Caption length.</strong> When user-editable, max 280 characters (TikTok caption limit headroom). Server validates length; client enforces.</p>\n<p>§1.20  <strong>Analytics taxonomy.</strong> <code>viral.share.invoked</code>, <code>viral.share.link_click</code>, <code>viral.export.failed { reason }</code> per TASK-OBS-001 §1.4.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 1080×1920.</strong> Plan §PART 4 — TikTok-native dimensions. Lower resolutions look pixelated; higher inflate file size.</p>\n<p><strong>Why 6-second default.</strong> Plan §PART 3 — TikTok / Reels canonical clip length. 6s is enough to convey a moment, short enough to retain attention.</p>\n<p><strong>Why prefill hashtag.</strong> Lowers friction. Plan §PART 7 GTM — user-defined hashtags rare from casual users.</p>\n<p><strong>Why no SDK integration with TikTok at P1.</strong> TikTok's Share SDK is iOS-only (Android via web only) + adds review friction. OS-level share sheet works everywhere.</p>\n<p><strong>Why share-tracking link.</strong> Plan §PART 7 — measure viral coefficient (k-factor). Without it, share-to-install attribution is impossible.</p>\n<p><strong>Why kids SKU hidden.</strong> External-link gate complexity per TASK-LEGAL-003.</p>\n<p><strong>Why no automated posts.</strong> Plan §PART 11 trust model + plan §PART 8 — credentialed auto-posting opens many surfaces (token theft, abuse, etc.).</p>\n<p><strong>Why no captions at P1.</strong> Plan §PART 3 — auto-generated captions need TASK-AI-002 review. Punting reduces P1 scope.</p>\n<p><strong>Why trending audio metadata only (no real audio).</strong> Music licensing is a P3+ concern. Metadata hint is a free-via-precedent feature.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/viral/VideoExporter.ts\nexport class VideoExporter {\n  async export(opts: { input_video_uri: string; duration_sec: number; locale: string }): Promise&lt;{ output_uri: string; watermarked: true }&gt; {\n    if (opts.duration_sec &gt; 15) throw new Error('TASK-VIRAL-001 §1.2 max 15s');\n    const trending = TRENDING_AUDIO[opts.locale]?.[currentMonth()] ?? '';\n    return NativeBridge.exportVerticalMp4({\n      input: opts.input_video_uri, duration_sec: opts.duration_sec,\n      watermark: 'tamagochi.app',\n      itunes_song_metadata: trending,\n    });\n  }\n}\n\nexport class ShareIntent {\n  async invoke(opts: { video_uri: string; caption: string; source: 'ar'|'daily_drama'|'evolution'|'wedding' }): Promise&lt;{ invoked: true; share_link_id?: string }&gt; {\n    if (await this.isKidsSku()) throw new Error('TASK-VIRAL-001 §1.9 kids cannot share');\n    const linkId = await api.post('/v1/viral/share/create-link', { source: opts.source });\n    const captionWithLink = `${opts.caption} https://tamagochi.app/s/${linkId}`;\n    await NativeBridge.openShareSheet({ video_uri: opts.video_uri, caption: captionWithLink });\n    return { invoked: true, share_link_id: linkId };\n  }\n}</code></pre>\n<pre><code class=\"language-sql\">-- infra/supabase/standard/migrations/20260517_015_share_tracking.sql\ncreate table public.share_tracking (\n  id text primary key,\n  user_id uuid references auth.users(id) on delete cascade,\n  source text not null check (source in ('ar','daily_drama','evolution','wedding')),\n  created_at timestamptz not null default now(),\n  click_count int not null default 0,\n  install_count int not null default 0,\n  expires_at timestamptz not null default now() + interval '90 days'\n);\ncreate index on public.share_tracking (user_id);\nalter table public.share_tracking enable row level security;\ncreate policy \"share self\" on public.share_tracking for select using (user_id = auth.uid());</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/viral/share-tracking.controller.ts\n@Controller('v1/viral/share')\n@UseGuards(SupabaseJwtGuard)\nexport class ShareTrackingController {\n  @Post('create-link')\n  async createLink(@CurrentUser() u: AuthedUser, @Body() body: { source: 'ar'|'daily_drama'|'evolution'|'wedding' }) {\n    const id = generateUlid();\n    await this.supa.from('share_tracking').insert({ id, user_id: u.id, source: body.source });\n    return id;\n  }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Export produces 1080×1920 H.264 MP4 with watermark embedded. Verified by ffprobe inspection. <strong>AC2.</strong> Default duration 6s; AR uses 15s. Verified. <strong>AC3.</strong> Kids SKU has no Share button visible. Verified by Playwright. <strong>AC4.</strong> Share button invokes OS share sheet on iOS + Android. Verified by Detox. <strong>AC5.</strong> Prefilled hashtag localised correctly. Verified across EN + VI builds. <strong>AC6.</strong> Share link created server-side returns ULID. Verified. <strong>AC7.</strong> Share link click increments <code>click_count</code>. Verified. <strong>AC8.</strong> Trending audio metadata embedded. Verified by ffprobe. <strong>AC9.</strong> Export latency ≤ 3s on baseline emulator. Verified. <strong>AC10.</strong> No automated post path exists in code. Verified by grep for \"post_to_tiktok\" etc. <strong>AC11.</strong> Share tracking retention 90 days. Verified by retention policy migration. <strong>AC12.</strong> Caption length validation 280 chars. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/viral/__tests__/VideoExporter.spec.ts\ndescribe('TASK-VIRAL-001 — exporter', () =&gt; {\n  it('rejects &gt;15s', async () =&gt; {\n    await expect(exp.export({ input_video_uri: 'x', duration_sec: 20, locale: 'en' })).rejects.toThrow(/max 15s/);\n  });\n\n  it('embeds locale-correct hashtags', async () =&gt; {\n    const out = await exp.export({ input_video_uri: 'x', duration_sec: 6, locale: 'vi' });\n    expect(out.itunes_song_metadata).toContain('thúảo');\n  });\n\n  it('blocks share on kids SKU', async () =&gt; {\n    (globalThis as any).__BUILD_TARGET__ = 'kids';\n    await expect(intent.invoke({ video_uri: 'x', caption: 'hi', source: 'ar' })).rejects.toThrow(/kids cannot share/);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-swift\">// apps/cocos/native/ios/ShareSheetBridge.swift\n@objc class ShareSheetBridge: NSObject {\n  @objc static func open(_ videoUri: String, caption: String) {\n    let url = URL(fileURLWithPath: videoUri)\n    let activityVC = UIActivityViewController(\n      activityItems: [url, caption],\n      applicationActivities: nil\n    )\n    let topVC = UIApplication.shared.windows.first?.rootViewController\n    topVC?.present(activityVC, animated: true)\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> iOS UIActivityViewController; Android Intent.ACTION_SEND; AVAssetWriter / MediaCodec. <strong>Internal:</strong> TASK-AR-001 (provides input video URI), TASK-PET-002 (evolution share moment), TASK-LEGAL-003 (kids gate), TASK-I18N-001 (locale). <strong>Blocks:</strong> TASK-VIRAL-002 (Daily Drama share), TASK-VIRAL-003 (generative pet share), TASK-SOCIAL-004 (wedding share).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/viral/share/create-link\n{ \"source\": \"ar\" }\n→ 200 { \"id\": \"01HCSHARE...\" }</code></pre>\n<pre><code class=\"language-json\">{\n  \"event\": \"viral.share.invoked\",\n  \"user_id\": \"01HC...\",\n  \"source\": \"ar\",\n  \"video_duration_sec\": 6,\n  \"platform\": \"ios\"\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"id\": \"01HCSHARE...\",\n  \"user_id\": \"01HC...\",\n  \"source\": \"ar\",\n  \"click_count\": 12,\n  \"install_count\": 3,\n  \"created_at\": \"2026-08-12T14:36:01Z\"\n}</code></pre>\n<pre><code class=\"language-text\">Caption rendered by user before posting:\n\"My pet Mochi! #mochilife #virtualpet https://tamagochi.app/s/01HCSHARE...\"</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> TikTok SDK integration at P1? → §2 — OS share sheet only.</li><li><strong>OQ-2 (resolved):</strong> Captions at P1? → §1.18 — no (P2+).</li><li><strong>OQ-3 (resolved):</strong> Audio licensing? → §1.5 + §2 — metadata hint only, no real audio.</li><li><strong>OQ-4 (resolved):</strong> Auto-post path? → §<code>disallowed_tools</code> + §2 — forbidden.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Export encoding fails on low-end Android</td><td>MediaCodec error</td><td>UX show \"try again\"</td><td>Retry with lower bitrate</td></tr><tr><td>2</td><td>Share sheet cancelled by user</td><td>OS callback</td><td>Telemetry as \"intent_dismissed\"</td><td>No further action</td></tr><tr><td>3</td><td>Share link DB write fails</td><td>Server error</td><td>Share-without-link works</td><td>Surface telemetry-only mode</td></tr><tr><td>4</td><td>Trending audio metadata invalid</td><td>Player support</td><td>UX OK; metadata blank</td><td>Restore from Mixpanel cache</td></tr><tr><td>5</td><td>Watermark omitted at encode</td><td>CI assert</td><td>Build blocked</td><td>Re-encode with correct overlay</td></tr><tr><td>6</td><td>Caption with emoji breaking share</td><td>Detox UI</td><td>Sanitize before pass</td><td>Strip incompatible chars</td></tr><tr><td>7</td><td>Storage permission denied on Android</td><td>OS API</td><td>Fallback: share without save</td><td>Surface \"share-only\" UX</td></tr><tr><td>8</td><td>Click tracking retention exceeded</td><td>Disk</td><td>Retention policy</td><td>90-day cutoff</td></tr><tr><td>9</td><td>Kids SKU share button accidentally visible</td><td>Playwright catches</td><td>COPPA risk</td><td>Hot-fix to hide</td></tr><tr><td>10</td><td>TikTok blocks tamagochi.app domain</td><td>UA monitoring</td><td>Surface alternate domain</td><td>Pivot to cyberskill.world subdomain</td></tr><tr><td>11</td><td>Export latency &gt;3s persistently</td><td>Perf metric</td><td>UX hang</td><td>Lower bitrate or pre-cache hot codec init</td></tr><tr><td>12</td><td>Share intent breaks on app rotation</td><td>OS state</td><td>Crash</td><td>Persist intent payload to allow recovery</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 viral hook #5 (TikTok-native exports), plan §PART 7 GTM, plan §PART 4 (no TikTok SDK at P1).</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Hashtag list per locale — UA-curated monthly.</li><li>Trending audio metadata per region — Mixpanel-flag-tuned.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Automated posting.</li><li>Removable watermark.</li><li>Kids share without parental gate.</li><li>User-supplied caption overlay text at P1.</li></ul>\n<p><strong>Cross-reference:</strong> Closes P1 visual viral loop. TASK-VIRAL-002 / 003 / TASK-SOCIAL-004 all use this export pipeline.</p>";
