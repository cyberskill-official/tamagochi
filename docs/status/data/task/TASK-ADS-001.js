window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-ADS-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Only rewarded video.</strong> No interstitials (per TASK-LEGAL-003 + kid-app guideline). No banners. No native ads. Only rewarded video.</p>\n<p>§1.2  <strong>Mediation waterfall.</strong> <strong>LevelPlay (IronSource) primary</strong> + <strong>AppLovin MAX fallback</strong>. Per TASK-LEGAL-001 §1.5(a) — only on standard SKU.</p>\n<p>§1.3  <strong>Reward.</strong> Each completed rewarded view grants 50 Coins via TASK-ECON-001 ledger (<code>ref = 'ads.rewarded:&lt;view_id&gt;'</code>).</p>\n<p>§1.4  <strong>Daily cap.</strong> 8 rewarded views per player per day. Anti-grind + UX-protective. Configurable via Mixpanel flag.</p>\n<p>§1.5  <strong>Per-session cap.</strong> Max 3 views per app-session (avoids ad-spam UX).</p>\n<p>§1.6  <strong>Cooldown.</strong> 5 minutes between rewarded views. Player taps \"Watch ad\" → if within cooldown → 429 with <code>retry_after_sec</code>.</p>\n<p>§1.7  <strong>Server-side validation (SSV).</strong> SDK's SSV callback hits our API. The reward IS granted only after SSV confirms.</p>\n<p>§1.8  <strong>No Pet+ ads.</strong> Pet+ subscribers (TASK-SUB-001 §1.2) have \"ad-free\" benefit. Endpoint returns 403 <code>ads.pet_plus_disabled</code> if attempted.</p>\n<p>§1.9  <strong>Kids SKU forbidden.</strong> This task is 13+ only. Kids contextual-only ads via TASK-ADS-002.</p>\n<p>§1.10  <strong>Endpoint — request rewarded.</strong> <code>POST /v1/ads/rewarded/request</code> returns SDK config + view_id. Then SDK plays ad.</p>\n<p>§1.11  <strong>SSV webhook.</strong> <code>POST /webhook/ads/levelplay/ssv</code> and <code>/webhook/ads/applovin/ssv</code> — validates SDK signature + matches view_id → grants Coins.</p>\n<p>§1.12  <strong>Anti-fraud.</strong> SSV signature verification mandatory. Replay protection via view_id uniqueness.</p>\n<p>§1.13  <strong>Latency budget.</strong> Ad load &lt; 5s; reward grant after SSV &lt; 1s. UX shows loading state.</p>\n<p>§1.14  <strong>No personalised ads.</strong> Plan §PART 8 — even on standard SKU, default to contextual ads (non-personalised) for additional regulator margin. Player can opt-in to personalised via Settings → Ad preferences.</p>\n<p>§1.15  <strong>Ad-units configured per region.</strong> VN-specific ad units (LevelPlay supports VN inventory).</p>\n<p>§1.16  <strong>EEA + UK consent flow.</strong> Per GDPR — collect ad consent via OS-native UX (App Tracking Transparency on iOS; ConsentInformation on Android). Without consent → contextual ads only.</p>\n<p>§1.17  <strong>Audit.</strong> Every ad view + grant audited. 7-year retention (kids) / 2-year (standard).</p>\n<p>§1.18  <strong>Sentry on ad SDK error.</strong> Catch SDK loading / playback errors.</p>\n<p>§1.19  <strong>No ad before onboarding.</strong> Per TASK-PET-004 §1.7 — no ads during onboarding flow.</p>\n<p>§1.20  <strong>Analytics.</strong> <code>ads.rewarded.requested</code>, <code>ads.rewarded.shown</code>, <code>ads.rewarded.completed</code>, <code>ads.rewarded.skipped</code>, <code>ads.rewarded.cap_hit</code>, <code>ads.rewarded.failed { reason }</code> per TASK-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why rewarded-only.</strong> Plan §PART 6 — interstitials destroy retention especially for kid-skewing apps. Banners feel low-rent.</p>\n<p><strong>Why LevelPlay + AppLovin.</strong> Plan §PART 6 — LevelPlay has VN inventory; AppLovin MAX is the fallback giant. Mediation maximises fill rate.</p>\n<p><strong>Why daily 8 cap.</strong> Anti-grind. 8 × 50 = 400 Coins/day from ads ≈ same as 2 surprise-egg openings. Coherent with the TASK-PET-006 economy.</p>\n<p><strong>Why SSV.</strong> Plan §PART 4 — client-claimed rewards are forged trivially.</p>\n<p><strong>Why no Pet+ ads.</strong> Plan §PART 6 — ad-free is a key subscription perk.</p>\n<p><strong>Why contextual default.</strong> Plan §PART 8 — GDPR + Apple ATT + Google Privacy Sandbox. Personalised has higher fill but compliance cost.</p>\n<p><strong>Why no onboarding ads.</strong> Plan §PART 5 + Apple Guideline.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/ads/rewarded.service.ts (excerpt)\nasync requestRewarded(u: AuthedUser) {\n  if (await this.sub.tier(u.id) === 'pet_plus') throw new HttpException('ads.pet_plus_disabled', 403);\n  if (await this.sku(u) === 'kids') throw new HttpException('ads.kids_forbidden', 403);\n  await this.cooldown.assertNotInCooldown(u.id);\n  if (await this.dailyCount(u.id) &gt;= 8) throw new HttpException('ads.daily_cap', 429);\n  if (await this.sessionCount(u.id) &gt;= 3) throw new HttpException('ads.session_cap', 429);\n  const viewId = generateUlid();\n  await this.supa.from('ad_views').insert({ id: viewId, user_id: u.id, requested_at: new Date(), tenant_id: u.tenant_id });\n  return { view_id: viewId, sdk_config: this.sdkConfig(u) };\n}\n\nasync handleSsv(req: Request, vendor: 'levelplay' | 'applovin') {\n  const validator = vendor === 'levelplay' ? this.lpValidator : this.alValidator;\n  const result = await validator.verify(req);\n  if (!result.valid) { this.audit.emit('ads.ssv.invalid', { vendor }); throw new HttpException('invalid', 401); }\n  const view = await this.supa.from('ad_views').select('*').eq('id', result.view_id).single();\n  if (view.data.completed) return;  // idempotent\n  if (view.data.user_id !== result.user_id) throw new HttpException('mismatched', 422);\n  await this.ledger.grant(view.data.user_id, 'coins', 50, `ads.rewarded:${result.view_id}`, view.data.tenant_id, 'system_source');\n  await this.supa.from('ad_views').update({ completed: true, completed_at: new Date(), vendor }).eq('id', result.view_id);\n  this.audit.emit('ads.rewarded.completed', { view_id: result.view_id });\n}</code></pre>\n<pre><code class=\"language-sql\">create table public.ad_views (\n  id text primary key,\n  user_id uuid not null references auth.users(id) on delete restrict,\n  vendor text check (vendor in ('levelplay','applovin')),\n  requested_at timestamptz not null,\n  completed boolean not null default false,\n  completed_at timestamptz,\n  reward_coins int not null default 50,\n  ssv_signature text,\n  tenant_id text not null default 'mochi'\n);\ncreate index on public.ad_views (user_id, requested_at desc);\nalter table public.ad_views enable row level security;\ncreate policy \"ad_views self\" on public.ad_views for select using (user_id = auth.uid());</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Rewarded view request returns view_id. Verified. <strong>AC2.</strong> SSV validates + grants Coins. Verified. <strong>AC3.</strong> Pet+ user request returns 403. Verified. <strong>AC4.</strong> Kids SKU returns 403. Verified. <strong>AC5.</strong> Daily cap 8 enforced. Verified. <strong>AC6.</strong> Session cap 3 enforced. Verified. <strong>AC7.</strong> 5-min cooldown enforced. Verified. <strong>AC8.</strong> SSV signature forgery rejected. Verified. <strong>AC9.</strong> Replay of same view_id no double-grant. Verified. <strong>AC10.</strong> No personalised ads by default (consent default contextual). Verified. <strong>AC11.</strong> No ads during onboarding. Verified. <strong>AC12.</strong> AppLovin fallback on LevelPlay failure. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('TASK-ADS-001 — rewarded', () =&gt; {\n  it('grants 50 Coins on valid SSV', async () =&gt; {\n    const { view_id } = await svc.requestRewarded(user('u1'));\n    await svc.handleSsv(mockSsvRequest({ view_id, user_id: user('u1').id }), 'levelplay');\n    expect(await ledger.balance('u1', 'coins')).toBe(50);\n  });\n\n  it('idempotent on duplicate SSV', async () =&gt; {\n    const { view_id } = await svc.requestRewarded(user('u1'));\n    await svc.handleSsv(mockSsvRequest({ view_id, user_id: user('u1').id }), 'levelplay');\n    await svc.handleSsv(mockSsvRequest({ view_id, user_id: user('u1').id }), 'levelplay');\n    expect(await ledger.balance('u1', 'coins')).toBe(50); // not 100\n  });\n\n  it('blocks Pet+ users', async () =&gt; {\n    await mockPetPlus('u1');\n    await expect(svc.requestRewarded(user('u1'))).rejects.toMatchObject({ status: 403 });\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/ads/levelplay-validator.ts\n@Injectable()\nexport class LevelPlayValidator {\n  async verify(req: Request): Promise&lt;{ valid: boolean; view_id?: string; user_id?: string }&gt; {\n    const sig = req.headers['x-ironsrc-signature'] as string;\n    const body = await req.text();\n    const expected = createHmac('sha256', process.env.LEVELPLAY_SECRET!).update(body).digest('hex');\n    if (sig !== expected) return { valid: false };\n    const j = JSON.parse(body);\n    return { valid: true, view_id: j.customParameters?.view_id, user_id: j.customParameters?.user_id };\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> LevelPlay (IronSource); AppLovin MAX SDK. <strong>Internal:</strong> TASK-ECON-001 ledger, TASK-LEGAL-003 SKU + ad policy, TASK-SUB-001 (Pet+ ad-free). <strong>Blocks:</strong> TASK-ADS-002 (kids contextual gate).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/ads/rewarded/request\n→ 200 { \"view_id\": \"01HAD...\", \"sdk_config\": {...}, \"expected_reward_coins\": 50 }</code></pre>\n<pre><code class=\"language-http\">POST /webhook/ads/levelplay/ssv\nX-IronSrc-Signature: ...\n{ \"customParameters\": { \"view_id\": \"01HAD...\", \"user_id\": \"01HU...\" }, ... }\n→ 200 ok</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"ads.rewarded.completed\", \"view_id\": \"01HAD...\", \"user_id\": \"01HU...\", \"vendor\": \"levelplay\", \"reward_coins\": 50 }</code></pre>\n<pre><code class=\"language-json\">{ \"error\": \"ads.daily_cap\", \"current\": 8, \"limit\": 8 }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Interstitials? → §<code>disallowed_tools</code> — never.</li><li><strong>OQ-2 (resolved):</strong> Daily cap 8? → §1.4 + §2.</li><li><strong>OQ-3 (resolved):</strong> Personalised default? → §1.14 — no.</li><li><strong>OQ-4 (resolved):</strong> Pet+ exclusion? → §1.8 — yes.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>LevelPlay fill rate low</td><td>Metric</td><td>Fallback to AppLovin</td><td>Mediation handles</td></tr><tr><td>2</td><td>SSV signature mismatch</td><td>Validator</td><td>Reject + audit</td><td>Investigate vendor secret rotation</td></tr><tr><td>3</td><td>SSV not received</td><td>Polling</td><td>Reward not granted</td><td>Manual reconciliation; ledger drift OK if user re-watches</td></tr><tr><td>4</td><td>Pet+ entitlement stub returns wrong</td><td>TASK-SUB-001 wired</td><td>Ads to Pet+ accidentally</td><td>Cache invalidation</td></tr><tr><td>5</td><td>Kids SKU bypass</td><td>RLS</td><td>Blocked</td><td>Verified</td></tr><tr><td>6</td><td>Daily cap counter Redis lost</td><td>Conservative</td><td>Cap holds</td><td>Reset on recovery</td></tr><tr><td>7</td><td>Onboarding ad shown</td><td>UI test</td><td>Apple/Google review risk</td><td>Build-target gate</td></tr><tr><td>8</td><td>Behavioural ad on kids accidentally</td><td>TASK-ADS-002 catches</td><td>COPPA risk</td><td>Audit</td></tr><tr><td>9</td><td>Consent flow not invoked in EEA</td><td>Apple ATT</td><td>Personalised data leaked</td><td>Pre-launch QA</td></tr><tr><td>10</td><td>Ad unit ID misconfig</td><td>Vendor monitor</td><td>No ads served</td><td>Rotate config</td></tr><tr><td>11</td><td>Audit growth</td><td>Disk</td><td>7-yr / 2-yr retention</td><td>Configured</td></tr><tr><td>12</td><td>Reward exceeds cap via timezone drift</td><td>TZ-aware</td><td>Bounded</td><td>Locked</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 6 (rewarded video + LevelPlay + AppLovin).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Per-ad-unit pricing + per-region waterfall.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Interstitials.</li><li>Banners.</li><li>Client-claimed reward.</li><li>Ads on Pet+.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-ADS-002 contextual kid gate; TASK-SUB-001 ad-free benefit.</p>";
