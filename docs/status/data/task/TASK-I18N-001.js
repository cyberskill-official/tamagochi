window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-I18N-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Launch wave — 9 locales.</strong> EN (en), VI (vi), ID (id), TH (th), PT-BR (pt-BR), ES-LATAM (es-419), JA (ja), KO (ko), ZH-Hant (zh-Hant).</p>\n<p>§1.2  <strong>Locale keys.</strong> All UI strings + push notifications + email templates + audio captions MUST be keyed. Hardcoded strings forbidden in production code.</p>\n<p>§1.3  <strong>Key registry.</strong> A central registry <code>apps/cocos/assets/i18n/_keys-registry.json</code> lists ALL keys + their EN source string + context comment. The registry is the source of truth for Crowdin sync.</p>\n<p>§1.4  <strong>Crowdin integration.</strong> Translators work in Crowdin; CI syncs back to <code>apps/cocos/assets/i18n/&lt;locale&gt;/*.json</code> via <code>scripts/i18n/crowdin-sync.mjs</code>. Manual edits to locale JSON forbidden — Crowdin is canonical.</p>\n<p>§1.5  <strong>Per-feature namespacing.</strong> Keys are namespaced: <code>care.feed.success</code>, <code>petpair.invite.received</code>, <code>streak.milestone.30</code>, etc. Aligns with TASK-OBS-001 §1.4 event taxonomy.</p>\n<p>§1.6  <strong>ICU MessageFormat.</strong> Pluralization + gender handled by ICU MessageFormat. Standard for kid-friendly + Asian-language correctness.</p>\n<p>§1.7  <strong>Font fallback chains.</strong> Locale-specific font stacks. For VI, Noto Sans Vietnamese (diacritics-safe — plan §PART 5). For JA: Noto Sans JP. For ZH-Hant: Noto Sans TC. Loaded as Cocos asset bundles.</p>\n<p>§1.8  <strong>Diacritics test.</strong> Vietnamese diacritics (e.g. \"ấ\", \"ề\", \"ỗ\") MUST render correctly. CI snapshot test on each VI string verifying no glyph missing.</p>\n<p>§1.9  <strong>RTL stub.</strong> Future Arabic / Hebrew support deferred but stub'd: <code>I18nService.isRtl(locale)</code> returns boolean, UI layouts read this. Initial wave is all LTR; RTL added in future task.</p>\n<p>§1.10  <strong>Locale detection.</strong> Detected from TASK-AUTH-001 <code>region_of_record</code>, then user override via Settings, then OS locale, then 'en' fallback.</p>\n<p>§1.11  <strong>Missing-key fallback.</strong> Cocos <code>I18nService</code> returns key string surrounded by <code>~missing~</code> brackets if key not in current locale; falls back to EN if EN has it; emits <code>i18n.missing_key { key, locale }</code> event to TASK-OBS-001 for triage.</p>\n<p>§1.12  <strong>Coverage CI.</strong> <code>scripts/i18n/check-keys-coverage.mjs</code> enforces: every key in registry must exist in EN (mandatory); coverage in other locales reported (production-ready locales = 100% EN coverage). Build blocked on EN coverage &lt; 100%.</p>\n<p>§1.13  <strong>Server-side i18n.</strong> Push notifications + emails use the same registry. Server-rendered strings via <code>i18n.t(key, locale, params)</code>.</p>\n<p>§1.14  <strong>Translation review.</strong> All translations DPO-reviewed for kid-appropriateness + cultural sensitivity (plan §PART 5). DPO sign-off required before locale goes production.</p>\n<p>§1.15  <strong>No-machine-translation default.</strong> Plan §PART 5 + brand integrity — Crowdin machine-translation may seed but human review is mandatory.</p>\n<p>§1.16  <strong>Currency + number locale.</strong> Per-locale formatting via ICU (e.g. \"₫29.000\" for VN, \"Rp 29.000\" for ID).</p>\n<p>§1.17  <strong>Date / time format.</strong> Per-locale (e.g. ISO 8601 for international, locale-specific for in-app).</p>\n<p>§1.18  <strong>Tenant-aware copy override.</strong> B2B tenants (TASK-B2B-001) may override specific keys per tenant + locale. Override path: <code>i18n/tenants/&lt;slug&gt;/&lt;locale&gt;/*.json</code>.</p>\n<p>§1.19  <strong>Audit + analytics.</strong> <code>i18n.locale_changed</code>, <code>i18n.missing_key</code>, <code>i18n.font_fallback_applied</code> per TASK-OBS-001.</p>\n<p>§1.20  <strong>Production locale gate.</strong> A locale is \"production-ready\" only when: 100% key coverage + DPO sign-off + diacritics CI test passing + 14-day soft-launch validation.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 9 locales.</strong> Plan §PART 5 — covers VN soft-launch + global P4 launch + JP/KR for Asia + LATAM for emerging markets.</p>\n<p><strong>Why Crowdin.</strong> Plan §PART 5 — industry standard for game localization. Translation memory + glossary + screenshots support.</p>\n<p><strong>Why keys not hardcoded strings.</strong> Plan §PART 5 — single source of truth + reusable + maintainable.</p>\n<p><strong>Why ICU MessageFormat.</strong> Plan §PART 5 — pluralization in JA/KO/AR/RU is grammatically complex. ICU handles it correctly.</p>\n<p><strong>Why VI diacritics tested.</strong> Plan §PART 5 — many fonts butcher Vietnamese diacritics. Snapshot test catches regression.</p>\n<p><strong>Why RTL stub.</strong> Plan §PART 5 — Arabic + Hebrew likely future markets. Stub keeps the door open without overcomplicating P4.</p>\n<p><strong>Why DPO review.</strong> Plan §PART 5 + plan §PART 8 — kid-appropriate translation requires cultural review.</p>\n<p><strong>Why 14-day soft-launch validation.</strong> Catches edge cases real users encounter.</p>\n<p><strong>Why tenant-aware override.</strong> Plan §PART 6 — Techcombank wants their own branded copy.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/i18n/I18nService.ts\nexport class I18nService {\n  private bundles = new Map&lt;string, Record&lt;string, string&gt;&gt;();\n  private current: string = 'en';\n\n  async loadLocale(locale: string): Promise&lt;void&gt; {\n    const bundle = await fetch(`/i18n/${locale}/strings.json`).then(r =&gt; r.json());\n    this.bundles.set(locale, bundle);\n    this.current = locale;\n  }\n\n  t(key: string, params?: Record&lt;string, any&gt;): string {\n    const bundle = this.bundles.get(this.current) ?? {};\n    const fallback = this.bundles.get('en') ?? {};\n    const template = bundle[key] ?? fallback[key];\n    if (!template) {\n      this.emitMissing(key);\n      return `~missing:${key}~`;\n    }\n    return params ? this.interpolateIcu(template, params) : template;\n  }\n\n  isRtl(locale: string): boolean {\n    return ['ar','he','fa','ur'].includes(locale.split('-')[0]);\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/api/src/i18n/locale.service.ts (server-side)\n@Injectable()\nexport class LocaleService {\n  private bundles = new Map&lt;string, Record&lt;string, string&gt;&gt;();\n\n  async t(key: string, locale: string, params?: Record&lt;string, any&gt;): Promise&lt;string&gt; {\n    const bundle = this.bundles.get(locale) ?? this.bundles.get('en')!;\n    const template = bundle[key];\n    if (!template) {\n      this.audit.emit('i18n.missing_key', { key, locale });\n      return `~missing:${key}~`;\n    }\n    return params ? this.icu.format(template, params) : template;\n  }\n}</code></pre>\n<pre><code class=\"language-javascript\">// scripts/i18n/check-keys-coverage.mjs\nimport { readFileSync, readdirSync } from 'node:fs';\nconst registry = JSON.parse(readFileSync('apps/cocos/assets/i18n/_keys-registry.json', 'utf8'));\nconst allKeys = Object.keys(registry);\nconst enBundle = JSON.parse(readFileSync('apps/cocos/assets/i18n/en/strings.json', 'utf8'));\nconst missing = allKeys.filter(k =&gt; !enBundle[k]);\nif (missing.length) { console.error(`Missing in EN: ${missing}`); process.exit(1); }\nconsole.log(`EN coverage: ${allKeys.length}/${allKeys.length}`);\n\nconst locales = readdirSync('apps/cocos/assets/i18n').filter(d =&gt; !d.startsWith('_'));\nfor (const l of locales) {\n  if (l === 'en') continue;\n  const bundle = JSON.parse(readFileSync(`apps/cocos/assets/i18n/${l}/strings.json`, 'utf8'));\n  const present = allKeys.filter(k =&gt; bundle[k]).length;\n  console.log(`${l}: ${present}/${allKeys.length} (${(100*present/allKeys.length).toFixed(1)}%)`);\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> All 9 locales have bundles. Verified. <strong>AC2.</strong> Keys registry exists with all UI keys. Verified. <strong>AC3.</strong> EN coverage 100% (CI). Verified. <strong>AC4.</strong> VI diacritics render correctly (snapshot). Verified. <strong>AC5.</strong> ICU MessageFormat plurals work in JA + KO. Verified. <strong>AC6.</strong> Missing-key fallback to EN with audit emission. Verified. <strong>AC7.</strong> Locale detection chain works. Verified. <strong>AC8.</strong> Font fallback for each locale. Verified. <strong>AC9.</strong> Crowdin sync round-trip. Verified. <strong>AC10.</strong> Server-side <code>i18n.t()</code> returns localised string. Verified. <strong>AC11.</strong> Tenant-override key resolution. Verified. <strong>AC12.</strong> RTL stub returns correct boolean. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('TASK-I18N-001 — i18n', () =&gt; {\n  it('falls back to EN on missing key', async () =&gt; {\n    await svc.loadLocale('vi');\n    const r = svc.t('non.existent.key');\n    expect(r).toMatch(/missing/);\n  });\n\n  it('localises VN pet greeting', async () =&gt; {\n    await svc.loadLocale('vi');\n    expect(svc.t('pet.greeting', { name: 'Mochi' })).toContain('Mochi');\n  });\n\n  it('ICU plurals for KO', async () =&gt; {\n    await svc.loadLocale('ko');\n    const r = svc.t('streak.day_count', { count: 7 });\n    expect(r).toMatch(/7/);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/i18n/FontFallback.ts\nconst FONT_STACK = {\n  vi: ['Noto Sans Vietnamese', 'Noto Sans', 'sans-serif'],\n  ja: ['Noto Sans JP', 'Noto Sans', 'sans-serif'],\n  ko: ['Noto Sans KR', 'Noto Sans', 'sans-serif'],\n  th: ['Noto Sans Thai', 'Noto Sans', 'sans-serif'],\n  'zh-Hant': ['Noto Sans TC', 'Noto Sans', 'sans-serif'],\n};\n\nexport function getFontStack(locale: string): string[] {\n  return FONT_STACK[locale] ?? ['Noto Sans', 'sans-serif'];\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Crowdin account; Noto Sans font family. <strong>Internal:</strong> TASK-INFRA-001 (Cocos asset bundle), TASK-AUTH-001 (region_of_record), TASK-OBS-001 (missing-key event). <strong>Blocks:</strong> TASK-I18N-002 (payment localisation), TASK-A11Y-001 (locale-aware A11Y).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">GET /v1/me/locale → 200 { \"locale\": \"vi\", \"rtl\": false, \"font_stack\": [\"Noto Sans Vietnamese\", \"Noto Sans\", \"sans-serif\"] }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"i18n.missing_key\", \"key\": \"pet.couture.preview.title\", \"locale\": \"th\" }</code></pre>\n<pre><code class=\"language-jsonc\">// _keys-registry.json\n{ \"pet.greeting\": { \"en\": \"Hello, {name}!\", \"context\": \"Pet greets player on hatch\" } }</code></pre>\n<pre><code class=\"language-jsonc\">// vi/strings.json\n{ \"pet.greeting\": \"Xin chào, {name}!\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> 9 locales at launch? → §1.1.</li><li><strong>OQ-2 (resolved):</strong> Crowdin vs Lokalise? → §1.4 — Crowdin (cheaper + better game-specific).</li><li><strong>OQ-3 (resolved):</strong> RTL at P4? → §1.9 — stub only.</li><li><strong>OQ-4 (resolved):</strong> Machine translation? → §1.15 — seed only, human review required.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Locale bundle fails to load</td><td>Cocos error</td><td>EN fallback</td><td>Surface \"locale unavailable\"</td></tr><tr><td>2</td><td>VI diacritic font missing</td><td>Snapshot test</td><td>Build blocked</td><td>Add font asset</td></tr><tr><td>3</td><td>Translation drift (out-of-Crowdin edit)</td><td>CI lint</td><td>PR blocked</td><td>Sync from Crowdin</td></tr><tr><td>4</td><td>DPO sign-off skipped</td><td>Manual gate</td><td>Cannot go-prod</td><td>DPO review step</td></tr><tr><td>5</td><td>Missing key in production</td><td>Audit event</td><td>EN fallback + alert</td><td>Add missing key</td></tr><tr><td>6</td><td>ICU plural form wrong</td><td>Translation review</td><td>UX bug</td><td>DPO + translator</td></tr><tr><td>7</td><td>RTL stub bug breaks layout</td><td>UI test</td><td>Layout broken</td><td>Disable RTL until ready</td></tr><tr><td>8</td><td>Tenant override missing required key</td><td>Validation</td><td>Fallback to consumer locale</td><td>Lint catches</td></tr><tr><td>9</td><td>Crowdin sync fails</td><td>CI error</td><td>Stale locales</td><td>Manual sync retry</td></tr><tr><td>10</td><td>Cross-tenant translation reuse leaks</td><td>RLS</td><td>Privacy</td><td>Tenant scope check</td></tr><tr><td>11</td><td>Currency formatting wrong</td><td>Test</td><td>UX issue</td><td>ICU correction</td></tr><tr><td>12</td><td>Soft-launch finds locale issues</td><td>14-day validation</td><td>Locale held back</td><td>Iterate before go-prod</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 5 localization order; plan §PART 7 VN soft launch.</p>\n<p><strong>Sub-decisions punted to ops:</strong> Per-locale translator hiring + Crowdin glossary.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Hardcoded strings.</li><li>Direct edits to locale JSON bypassing Crowdin.</li><li>Production launch without DPO sign-off.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-I18N-002 payment localisation; TASK-A11Y-001 reads font fallback.</p>";
