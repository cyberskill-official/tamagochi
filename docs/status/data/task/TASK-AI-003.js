window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AI-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>Standard SKU only.</strong> Per TASK-AI-001 §1.1 + TASK-LEGAL-001 §1.5 — kids SKU stays scripted via TASK-AI-002 dialogue trees. No LLM enrichment for under-13.</p>\n<p>§1.2  <strong>Selfie-aware reactions.</strong> When player invokes TASK-VIRAL-003 generative-pet flow OR explicitly shares a selfie via Settings, the AI may reference visual changes (\"You got a haircut!\"). Implementation:</p>\n<ul><li>Player opt-in: settings toggle \"Notice my appearance changes\".</li><li>On opt-in, periodic selfie comparison flow (optional, 1×/week).</li><li>Selfie processed in-memory (NO storage per TASK-LEGAL-001 §1.4).</li><li>Dominant-color delta vs last reference → triggers \"you look different!\" line.</li><li>Reference: 3-5 RGB values only persisted (no image).</li></ul>\n<p>§1.3  <strong>Name-aware memory.</strong> Player's name (display_name from TASK-AUTH-001) included in persona prompt context. AI may reference: \"Hi Linh!\", \"Linh, did you eat lunch?\". DPO-reviewed for kid-appropriateness even on 13+.</p>\n<p>§1.4  <strong>Birthday awareness.</strong> Player's birthday (from sign-up flow + TASK-AUTH-001 — opt-in only) triggers \"Happy birthday Linh!\" line on the day.</p>\n<p>§1.5  <strong>Multi-pet sibling awareness.</strong> Per TASK-PET-005 multi-pet — AI persona context includes the player's roster. Siblings can reference each other: \"Bao is so noisy today!\" (when Bao is also owned). Tied to TASK-PET-007 breeding lineage if siblings are bred.</p>\n<p>§1.6  <strong>Cross-pet anti-affinity.</strong> Pets MAY exhibit slight personality affinities (best friends, mild rivals). Affinities deterministically derived from each pet's <code>palette_seed</code> hash. Not random.</p>\n<p>§1.7  <strong>Time-of-day awareness.</strong> Per TASK-AI-001 trigger <code>time_of_day</code> — v2 enriches: \"Good morning Linh!\" / \"It's getting late, are you about to sleep?\".</p>\n<p>§1.8  <strong>Event memory expansion.</strong> Per TASK-AI-001 §1.3 — increase 20-event memory to 40 for Pet+ subscribers (additional context for richer dialogue).</p>\n<p>§1.9  <strong>Cost cap raised for v2.</strong> Pet+ subscribers get persona-v2 quota 90 calls/pet/day (vs 60 for v1). Free tier remains 30/day. Hardly any v2 features available to free tier (only name-aware basics).</p>\n<p>§1.10  <strong>Content safety stricter for v2.</strong> v2 responses pass through TASK-AI-002 + additional name/birthday-leak check (don't echo back identifying info).</p>\n<p>§1.11  <strong>Endpoint — persona v2 speak.</strong> Same <code>/v1/ai/pet/:petId/speak</code> endpoint extended with <code>context_overrides: { include_sibling: true, include_selfie_delta: true, include_name: true }</code>.</p>\n<p>§1.12  <strong>Selfie consent flow.</strong> Settings → Privacy → \"Selfie-aware AI\" toggle (default OFF). On toggle, parental consent re-confirmation if account is under-18 (Apple Family Sharing or parental notification — even 13-18 is gradient).</p>\n<p>§1.13  <strong>Name-leak prevention.</strong> Per TASK-AI-002 — AI MUST NOT include the player's name in shared TikTok clips (TASK-VIRAL-001) or in any externally-rendered content. Only in in-app dialogue.</p>\n<p>§1.14  <strong>Persona evolution.</strong> Per pet, persona refines over time based on care patterns. E.g. a heavily-fed pet says \"Linh always knows when I'm hungry!\". State stored in <code>pet_personality_traits</code> table.</p>\n<p>§1.15  <strong>DPO-reviewed v2 prompt templates.</strong> Every v2 capability has a system-prompt template DPO-reviewed for boundary respect.</p>\n<p>§1.16  <strong>Audit retention.</strong> 2-year for standard (no kids on v2).</p>\n<p>§1.17  <strong>Tenant override.</strong> B2B tenants may use baseline v1 only (TASK-B2B-001 can disable v2 for tenant simplicity).</p>\n<p>§1.18  <strong>Performance budget.</strong> v2 latency budget remains 2s P95 (same as v1 per TASK-AI-001 §1.13).</p>\n<p>§1.19  <strong>Fallback to v1.</strong> If v2 fails (cost cap, safety block, vendor outage), fall back to v1 (scripted) per TASK-AI-001 fallback chain.</p>\n<p>§1.20  <strong>Analytics.</strong> <code>ai.persona_v2.feature_used { feature }</code>, <code>ai.persona_v2.name_referenced</code>, <code>ai.persona_v2.sibling_referenced</code>, <code>ai.persona_v2.selfie_delta_detected</code> per TASK-OBS-001.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why kids exclusion.</strong> Plan §PART 8 — generative + name-aware + selfie-aware for under-13 = COPPA-2025 super-toxic. Hard line.</p>\n<p><strong>Why opt-in selfie.</strong> Privacy first.</p>\n<p><strong>Why no selfie storage.</strong> Plan §PART 11 — selfies are sensitive.</p>\n<p><strong>Why name-awareness.</strong> Plan §PART 3 viral hook #3 — name in dialogue creates intimacy.</p>\n<p><strong>Why deterministic anti-affinity.</strong> Plan §PART 4 + TASK-LEGAL-002 — no random outcomes; auditable.</p>\n<p><strong>Why sibling awareness.</strong> Plan §PART 3 multi-pet — cross-pet references make pets feel like a family.</p>\n<p><strong>Why event memory 40 for Pet+.</strong> Plan §PART 6 — meaningful Pet+ perk beyond base game.</p>\n<p><strong>Why name-leak prevention in TikTok.</strong> Plan §PART 11 — adversarial input model; player's name should never be in publicly-shareable content.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/ai/persona-v2/persona-v2.service.ts\n@Injectable()\nexport class PersonaV2Service {\n  async speak(petId: string, ownerId: string, trigger: PersonaTrigger, overrides?: PersonaContextOverrides) {\n    if (await this.sku(ownerId) === 'kids') throw new HttpException('ai.kids.forbidden', 403);\n    const owner = await this.users.byId(ownerId);\n    const siblings = overrides?.include_sibling ? await this.siblings(ownerId, petId) : [];\n    const selfieDelta = overrides?.include_selfie_delta ? await this.selfieDelta(ownerId) : null;\n    const promptCtx = {\n      ...await this.basePersona.context(petId),\n      owner_name: overrides?.include_name ? owner.display_name : undefined,\n      birthday_today: this.isBirthday(owner.birth_date),\n      siblings: siblings.map(s =&gt; ({ id: s.id, species: s.species, current_mood: this.mood(s) })),\n      selfie_delta: selfieDelta,\n    };\n    try {\n      const r = await this.llmRouter.complete(this.buildV2Prompt(promptCtx), { maxTokens: 100 });\n      const safe = await this.safety.assertSafe(r.text, { audience: '13+' });\n      if (!safe.ok) throw new Error('safety_block');\n      // Name-leak prevention for shareable content (per TASK-VIRAL-001)\n      if (trigger === 'shareable' &amp;&amp; this.containsName(r.text, owner.display_name)) {\n        r.text = this.redactName(r.text);\n      }\n      return r;\n    } catch {\n      return this.fallbackToV1(petId, trigger);\n    }\n  }\n}</code></pre>\n<pre><code class=\"language-sql\">create table public.pet_personality_traits (\n  pet_id text primary key references public.pets(id) on delete cascade,\n  feed_affinity int default 0,\n  clean_affinity int default 0,\n  hug_affinity int default 0,\n  mini_game_affinity int default 0,\n  evolved_at timestamptz default now(),\n  tenant_id text not null default 'mochi'\n);\n\ncreate table public.user_selfie_reference (\n  user_id uuid primary key references auth.users(id) on delete cascade,\n  dominant_rgb jsonb,\n  consent_opt_in boolean not null default false,\n  consented_at timestamptz,\n  last_updated timestamptz default now(),\n  tenant_id text not null default 'mochi'\n);</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Kids SKU rejected for v2. Verified. <strong>AC2.</strong> Name in dialogue when opted-in. Verified. <strong>AC3.</strong> Sibling pet referenced when owned. Verified. <strong>AC4.</strong> Selfie delta computed in-memory (no storage). Verified. <strong>AC5.</strong> Birthday triggers special line. Verified. <strong>AC6.</strong> Name redacted in shareable content. Verified. <strong>AC7.</strong> Pet+ event memory 40 events. Verified. <strong>AC8.</strong> Fallback to v1 on failure. Verified. <strong>AC9.</strong> Anti-affinity deterministic. Verified. <strong>AC10.</strong> Settings selfie toggle works + parental gate. Verified. <strong>AC11.</strong> Personality traits evolve over time. Verified. <strong>AC12.</strong> Tenant can disable v2. Verified.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">describe('TASK-AI-003 — persona v2', () =&gt; {\n  it('references sibling pet', async () =&gt; {\n    await seedPets('u1', ['mochi', 'bao']);\n    const r = await svc.speak('pet-mochi', 'u1', 'idle', { include_sibling: true });\n    expect(r.text).toMatch(/Bao/i);\n  });\n\n  it('redacts name in shareable content', async () =&gt; {\n    const r = await svc.speak('pet1', 'u1', 'shareable', { include_name: true });\n    expect(r.text).not.toContain('Linh');\n  });\n\n  it('falls back to v1 on safety block', async () =&gt; {\n    mockSafetyBlock();\n    const r = await svc.speak('pet1', 'u1', 'idle');\n    expect(r.source_model).toMatch(/v1|scripted/);\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// apps/api/src/ai/persona-v2/selfie-context.service.ts\n@Injectable()\nexport class SelfieContextService {\n  async selfieDelta(userId: string, newRgb?: number[][]): Promise&lt;{ changed: boolean; description?: string } | null&gt; {\n    if (!newRgb) return null;\n    const { data: ref } = await this.supa.from('user_selfie_reference').select('*').eq('user_id', userId).maybeSingle();\n    if (!ref) return null;\n    const delta = this.computeColorDelta(ref.dominant_rgb, newRgb);\n    if (delta &gt; 0.15) {\n      await this.supa.from('user_selfie_reference').update({ dominant_rgb: newRgb, last_updated: new Date() }).eq('user_id', userId);\n      return { changed: true, description: this.deltaToDescription(delta) };\n    }\n    return { changed: false };\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> Same as TASK-AI-001 (Claude Haiku, Gemini Flash). <strong>Internal:</strong> TASK-AI-001 (LLM router), TASK-AI-002 (safety), TASK-PET-005 (multi-pet), TASK-AUTH-003 (kids exclusion). <strong>Blocks:</strong> none.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{ \"text\": \"Hi Linh! Bao is really noisy today — wanna play with us?\", \"source_model\": \"claude-haiku\", \"v2_features_used\": [\"name\", \"sibling\"] }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"ai.persona_v2.feature_used\", \"user_id\": \"01HU...\", \"feature\": \"selfie_delta\" }</code></pre>\n<pre><code class=\"language-json\">{\n  \"context_overrides\": {\n    \"include_sibling\": true,\n    \"include_name\": true,\n    \"include_selfie_delta\": false\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{ \"pet_id\": \"01HC...\", \"feed_affinity\": 8, \"clean_affinity\": 3, \"hug_affinity\": 12, \"mini_game_affinity\": 5 }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> Kids v2? → §1.1 — never.</li><li><strong>OQ-2 (resolved):</strong> Selfie storage? → §1.2 — RGB-only, no image.</li><li><strong>OQ-3 (resolved):</strong> Sibling cross-user? → §<code>disallowed_tools</code>.</li><li><strong>OQ-4 (resolved):</strong> Name in shareable content? → §1.13 — redacted.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Name leak in TikTok export</td><td>Redaction filter</td><td>UX issue</td><td>Tighten filter</td></tr><tr><td>2</td><td>Selfie consent revoked mid-session</td><td>DSR</td><td>Selfie ignored</td><td>Verified</td></tr><tr><td>3</td><td>Sibling reference confusing (deleted sibling)</td><td>Roster sync</td><td>Reference removed</td><td>Refresh siblings</td></tr><tr><td>4</td><td>LLM hallucinates wrong birthday</td><td>Sanity check</td><td>Avoid</td><td>Validate birthday</td></tr><tr><td>5</td><td>Anti-affinity feels mechanical</td><td>Player support</td><td>Soften prompt</td><td>DPO tune</td></tr><tr><td>6</td><td>Cost cap raise blows budget</td><td>Daily reconciliation</td><td>Tighten</td><td>Adjust Pet+ caps</td></tr><tr><td>7</td><td>Safety block frequent on v2</td><td>Metric</td><td>Frustration</td><td>Tune classifier</td></tr><tr><td>8</td><td>Cross-tenant selfie reference leak</td><td>RLS</td><td>Privacy</td><td>Audit</td></tr><tr><td>9</td><td>Selfie storage accidentally created</td><td>Audit</td><td>Privacy breach</td><td>Code review</td></tr><tr><td>10</td><td>Personality traits drift erratic</td><td>Daily check</td><td>UX confusion</td><td>Cap drift rate</td></tr><tr><td>11</td><td>Audit retention 2-year configured</td><td>DPO</td><td>OK</td><td>Verified</td></tr><tr><td>12</td><td>Tenant disables v2 broken</td><td>UI test</td><td>Bug</td><td>Fix flag</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 viral hook #3 (AI personality v2).</p>\n<p><strong>Sub-decisions punted to ops:</strong> Affinity tuning + selfie consent UX copy.</p>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>v2 on kids.</li><li>Selfie storage.</li><li>Cross-user sibling.</li><li>Name in shareable content.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-AI-001 fallback; TASK-PET-005 multi-pet roster; TASK-VIRAL-001 share redaction.</p>";
