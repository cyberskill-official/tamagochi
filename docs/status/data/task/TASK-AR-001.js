window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AR-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>§1.1  <strong>AR support detection.</strong> App MUST detect AR capability at runtime:</p>\n<ul><li>iOS: <code>ARWorldTrackingConfiguration.isSupported</code>.</li><li>Android: <code>ArCoreApk.checkAvailability()</code>.</li><li>Fallback: surface \"Photo Studio\" mode (Cocos render to texture without AR; static background) for unsupported devices.</li></ul>\n<p>§1.2  <strong>Permission flow.</strong> Camera + (Android only) ARCore install permissions MUST be requested at first AR session start, not at app launch. Requests follow OS-level permission patterns; denial routes to Photo Studio fallback.</p>\n<p>§1.3  <strong>Plane detection.</strong> AR session detects horizontal planes (floor, table). Pet is placed only on horizontal planes ≥ 0.5 m² confidence. Vertical-plane mode forbidden at P1 (placement complexity + lighting variance).</p>\n<p>§1.4  <strong>Pet anchor.</strong> Once placed, the pet's anchor is a <code>tracking_session.anchors[0]</code> reference. The pet renders at a fixed 30 cm height in world space (kawaii proportion vs typical room scale).</p>\n<p>§1.5  <strong>Spine in AR.</strong> The Cocos <code>sp.Skeleton</code> component used in the main game renders directly into the AR scene (same skeleton bundle, same contract animations). Critical animations triggered: <code>ar_greet</code> (on placement), <code>idle_&lt;stage&gt;</code> (default), <code>dance</code> (button-tap from user), <code>wave</code> (button-tap from user).</p>\n<p>§1.6  <strong>Video capture — 9:16 vertical.</strong> A \"Record\" button captures <strong>up to 15 seconds</strong> of video at:</p>\n<ul><li>iOS: AVAssetWriter, 1080×1920, H.264, 30 fps, ~6 Mbps.</li><li>Android: MediaCodec, same params.</li></ul>\n<p>§1.7  <strong>Watermark overlay.</strong> Captured video MUST be overlaid with \"tamagochi.app\" watermark in bottom-right corner (12pt font, semi-transparent), via <code>WatermarkOverlay.ts</code> applied at encode time. Watermark MUST be impossible to disable in production.</p>\n<p>§1.8  <strong>Save to camera roll.</strong> Captured video saved to the OS camera roll (with explicit user consent gate per platform). Returns the file URI to the Cocos layer for TASK-VIRAL-001 share-intent.</p>\n<p>§1.9  <strong>No persistent geometry storage.</strong> Plane-detection meshes MUST NOT be persisted server-side or shared with external services. AR session state lives only in memory; on session end, all geometry is discarded. Privacy: user's room layout is sensitive.</p>\n<p>§1.10  <strong>Kids SKU disabled.</strong> Per TASK-LEGAL-003 §1.4, the AR-share path goes outside the app (camera roll → user's other apps). Under-13 must use parental gate (math problem). At P1, the kids SKU <strong>disables the AR feature entirely</strong> — the entry point is hidden. Re-enable in P3 as part of the parental-controlled gate flow.</p>\n<p>§1.11  <strong>Stage gate.</strong> AR placement requires <code>stage ∈ {teen, adult}</code> (per TASK-PET-002 §1.6) — eggs/babies cannot be ARed (UX rationale: AR is a \"we go places together\" intimate experience, fits maturity).</p>\n<p>§1.12  <strong>AR session telemetry.</strong> <code>ar.session.started</code>, <code>ar.session.placed { plane_count, time_to_place_ms }</code>, <code>ar.session.recorded { duration_sec }</code>, <code>ar.session.failed { reason }</code> per TASK-OBS-001.</p>\n<p>§1.13  <strong>Performance budget.</strong> AR rendering MUST sustain ≥ 30 fps on the TASK-INFRA-001 §1.20 baseline VN 1 GB Android device. Falls below → graceful degrade (lower Spine sample rate; warn user with \"Photo Studio mode might work better\").</p>\n<p>§1.14  <strong>A11Y reduce-motion.</strong> When <code>prefers-reduced-motion: reduce</code>, animations slow + screen-shake (none in AR but documented for completeness) disabled. Voice-over hint reads aloud: \"Pet placed near the chair.\"</p>\n<p>§1.15  <strong>Lighting estimation.</strong> ARKit / ARCore lighting-estimation API drives the Spine skeleton's tint: in low light, pet renders slightly desaturated; in bright light, slight warm tint. Cocos <code>sp.Skeleton.color</code> modulation.</p>\n<p>§1.16  <strong>Server-side AR session log.</strong> <code>ar_sessions</code> table records: <code>(id, user_id, pet_id, started_at, ended_at, placed boolean, recorded boolean, fallback_mode boolean)</code>. Used for analytics + abuse monitoring (rapid AR-record cycles could indicate UGC farming).</p>\n<p>§1.17  <strong>No persistent device-info leak.</strong> Camera serial number, IMU sensor IDs, model-specific IDs MUST NOT be sent to the server. Only categorical fields (\"iOS 17\", \"ARKit-capable\").</p>\n<p>§1.18  <strong>Pre-record consent screen.</strong> Before the first AR record, a one-time consent screen explains: \"We don't store your room — the recording is yours alone.\" Consent is per-device, not per-account.</p>\n<p>§1.19  <strong>Photo Studio fallback flow.</strong> Non-AR devices see a 2D scene with selectable backgrounds (forest / room / cafe). Same Spine rendering + same video capture. Photo Studio respects the same kids-SKU disable.</p>\n<p>§1.20  <strong>Cocos integration.</strong> <code>PetArPlacer.ts</code> orchestrates: bridge to native ARKit/ARCore → poll for planes → on tap, place pet on plane → trigger <code>ar_greet</code> animation → expose Record button → call native <code>VideoCapture</code> on record.</p>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why ARKit + ARCore native bridges.</strong> Plan §PART 4. Cocos plugins for AR are still maturing; native bridge is the safer P1 path.</p>\n<p><strong>Why horizontal-plane only.</strong> Horizontal floors/tables are the most reliable surfaces. Vertical-plane support (e.g. pet on a wall) is a P3+ feature.</p>\n<p><strong>Why 30 cm pet height.</strong> Empirical UX testing — pets shorter than 20 cm feel unimportant; pets taller than 50 cm feel intimidating. 30 cm sits in the kawaii sweet spot.</p>\n<p><strong>Why 15-second max recording.</strong> TikTok / Reels native format. Longer recordings inflate file size + complicate share UX.</p>\n<p><strong>Why watermark not removable.</strong> Anti-piracy + brand awareness. Plan §PART 7.</p>\n<p><strong>Why no geometry persistence.</strong> Plan §PART 11 — privacy. Room mesh is sensitive (could reveal floor plan).</p>\n<p><strong>Why kids SKU disabled at P1.</strong> External-link gate complexity. Re-enable in P3 with parental-dashboard control.</p>\n<p><strong>Why teen+adult stage only.</strong> Plan §PART 3 — bonding mechanic for maturity moments.</p>\n<p><strong>Why server-side session log.</strong> Anti-abuse monitoring (UGC farming).</p>\n<p><strong>Why Photo Studio fallback.</strong> Plan §PART 7 — emerging-market reach. Many VN Android devices don't support AR.</p>\n<p><strong>Why lighting estimation.</strong> Increases the \"pet is really in my room\" feeling.</p>\n<hr>\n<h2>§3 — API contract &amp; code shape</h2>\n<pre><code class=\"language-swift\">// apps/cocos/native/ios/ARKitBridge.swift\n@objc class ARKitBridge: NSObject, ARSessionDelegate {\n  static let shared = ARKitBridge()\n  private var session: ARSession?\n\n  @objc func startSession() -&gt; Bool {\n    guard ARWorldTrackingConfiguration.isSupported else { return false }\n    let config = ARWorldTrackingConfiguration()\n    config.planeDetection = .horizontal\n    config.environmentTexturing = .automatic\n    session = ARSession(); session?.delegate = self; session?.run(config)\n    return true\n  }\n\n  func session(_ session: ARSession, didAdd anchors: [ARAnchor]) {\n    for anchor in anchors where anchor is ARPlaneAnchor {\n      CocosBridge.emit(\"ar.plane.detected\", [\"anchor_id\": anchor.identifier.uuidString])\n    }\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/ar/PetArPlacer.ts\nimport { _decorator, Component, EventTouch } from 'cc';\nimport { ArBridge } from './ArBridge';\nimport { SpineLoader } from '../art/SpineLoader';\n\n@ccclass('PetArPlacer')\nexport class PetArPlacer extends Component {\n  async start() {\n    if (await this.isKidsSku()) { this.swapToPhotoStudio(); return; }\n    const supported = await ArBridge.startSession();\n    if (!supported) { this.swapToPhotoStudio(); return; }\n    this.node.on(Node.EventType.TOUCH_END, (e: EventTouch) =&gt; this.onTap(e));\n  }\n\n  private async onTap(e: EventTouch) {\n    const planeHit = await ArBridge.raycast(e.getLocationX(), e.getLocationY());\n    if (!planeHit) return;\n    const skel = await new SpineLoader().loadPetSkeleton(this.petId);\n    ArBridge.placePet(skel, planeHit.anchor_id);\n    new SpineLoader().playContractAnimation(skel.component, 'ar_greet');\n    this.audit.emit('ar.session.placed', { plane_count: planeHit.plane_count });\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/ar/VideoCapture.ts\nexport class VideoCapture {\n  async record(durationSec: number): Promise&lt;{ file_uri: string; duration_sec: number }&gt; {\n    if (durationSec &gt; 15) durationSec = 15;\n    const result = await ArBridge.recordVideo({ duration_sec: durationSec, resolution: '1080x1920', fps: 30 });\n    return result;  // bridge handles watermark overlay\n  }\n}</code></pre>\n<pre><code class=\"language-sql\">-- infra/supabase/standard/migrations/20260517_014_ar_sessions.sql\ncreate table public.ar_sessions (\n  id text primary key,\n  user_id uuid not null references auth.users(id) on delete cascade,\n  pet_id text not null references public.pets(id) on delete cascade,\n  started_at timestamptz not null default now(),\n  ended_at timestamptz,\n  placed boolean default false,\n  recorded boolean default false,\n  fallback_mode boolean default false,\n  platform text not null check (platform in ('ios','android')),\n  tenant_id text not null default 'mochi'\n);\nalter table public.ar_sessions enable row level security;\ncreate policy \"ar self\" on public.ar_sessions for select using (user_id = auth.uid());</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<p><strong>AC1.</strong> Standard SKU on AR-capable iOS surfaces AR session. Verified by Detox + AR test fixture. <strong>AC2.</strong> Non-AR-capable device falls back to Photo Studio. Verified. <strong>AC3.</strong> Kids SKU has AR entry-point hidden. Verified by Playwright. <strong>AC4.</strong> Plane detection finds horizontal plane in test fixture. Verified by Detox. <strong>AC5.</strong> Pet placement triggers <code>ar_greet</code> animation. Verified. <strong>AC6.</strong> Video capture 15s max. Verified. <strong>AC7.</strong> Watermark visible bottom-right of captured video. Verified by inspecting output frame. <strong>AC8.</strong> Video saved to camera roll with consent. Verified. <strong>AC9.</strong> Stage-locked: AR on baby pet returns 403 + UX redirects. Verified. <strong>AC10.</strong> Pre-record consent screen shown once per device. Verified by Detox. <strong>AC11.</strong> AR fps ≥ 30 on baseline emulator. Verified. <strong>AC12.</strong> No camera serial / IMU id sent to server. Verified by network proxy inspection.</p>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// apps/cocos/assets/_root/ar/__tests__/PetArPlacer.spec.ts\ndescribe('TASK-AR-001 — PetArPlacer', () =&gt; {\n  it('falls back to Photo Studio when AR unsupported', async () =&gt; {\n    vi.spyOn(ArBridge, 'startSession').mockResolvedValue(false);\n    const placer = new PetArPlacer();\n    await placer.start();\n    expect(placer.fallbackActive).toBe(true);\n  });\n\n  it('disables AR on kids SKU', async () =&gt; {\n    (globalThis as any).__BUILD_TARGET__ = 'kids';\n    const placer = new PetArPlacer();\n    await placer.start();\n    expect(placer.fallbackActive).toBe(true);\n  });\n\n  it('triggers ar_greet on placement', async () =&gt; {\n    const spy = vi.spyOn(SpineLoader.prototype, 'playContractAnimation');\n    await placer.onTap({ getLocationX: () =&gt; 0, getLocationY: () =&gt; 0 } as any);\n    expect(spy).toHaveBeenCalledWith(expect.anything(), 'ar_greet');\n  });\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-kotlin\">// apps/cocos/native/android/ARCoreBridge.kt (excerpt)\nclass ARCoreBridge(private val ctx: Context) {\n  private var session: Session? = null\n  fun startSession(): Boolean {\n    val avail = ArCoreApk.getInstance().checkAvailability(ctx)\n    if (avail != ArCoreApk.Availability.SUPPORTED_INSTALLED) return false\n    session = Session(ctx).apply {\n      val config = Config(this).apply { planeFindingMode = Config.PlaneFindingMode.HORIZONTAL }\n      configure(config); resume()\n    }\n    return true\n  }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>External:</strong> ARKit (iOS 15+); ARCore (Google Play Services for AR); AVAssetWriter (iOS); MediaCodec (Android). <strong>Internal:</strong> TASK-INFRA-001 (Cocos scaffold), TASK-PET-002 (stage gate), TASK-ART-001 (Spine <code>ar_greet</code>). <strong>Blocks:</strong> TASK-VIRAL-001 (TikTok export pipeline reads AR video file URI).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-http\">POST /v1/ar/session\n{ \"pet_id\": \"01HC...\" }\n→ 200 { \"session_id\": \"01HCARS...\", \"platform_capabilities\": { \"ar\": true, \"lidar\": false } }</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"ar.session.placed\", \"session_id\": \"01HCARS...\", \"plane_count\": 2, \"time_to_place_ms\": 4123 }</code></pre>\n<pre><code class=\"language-json\">{\n  \"session_id\": \"01HCARS...\",\n  \"user_id\": \"01HC...\",\n  \"pet_id\": \"01HC...\",\n  \"started_at\": \"2026-08-12T14:36:01Z\",\n  \"placed\": true,\n  \"recorded\": true,\n  \"fallback_mode\": false,\n  \"platform\": \"ios\"\n}</code></pre>\n<pre><code class=\"language-json\">{ \"event\": \"ar.session.failed\", \"reason\": \"permission_denied\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved:</p>\n<ul><li><strong>OQ-1 (resolved):</strong> AR plugin vs native bridge? → §<code>allowed_tools</code> + §2 — native bridge.</li><li><strong>OQ-2 (resolved):</strong> Vertical-plane mode? → §1.3 — P3+.</li><li><strong>OQ-3 (resolved):</strong> Watermark removable for Pet+? → §1.7 + §2 — never removable.</li><li><strong>OQ-4 (resolved):</strong> Kids SKU at P1 vs P3? → §1.10 — disabled P1, enabled P3 with parental gate.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>1</td><td>Plane detection times out (poor lighting)</td><td>UX wait &gt; 10s</td><td>Surface \"find better lighting\" hint</td><td>User retry; or fallback to Photo Studio</td></tr><tr><td>2</td><td>ARCore SDK requires update on Android</td><td>ArCoreApk check</td><td>Prompt user to update Google Play Services for AR</td><td>Or fallback to Photo Studio</td></tr><tr><td>3</td><td>Video recording mid-capture interrupted (phone call)</td><td>OS event</td><td>Discard partial file</td><td>Retry; show UX</td></tr><tr><td>4</td><td>Camera permission denied</td><td>Permission API</td><td>Fallback to Photo Studio</td><td>Re-request opportunity in Settings</td></tr><tr><td>5</td><td>Pet skeleton fails to render in AR</td><td>Cocos error</td><td>UX broken</td><td>Reset session; reload skeleton</td></tr><tr><td>6</td><td>AR fps drops below 30 on low-end device</td><td>Sentry perf metric</td><td>Graceful degrade</td><td>Lower Spine sample rate; warn user</td></tr><tr><td>7</td><td>Camera roll write permission denied</td><td>Save fails</td><td>UX error</td><td>Re-prompt; or surface \"share without saving\"</td></tr><tr><td>8</td><td>Watermark accidentally omitted in encode</td><td>Build assert</td><td>Patches blocked</td><td>Verify in CI before submission</td></tr><tr><td>9</td><td>Lighting estimation drifts wildly</td><td>Visual artifacts</td><td>Pet color unstable</td><td>Cap modulation strength</td></tr><tr><td>10</td><td>Plane geometry mistakenly persisted to crash log</td><td>Sentry data review</td><td>Privacy leak</td><td>PII scrubber strips ARWorldMap fields</td></tr><tr><td>11</td><td>AR session memory leak (long sessions)</td><td>OS terminates</td><td>Crash</td><td>Tear down session on background</td></tr><tr><td>12</td><td>iOS-only iPhone-12+ AR-features used on iPhone-X</td><td>Feature detection</td><td>Graceful degrade</td><td>Check capability before invoking</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<p><strong>Plan refs:</strong> plan §PART 3 viral hooks #2 (Bedroom Cam), §PART 4 (ARKit/ARCore), §PART 5 (graceful fallback).</p>\n<p><strong>Sub-decisions punted to ops:</strong></p>\n<ul><li>Photo Studio backgrounds — designer-curated.</li><li>Watermark exact styling — designer locks per locale.</li></ul>\n<p><strong>Anti-patterns explicitly forbidden:</strong></p>\n<ul><li>Persistent ARWorldMap to server.</li><li>AR on kids SKU at P1.</li><li>Removable watermark.</li><li>AR placement on egg/baby pets.</li></ul>\n<p><strong>Cross-reference:</strong> TASK-VIRAL-001 consumes the captured video URI for share-intent. TASK-A11Y-001 reduce-motion applies.</p>";
